## fernwood-15 · Unbundle the CREDENTIAL axis and ship the per-run unspent invite now?

- project: Tate-Tracker
- loop: tate-tracker
- source: `.plans/2026-09-10-testing-architecture-PLAN.md` §6, minted 2026-09-10
- options: unbundle-now | hold-for-the-redesign | unbundle-but-time-boxed

### Why it's here
⚡ **Live consequence: gate ① is red on an instrument defect, not a product one.**

The remedy is invariant across every candidate design (plan §3a), so waiting settles nothing while gate ① stays red. Every fresh walk in this project's history was an existing person shown a signup form by a cache.

### What it means
This card exists so the question is **reachable**. The plan it came from is 534 lines and its own
thesis is *"a capability the loop cannot reach by running its own procedure is not a capability the
loop has"* — and it then put eight rulings into prose nothing reads. ⛔ It stalled for an hour on
exactly that the day it landed. The card carries the recommendation and the alternatives so the
ruling can be made without re-reading the plan.

### Recommendation
**YES, unbundle** — and build it as a property of *the arrival*, never as a new `--role`.

**Alternatives.** **(b)** hold it, as the backlog row rules — costs a red gate ① for the length of the redesign, buys strict ordering discipline. **(c)** unbundle but time-box: if P1 has not started within a lap, the credential fix ships alone anyway.
