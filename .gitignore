.DS_Store
*.swp
*~
.research/
.private/

# Manual source PDFs — local-only (public repo; avoid republishing copyrighted manuals + repo bloat).
# Extracted text in manuals/text/ IS committed as the queryable substrate.
manuals/pdf/
# EXCEPTION: the LMC FD.88 catalog text stays local-only too — a current commercial
# price catalog is not a manual; don't republish 180 pages of LMC's pricing publicly.
manuals/text/bronco-1989-lmc-catalog-fd88.txt
REINTEGRATION-BACKLOG-2026-07-09.md

# Tooling scratch — never source (added 2026-07-16)
.playwright-mcp/
__pycache__/

# claude --bg agent worktrees — never tracked (added 2026-07-31)
.claude/worktrees/

# Derived KML exported for editing in Google Earth Pro (tools/zones-to-kml.py).
# Regenerable from zones.json; the record is zones.json, never the export.
exports/

# High-res Esri tracing frame — NOT redistributable, local display only.
# tools/fetch-trace-hires.py regenerates it. Never commit it.
images/property-map/.local/

# Google Earth Web captures — Google imagery, display-only, never redistributable.
images/property-map/.local/gearth/
.wrangler/
