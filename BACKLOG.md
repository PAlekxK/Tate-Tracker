# Fernwood — backlog

**Single source of truth for Fernwood backlog statuses.** When any other doc (CLAUDE.md fragments, memory, design docs) disagrees, **this file wins** and the stale source should point here. First consolidated 2026-07-13; **re-organized into two tracks 2026-07-17** (intent-first rationalization — spec: `.plans/2026-07-17-backlog-rationalization.md`).

**Why two tracks (2026-07-17):** Fernwood carries **two products in one repo** — **Mom's field journal** (Track A: Mom-facing, field-journal tone, the whole W-series arc, plants/wildlife/weather/map) and **Paul's fleet & equipment tracker** (Track B: Paul-facing, utilitarian, ~~deadline-bearing~~ — **no live deadline as of 2026-07-29**; the overdue registration that made it the clock-bearing track was resolved 7/22, next hard date 2027-06-03). Different users, tone, cadence, and definition of "done." They're split so each is legible on its own terms and the Mom arc doesn't read as accretion. Track C is cross-cutting infra/doctrine. **Shared reference** (KILLED / SHIPPED / reconciliation) is at the bottom.

**Status taxonomy:**
- **SHIPPED** — live in production (GH Pages + Worker).
- **ACTIVE** — being worked right now, or a live measurement phase.
- **DEFERRED** — decided-not-now; each carries the **gate** that would unblock it.
- **IDEATION** — raised, not yet designed or decided.
- **KILLED / SUPERSEDED** — abandoned or folded into something else.

The dated **"Pickup point"** trail was archived to `PICKUP-LOG-ARCHIVE.md` (2026-07-17) — it's history, not status. Read status here.

---
# ▶️ NEXT — the one true list (rationalized 2026-07-29)

> **This replaces the old TOP ITEM block and the two colliding `▶️ NEXT` tables.** The rationalization
> Paul commissioned on 2026-07-28 **ran on 2026-07-29**: five expert seats (ux · user-research ·
> engineering · content · ai-advisor) plus a mechanical verification sweep that classified **140 rows**
> against git and the live app. Proposal + full reasoning: `.plans/2026-07-29-rationalized-backlog-PROPOSAL.md`.
> Panel reports: `.ux-reviews/`, `.user-research/`, `.engineering/`, `.content-reviews/`, `.ai-advisor/`,
> `.plans/2026-07-29-verification-sweep.md`. The old head is preserved in git.

**This is a POINTER list, not a second tracker.** Status stays in the row it points to. The ~156 rows
below are the **decision record** — read them for *why*; read this for *what now*.

**Sorted by Paul's disposition tiers** (`[paul-stated]` 2026-07-29): the tier is defined by **what
unblocks a row**, not what area it belongs to. Track A/B stays as the product boundary; the tiers are
the priority cut laid over it.

| Tier | Unblocked by |
|---|---|
| **1 · FIX NOW** | Nothing. Just do it. |
| **2 · CONFIRMED** | An answer Mom has **already given** — build it |
| **3 · STEER** | A question **not yet asked** — and the row is INCOMPLETE until it names ① the question and ② how the answer gets captured |

---

## ⭐⭐ THE ORIENTING PRINCIPLE (Paul, 2026-07-29) — steer on her signal, and clean the instrument first

Two clauses, both standing.

**① Orient around her feedback — explicit AND behavioural.** Paul: *"let's always orient around her
feedback, be that explicit — like taps on yes or no or free response — or just how she's clearly using
the app, what she's looking at, what she's not."* Both are evidence: taps, notes, Guru questions, zone
audio and anything Paul relays; **and** card expansions, what she opens, what she never does, what she
returns to. A recommendation citing neither is a weaker class of evidence and must say so.

**② The UX cleanup is MEASUREMENT HYGIENE, not polish — which is why it goes first.** Paul: *"cleaning
up the UX… especially where we ask for her input… right now it's a little confusing, so it introduces
some noise into the feedback data that she provides us and the usage data."* Read literally: **the
confusing input stack is contaminating the instrument we steer by.** We cannot distinguish *she
declined* from *she never understood which thing she was answering*. **Anything that disambiguates a
surface outranks anything that adds an ask** — the second is unmeasurable until the first lands.

---

## 🚨 READ THIS BEFORE CITING ANY ENGAGEMENT NUMBER

**The funnel that justified this project's headline finding counted the wrong person.**
`tools/people.json` is unambiguous — the device mapping was **backwards until 2026-07-28**, and its own
`_meta` says *"Do not cite any pre-2026-07-28 funnel verdict; re-derive it."*

- **"Offered 35 → viewed 33 → tapped 1" is NOT her.** ✅ **STRUCK EVERYWHERE 2026-08-01** — A3 (row
  334), `q-top-categories._note`, the `/mom-cycle` Skill, and the whole telemetry tier of
  `.user-research/persona-mom.md` (which `ux-expert` reads for user context, so it was feeding a
  portrait of Paul's dogfooding into every design review). **Re-derived, not estimated**, via
  `read-mom-funnel.py` + `analyze-fernwood.py` over **2026-07-13 → 2026-08-01**, builder devices
  excluded: **offered 14 → viewed 9 → tapped 2 → answered 4**; front door **offered 10 → viewed 7 →
  tapped 0**. Her device is **11 sessions / 6 active days** against Paul's **69 / 18**.
  That is not a disengaged user.
  - ✅ **RE-ATTRIBUTED 2026-08-01 (Paul: go-ahead).** All 25 conversations in 05-01→08-01 mapped to
    a person. **Root cause found: the conversation records carry `deviceId: null`** — attribution
    was never checkable from conversation data, only from the `conversation_started` metrics event.
    Joined by timestamp; nearest ≤12s, closest two start events 51s apart, **0 ambiguous joins**.
    **Corpus: Paul 12 · Mom 9 (05-21→07-26) · 4 unattributed** (all 05-20 19:38–19:41).
    - **Void:** "adoption is durable, 27 of ~40 days" (Paul) · the **lily-pad new-job proposal**
      (Paul's utterance, 07-02T18:24 — a persona-level job proposed off one observation).
    - **Survives:** "her Guru questions are stewardship-lookup" — the artifact had logged
      `d-szqlt0h7` as *"7 conversations, stewardship-voiced"* **under a Paul heading**. It read the
      voice right and attached it to the wrong person. *Content-reading was the sound instrument.*
    - **BETTER than we thought:** the 8-turn conversation is **Paul's**, and Mom has **two**
      multi-turn conversations (5/28 4-turn, **7/26 6-turn**), not one. She follows up more than the
      anti-persona said.
    - ⚠️ **`turnCount` is a TURN PAIR** — `turnCount: 2` = one user turn. Several old claims read as
      if 2 were a follow-up. Read ≥4 as "she came back."
  - ⛔️ **RE-OPENED: does Mom open the app unprompted, and how often?** The persona had it struck
    through as *"YES, ~4.5 sessions/day. Closed."* — closed on **Paul's** data. Re-derived: **11
    sessions / 6 active days ≈ 0.55/day** vs Paul's 69/18. Real recurring non-builder use, but not
    the "adoption is no longer hypothetical" verdict carried since May. **This is the make-or-break
    question and it is OPEN**; the clean window closes **2026-08-10**. *A question closed on the
    wrong person's data is not closed, and leaving it struck through tells every later reader it is.*
- **The fear hypothesis loses its quantitative leg.** What remains is self-report, n=1, offered to a
  reassuring son — this project's own weakest evidence class.
- **A rival now fits better: TOPIC ORIGIN.** Every card authored from *our* uncertainty markers and
  offered after 7/14 is unanswered (**0 of 4**). The one seeded from *her own words* —
  `q-almanac-name` — was answered **in under a day**. A third candidate, occasion/effort, is confounded
  with it in every row and stays on the board.
- **She has NEVER used the A/A+ toggle** (Paul's device fired it 22×). Reading-difficulty still stands
  on Paul's testimony, but its behavioural corroboration is gone — **so tune the DEFAULT type scale,
  not `body.text-lg`.**
- **`persona-mom.md`'s entire telemetry tier is Paul's device** and must be re-derived before anything
  reads it as user context.
- **Three irreconcilable funnel figures** (35/33/1 · 9→3 · 60/42/2) sit in three sections. None is
  current. Re-derive from 2026-07-28 forward.

---

## ✅ SHIPPED 2026-07-29 — the Tier-1 correctness pass

*All verified in a real browser at 390×844 or by rebuild. Detail in the proposal doc.*

| Item | What landed |
|---|---|
| ✅ **The falsified Bolores tire size no longer renders** | `maintenance.tires` said **33**X12.50R15LT, stamped `"confidence":"verified"`, still carrying the exact *"size read off the actual sidewalls"* sentence `7494b46` **proved false** — and it was in Guru's digest. The 7/29 fix had reached `specs.tires` and stopped there. **A clear must reach every asserting line.** |
| ✅ **Guru's digest held a FABRICATED zone and none of the real ones** | `property.json.propertyZones` still contained the shipped-template placeholder `zone-placeholder` / *"Example: Front Beds"* — and it flowed into the digest, while **zero** of the ten real zones were there. The assistant carried an invented place-name on the surface Mom's voice walks and her map are built on. Stub removed; **real zones now in the digest lean** (id/name/type/status — `vertices`/`history` stripped as pure distractor mass), plus `turf.json`, closing the *"Guru reads an id it cannot resolve"* hole behind the weeds' `observedZones`. |
| ✅ **Every text box triggered an iOS Safari viewport zoom** | All three inputs rendered below the 16px threshold (`.ui-textarea` 14, `.mom-queue-correction` 15, `.feedback-panel-input` 15.5). **She tapped a box and the page jumped to ~1.2× and did not come back.** Never caught because the app had only ever been reviewed on a desktop viewport. 16px is now a documented functional floor. |
| ✅ **The carousel arrows silently destroyed typed-but-unsent notes** | `‹`/`›` called `render()` — which rebuilds the host — without ever reading the textarea. The codebase already knew the hazard: `showAck(keep)` carries an explicit no-wipe branch commented *"her text stays exactly where she left it."* **The arrows never got the same guard.** Now an in-memory per-card draft store, bound on input so **every** re-render path is covered, cleared only on a send that did not fail. Browser-verified: type → step away → step back → her words are still there. |
| ✅ **Guru recited a falsified soil series** | Cecil/Pacolet are thermic Piedmont series capped near 900 ft; the property is 2,959 ft. `plants.json._meta.soilSeries` still **listed** them 4 days after `property.json` retracted them, and both files' citations still named them. Corrected + aligned. The **17** per-plant `soilNotes` are W9-gated (they get rewritten once, well, against a measured pH) so they are scrubbed **in the derived digest only** — the repo keeps its record, the assistant stops asserting a retracted fact. |
| ✅ **`check-mom-ack.py:229` asserted a behaviour that does not exist** | It told the reader `read-mom-zone-audio.py` "marks its own channel." It never calls `mark_channel_read` and holds no `momlib` reference at all — so zone-audio read as self-attesting while nothing was reading it. |
| ✅ **`CLAUDE.md` was wrong about its own codebase** | Documented `viewer.html` as "~4,600 lines"; it is **17,878**. And *"fetched at page load, inlined copies as fallback"* is true of only **4 of 21** JSON files — for the other 17 the inlined constant **is** what renders, which is why `check-data-inline.py` is a correctness gate, not a tidiness check. |
| ✅ **A live release note promised a feature that is gone** | *"Anything you've starred stays starred"* — the star UI was retired. Rewritten and re-inlined. |

---

## ✅ SHIPPED 2026-07-29 (evening) — the agent-drivable sweep + Paul's four calls

*All browser-verified at 390×844. Tier-1 #1–#5, #7 and Tier-2 #1, #4, #5, #6 are closed above;
#6 (the ✓) is held open by Paul. Three backlog premises were checked and found WRONG — each is
corrected in its own row rather than quietly fixed, because a wrong SSOT row is the failure this
repo keeps re-learning.*

**Paul's four calls this session, all now in code:**
1. **The ✓ stays on the ribbon** for now, kept as an option (Tier-1 #6).
2. **The ribbon covers everything since the last one she gave input on, each linked** to where she
   can see it. This required a renderer change — one `linkPhrase`/`linkCard` pair became
   `links: [{phrase, card}]`.
3. **Promote stiltgrass, and order the queue by the data most helpful to us.** Ordering axis now
   lives in `questions.json._ordering`: an answer that unblocks a BUILD > one that fills a canon
   gap > a verdict on our own guess.
4. **Bloom labels are `It's blooming · Not blooming · Snooze card`**, snooze meaning dismiss-and-
   resurface (the mechanism already worked that way — `notSure()` snoozes for the day only).

**⭐ And one doctrine REVERSAL worth remembering:** Tier-1 #4 wanted the two green affirmatives made
*more different*. Paul reversed it — consistency over precision, because one learnable signal beats
two she has to tell apart. The affirmative grammar is now a single shared component set.

### ⚠️ LOOSE THREADS LEFT OPEN — named, not buried

| Thread | Why it's open |
|---|---|
| ~~**The third button is inconsistent across cards**~~ | ✅ **DECIDED 2026-07-31 (Paul) — all six collapsed to `Snooze card`.** The four remaining wordings (`I haven't looked` ×2, `I'll think on it`, `Haven't thought about it`) are gone. His reasoning: the label should carry the **promise**, not the state — *"it's more clear that the card doesn't just disappear, it's not embarrassing to hit snooze, and it also doesn't create any worry that the card will not pop back up."* Note this changed **what she is told, not what happens** — all three wordings always ran the same `snoozed[id] !== today` path (hidden for the day, back tomorrow). Set as an **initial direction under watch**, not a settled answer — see the row below. |
| **👁 WATCH — `Snooze card` is action-shaped, not state-shaped** (the monitored variable in Mom's feedback loop) | The 7/26 doctrine chose STATE language on the reasoning that a state is unembarrassing to someone afraid of being wrong, and `check-cards.py` lints deferral-shaped labels. `Snooze card` does not trip the lint and Paul weighed clarity higher — exemption recorded in `check-cards.py`, widened 2026-07-31 to all cards. **This is now the single deliberate reversal of a finding grounded in her own words, applied app-wide** — so it is the first variable to revisit if deferral behaviour shifts.<br><br>**What to watch:** third-button tap rate, and whether a card gets snoozed repeatedly without ever being answered (a snooze loop = the label made deferring too easy, which is the failure mode the state framing was protecting against). **Escalation if it shifts: ask her directly** — a card asking which wording she prefers, human-confirmed like any authored content. **⚠️ Do not read the pre-07-31 window as a baseline:** only the 2 bloom cards carried `Snooze card` before, so any comparison across that date pools two different treatments — the same over-read the A1 gate suffered. The clean series starts 2026-07-31. |
| ~~**13 sub-44px targets app-wide**~~ | ✅ **SWEPT 2026-08-01 — 12 of 14 fixed; 2 held for Paul.** ⚠️ **The old row's enumeration was wrong in both directions and is corrected here**, per the standing "verify a row against the app before acting" rule. It was measured, not re-read: every interactive element rendered in a **real 390px viewport** (iframe, so `@media (max-width:480px)` resolves), all cards expanded. **Wrong-high:** `.fn-mic-btn` (called out as "the sharpest of these — a Mom-facing capture control at 38px") is **dead CSS — no such element exists in the DOM**; the live mic is `.ui-glyph-btn.ui-mic-btn` at 40px. `.ref-drawer-toggle .main-card-icon` 38px is a **decorative icon inside a `min-height:44px` toggle**, never a target. `.ui-glyph-btn` is a 40×40 button, not "inner glyphs 13/24." **Wrong-low — four real targets the row never listed**, two of them Mom-facing: `.ack-inline-link` 60×22 (the tappable phrases in the acknowledgment ribbon — the very surface the 7/29 pass hardened), `.mom-queue-launcher-dismiss` 90×25, `.feedback-close` 29×30, `.radar-toggle-btn` 58×21, `.fish-tab` 90×29, `.ref-category-header` 363×43, `.feedback-send` 324×43.<br><br>**Two techniques, chosen per control** (see the `.tap44` comment block at the top of viewer.html's CSS): **grow the box** where the control is already substantial and a bigger visual helps (`.fish-tab` 29→44, `.text-size-btn` 36→44, `.pmap-add-btn` 36→44, `.pmap-sync-action-btn` 38→44, `.ref-category-header`, `.feedback-send`), or **`.tap44` — grow only the hit area** via a centered transparent `::after`, leaving paint and layout untouched, for the small quiet icon buttons (`.bio-sound-btn` ×16, `.pmap-ctrl-btn`, `.ui-glyph-btn`, `.radar-toggle-btn`, `.fn-sync-btn`, `.feedback-close`). The second technique is what let this land on Mom's surfaces without redesigning them.<br><br>**Verified, not assumed:** an A/B hit-test against the unmodified HEAD build (both served, same probe, 61 controls) returned **byte-identical hit-stealing lists** — the enlarged areas steal **zero** new taps. Side-by-side screenshots show no visual change outside the grown boxes. **STILL OPEN, deliberately — both Mom-facing, both judgment calls, see the two rows below.** |
| ~~**⏸ `.ack-inline-link` 60×22 — held, needs Paul**~~ | ✅ **DECIDED 2026-08-02 — leave it as-is, row closed.** Paul: *"I'm good with just leaving it and close it."* This is not a deferral: **WCAG 2.2 target-size carries an explicit exception for a target inside a sentence**, precisely because the height of an inline link is the height of a line of text and cannot be raised without changing the prose around it. If the ribbon's typography is ever revisited, a taller line-height may be worth it **as a readability improvement for Mom, measured on its own terms** — not smuggled in as a tap fix. Original reasoning, unchanged: the tappable phrases *inside* the acknowledgment sentence ("under Plants", "at the top now", "day by day"). They are **inline in running prose by design** — the CSS comment says it: *"it reads as part of the sentence, not as a control bolted on."* A 44px hit area on an inline link overlaps the surrounding text and the adjacent links, so the mechanical fix is the one thing that would break it. Real options are design calls on her surface: give them more line-height so the row is naturally 44px, or promote them out of the sentence into chips (which the 7/26 doctrine argues against — the sentence *is* the point). |
| ~~**⏸ `.mom-queue-launcher-dismiss` 90×25 — held, needs Paul**~~ | ✅ **DECIDED 2026-08-02 — it stays small.** Paul: *"we can keep that small, that's fine."* Recorded as a **ratified exception** in viewer.html's `.tap44` comment block so the next sweep measures 90×25 and does not read it as an oversight. Original reasoning, unchanged: enlarging a **dismiss** control is not obviously an improvement: a bigger target for "make this go away" makes accidentally dropping her card queue easier, and the queue is the thing the whole loop depends on. The 44px floor exists so she doesn't miss what she meant to hit — it does not argue for making the destructive option easier to hit by accident. Paul's call. |
| **The ribbon has NOT told her about the month view** as of this commit | `feedback-log.json` carries `addressed: true` / `acknowledgedToHer: false` for `fb-946dp0qk-ms639ds6` deliberately — she was told "not built yet," and that answer is now obsolete. This ribbon fixes it; the flag flips once it's pushed and she taps. |
| ~~**`q-strategy-pollinators` is now active-but-invisible**~~ | ↻ **RESOLVED 2026-07-31 into a third option — neither retire nor promote.** Correction to this row's own framing: it is **not** "a card serving nobody." `outstanding()` filters unanswered *then* slices 5, so pollinators enters the visible set the moment Mom answers any one of the five above it — it self-heals. It stays active and stays where it is. **The real work it exposed is the bench** (7 cards drafted and never served, no re-ranking, and freshness checked only at birth) — now scoped as its own A3 row with Paul's two design calls settled. |
| **`read-feedback-sections.py` coverage is 14%** | By construction — stamping is prospective. Do not read the uninstrumented 12 as a signal about her; the tool refuses to let you. |

---

## 🔥 TIER 1 · FIX NOW — remaining

*Nothing blocks these. All agent-drivable. **The Mom-facing ones are wording changes and stay gated on
Paul's read** (the AI boundary: anything reaching her is human-confirmed).*

| # | Item | Where | Note |
|---|---|---|---|
| ~~**1**~~ | ✅ **DONE 2026-07-29 — and the row's arithmetic was WRONG.** It said 7 active against a cap of 5 with **two** cards rendering to nobody. But `SUPPORTED_KINDS = ["confirm"]` and `q-open-standing` is `kind: open` — the standing foot-line never competes for a slot. So it was **6 confirms against 5**, and exactly **ONE** card was invisible: `q-weed-stiltgrass`. `q-strategy-pollinators` was visible in slot 5 the whole time. Verified in a browser, not inferred. **Fixed:** `questions.json` reordered by INFORMATION VALUE (rationale in its `_ordering` key) — stiltgrass promoted 6th→2nd; pollinators takes its place out of view, verified unanswered first so it cannot pin the watermark. | A3 · A7 | ✅ |
| ~~**2**~~ | ✅ **RESOLVED 2026-07-29 by REFRAMING, not retiring (Paul's call) — and the "no label at all" premise was about the JSON, not the screen.** The row said two cards had **no `"I haven't looked"` label at all**. True of `questions.json`; **false of what Mom sees** — the viewer defaults a missing `later` (`L.later || "I haven't looked"`), so all three buttons always rendered. Verified on screen. There was no functional gap; the **prompt** was the real defect. Also: only **2** un-reframed verdict cards existed in the visible set, not 3. **Fixed:** both prompts reframed to the record's-own-gap template and labels now emitted EXPLICITLY so the file and the screen agree. | A3 | ✅ |
| ~~**3**~~ | ✅ **SHIPPED 2026-07-29.** `TEMPLATES["bloom"]` rewritten to content-steward's draft (plant = subject; hedge = the record's own gap; "yet" makes *Not blooming* a fact about the season, not a verdict on us). `{where}` anchor wired from `zoneId` via `zones.json`, empty when unknown — never invented. **Labels are Paul's wording: `It's blooming · Not blooming · Snooze card`.** `TEMPLATES["variety"]` now emits a `⟨…⟩` SKELETON on purpose — no generic string can name the per-plant observable — and `check-cards.py` **fails any served card still carrying one**. ⚠️ A template fix cannot reach prompts already in `questions.json`; the 2 live cards were hand-edited to match. | A3 | ✅ |
| ~~**4**~~ | ⚠️ **REVERSED BY PAUL 2026-07-29 — and the measurement was wrong.** Measured at 390×844 they were **431px** apart, not 190, and they did **not** share a grammar: `Got it` was a pale outline pill in DM Sans 12.5px with no glyph; `That's all of them` was a solid `#3a6a3a` fill, Crimson 14px, ✓ glyph. **This row wanted them made MORE different. Paul's call is the opposite:** *"the cue and affirmative styling, we should make it consistent and a consistent signal of we hear you, and we've recorded your information … let's make it simple and consistent for mom."* One learnable signal beats two precise ones she must tell apart — and the app already HAD that grammar (ux-expert F4: fill + glyph carry the decision independent of label legibility), the ribbon just never adopted it. **Fixed:** the ribbon's buttons are now literally the same components (`gg-suggest-btn-yes` / `-neutral`), so they cannot drift apart on the next change. | A4 | ✅ |
| **5** | **The blue collision on the rainfall card** — `.rain-cell.rain-active` navy (`viewer.html:1659`) is the same family as the regional block, so blue encodes both *"notable"* and *"regional source"*, and her own gauge figure wears it. Hue = source only; salience via fill + weight | A3 · W8·b | 🤖 |
| **6** | ⏸ **HELD OPEN by Paul 2026-07-29** — *"the check mark is fine on the acknowledgment ribbon for now at least, but we can keep that as an option."* Not killed, not scheduled. Note that the ✓ now **rhymes** with the ✓ on every affirmative button (row 4), which arguably earns its place better than it did when it stood alone. Revisit if the ribbon ever reads as a checklist header. | W8·b ② | ⏸ Paul |
| ~~**7**~~ | ✅ **SHIPPED 2026-07-29 — and they were NOT the only ones.** `.ack-btn` 30→44px, and `.mom-queue-nav-arrow` was **40px** too (the arrows that were destroying her typed-but-unsent notes until 7/29, so a miss there used to cost her words) → 44×44. Both now inherit the shared button grammar. **STILL OPEN, app-wide, outside her input stack: 13 other interactive rules measure under 44px** — `.bio-sound-btn` 30 · `.pmap-ctrl-btn` 36 · `.text-size-btn` 36 · `.fn-mic-btn` 38 (a Mom-facing capture control) · `.pmap-sync-action-btn` 38 · `.ref-drawer-toggle .main-card-icon` 38 · `.ui-glyph-btn` inner glyphs 13/24 · sync chip 24 · and the "Add a place" button 36. Deliberately NOT swept in — an app-wide target pass changes many surfaces at once and wants its own review. | A4 | 🤖 (residual) |

---

## ✅ TIER 2 · CONFIRMED — she already answered; build it

| # | Item | The answer that authorizes it | Where |
|---|---|---|---|
| ~~**1**~~ ✅ **SHIPPED 2026-07-29** — Journal tile added to the top strip, sub adopts HER framing ("Look back at what you've written"). | She asked *"Is there a way to look back at these?"* **and answered the naming card Yes.** It is the **most-opened card in the app** (41 of 139 expansions) sitting **8th of 13, with no dashboard tile.** She asked to look back and still cannot find it | A6 |
| ~~**2**~~ ✅ **SHIPPED 2026-07-29**, then SUPERSEDED the same day: the ribbon now covers the moss AND the Journal naming AND the rainfall, with a link each (`links: [{phrase, card}]` — the old single `linkPhrase`/`linkCard` pair could only point at one). | She gave it 7/26; the record has it, credited to her by name; **she has never been told.** Point the next ribbon at the moss record via the shipped `linkPhrase`/`linkCard`. Asks nothing; produces an object she can see | A2 · A3 |
| **3** | **Household systems renders to nobody** — 7 vehicle, 9 equipment, **0 household-system** | She proposed the category unprompted and derived the taxonomy herself. The cheapest possible demonstration that proposing something to this app makes it appear | B6 |
| ~~**4**~~ ✅ **SHIPPED 2026-07-29 — the MONTH. The YEAR does not exist and the card says so.** This row said "presentation only; station history is already local." Half right: `weather-history.json` starts **2026-05-02** (84 days), so there is no year of measured data. The ERA5 grid does cover a year, and using it would answer a question about HER gauge with a model — the 14× substitution she caught. Shipped: last-30-days + whole-record totals, plus a caveat sized to be READ. `stationRainRange()` returns coverage beside every total so a partial window can't pose as a whole one. | Her direct ask, 7/29 (`fb-946dp0qk-ms639ds6`). Station history is already local; presentation only | W8·c |
| ~~**5**~~ ✅ **SHIPPED 2026-07-29 — and it was a WRITE task, not a read task.** This row said "every note already carries which door it came through." It did not: `section` was set on **2 of 14** records, because only 1 of 5 `postFeedback` call sites passed it. All five doors now stamp themselves; the helper writes `"unspecified"` rather than omitting so a future gap is visible. `tools/read-feedback-sections.py` keeps THREE kinds of unknown apart (uninstrumented / unspecified / floor-bypassed). Prospective only — the 12 existing records can never be attributed to a surface. Coverage at ship: **14%**. | The instrument itself. Every note already carries which door it came through — **nobody has ever read it that way**, which is why we cannot tell a decline from a misunderstanding | A1 · W8·a |
| ~~**6**~~ ✅ **SHIPPED + DEPLOYED 2026-07-29.** `origin` predicate at three call sites, sticky and one-way. Verified end-to-end against the live Worker: probe hidden by default, visible with `?origin=all`, `excludedNonApp: 1` reported rather than silent, and momlib's `guru` channel unmoved. | Test turns persist to `conversation:<id>` → listed by `/api/conversations` → read as an **arrival**, so probing makes Paul read as owing Mom a reply. Worse: a test conversation lands in the store **the Journal reads back** — it is visible to Mom. One `origin` predicate at three call sites | A6 |

---

## 🧭 TIER 3 · STEER — each carries its question AND its capture path

*Per Paul's rule: a Tier-3 row without both is on the kill list.*

| # | Question | Ask via | Capture path |
|---|---|---|---|
| **0** | `q-top-categories` — **IN FLIGHT**, leads the queue | Card (live) | ⚠️ **Unprobeable — pre-schedule the hand-retire** (`active:false` + `resolvedAt`) or it pins the feedback watermark exactly as `q-almanac-name` did |
| **1** | **The real discriminator** — zero wrongness risk but **our** topic: *"We've never written down what's actually coming to the feeders this summer — only what the books say should be. From where you sit, are the hummingbirds still working the feeders, or have they thinned out?"* | ONE card, after Tier-1 #1–2 clear the queue | Needs a `_foldTarget` → a dated `observedHere` line on the Birds card, **in her name**. Answers → our-topic asks are repairable. Silence → **topic origin binds and `harvest-questions.py`'s whole supply chain is dead** |
| **2** | `q-fairway-grass-seedheads` — already staged verbatim | Flip `active:true` **in August** when seed-heads emerge | Existing fold path; a second sample in the same cell as #1 |
| ~~**3**~~ | ~~*"The last time something in the house needed a repair or a part — what were you trying to find, and where did you end up looking?"*~~ | ✅ **RETIRED 2026-08-02** | Closed with the relayed-conversations row above (Paul: *"that seems out of date"*). B6 proceeds on the shape she already gave unprompted |
| ~~**4**~~ | ~~*"When you wrote in that the rainfall number looked wrong — what did you figure would happen with that?"*~~ | ✅ **RETIRED 2026-08-02** | Closed with the same call. Was the only planned read on what she expects back when she reports something; the return leg now proceeds on inference. Re-ask fresh if that gap bites — do not revive this wording |
| **5** | **Guru turn audit** — opens conversation content | **Paul ratifies** (not Mom) | v1 needs **no AI**: the fact table gives exact strings, so `2,800` in an assistant turn is a regex hit. Shrinks the ask from *"AI may analyze conversation content"* to *"a script may grep our own output"* |
| **6** | **W6 instance model** — what is its real shape? | **Paul** | Design doc first. **Not Mom's** — asking her to adjudicate a schema is the exact class A3 just deprioritised |
| **7** | **Wildlife confidence markers** — 67 records assert an animal is present with no way to say *"we think"* | **Paul** | Schema call; see the taxonomy answer in the proposal doc §6 |
| **8** ⭐ | **Getting her to adopt "Almanac"** — on 2026-07-30 the record card was renamed **Journal → Fernwood Almanac**, so the write surface, the record and the voice all share one name. That knowingly overrides her own word: she answered `q-almanac-name` Yes on 7/29 and "Journal" was hers. Paul: *"let's try to get her to adopt Almanac. I think it's the more appropriate. we'll work on that with her, though."*<br><br>**↻ RATIONALE RECORDED 2026-07-31 (Paul) — the rename is a CONSOLIDATION, and that is the part that is settled.** *"We had Almanac and Journal and Garden Guru and all kinds of stuff scattered around… I think the fact that we had so many names in the past confused mom. We're starting with Almanac. That's something we can measure and ask mom about, but it was important to first have a clean, aligned, consistent language."* **Verified name inventory at the time of the rename:** `Almanac` carried **3 referents** (the header tagline *"An Appalachian Almanac for Fernwood"*, the voice you ask, and the look-back card) · `Garden Guru` carried 1 (the same voice, older name) · `Journal` carried 1 (the look-back card only, and only after 7/29). So the voice had **two names at once**, and anchoring on Almanac moved **one** string while anchoring on Journal would have moved three and cost the property's own tagline. This is the third application of the ratified **consistency-over-precision** rule on her surfaces (after the affirmative grammar 7/29 and the third button 7/31). | **PAUL, in conversation — not a card.** *"You do not need to worry about that. I will work on her, not we."* A drafted confirm card was written and then **deleted** on that instruction; do not re-add one | Paul relays whatever comes back. **If she wants Journal, it goes back everywhere** — that is the loop working, not a reversal. Standing rule this follows: Paul drives outreach, the app iterates on the signal he brings back.<br><br>**⚠️ Keep the two claims separate when measuring.** Consolidation (one name, whatever it is) is justified by the inventory above and is **not** what is under test. *Which* name wins is **not** settled by that argument — she was asked, she said Journal, and it moved a day later. **The measurable question is only ever "does Almanac land for her,"** and the rename has been live and in the release notes since 07-30, so she has already seen it. Do not let the strength of the consolidation case be read as evidence for the name choice. |

**Resolved with #8, same day:** the acknowledgment ribbon used to read *"You called it the journal, so
that is its name: the Journal sits at the top now."* The rename made that sentence false on her screen,
so **the naming clause was removed** at Paul's direction — not rewritten to praise the new name. What
she is still told is the part that stayed true: what she's written now sits at the top. The `links`
entry moved from the phrase *"the Journal"* to *"at the top now"*, pointing at the same card, so she
loses no door.

**⚠️ PRE-COMMITTED, IN WRITING:** a miss on **#1** is **NOT** evidence for the topic-origin hypothesis
until the input-stack cleanup ships and the card is re-offered on a clean surface. Otherwise a null gets
over-read exactly the way the A1 gate was.

**↻ 2026-07-30 — THE PRECONDITION IS NOW MET. The clock starts today, not earlier.** The input-stack
cleanup shipped (`ca6939f`→`4145385`, W8·a below), so a null on #1 from **2026-07-30 onward** is
admissible evidence. Anything before today still is not, and the two must not be pooled — the whole
point of writing this down in advance was to stop a pre-cleanup null from being read as a verdict.
⚠️ Note this lands **inside** the clean measurement time-box that closes 2026-08-10, and the restyle
touched all four surfaces at once. Launcher counts in particular must not be pooled across 07-30.

---

## 🎨 The shape system — SHIPPED 2026-08-02 (Paul-commissioned UX audit)

Commissioned by Paul as a general UX audit, three named areas plus a whole-viewer sweep. Full findings:
`.ux-reviews/2026-08-02-button-system-weather-collapse-disclosure.json` (18 findings). Shipped and
**live on Pages** in `582607f`→`b386597`.

| What shipped | Detail |
|---|---|
| ✅ **The button system is named** | 110 buttons / 31 visual signatures / 10 button radii → **8, all on a token scale**. The key finding: the system was **already 90% emergent** — pills are choices, 10px rects are commits — and had simply never been written down, which is why it broke just often enough to be unlearnable. Tokens + 4 role classes (`.btn-commit` / `.btn-choice` / `.btn-quiet` / `.btn-link`) in `viewer.html`, plus the rule that **a new visual signature is a claim that a fifth role exists**. 118 literal radii converted to tokens with zero rendering change. **Standing rule 1 is NAMED by this, not altered.** |
| ✅ **The by-day rainfall strip stopped overflowing** | `repeat(7, 1fr)` floors every track at the widest cell's min-content, so the single 5-char label ("Today") sized all seven → ~21px past the right edge, **only in A+ on a phone**. `minmax(0,1fr)` + `min-width:0`, plus a 4+3 wrap at A+. ⚠️ **Verified by arithmetic and mechanism, NOT at a true 390px A+ viewport** — no reproduction was achieved this session. Worth one look on a phone in large text. |
| ✅ **The affirmative pill stays a pill at any height** | `22px` was a pill only because `min-height` happened to be 44px; `text-lg` raises it to 52px and left the radius, so the app's one "we heard you" control became a rounded rect sharing the Save button's silhouette. Now `999px`. Touched a ratified component — **Paul's explicit go, 2026-08-02.** |
| ✅ **The weather card says each thing once** | Root cause was **two independent rule cascades over the same four inputs** (`generateGardenerInsight` first-match-wins vs `generateAlerts` all-match) at **mismatched thresholds** — rain 0.05/0.02, saturation 0.5/0.75 — so a light-rain afternoon had one saying it was raining and the other silent. Thresholds aligned to the lower, every rule keyed, the glance's key subtracted from the alert list. Six rules stopped baking figures into their sentences; the frost low keeps its number (it appears nowhere else, and there the figure IS the meaning). Collapsed header appends the temp as its own element rather than forking the generator. |
| ✅ **The fungal advisory has a subject** | Paul: *"is that a plant thing, or what is that?"* It had none. `property.json:100` names boxwood blight / powdery mildew for exactly this condition and boxwood is in canon. Now *"Fungal weather for the boxwood and anything crowded — water at the base, not the leaves."* Deterministic lookup, ordered by property.json's own risk language, degrades honestly if canon changes. |
| ✅ **The confirm-card action row is even** | Paul: *"that's all of them is kind of on top of a row where something's missing."* A wrap artifact: three pills needing ~394px in a 336px card, so the deferral wrapped alone leaving ~200px of empty row. Now a 2-col grid with the deferral spanning. **A true 3-up is not reachable at 390px** (three even cells give 106px; "I haven't looked" needs ~136px at her type floor) — Paul chose 2+1 over dropping below the floor. |
| ✅ **`＋ Add a note` → `Tell me in your own words`** | Recommended 2026-07-26, never shipped until now. Names what opens, in her register, and drops a glyph carrying neither decision nor topic. **Paul-approved wording, 2026-08-02** (Mom-facing authored content — gated, then pushed). |

**⏸ HELD — do not ship before 2026-08-10.** The launcher's 🎤 glyph and the `.fish-tab` restyle (which
would return the filled dark-green pill to the affirmative and give the app ONE tab grammar). Both sit
inside the clean measurement window above, and the 07-30 shell restyle already spent the one recoverable
confound. **Gate: 2026-08-10** — take them in the same change that reads the launcher's result.

**⛔ DECLINED, with reasoning recorded at the call site in `viewer.html`.** The audit proposed pulling
`.mom-queue-addnote` into the verdict row as a fourth choice chip. That would have reversed **both**
halves of Paul's 2026-07-14 steer — full-width, *"as legible as the answer buttons"*, with the dashed
border marking it a **modifier, not a 4th answer**. The ragged-row complaint is fully answered by the
grid without it.

⚠️ **A premise in that review was WRONG and is corrected here rather than quietly fixed past.** It
justified two findings on "A+ is the mode Mom is most likely using" / "Mom's configuration." **Line 101
of this file says the opposite from telemetry: she has NEVER fired the A/A+ toggle; Paul's device fired
it 22×.** A+ is *Paul's* mode. Both fixes stand on their own merits — an overflow is a defect for
whoever meets it, and a shape rule holding in only one of two text modes is broken in both — but the
premise must not propagate. The standing guidance is unchanged: **tune the DEFAULT type scale, not
`body.text-lg`.**

---

## 🔭 Two-pass fresh-eyes review — RAN 2026-08-03; punch list mostly FROZEN

The founding `/ux-sweep` run (skill formalized same day, `~/.claude/skills/ux-sweep/SKILL.md`).
Full trail + adjudications: **`.ux-reviews/2026-08-03-two-pass-fresh-eyes-pilot.md`** — status of
each punch item lives THERE; this section is the pointer plus the gates.

- ✅ **Shipped 2026-08-03:** Save-button geometry freeze (`6c5d462`) · Mama's Perspective folded
  receipt + one-question view + one Write-me-back (`05db30a`, release-noted `a584cb1`).
- ⛔ **Declined `[paul-stated 2026-08-03]`:** always-slim feedback ribbon — the chat bubble stays
  as-is; momentary cover-ups accepted as natural. Recorded in the review file; do not re-propose.
- ⏸ **11 punch items PARKED behind Paul's freeze:** *"let's not do too much more before getting
  mom's feedback."* **Gate: Mom's next feedback round lands, or Paul lifts it by name** (he lifted
  it once, scoped to the Mama's Perspective card only). Includes the one-line `FN_STORAGE_KEY` TDZ
  fix (vehicle field notes silently never render — invisible to Mom, eligible on a word) and the
  clock/pressure/provenance unifications.
- ❓ **Parked questions `[paul-stated 2026-08-03]`:** machine status chrome (skipped, revisit
  someday) · Mama's-card default-collapse / strip altitude (explore later).

| Kill | Because |
|---|---|
| **The moss card as an experiment** | The experiment already ran — `q-almanac-name` was the same cell (zero wrongness, her words, answerable from a chair) and was answered in a day. Moss is **her** topic, so all three hypotheses predict she answers it: **it discriminates nothing.** And the record already shipped 7/26. → converted to the Tier-2 return leg |
| **W8·b ①** (the rainfall type-scale inversion) | **Shipped 7/26** (`0ef98e5`) — gauge 18px, by-day 16px, ERA5 15px, plus 8 `body.text-lg` rules. It was written on 7/29 describing the **pre-fix** state and was used as the axis table's worked Tier-1 example. **Deleted, not folded** — a spot fix is exactly what the typography pass would undo |
| **The 80K digest gate** | A tripwire nobody acts on, computed by an estimator that under-reads ~13–15%, proxying a **$2.47 lifetime** cost. It was crossed 16 days before someone wrote "back under the ceiling" |
| **"Does per-card Add-a-note earn its place?" as a question for Mom** (W7) | Verdict-shaped, about our own design, and answerable from telemetry we already hold (`context.section`) |
| **Add-a-photo-on-card** | Adds a **sixth** input affordance to the surface we are disambiguating |
| **"Tell her a wrong answer costs nothing"** | Reassurance the product has not demonstrated erodes rather than reassures. The queue header already says it once, correctly. Instead: make wrongness structurally impossible, and fire a receipt at the disagreeing tap — *"Noted — the record had it wrong, and now it doesn't."* |
| **R2-vs-Drive backup row** · **candidates/devices into the digest** · **LLM-judge in harness v1** · **vector DBs** · **any framework/bundler migration** | No askable question, or over-engineering for a two-user app at 0.54 turns/day |

---

## ⚖️ TRACK A vs TRACK B — the ranking that had never been made

**Track A first, and the carve-out turned out to be unnecessary.**

- **B1 is no longer deadline-bearing.** The overdue registration that made it *"the only clock-bearing
  track"* was **resolved 2026-07-22**; the next hard date is **2027-06-03**, ~10 months out. **Track B
  has no clock at all right now.** *(Real residual: `lastEmissions` still reads 2025-06-30 @ 75,731, and
  Fulton/DeKalb requires it annually — the kiosk renewal implies 2026 compliance was on file but the
  card was never updated. And MaxCare expired ~April 2026, so there is no warranty coverage today.)*
- **Track B waits** — not because Mom's product matters more, but because **Track B's decisions come
  from Paul's own knowledge and are unaffected by the noise, while Track A is actively accruing bad
  evidence.** Track B is *pausable without loss*; Track A is not.
- **B6 household systems is Track A wearing Track B's clothes** — she proposed it. It sits in Tier 2.

---

## 👤 WAITING ON PAUL — and nothing else is

| Item | What's needed | Where |
|---|---|---|
| **🔑 Ambient Weather key rotation** | ⏸ **TABLED 2026-08-02 by Paul mid-flight** — resume when he has gas for it. **Step 1 of 3 is DONE:** the Worker proxy exists and is deployed — `/api/ambient`, cached 120s, `limit` clamped, upstream error bodies never echoed (an Ambient error can quote the query string back). It is **deliberately ahead of the token gate**: the call it replaces was a direct browser fetch that worked on every device paired or not, and the token is pasted per-device, so gating it would blank the conditions on Mom's device — the 2026-07-16 silent-void failure. `/health` reports `ambient:false` until secrets exist. **Step 2 is PAUL'S and needs a real TTY** (`npx wrangler secret put AMBIENT_APP_KEY` / `AMBIENT_API_KEY` from `worker/`, values at `viewer.html:6990-6991`) — ⚠️ never via `!`, which uploads an EMPTY secret and prints success. **Step 3 (agent):** switch viewer.html to the proxy and strip the literals — **deliberately NOT done yet**, because doing it before the secrets exist breaks the live dashboard. Only then does Paul rotate, and by that point no key is in any client so the rotation is final. | C |
| ⏸ **The zone map now reads ALL-DASHED** — **HELD 2026-07-31, and so is all further zone work** | All 10 zones are `status: draft`, so the draft-rendering that shipped 7/28 made the whole map dashed. If that reads as *"nothing here is settled,"* the fix would be to **confirm the zones we trust**, not soften the rendering. **Paul's call 2026-07-31: hold this AND every other zone thread** — *"hold on any further zone work, basically, until we get some signal from mom that the zones are important."* **UN-PARK TRIGGER: a signal from Mom that zones matter to her** — a zone-audio recording she initiates, a zone named or corrected in her own words, a Guru question about a zone, or anything she says to Paul about the map. Today the front door has **0 taps from her device** and none of her four real inputs has ever been about a zone, so this is holding a thread that has no demand behind it — the [[feedback_defer_affordances_pending_signal]] posture applied to a whole track rather than one affordance. ⚠️ **What this hold covers:** confirming zone `status`, the naming-completeness pass, `zoneAffinity`, zone-journey v2, the map-highlight. ⚠️ **What it does NOT cover:** `zoneId` assignment on the 23 null plants (re-derived 2026-08-02; was 24 when the record held fewer plants) (that is canon structure, invisible to her, and W9's soil fold depends on it) and W9 soil sampling itself, which is Paul's fall fieldwork and waits on nothing. | A2 · ⏸ HELD |
| ~~**🗣 Get Mom to adopt "Almanac"**~~ | ✅ **DROPPED FROM TRACKING 2026-08-02 — Paul: *"go ahead and drop number seven, like, you don't need to keep track of that."*** Not reversed, not decided against — simply no longer a tracked item. He owns the conversation with her and does not need a row reminding him. The 07-30 release note already told her *"If a different name suits you better, say so,"* so the door stays open in the app without anything here. **Do not re-add a card** (one was drafted and deleted on his instruction). | Tier-3 STEER #8 |
| **🪑 Approve a batch of bench cards** | ✅ **VERIFIED 2026-08-02 — nothing is urgent, and nothing is owed to her.** Live queue is **full and healthy**: 5 confirm cards, every observable exists today, **0 open slots**, so approving changes nothing until one is answered. *(Checked the scare too: 6 cards read as `active`, but `SUPPORTED_KINDS = ["confirm"]` filters the open-standing card out of the 5-slice and `generalQ()` renders it separately as the foot-line — it is visible, via a different path. Not a bug.)* Loop state: **nothing ready to fold, nothing pinning the watermark, no unanswered notes** — `q-almanac-name` already handled-then-retired, which released the ceiling. **Supply is now 10 in the pipe** (8 on the bench + virginia-creeper and wild-violet, newly harvestable after M1). **RIPE:** `q-fairway-grass-seedheads` — flip in August, the seed spikes are the tell. **SKIP (out of season):** `q-spiderwort-bloom`, `q-hydrangea-dreamcloud-bloom` (reopens 09-01), `q-lizards-tail-bloom`. ⚠️ **`q-wild-violet`'s observable is "the little purple flowers next spring"** — the harvester flags it; do not serve it in August. **The constraint has flipped:** it is no longer supply, it is the gate and the cap. An agent may run the report; only Paul runs `--approve`. | A3 · "THE BENCH" |
| ~~**178 season notes await a spot-check**~~ | ↻ **REFRAMED 2026-08-02 — Paul will not read 178 notes, and the row should stop asking.** *"I'm not gonna do that realistically… let's have a strategic way of going through this."* Two facts reshaped it: **only the CURRENT month ever renders** (`plant.seasonNotes[String(currentMonth)]` — August is 24, never 178), and the v7 rule **forbids date claims outright**, so the notes are deliberately unfalsifiable by timing. **Tier 1 SHIPPED:** `tools/check-season-notes.py` audits all 178 against each plant's own bloom/care months — heuristics over prose, so it flags and never fixes. **Tier 2 is dead as written** (Paul reading a month) — he wants **Mom** doing this, in the app, inside the existing capture/feedback loop. **Tier 3** = season-note cards that ask about the OBSERVABLE the note describes, never about our prose; unblocked by M1 but competes for the 5-slot cap. | A2 · #8 |
| ~~**Three relayed conversations**~~ | ✅ **CLOSED 2026-08-02 — Paul: *"let's just close nine. That seems out of date."*** Retires Tier-3 **#3** (the household-repair question feeding B6) and **#4** (what she expected to happen when she reported the rainfall number), both authored off the 2026-07-26 feedback, plus the categories follow-up. ⚠️ **What closes with them:** #4 was the only planned read on *what she expects back when she reports something* — the return-leg design now proceeds on inference rather than her account. If that gap bites, re-ask it fresh rather than reviving these — a question written for a week-old moment is the staleness that closed this row. | A1 · B6 |
| **GTI spare key + service bundle** | ~$450–500 dealer job, no clock. Bundle with the Chamblee recall trip | B1 |

---
# TRACK A — Mom's field journal

*Intent: a hyper-personal field journal that helps Mom see and record what only she can know from standing on the ground — and gets more trustworthy the more of her ground-truth it folds back in. Glance → repository → loop. Capture stays deterministic and AI-free; AI lives only on the ask path, behind Paul's gate.*

## A1 · ✅ PASSED 2026-07-26 — the engagement question is answered; the return leg is now the work ⭐

*Original intent: the whole track was gated here — does Mom actually engage? **Paul's call 2026-07-26: A1 is PASSED and the gate is RETIRED.** Track A is unblocked.*

**Why it was blocking on a bar nobody wrote.** The gate enforced here read *"a **non-gimme answer** AND a later-day return,"* credited to the user-researcher's 7/13 panel. That document says something different — line 138: *"**Grow / validated:** Mom answers ≥1 confirm — tap **or** note — on a day the confirm was `viewed`. **Even one genuine 'not sure' counts.**"* The phrase "non-gimme" appears **once** in that file, at line 142, under a heading reading **"Guardrail (health, not success)"** — a caution about agreeableness artifacts, never a success criterion. The hardening came from a 7/16 restatement and was attributed backwards. **By its written terms the gate fired 2026-07-13**, the day the probe shipped. Track A sat blocked ~2 weeks on a drafting error. *(Found and self-reported by the user-researcher, 2026-07-26; verified independently against the source document before acting.)*

**And the hard version was an invalid instrument anyway.** It measured the **confirm tap** — the one path her stated fear of being wrong blocks — while she contributed freely through three others: two real Garden Guru questions with a genuine follow-up, self-reported repeat opens, a discoverability report, an unprompted new domain *with its IA*, and moss the record didn't know existed. It was also confounded (the confirm surface has never had a fair trial: A-or-B questions on Yes/No buttons, "Ask me later" as the not-sure label, and **not one loop ever visibly closed**) and measurement-invalid (Safari ITP can erase "later-day return" on exactly the intermittent usage it tries to detect).

**⭐ The failure condition has flipped, and this is the point.** It is no longer *"is she engaged?"* — she is. It is ***"did we answer her?"*** — **which is ours, not hers.** What replaces the gate:

| Metric | What it measures | Threshold |
|---|---|---|
| **Arrivals / month** | ground-truth arriving unprompted through **any app channel, plus whatever Paul relays** | trend, not a bar. **Text is NOT a channel** (Paul, 2026-07-26) — the app is the feedback mechanism; no automatic read of her messages exists or is planned, and nothing here may depend on one. |
| **Fold latency** | arrival → in the record | days, watch the tail |
| **R1 · ack staleness** | `today − MOM_ACK_DATA.answeredOn` | 🟢 ≤3d · 🟡 4–7d · 🔴 >7d — **ping on amber**, red means it already failed |
| **R2 · unacknowledged arrivals** | input newer than the ack (count + age of oldest), read from `/api/feedback` + `/api/observations` + `/api/zone-audio` | any ≥1 surfaces · oldest >72h = 🔴. **No text ledger** — the design engineering-partner and user-researcher both leaned on is removed by Paul's channel call, and the check is simpler for it. |
| **R3 · specificity** | does the ribbon name the newest arrival's topic? | boolean; false = stale even at green |
| **R4 · delivery** | — | **explicitly unmeasurable.** `momack_shown` is exposure, not receipt. Never read it as "she felt heard." |

*R1–R4 from the user-researcher; the mechanism to compute them is engineering-partner's `check-mom-ack.py` + an `acknowledgedThrough` field (Track C). **R1/R2 are PROCESS metrics — green means the loop works on us; the arrow from there to "she gives more" stays an `assumption`.***

### 📣 Mom gave us a lot of feedback on 2026-07-26 — summary

*Her exact wording lives in `.private/mom-feedback-2026-07-26.md` (gitignored). **This repo is public**, and her words about her own uncertainty are hers to publish, not ours — same rule as her zone-audio blobs. Purely personal conversation from that thread is recorded nowhere in this project. What follows is the substance; the design findings it seeded are tracked as rows across A1/A2/A3/A6/B6.*

Mom provided this feedback (texts to Paul, plus in-app activity the same morning):

- **She loves the Weeds section** — and told us why: it puts her adversaries in plain sight. It's the one section that **asks no verdict of her**, so there's nothing to get wrong. Built as reference; she uses it as a **target list**.
- **She's using it repeatedly and notices it improving** — *"Its getting better every time I open it."*
- **She told Paul she hesitates to answer the confirm cards because she's afraid of being wrong**, and was visibly relieved when he reassured her. This is the mechanism behind the whole confirm-queue funnel, stated by the user unprompted → **worked as the headline row in A3.**
- **She asked Garden Guru two real questions** (8:57 and 9:00 AM ET) — how to use the rich water off the pond filter, then a follow-up on a term Guru used that she didn't know. **Meanwhile she left unanswered the confirm card she had just said she'd answer.** She'll ask; she won't answer.
- **She asked how to find her past questions again** — *"Is there a way to look back at these, eg in the 'journal'?"* → **A6, promoted.**
- **She proposed a whole new domain and its structure, unprompted** — household systems (furnace, hot water heater) with make/model/age and attached receipts or service orders, and she derived the vehicles / equipment / household-systems split herself → **new section B6.**
- **She's tending moss the record doesn't know exists**, and shared the buttermilk-slurry technique for it → **A2.** (Buttermilk was tracked as a Tate-trip shopping item — **❌ KILLED 2026-07-27, Paul's call:** *"that's not something we actually need to do. Mom can do that if she's excited."* The moss RECORD work in A2 is unaffected and stays open; only the errand is dropped.)
- **A bug she was exposed to:** Guru told her the pond sits at 2,800 ft — that's Lake Sequoyah's elevation, not the property's 2,959 ft → **A6.**
- **⭐ A SECOND app report, in the app, that nobody had seen** (9:20 AM ET, surfaced 2026-07-26 by the new `check-mom-ack.py` on its **first run**): she finds the **7-day rainfall figure** confusing and does not believe it. Row below. Worth noting *how* this arrived — through the general-feedback box, i.e. **the affordance the 7/13 panel said DON'T BUILD and her own ask reopened** (see W5 / the KILLED table). It sat unread for four hours in a repo full of people looking for exactly this.

| Item | What it is | Gate / next |
|---|---|---|
| **Mama's Perspective — validation gate** | Mom answered her **first 2 confirms** (7/13, both Yes → folded to canon). The loop was then **automated end-to-end** (7/14, full-panel design): harvest → serve → answer → read → assisted-fold → visible provenance chip (guess→"confirmed on the ground") + reseed, guardrails (cap 5), AI-boundary doctrine. Live queue now 5 varied cards (variety + bloom + a reflective "armchair" card). Loop spec: `CLAUDE.md` "Mama's Perspective" section; tools: `harvest-questions.py`, `fold-answer.py`, `read-mom-feedback.py`, `reinline.py`. **🪦 GATE RETIRED 2026-07-26 — read the section header above, not this cell.** ~~**Grow** = Mom answers a *non-gimme* (a "Not quite"/"Not yet") AND returns on a *later day* (user-researcher's V1+V2) → then build the deferred surface + AI log-summarizer. **Kill** = `offered`+`viewed`, zero `tapped` → dead affordance.~~ *(Kept as history: this is the bar that was mis-transcribed from the 7/13 panel and then blocked Track A for ~2 weeks. The replacement metrics are the R1–R4 table above.)* Watch `momqueue_offered/viewed/tapped/answered` + `firstOfferedAt` latency. n=2 is one episode, not validated — don't over-scale. **⚡ 3rd answer WORKED THROUGH THE FULL LOOP 2026-07-22 (run 1):** `q-panicle-hydrangea-bloom` → "Looks right", answered **Sat 7/18 10:59 PM ET** (paired device; Paul attributed to Mom, 99%). **V2 (later-day return) FIRED; V1 (non-gimme) still outstanding.** Folded as a second-witness bloom note (canon was already verified — the card was stale-premised); card retired; **acknowledgment ribbon shipped** (`MOM_ACK_DATA` — standing top-of-queue "we got your feedback" strip, replaced-not-cleared each fold, Paul's design); harvest re-run (no new candidates; August drafts are next). Full run log + findings: `.engineering/2026-07-22-mom-loop-first-run.md`. |
| **✅ DECIDED 2026-07-26 — ~~Re-examine the gate itself~~; it WAS measuring the one path she cannot use** ⭐ | The keystone fires on *"a non-gimme **answer** AND a later-day return."* On 7/26 Mom produced: a real unprompted Guru question about her own pond filter, a genuine follow-up when it used a term she didn't know, self-reported repeat opens (*"Its getting better every time I open it"*), a discoverability report, and a full new-domain proposal with its IA — **and still zero confirm-card answers**, because *answering* is exactly what her stated fear of being wrong blocks (her words: `.private/mom-feedback-2026-07-26.md`). **A gate keyed to `answer` can stay red indefinitely while she is visibly, richly engaged.** | **✅ CALLED THE SAME DAY — A1 PASSED, the gate is retired** (section header above). Paul took the re-cut: a non-gimme **question**, a **follow-up turn**, and an **unprompted feature ask** all counted as evidence the loop is wanted; `answer` survives only as a narrower signal about the *confirm surface specifically*, which is now the A3 "FIX THE ASK" work. Nothing is waiting on this row — it stays as the reasoning. |
| **Measurement integrity — a deviceId is not a person, and Safari evicts it** (2026-07-26) | `deviceId` = `d-<8 random>-<8 random>-<base36 Date.now()>`, minted once into `localStorage`. No hardware fingerprint, nothing location-aware. It identifies **one browser storage bucket** (origin × browser × profile), so it **splits** on a different browser, a Home-Screen web app vs a Safari tab, private browsing, cleared site data, a new phone — **and on Safari ITP evicting script-writable storage after ~7 days without interaction**, which silently re-identifies an intermittent user as a stranger. It **merges** on a shared phone. **Proven live 7/26:** Mom's Guru conversation came from a bucket absent from `people.json`, while her mapped id showed 0 conversations — she uses **at least two**. Also: `analyze-fernwood.py`'s `first-seen` column is **range-scoped**, not first-ever (it read as a "new device" when the id's own embedded timestamp says 2026-05-21). | **Read before trusting any engagement number.** Decode the id's trailing timestamp rather than trusting a range-scoped column. The ITP window means "does she return?" can reset on exactly the usage pattern it is trying to detect — weigh that before the funnel is treated as a verdict on her. |
| **🌧️ "I don't believe it's literally the past seven days" — she's right, on both counts** ⭐ (Mom, 2026-07-26 9:20 AM ET) | Left in the **general-feedback box** and unread for four hours; surfaced by `check-mom-ack.py` on its first run. Her words (app feedback, quotable): *"It's confusing to read the rainfall over the past seven days because I don't believe it's literally the past seven days. Today it shows as .14 inches over the last seven days, which is certainly not correct in a perfect world. I could look back and see the rainfall by day."* **Verified in code — she is correct twice over.** ① It is **not** the past seven days: `fetchRecentPrecip` builds `past7days` from `todayIdx - 7` up to *but excluding* `todayIdx`, so it is the last seven **complete** days and **today's rain is not in it**. ② The number is **ERA5 / Open-Meteo gridded reanalysis, not her own rain gauge** — while the on-site Ambient station block sits inches away on the same card showing a different figure. The rainfall panel labels its columns *"Regional rainfall vs. 25-year historical normal"*, but the **dashboard-strip tease** (`renderDashboardStrip`) prints the same `past7total` **bare, with no regional qualifier** — which is almost certainly the one she read. The codebase already knows which source is better: `daysSinceMeaningfulRain()` deliberately prefers the station, commented *"closer to ground truth at the property."* **And her second sentence is a feature ask that is already paid for:** `past7days` is fetched as a per-day array (`{date, label, precip}`) and **never rendered** — the by-day view she is asking for is data the app already holds. | **✅ FIXED + LIVE 2026-07-26 (`f38c275`).** She was right by **14×** — her gauge had 2.01" for the window the grid called 0.14", including 1.96" in one storm on 7/24 that the ~10–25 km cell never saw. Four corrections, all verified headless: **(a)** the strip now leads with **her own gauge** — `2.00" past 7d here` — falling back to the grid only when history is late, and saying so when it does; **(b)** the window now **ends today**, so "past 7 days" is literally that; **(c)** the gauge tile's `WEEK` was the station's `weeklyrainin`, which **resets on the week boundary** — on a Sunday it read 0.02" two days after 1.96" fell, which is why the headline said *"Dry stretch."* Now a rolling last-7-days that agrees with the row below it, and the headline reads *"Recent rain."* **(d)** the regional columns are relabelled **"Region · 7 Days"** so two numbers measuring different things can't be read as contradicting. Plus **the by-day view she asked for is built** — the per-day array was already being fetched and never rendered. **⚠️ Residual for Paul:** the new user-facing strings (*"past 7d here"*, *"Day by day at our gauge"*, *"Last 7 Days"*, *"Region · 7 Days"*, *"Recent rain · X in the last 7 days"*) shipped as a correctness fix — **a wrong number is worse than an imperfect label** — but the wording is yours to revise. And **she has not been told yet**: `feedback-log.json` records this as *addressed but not acknowledged*, deliberately. |
| **Zone-journey front door + funnel** | The position-1 front-door card → accessible one-zone voice walk → the existing AI-free recorder. **✅ v1 SHIPPED + LIVE 2026-07-17** (`b52ce03`; leads with Eastern Patio). Design + hypotheses: `.user-research/2026-07-17-zone-journey-panel-synthesis.md` (five-lens panel). "Trackable/automated" = a **hypotheses & signals register (H1–H5)** wired to a `flowId` funnel on `/api/metrics` + the **4-week time-box**. | **✅ Read tool + autonomous tracker built 2026-07-17.** `tools/read-mom-funnel.py` reads `/api/metrics`, computes the H1–H5 register + a deterministic GROW/HOLD/KILL verdict (raw counts, no AI narrative), `--pickup`/`--json`/`--notify` modes. A launchd job (`com.fernwood.momfunnel-watch`, daily 8pm ET) runs it with `--notify` and pings Paul only when the engagement state advances (quiet→tapped→captured→returned). Day 1 of the 4-week box: all zeros (Mom hasn't hit the front door yet). Same Grow/Kill posture as the confirm-queue gate above; the non-gimme-answer half comes from `read-mom-feedback.py`. **v2 (gated on v1 lift):** the map-highlight, the richer journey, the off-device summarizer seat. ⚠️ **THE FUNNEL'S NUMBERS WERE THE WRONG PERSON'S — corrected 2026-07-28** (`4878994`). `tools/people.json` recorded Paul's builder device as Mom's, so his own app-opens were counted as her engagement. The two figures that drove decisions — *0 launcher taps in nine days* and *a carousel declined 33/33* — do not reproduce. Corrected from authored CONTENT (all four of her real inputs carry `d-szqlt0h7`; the misattributed device has produced zero feedback records ever, and its only zone recordings are Paul saying 'this is Paul testing'). Builder devices are now dropped deterministically via `excludeFromEngagement` rather than a localStorage flag that had to be re-set per browser. **Time-box re-anchored to 2026-07-13, her first confirm** (Paul's call; the contamination was per-device, not per-date). Clean read: confirm carousel offered 9 → answered **3**; launcher offered **5** → tapped 0. The front door is not a dead affordance — it is barely a sample. |
| **W1** ✅ | **Fix capture** — open write-only `POST /api/feedback` (Paul's chosen auth model, 2026-07-16); await the POST, ack only on 2xx; durable outbox (replays her 7/15 words off the MacBook for free); a dark device must announce itself; `people.json` attribution is invalid (Paul shares his phone with her). | **✅ SHIPPED + LIVE 2026-07-16** (`33541bf`). Worker deployed; Pages serving it. **Verified end-to-end on the real origin:** a never-paired browser on `palekxk.github.io` POSTs to the Worker → `{"stored":1}` → ack reads "Noted — it's in the record. ✓". Worker properties (curl): unpaired POST 200; replay of the same id → `duplicate:true`, no double-write; GET still 401; 9KB → 413. Client (headless Chromium): offline → queued copy + words held; reload → replays + drains; send *and* disk both failing → honest failure copy, claims nothing, her text survives the ack timer. Baked `FEEDBACK_ENDPOINT` was required and unplanned: `cfg()` needs a token AND a url, so a dark device had nowhere to POST. **⚠️ Pages did not rebuild on the push** (served the 7/14 bundle for ~45 min); an **empty commit** re-triggered it and it went live in 60s. If a Fernwood change ever seems not to land, check `curl -s <pages-url>/viewer.html \| grep -c feedbackOutbox` before assuming the code is wrong. |

## A2 · The record about her place

*Intent: make the record about HER ground — her zones, her plants, her own photos of her own individuals — not a stranger's stock species photo.*

| # | Item | Status |
|---|---|---|
| **W2** | **Zones — Paul draws, she reconciles.** Paul walked them with her; he's sure; he draws (not relitigated). Tag each `heard-from-her` vs `paul-inferred`. Fix the `property.json.propertyZones` placeholder-stub SSOT break. **Demote the confirm button** — ask *"which of these is wrong?"*: a confirm cannot surface an omission, and her base rate is 2-for-2 Yes. The disagreement UI **already exists** (built May, never pointed at her). | **🟢 UNBLOCKED — Paul draws.** Schema v2 shipped + Worker deployed. Open Property card → **+ Add a place** → tap → name. 8 zone **names/ids survive** (`zoneId` refs) with **geometry cleared** — the v1 polygons were welded to the oblique 2015 image and are unsalvageable (git has them). **Then: assign `zoneId` on plants** (**23 of 36 null as of 2026-08-02** — the old "24/26" was written when the record held 26 plants; the denominator moved and the row did not, so it read as *nearly all* when it is now ~two-thirds) — Paul's "most important part". **✅ 7 zones drawn + synced to canon 2026-07-17** (durable in git HEAD + KV, 7 geometries): Pond Area · Stable Grounds · Eastern Patio (Paul: *replaces* the old Eastern Garden placeholder) · Western Garden · Fairway Fringe · Lower 40 · Upper-Uber Wall Area. **Undrawn/reserved:** Fairway (holds the `fairway-turf` plant ref — draw later) · Parking Bank. **✅ Reconciliation DONE 2026-07-17** (`646d57e`+`c5e5e07`): renamed the 4 `-2` ids → canonical, dropped 6 empty ghosts (eastern-garden, pond-area-3, + 4 same-name empties); 55 vertices preserved exactly (verified via assertions before writing). Canon = **9 zones** (7 drawn: pond-area, stable-grounds, eastern-patio, western-garden, fairway-fringe, lower-40, upper-uber-wall-area · 2 reserved-empty: fairway, parking-bank). Plant refs valid: `fairway-meadow`→`fairway-fringe` now has geometry; `fairway-turf`→`fairway` awaits the fairway being drawn. Written via `/api/zone-save` (atomic KV + git + viewer re-inline), local pulled, `check-data-inline` clean. **→ NEXT: assign `zoneId` on the 23 null plants (re-derived 2026-08-02; was 24 when the record held fewer plants)** (Paul-driven; the W2 payoff). Drawing-tool fix shipped same day (hollow scale-compensated vertex markers, `e3eeeed`). |
| **W3** ✅ | **"What's growing here?" — voice, not text.** Tap a zone → 🎤. Her constraint is *text*, not speech (22 A/A+ events). **Store the audio, not the transcript** (Web Speech mangles the nicknames we're mining for). Wire the existing `createVoiceCapture` to every free-response field. Map to position 1, time-boxed 4 weeks, **no wizard/counter/progress**. | **✅ CAPTURE SHIPPED 2026-07-17** — tap a zone → **🎤 "What's growing here?"** → her verbatim voice stored, AI-free. Backend: new **write-only, no-token, DURABLE** `/api/zone-audio` (mirrors the /api/feedback W1 doctrine so an unpaired device still captures; unlike /api/audio-upload's 1-hr TTL, which would delete her words before Paul heard them; blobs in token-gated KV, **never git** — repo is public). Reuses `createAudioCapture` (parameterized idle emoji/label). Honest ack: saves only on 2xx, else "couldn't save — try again" (no false success; audio too big for a localStorage outbox). Review: `tools/read-mom-zone-audio.py`. Worker deployed + curl-verified end-to-end. **The schema/instance-model gate is sidestepped for capture** (store audio, don't assign zoneId — folding stays Paul's off-device call). **→ front-door v1 SHIPPED 2026-07-17 (see A1).** |
| **W6** | **⭐ The instance model — the photo is an IDENTITY KEY (Paul's correction, 2026-07-16).** She wants **her own photos of her own plants** so it's unambiguous **which individual** we mean — there can be **multiples of the same plant across zones, or several in one zone**. Not sentiment, not ownership: a stock species photo is useless *by definition*. **This breaks the schema:** `plants.json` is **species-level** (26 records); reality is **instance-level** (variant × zone × count × her photo of *that* individual). So `zoneId` on a species record is incoherent where a species spans zones — **the 24/26 nulls may be partly a schema failure, not missing data** — and her 7/13 "yes, 'Lucifer'" answered for the *species* when the crocosmia may not all be Lucifer. Precedent already straining: the hydrangea hub-and-roster. **Needs its own path-eval; do NOT quietly widen `plants.json`. Reframes W3:** "what's growing here?" is not a lookup against the 26 — it's an inventory that doesn't exist yet. **Interim procedure now written down:** the **Plant taxonomy & organization rule (v1, 2026-07-22)** in `CLAUDE.md` codifies the four current shapes (separate record · `variety` field · hub-and-roster · this deferred instance case) and names *this row* as the revisit gate — when the "same species, several individuals across zones" case shows up for real, that's the trigger to design the instance model and cut the rule to v2. | IDEATION — blocks the deep "inventory against the 26" work (NOT the audio capture, which sidesteps it). **v1 taxonomy rule is the interim procedure; this row is its v2 gate.** |
| **W4** | **Photos on confirm cards** (her ask #4). **Two halves, now split:** ✅ **(a) Reference photo + honest source — SHIPPED 2026-07-20** (`buildCard`). A confirm card about a plant we hold a photo for now shows it, captioned **"A reference picture — not one taken here"** + the Wikimedia credit (a property-submitted photo says **"taken here on the property"** instead) — so a match is HER judgment, and the reader knows it isn't a photo she took (Paul's ask). Reuses `plants.json` `photo`/`attribution`; degrades silently to no image. Also **fetched the panicle-hydrangea photo** (it was photoless, via `fetch-photos`/`wire-photos`) + **fixed a `q-panicle-hydrangea-bloom` entityRef id mismatch** (`panicle-hydrangea`→`hydrangea-panicle`). This **retires the "don't ask what you can't show" interim rule for reference-showable plants** and moots re-opening the two 7/13 answers (they now show a reference). **(b) Her OWN photo of her OWN individual — still gated** on the W3/W6 instance-model path (a stock photo is an identity-key non-starter per W6). | (a) ✅ DONE 2026-07-20 · (b) GATED on W3/W6 photo path |
| **Add-a-photo affordance on the card** (Paul 2026-07-20 — *"add the option to add a picture there"*) | An affordance ON a confirm card to **attach** a picture — the input analog of the reference photo (a). **Validate with Mom before building:** does she want to add a photo from a card, and is the card the right place vs. the zone-voice capture (W3)? Ties into **W6** (her photo = the identity key) and the **W7/W5** "too many text-entry surfaces" question — don't add a per-card photo picker without checking it earns its place in the solicitation stack. | IDEATION — validate with Mom; pairs with W6 + W7 |
| **"Our Tate story" — Property-card narrative from the family email archive** (2026-07-22, from the takeout mail mine). The family's lifelong Tate membership now has a primary source: 515 msgs 2010-2026 in iCloud (see research-resources.md Category 7 — TMC board minutes, Mom's 2011 directory work, Johnston boundary, Silt Pond, Poetry of Tate). Candidate: a membership-arc narrative on the Property card connecting Sam Tate history → the family → Fernwood; TMC minutes can ground community-infrastructure facts (dam, pond, roads). **Gate:** Paul curates which family details go public — the repo is public, archive stays in iCloud. | IDEATION — needs Paul's curation pass |
| **W9 · Soil truth — test by zone** ⭐ | **The single highest-accuracy fix in the whole app** (2026-07-25, from the hyperlocalization audit). **No soil test has ever been run** (Paul confirmed), yet ~18 plant `soilNotes` assert a native pH ("4.5–5.5") and amend-advice as settled fact — and the pH range they inherit comes from **soil series that cannot occur at 2,959 ft** (Cecil/Pacolet are Piedmont, cap 900 ft — audit §2.3). **14 of 27 plants want soil *sweeter* than the assumed native**, so if real pH runs higher we may be advising Mom to lime beds that don't need it. Fernwood already has a **9-zone model**, so test **by zone** (a limed fairway, an acid woodland edge, and a pond margin are three different chemistries). **Detailed plan: `.plans/2026-07-25-soil-sampling-plan.md`** — UGA Routine S1 (~$7–10/sample, Pickens County Extension), fall window, 10–20 cores composited per unit, depth by use (turf 4"/beds 6"/shrubs 8–12"); **Tier 1 = 5 samples (~$50):** fairway · ornamental beds · woodland edge · pond margin · upper-wall/stable. Fold-back: measured `soil{}` on `zones.json` (SSOT), rewrite `soilNotes` to reference it, flip the Property-card recommendation to a dated result. | **ACTIVE — Paul's to sample (fall 2026).** Sampling waits on nothing. Zone-level capture + the Cecil/Pacolet provenance fix don't wait on anything either. The **per-plant** fold is gated on **W2** `zoneId` assignment (24/27 still null). **↩ Merged in here 2026-07-26 (was its own row, "soilNotes W9 sweep", whose entire content was "folds into W9"):** ~15 plant `soilNotes` still name the removed Cecil/Pacolet series + the wrong "clay Bt argillic" physics + an inferred pH. `property.json` is already corrected; the per-plant prose waits on the test result + `zoneId` to be rewritten well. |
| **W0** | **Replace the basemap.** ~~Google Earth screenshot, macOS notification in the sky, oblique, March 2015.~~ | **✅ DONE 2026-07-16** (`b321060`). Now **NAIP 2022-01-10** — leaf-off, nadir, 1500×1500 @ 1.0 ft/px, no chrome, **public domain** (Esri may NOT be redistributed — that killed the Esri candidate; NAIP's leaf-off capture gives the same "see everything" legally). Georeference recorded in `_meta.bounds` + a `.bounds.json` sidecar. |
| **W2-SCHEMA** | **⭐ Vertices are now real WGS84 `[lon, lat]`; the basemap is a swappable VIEW** (schema v2, `b321060`). This is what makes the coming redraw the **last** one: a better basemap (newer NAIP, Paul's own drone ortho) is a **re-registration** (update `_meta.baseImage` + `bounds`), never a redraw. Verified: property anchor projects to frac **[0.5000, 0.5000]**; lon/lat→frac→lon/lat round-trips with **zero** error. 🚨 Fixed a latent data-destroyer first: `sanitizeZone` ran `clamp01()` on every vertex — fine for fractions, but a coordinate collapsed to `[1,0]` (image corner) while the Worker returned **200** and the chip said "live everywhere" — **third in the 7/15 bug family**. Now **rejects** out-of-envelope instead of clamping. Also de-hardcoded **4** coupling sites (incl. a CSS `aspect-ratio` that would have landed every tap offset from where polygons draw) and gave `ZONES_DATA` its first drift alarm. | **✅ DONE** |
| **Property map — zone-naming completeness pass** | The interactive zone map is **shipped** (lives at the top of the Property card, pan/zoom, tap/confirm). What's left is naming/populating the remaining zones + per-candidate `zoneAffinity`. | Paul's zone-naming pass. *(NB: the map surface itself is SHIPPED — the old "paused, don't build" note is stale.)* |
| **⭐ Zones as rich content-containers (not just polygons)** (Paul, 2026-07-25) | Make a zone a **place that holds and references its contents**, not just a named map shape. **Flagship example = the pond:** one Pond zone that surfaces its koi, its pump + filter (and what they do), and every plant planted in it. The plumbing is already half-there — the 10 pond plants carry `zoneId: pond-area`, so a zone view could roll up "what's growing here" for free, and the koi/infrastructure would be new per-zone fields. This is the direction that turns the zone map from a locator into a genuinely useful per-place record. **Explore later** (Paul's steer — a backlog item, not now); when picked up, it also gives the A8 "pond/koi record" its data-model home. **📍 THIS IS THE CANONICAL ROW for the pond (marked 2026-07-26).** Two A8 rows — *"Pond infrastructure"* and *"Pond/koi record"* — are its **spec, not competing items**: they hold the liner dims, the pump/filter/waterfall, the bear fence, and the koi-vs-tadpole tradeoff. Read them for the contents; decide the shape here. | IDEATION — explore later; pond is the worked example |
| **⭐ Input-stack IA — pin the Almanac, transient cards below** (Paul, 2026-08-02 evening) | Paul's distinction from the v2 button-system session: the **Almanac is the standing knowledge door** ("always gonna be there… a consistent thing you can go to to find knowledge") while the other three input cards are **transient feedback asks** expected to change. Candidate shape: Almanac pinned at/near the top; a **super-bare-bones jump strip** above it — "just the name of the card, not taking up much space… super quick access and you don't get lost or bogged down in all the requests for feedback" (his words; he floated "top five cards by traffic"); the transient cards grouped below, **each carrying the unified `.card-later-link` defer element** ("snooze becomes something we wanna have for all of them — a unifying design element"). Changes the ratified 2026-07-27 card order (give-back → her surface → the asks), so it needs mocks + Paul's eyes, not a quiet edit. | ✅ **SHIPPED 2026-08-02 same night** (commits `890513f` → `6d742df`) via two /design-options rounds — R2-C won: jump strip + pinned Almanac + the Mama's Perspective envelope; then the envelope's collapse (main-card Open/Close pill) with the condensed ✓-ack line that never hides, the white composer, and the photo-branch save fix. **Everything further → the 🗓 8/10 RATIONALIZATION block below** (Paul: "let's just keep this as it is — all queued, ready to go for August tenth"). |
| **⚠️ W12 · 23 instrumented events have NEVER fired** `[found 2026-08-04 by tools/check-telemetry.py, first run]` | Every zero on these is **uninterpretable**, and this lap proved that is not theoretical — `jumpstrip_tapped`'s zero was written into this backlog and the cycle log as a finding when it was simply unmeasured. Some of the 23 are legitimately rare (`species_promoted`, `zone_deleted`, `remove_confirmed`). Others are on paths that should have run by now and are the ones to check first: **`momack_tapped`** · **`momqueue_general_sent`** · **`log_saved`** · **`log_offered`** · **`momack_unfolded`** · **`launcher_dismissed`**. Method: trigger each path once by hand and confirm it lands in `/api/metrics` — a fired event is the only proof that the wiring, the deploy and the POST all work. ⚠️ Until then, **do not read a zero on any of them as behaviour.** | **OPEN.** Detector shipped; the triggering is manual and is Paul's or an agent's, not automatic. |
| **⭐ W14 · `build-control.py --selftest` — known-answer fixtures on the claims about HER** `[paul-deferred to pickup 2026-08-04]` — 🥇 **FIRST ITEM AT NEXT PICKUP** | Every other control in this repo ships a `--selftest` that proves it can fail. The page generator — **the one artifact that makes claims about a person** — ships none, which is backwards. Assert a handful of independently-knowable facts and let them fail loudly when the data legitimately moves: **3 ack receipts exist, 2 carry her deviceId, 1 carries none** · her deviceId is never hardcoded, and an unmapped device renders UNMAPPED not `0` · a never-fired event renders the WORD, never a numeral · a detector that did not run makes the position underivable rather than green. **Why it earns priority:** on 2026-08-04 a fix to the attribution filter read the people map off the wrong dict, so the page asserted *"none from a device mapped to her — this is not evidence she was acknowledged"* when **two of three are hers**. A false negative that CLAIMS something, and it reads as rigour. This selftest catches it in ~2 seconds. ⚠️ **The shape it addresses:** three separate failures that day were all *verifying an artifact against itself* — a stale test passing its own regex, a coherent-reading backlog row, a page rendering without error. Self-consistency was accepted as correctness, and all three landed in the minutes right after a change, when a fix carries conviction. The discipline exists here; it was attached to the word "test" rather than to "any derived claim." **Companion rendering rule (already applied once, worth generalising):** a derived claim about a person prints the counts it came from — the corrected receipt line says *2 hers / 1 no-device* on its face, which is why it is now checkable without a trip to the API. | **QUEUED — Paul's call: first item at next pickup, alongside opening the page itself.** |
| **⭐ W13 · Should Gardening be ONE card? — the lead lap-2 decision** `[paul-raised 2026-08-04, deferred with his agreement]` | Her taxonomy has five buckets; the page has six cards for two of them (Gardening → Plants + The Fairway + Weeds; Wildlife → Wildlife + Fishing). Matching her meant SPLITTING for machines and would mean MERGING here. **Against:** we split Machines on 08-04 precisely because sections inside a collapsed card are not glance objects, and merging rebuilds that on the domain with 36 records. **Also against:** the Weeds card holds the only unprompted praise in project history `[validated]`, for asking nothing — which binds wherever that content lands regardless of the outcome. **For:** it is her word and her model, and 1:1 tab→card is what the other three now have. **Gate:** `jumpstrip_viewed`/`jumpstrip_tapped` shipped 08-04, so lap 2 is the first that can see whether she taps Gardening and whether she ever reaches Turf or Weeds. **Changes an organizing model → all three seats.** ⚠️ Whatever is decided for Gardening, decide Wildlife+Fishing the same way or the page is inconsistent again. | **DEFERRED to lap 2 — the 🌿 collision underneath it shipped 08-04 (W11 closed).** |
| **⭐ M2 · Make the SCHEMA match her model, not just the labels** `[paul-stated 2026-08-04]` — ⏸ **GATED ON DATA** | Shipped 08-04 is a **presentation** change: her three categories became three cards off the existing `group` field, and the labels became her words. The RECORD underneath is unchanged — vehicles, equipment and household systems all still live in `vehicles.json`, a filename kept for legacy-identifier reasons, with `group` as the only thing separating them. Paul's rider when approving the split: *"a backlog note to ourselves that we need to not just have the labels, but eventually the schema match Mom's expectations — but we can do that after we have some data that this is the way she really wants to go, or confirm that there's no change from this signal."* **THE GATE IS EXPLICIT AND IT IS NOT A DATE:** do not migrate until either (a) telemetry shows she actually uses the three cards (`card_expanded` / `jumpstrip_tapped` on `card-vehicles` / `card-equipment` / `card-household` — she has **never** expanded the old vehicles card once, so any use at all is new signal), or (b) she says the organization is right. **Absence of complaint is NOT confirmation** — she has never tapped the jump strip either, and told Paul she liked it. What a migration would mean, so it is scoped rather than vague: `household-system` records probably want their own file and their own fields (a furnace has a fuel and a service interval, not a spark-plug gap), and `momlib.DOMAINS` would gain a real domain rather than a `group` value. ⚠️ It would also break `photo-organizer`'s in-place read of `vehicles.json` and the `service:<vehicle-id>:<sr-id>` reference convention — a cross-repo contract, so this is not a quiet refactor. | **PARKED — do not start on the strength of one confirm.** |
| **W9 · The strip is a one-way trip** `[ux-expert 2026-08-04]` | Jumping to Vehicles or Household Systems leaves no route back but scrolling, and the strip is `position: static`. UX's recommendation: **not sticky** — a 3-row strip is >20% of her ~700px viewport permanently on a reading surface — but a quiet return link at the **foot of each expanded card body** (one shared builder), which costs zero pixels at rest and sits where she is when she's done. ⚠️ **Second, separate defect it names:** `expandCard` only ever ADDS `.expanded`, so the more she navigates the longer the trip back gets. Candidate fix is collapsing the previously *strip*-opened card — **scoped to strip targets only, never a card she opened herself.** | **OPEN — not shipped 08-04.** Full finding: `.ux-reviews/2026-08-04-jump-strip-mom-taxonomy-rebuild.json` |
| **W10 · Group item-counts read as a completeness meter** `[ux-expert 2026-08-04, paul-deferred same day]` | `groupHeader()` prints `N items` in 10px uppercase. With the split this no longer puts `1 ITEM` beside `9 ITEMS` on one screen, which was the acute version — but each card still announces its own count, and Household Systems announces 1. Paul, 08-04: *"y'all just not worry about the counts right now."* | **DEFERRED by Paul — recorded so it is a decision, not an oversight.** |
| **W11 · Plants and Weeds share the 🌿 glyph** `[ux-expert 2026-08-04]` | Two cards wearing one mark is the Astronomy/Sky-&-Stars failure the repo already named once. It now also appears in the jump strip, where "Gardening" carries 🌿 — so the same glyph means *the Gardening tab*, *the Plants card* and *the Weeds card*. On a surface where icon+size+color+position carry meaning for a reader who reads with difficulty, that is a real collision. Needs an authoring call from Paul, not a swap by an agent. | **OPEN — needs Paul's pick.** |
| **🗓 THE 8/10 RATIONALIZATION** `[paul-stated 2026-08-02]` — when the clean measurement window closes: one big analysis of her feedback + the queued changes, decided together | Paul's framing: *"a big rationalization and analysis of her feedback and see what changes to make."* **Inputs, all ready:** ① **the add-context change** — remove the confirm card's pre-answer "Write me back," post-tap open field bound to the questionId, kill the 2600ms auto-advance when showing; full spec + draft Mom-facing copy (needs Paul's wording confirm) in `.user-research/2026-08-02-free-text-journeys.md` — **the definitive journeys report; read it first**; ② **strip names by traffic** — replace the hand-picked five with top-cards-by-traffic derived from telemetry. **⭐ SHE HAS SEEN IT AND SHE LIKES IT** `[paul-relayed 2026-08-04]` — Mom told Paul in person that she has been on the app, saw the tabs across the top, and liked them. Provenance is a relay, not her verbatim: do not quote it as her words. **What the record independently shows**, without needing the relay: she was on 2026-08-03, 7:52–7:56 AM ET (device `d-szqlt0h7`, iPhone, one session, 212s — the first session after the strip shipped 08-02, `890513f`), and in it she answered `q-top-categories` **Yes / "That's all of them"** (sentiment `landed`), tapped the ack ribbon's "Got it," and left a voice field note that opened an Almanac conversation. ⛔ **CORRECTED 2026-08-04 — HER ZERO IS UNMEASURED, NOT MEASURED.** This row first said *"`jumpstrip_tapped` from her device: ZERO — so the evidence is she likes seeing it, NOT that she navigates with it,"* which reads as a measured finding. **It is not one.** Paul asked the right question — *zero taps truly, or did we not have telemetry for it?* — and the record answers it: the three events instrumented at 08-02 22:58 ET (`bbf764a`) have between them fired **exactly once, ever**, at **2026-08-04T00:02:55Z = 08-03 8:02 PM ET** — **twelve hours AFTER her only session** (08-03 7:52 AM ET). The other two (`mp_envelope_toggled`, `composer_empty_tap`) have **never fired at all**. So there is no evidence that commit's code was running on the build she loaded, and *she did not tap* is indistinguishable from *nothing could have recorded it if she had*. **Draw NOTHING from the zero.** ([[feedback_absence_of_records_is_weak_evidence]] — report the searched-negative, never promote it to a finding.) `jumpstrip_viewed` shipped 08-04 to give the tap count a denominator; until it carries values, this stays unmeasured. ⚠️ **And the strip does not yet carry her list.** It ships Almanac / Weather / Plants / Wildlife / Mama's Perspective; her five, confirmed by that very Yes, are **vehicles / equipment / house systems / gardening / wildlife**. So this row now has BOTH inputs it was waiting on — her confirmed category list and a traffic derivation — and they disagree; the 8/10 decision is which one the strip is built from, not whether to build it. ③ **defer-on-ack** — decide whether her note section wants the unified defer link (main-session rec: no — a give-back isn't an ask; the envelope collapse covers "not now"); ④ **agent-side card rotation** — a repeatedly-snoozed card rotates to the bench for Paul's review (NO Mom-facing permanent dismiss — graded-feeling choice, starves the queue); gate: the snooze signal actually appearing; ⑤ **`/api/observations` needs-reply state** — the composer predates the 7/26 capture-is-not-a-loop rule and has never been tested against it; blocks any future one-door consolidation; ⑥ **funnel re-derivation** — resolve whether "front door 0/10" = ack card or zone launcher (`read-mom-funnel.py`); do NOT pool across the 7/30 and 8/02 redesign lines. Three NEW events instrumented 8/02 late (commit `bbf764a`) cover the new surfaces — `jumpstrip_tapped {target}` · `mp_envelope_toggled {collapsed, source}` · `composer_empty_tap` — so the window's final week measures them; everything older was already covered. | **QUEUED — hold until 2026-08-10.** Nothing here ships early; tonight's surface gets clean air for the window's final week. |
| **⭐ Free-text journeys audit — three doors, one dead end** (found 2026-08-02, code-verified) | Paul: *"what's the natural path for just free text capture? Do we really expect someone to click write me back?… we need to think through all the journeys."* Verified in code: **three different free-text doors with three different mechanics** — card 1's "Write me back" opens the General-feedback panel; card 2's composer is the real writing surface; card 3's "Write me back" opens an inline note that only rides along **if opened before tapping an answer** — tapping "That's all of them" posts immediately and **there is no add-context moment after** (a real dead end). Candidate direction: one honest door — route the invites into the Almanac composer. Decides whether "Write me back" survives at all; the map is user-researcher territory (journey map: answer-then-elaborate, snooze-and-return, wrong-tap recovery). | IDEATION — journeys before more button work; pairs with the IA row above |
| **🌿 Moss — a plant she planted, invisible in the app** ⭐ (Mom, 2026-07-26) | **Verified absent from all 35 plant records.** She has **new moss plantings** and shared how she feeds them — a **buttermilk slurry**, a real moss-propagation technique, to be recorded as **hers**. She also wants to find more sources of moss on the property, possibly near the barn. She is actively tending something the record does not know exists. Species unknown — mark `confidence: inferred`, `askable: true`; the moss itself is a good low-stakes confirm candidate *if* the confirm surface gets fixed (see A3). | **Add the record** (agent-draftable, Paul approves per the canon rule). **Buttermilk is a Tate-trip shopping item — she asked twice.** "More sources of moss, maybe by the barn" is a walk-the-property task, not a data task. |
| **⭐ Japanese practice — moss management + niwaki pruning** (Paul, 2026-07-26) | Paul and Mom are *"very taken by"* Japanese moss-management technique, and separately by **niwaki** (庭木 — Japanese pruning/training of trees). **Both already have hooks in canon; neither is a new interest bolted on.** *Moss:* the hand-weeding discipline is already written into the new `moss` record and sourced to **Saihō-ji**, the koke-dera, running since 1339 — 120+ species held in an unbroken plane by hand-weeding and sweeping leaf litter, *"carefully, repeatedly, and with restraint."* Deeper study = species selection (sugi-goke/*Polytrichum*, hai-goke/*Hypnum*, sunagoke/*Racomitrium*), sweeping practice, watering regimes, and specifically how **joint/flagstone moss** is managed — which is exactly the Eastern Patio case. *Niwaki:* **`boxwood` already carries a "Selective / cloud pruning" subcategory** (Apr–Jul, hand-prune individual branches for a natural form and better airflow) — that is cloud pruning in all but name — and **`japanese-maple` already has structural (Jan–Feb) and summer-refinement (Jul–Aug) windows**, the classic niwaki subject. So this deepens two care blocks that exist. Standard English source: Jake Hobson, *Niwaki: Pruning, Training and Shaping Trees the Japanese Way*. | **Route through the corpus thread (A6), not as loose links** — this is exactly the "curated depth per topic" the RAG item calls for, and it is a good second topic after pond water. Fold results into `research-resources.md` and back into the two care blocks + the moss record. |
| **Zone `status` is invisible on the map** (found at close-out 2026-07-27) | `zones.json` carries a per-zone `status` (`draft` / confirmed), and the **house** zone is `draft` — an agent's trace off the NAIP leaf-off basemap, not a surveyed outline; its own `history` says *"agent-proposed hypothesis … Paul corrects."* **Nothing in the viewer renders `status` differently**, so a traced guess is indistinguishable from ground Paul walked. That is the same honesty-marker gap the plant cards already solve with confidence chips, and it violates *trust is the load-bearing emotion* on a surface where Mom cannot check the difference. Cheap fix: a dashed stroke or a small chip on `status:"draft"` zones. | Agent-drivable. Paul accepted the house footprint as provisional (*"for now … can always be revisited"*), so this is about SHOWING that provisionality, not re-tracing it. **✅ SHIPPED 2026-07-28** (`4878994`): a non-`confirmed` zone renders with a broken outline + a fill under half the confirmed density — texture and weight, never colour alone, per the Mom-legibility rule; stroke 2→2.5 because dashes remove ~40% of the ink, so a draft zone is quieter, never fainter. Fails safe (anything not `confirmed` reads as draft) and emits `data-zone-status` so the rendered page can be checked against the record. ⚠ **Scope was bigger than this row implied — ALL 10 zones are `draft`, not just the house**, so the whole map now reads dashed. Paul has not yet eyeballed it; if that reads as 'nothing here is settled', the fix is to confirm the zones we trust, not to soften the rendering. |
| **Plants-to-consider gaps** | GFC 2026-27 seedling catalog (~Jul 1), UGA nursery list refresh, TACF, HRI, Mt. Cuba genera. | Time/source-gated. |
| **#8 · Month-keyed season notes** (from the 2026-07-25 audit) | `currentSeasonNote` (single drifting string, was showing May text in July) → decided: month-keyed `seasonNotes` object, card renders today's month, silent otherwise. Schema + voice locked. **AUTHORED 2026-07-26** — schema v7, 170 notes across all **35** plants (not 27; 8 records were added after the audit), avg 4.9/plant, every month covered, render wired + verified across all 12 months. Voice re-derived and re-approved this session (the 7/25 exemplars were never written to disk). Paul added the governing constraint: the note carries the *character* of the month, `peakDates`/`peakWindow` keep the dates — full rule in `CLAUDE.md`, lint-enforced at authoring (0 violations). | **AWAITING PAUL'S SPOT-CHECK** — read the notes on the plants you know best; then the only residual is retiring `currentSeasonNote` (dead in the render already, kept only as a net for a record added without notes). **Worker deployed 2026-07-26 by the agent** — deploy is no longer a Paul step (see `CLAUDE.md`). |

**Drawing-tool refinements (from Paul's 2026-07-17 draw session — 7 zones drawn):**
1. **Vertex markers occlude the space being defined.** ~~They render at `r=16` SVG units *inside* the zoomed canvas, so at 6× zoom a marker is ~96px — it blocks the very ground you're trying to trace around.~~ ✅ Fixed 2026-07-17 (`e3eeeed`): hollow, scale-compensated (`r = k / scale`) markers.
2. **Basemap pixelates on zoom.** NAIP base is 1500×1500 native (~0.6 m/px sensor limit); `MAX_SCALE=6` magnifies ~10× past real detail → interpolation mush. No more true detail exists to show. Interim: cap/tune zoom so it can't over-magnify into blur. Durable: a sharper basemap — **Paul's future drone ortho** (freely publishable). *(The on-disk Esri z19 `base-esri-z19-wide.webp` can't be redistributed on a public site, and the private-repo/Worker-serve migration that would have unlocked it is **not being pursued** — light-privacy recalibration 7/17 — so the drone ortho is the durable path.)* ⭐ Because vertices are now real WGS84 (W2-SCHEMA), a sharper basemap is a **re-registration, not a redraw** — today's 7 zones survive the upgrade.

## A3 · The loop (invite + fold back)

*Intent: the flywheel — pair a fresh localized signal with a calm, timely invitation for the one ground-truth only Mom can give, and fold it back visibly. **A1 PASSED 2026-07-26 — this track is UNBLOCKED.** The loop is wanted; what is unproven is whether *our* half of it works, so sequence the **return leg** (acknowledge → answer her) ahead of any new *ask*. The three bloom/reaction items below are MERGED here (were separate DEFERRED rows) — they are one intent.*

| Item | What it is | Gate |
|---|---|---|
| **"is it open yet?" bloom ground-truth loop** | Invite + fold back ground-truth on bloom timing (the flywheel's concrete trigger). | Part of the loop; Mama's Perspective now carries the first bloom confirm (panicle hydrangea). **A1 unblocked 2026-07-26.** Reframe before building: content-steward's bloom template makes the plant the subject and the hedge *the record's gap* rather than a request for her verdict — *"we have it down to flower around now, though we've never actually watched it here. Is it in flower yet?"* Fixed once in `harvest-questions.py`, it corrects 8 live/staged cards at a stroke. **↩ Merged in here 2026-07-26 — the second leg, formerly its own row ("Bloom in *Worth noticing today* glance"):** surface bloom state in the **top glance**, after de-crowding the "Peak this week" area. It shares this row's single fix and must not run ahead of it — **hold until the ask is re-shaped**, because surfacing more verdict-shaped questions first would spend the A1 unblock on the one format she declines. |
| **Fairway / change-reactions confirm** | "Does the hub match the property?" reaction-to-a-change question (the Mama's Perspective schema already supports `kind: react`). | **DEPRIORITIZED, not killed (Paul, 2026-07-26).** Both reviewers wanted it dead — pure adjudication, asking her to grade *our* work, the class she declines (1 of 35). Paul's call is softer and better: **keep it in the backlog as filler if the queue ever needs a card, but stop treating verdict-shaped confirms as the default supply.** *"Those are getting less engagement. That makes sense."* **→ The real work is the replacement slate below.** |
| **⭐ Design the replacement card slate — expertise and observation, not verdict** (Paul, 2026-07-26) | Deprioritizing the verdict cards leaves a supply gap, and Paul's instruction is to fill it: *"come up with other cards to replace them."* Sort by **wrongness risk**, per the user-researcher: ①Verdict *(1 of 35 — dead as a default)* · ②Observation · ③Expertise · ④Preference · ⑤Offer. **②and ③ become the default supply; ① becomes a rare exception.** Real gaps in canon to draw on, counted 2026-07-26: **2 askable varieties** (`clematis` — live; `moss` — held for the re-cut format) · **4 blooms explicitly never observed here** (`pickerelweed`, `cardinal-flower`, `iris-japanese-variegated`, `pitcher-plant`) — the honest *"we've written this down from a book and never watched it happen here"* case, which is observation-shaped by construction · **20 inferred bloom windows** overall, reachable once the template is fixed once in `harvest-questions.py` · **household systems**, where she is the one who knows what is in her own house. | **The ③EXPERTISE cards are the ones to lead with** — moss and the buttermilk slurry are hers, the app is the novice, and it is an exchange she cannot lose. ⚠️ Also here: **`harvest-questions.py` is structurally a verdict-ask factory** — it seeds from *our* uncertainty markers, so it can only ever produce ①. Keep that harvest for Paul's work queue; **seed her cards from her last input instead.** Provenance of that move was routed to ai-advisor and is settled by the 2026-07-26 channel doctrine. |
| **⭐⭐ The confirm card asks her to risk being wrong — and she won't. FIX THE ASK.** (Mom, 2026-07-26 — the highest-confidence user finding this project has) | She said it plainly and unprompted, twice, and was visibly relieved when Paul reassured her — **she hesitates to answer because she is afraid of being wrong.** (Her exact wording: `.private/mom-feedback-2026-07-26.md` — kept private; this repo is public.) ~~This explains the whole funnel — **offered 35 → viewed 33 → tapped 1 → answered 1** — better than any prior theory (not disengagement, not reading difficulty, not modality).~~ ⛔️ **THE FUNNEL LEG IS STRUCK 2026-08-01 — that funnel was PAUL's device.** Re-derived over 07-13→08-01 with builder devices excluded: **offered 14 → viewed 9 → tapped 2 → answered 4.** The hypothesis is not refuted, but **it loses the quantitative leg that made it look decisive** — a 4-of-14 answer rate is not the near-total silence the theory was built to explain. What survives is her own self-report (n=1, to a reassuring son — this project's weakest evidence class) plus *which* cards she answered. Per the BACKLOG banner above, a rival now fits at least as well: **topic origin** (cards from our uncertainty markers go unanswered; the one seeded from her own words was answered in under a day). Corroborated by *which* cards she answered: the crocosmia, the Annabelle, the panicle bloom — **the three she'd have been certain of**. And decisively on 7/26 she spent nine minutes in the app **asking** Guru questions while leaving the clematis card she had just named unanswered. **She'll ask; she won't answer. Asking is safe; answering is exposure.** **✅ THE MECHANICAL HALF IS ALREADY FIXED — corrected 2026-07-26, this row was stale within five minutes of being written.** It read: *"`q-clematis-variety` poses an A-or-B question — 'or is it a deeper rose?' — wired to **Yes/No** buttons, so 'No' cannot say which."* True when the finding was recorded; **untrue by the time the row was committed.** `5ee31e1` (12:58 ET) un-trapped it; this row landed at 13:03. The card now reads *"The big clematis on the deck trellis — next time you're past it, **what colour are the flowers?**"* with real labels — **Pale pink with a stripe · Deeper rose all through · I haven't looked** — so it is already half-reframed (it asks what she SEES, not whether we guessed right) and "I haven't looked" names a **state** instead of a deferral. *(Same failure class this project spent 7/26 fixing: prose asserting a state nobody re-derived. Logged here rather than deleted, because the row being wrong about its own fix is the point.)* **⚠️ Residual: the fix landed on the two A/B cards only, NOT on the template or the card bank** — `q-strategy-pollinators` still says *"Ask me later"*, and `q-butterfly-weed-bloom` / `q-lizards-tail-bloom` carry **no `later` label at all**. **What survives untouched is the real finding:** the app admits ITS uncertainty ("our read from a photo · needs confirming") and gives her no safe way to voice HERS. That asymmetry is the thing to fix. | **Design against her sentence, not against the funnel.** ~~give A-or-B prompts real A-or-B buttons~~ ✅ done (`5ee31e1`). **Still open:** make "I'm not sure" a **first-class, unembarrassing answer** rather than a fallthrough · finish the reframe from *adjudicate our guess* to *what do you notice* · consider whether a **wrong answer costs her nothing and she is told so** · push the un-trap into the **template**, not just the two live cards.

**⭐ SHARPENED BY THE 2026-07-26 FOUR-LENS AUDIT** (`.user-research/2026-07-26-feedback-loop-audit.md`) — three things that change how to act on this row:
1. **The axis is not "high stakes," it is *can this statement be scored false and attributed to her?*** Read the grammar of her rainfall report: *"it's confusing to read," "I don't believe," "in a perfect world."* She delivered a **14× factual contradiction** of the app while keeping every escape hatch open. The avoidance shapes her **sentence construction**, not just her channel choice. Shortest form: **she'll adjudicate; she won't be examined.**
2. **A rival hypothesis fits the same evidence and contains no fear term — AUTHORSHIP.** Everything rich she gave was about what she was already thinking about that morning. Structural version: **her input lands when it becomes an OBJECT she can see, and dies when it adjusts an invisible PROPERTY** (`confidence: inferred→verified` + a chip). ⚠️ The fear reading rests on **self-reported cause, n=1, offered to a reassuring son** — the weakest class of validated evidence. Do not over-build on it.
3. **⚠️ CALIBRATION — the evidence against the confirm card is FUTILITY, not HARM.** No negative reaction has ever been observed; she viewed 33 cards and volunteered that the app keeps getting better. **Futility says *change the ask*; harm would say *remove the surface*** — and retiring it on futility evidence would repeat exactly the A1-gate over-read. Deprioritize verdict cards; do not kill the surface.

**→ THE NEXT MOVE IS THE MOSS CARD, AND IT IS A DISCRIMINATING INSTRUMENT, NOT JUST A NICER ASK.** It is expertise-class (zero wrongness risk) but still **ours-initiated** (authorship unchanged), so it separates the two hypotheses: **she answers → the wrongness-risk read holds, invest in re-shaping the slate. She doesn't → authorship binds, and confirm-card spend is capped permanently.** Two branches, very different budgets.

**⚠️ Card wording reaches Mom → human-confirmed before it ships** (AI-boundary rule). ⚠️ And **don't touch either live A/B card until she answers one** — that free experiment is running. |
| **✅ CORRECTION — her answers WERE folded; what was missing was telling her** (2026-07-26) | ⚠️ An earlier version of this row (and the researcher brief built on it) claimed her 3 confirms were *"answered but not folded."* **That was wrong — verified against canon 2026-07-26: all three are in.** `crocosmia` variety 'Lucifer' → `verified`, `verifiedOn: 2026-07` · `hydrangea-panicle` bloom → `verified`, note credits *"Mom confirmed it in flower on the ground 2026-07-18 (Mama's Perspective)"* · **'Annabelle'** → folded into the hydrangea roster 7/14 (*"confirmed on the ground July 2026"*) **plus the care correction her answer unlocked** — it's a smooth hydrangea, so new-wood pruning in late winter, not the bigleaf schedule; card `active:false`, `resolvedAt: 2026-07-14`. **Source of the error:** `read-mom-feedback.py` lists every answered confirm under *"Ready to fold into canon"* **regardless of current canon state** — the exact stale-premise trap `CLAUDE.md` warns about (*"before folding, check the target's CURRENT canon confidence"*). **The real gap was never the fold — it was that nothing ever told her.** She spent the week doubting answers that were already correct and already in the record. | **Fix the tool, not the canon:** have `read-mom-feedback.py` check the fold target's live confidence and drop already-folded items from the punch-list, so this can't re-surface. Still genuinely outstanding: **5 unlistened zone-audio recordings** (pond area, 7/17–18) and the moss record (A2). |
| **⭐ Capture is not a loop — every input channel gets a LIFECYCLE before it ships** (Paul, 2026-07-26 — now doctrine in `CLAUDE.md`) | Her rainfall report was captured flawlessly and still went unanswered, because a free-text note **had no state**: surfaced once while newer than the watermark, then aged out silently forever. Confirm answers had a probeable target and therefore a lifecycle; **notes had storage and nothing else.** Now: `feedback-log.json` records where each note went (never her words — public repo), `needs-reply` is **actionable** so the watermark can't bury it, and *"we fixed it"* is tracked **separately from** *"she was told"* — only the second closes the loop. `tools/test-feedback-cycle.py` asserts all six legs plus a watermark regression guard. | **STANDING RULE — apply to every future channel.** A new input surface is not shipped until a note arriving on it can be **surfaced, protected from the watermark, and closed**. The three channels that already exist (`/api/observations`, `/api/zone-audio`, Guru turns) are **covered for the ack check but do NOT yet have a per-item lifecycle** — zone-audio has 5 recordings, of which **3 were never listened to**. That is the same gap one layer over, and it is the next application of this rule. |
| **📋 Feedback-loop audit 2026-07-26 — the deferred findings** (4-lens: engineering / user-research / AI-boundary / UX). Full reports: `.engineering/2026-07-26-feedback-loop-audit.md` · `.user-research/2026-07-26-feedback-loop-audit.md` · `.ai-advisor/2026-07-26-feedback-loop-ai-seats.md` · `.ux-reviews/2026-07-26-feedback-loop-surfaces.json` | **Shipped that day:** the note lifecycle · per-channel read clocks · words-riding-on-a-tap · the `--acknowledged-through` cross-channel mute · rerouting `/api/zone-feedback` · removing the dormant `/api/classify` retry · `scan-mentions.py` · the tappable ribbon with **Got it / Write back**. **DEFERRED, in priority order:** ① **The rainfall card is correct but not legible** — regional ERA5 renders at 22px, her own gauge at 13.5–14px ten pixels away, and the new by-day strip wears the *regional* blue palette while the gauge block is green: her measured data is smaller than the model's and in the model's colours. Invert the type scale; move the regional block behind a disclosure (glance/repository). Also `body.text-lg` has **zero** rainfall rules, so in large-text mode the card gets relatively denser. ② **The ribbon promises "look back day by day" with no path to it** — the by-day strip is mid-body of a collapsed Weather card. `expandCard('card-weather')` already exists. ③ ✅ **SHIPPED 2026-07-27 (`8b0a92d`) — W8 stack re-ranked** (was: the composer she used unprompted sat *below* the carousel she's declined 33/33 and a launcher with 0 taps in nine days). Order is now ack ribbon → composer → asks, via a second host (`#mom-queue-ack`). **Still open, and Paul's call, not an agent's:** ux F5's other three moves — serve only ONE ask at full weight, demote the "Mama's Perspective" title into the card, and lead with a Weeds-shaped line that asks nothing. ④ **Guru's own turns are unaudited** — a factual audit of assistant turns against a canon fact table would have caught the 2,800 ft error; ⚖️ needs Paul to **ratify** a boundary change (it opens conversation content), containment = assistant turns only behind a deterministic pre-filter. ⑤ ✅ **SHIPPED 2026-07-27 (`12bcc49`)** — zone-audio watermark now stamps the newest recording actually staged, not `now`. ⑥ ✅ **SHIPPED 2026-07-27 (`0111be2`)** — a reflective card still pins (burying her preference would be silent loss), but the bucket, the `--pickup` block and the watermark message now NAME the card at the ceiling and the retire action that releases it; five PIN checks in `test-feedback-cycle.py` (now 20/20). ⑦ ✅ **SHIPPED 2026-07-27 (`d57c2c8`)** — `pending-species` added to `momlib.CHANNELS`. ⑧ Triage seat for her free-text notes — ai-advisor says **build it third, not first**: it would NOT have caught the rainfall note faster (the watcher runs 09:00/19:00; she wrote at 09:20 — cadence was the bottleneck, not comprehension). |
| **⚠️ CORRECTION — RAG is the wrong instrument for the Guru grounding bug** (ai-advisor, 2026-07-26) | A6 argues the corpus→RAG thread fixes the 2,800 ft error. **It would make it worse.** Lake Sequoyah's fishing content is semantically *adjacent* to pond water, so similarity search pulls it in — **retrieval solves volume, not confusability.** The right instrument for near-duplicate entities is the pinned disambiguation block, which **already shipped** (`worker/worker.js`, commit `a7d7725`: "2,800 ft is the LAKE, never the property"). | **Scope corpus curation and the tool-use migration on their own merits, not as the grounding fix.** When retrieval does come, **split the substrate**: structured canon → tool-use (`get_plant`, `list_species`); the ~85-resource prose library → retrieval. Don't vectorise the digest. Her words divide three ways — about the **place** → canon (already correct); about the **app** → never enters the substrate (else Guru quotes her back to herself); about **herself** → excluded at the corpus-build boundary with a hard assertion, not filtered at query time. |
| **⭐⭐ THE BENCH — a rationalized card queue that feeds the visible five** (Paul, 2026-07-31 — design settled, ready to scope) | **The problem, stated correctly.** `q-strategy-pollinators` was filed as *"a card serving nobody."* That **overstates it**: `outstanding()` filters unanswered then slices 5, so the 6th card enters the visible set the moment she answers any one — it is a card behind a queue that is not moving, not a card that can never be seen. **The real finding is the bench underneath it: 7 cards drafted and never served** (5 harvest bloom cards 07-14: `wisteria-summer-cascade`, `clematis-elpis`, `hydrangea-dreamcloud`, `endless-summer-pop-star-hydrangea`, `spiderwort`; 2 authored 07-20: `q-fairway-grass-seedheads`, `q-weed-beggars-lice`). **Three gaps, and the third is the sharp one:** ① `outstanding()` filters `active !== false`, so a benched card can never auto-promote — that wall IS Paul's confirm gate, working as designed; ② nothing re-ranks — `_ordering` states the axis in prose, no tool applies it; ③ **freshness is computed when a card is BORN and never again.** Measured 2026-07-31: `spiderwort`'s window (`05-15→07-15`) **closed 16 days ago**, and `hydrangea-dreamcloud` (`06-10→07-31`) **expires today**, reopening 09-01 — a 32-day dead zone. `harvest-questions.py` already computes in-window-now; it just only runs at draft time. A card can be correctly written, correctly ranked, and still be **wrong to serve today**. The same gap in prose form: A3's fairway row says *"flip it in August when seed-heads emerge"* — a scheduled trigger with nothing to fire it. | **PAUL'S DESIGN CALLS, 2026-07-31 — both settled, build against these.**<br><br>**① The clear gate = Paul approves cards before they reach the Mom-facing queue.** The confirm moves EARLIER, not away: batch-approve the bench at rationalization time (*"these are cleared to serve, in this order"*), after which promotion into the visible five is **mechanical**. This preserves the AI-boundary rule (authored content reaching Mom is human-confirmed) while getting the always-something-ready property. **Multiple supply streams feed the bench** — mechanical/harvest-created · surfaced from Mom's own feedback and approved · slotted in by hand — and **each stream needs its own approval definition**, still to be written. The gate above is the floor common to all of them.<br><br>**② MAX_VISIBLE STAYS 5, AND VARIETY IS A HARD CONSTRAINT.** Paul overruled the proposal to refill below the cap. His reasoning, which changes the spec: the five are **not a workload, they are a sample of what she can influence** — *"keep the variety so that mom can always flip through them and get a broad sense of everything that we're asking for and that she can influence."* ⚠️ **Consequence for the ranking: diversity is a FILTER over the ranked list, not a tiebreaker.** A pure information-value sort could legitimately stack all five slots with bloom cards — that would score well and communicate exactly the wrong thing. The visible five must span card types/domains by construction. ⚠️ **And constant refill pressure raises the stakes on gap ③**: with five slots always demanding fill, a stale card *will* get pulled in unless the seasonal check runs at PROMOTE time, not just at draft time.<br><br>**③ Home: the `/mom-cycle` rationalization pass** — it already runs triage → cheapest-resolution → expert seat → ship → draft the return leg. The bench pass slots in there rather than becoming a new ritual. It should re-rank existing bench cards against newly added ones using the data the cycle already has in hand.<br><br>**✅ BUILT 2026-07-31** — `momlib.in_season()` + `tools/rationalize-bench.py`, wired into the session-start check block. Its first run rested `q-lizards-tail-bloom` hours before it would have gone stale in front of her, and freeing that slot made `q-strategy-pollinators` visible, closing that thread organically.<br><br>**👁 WATCH — this tool is ONE DAY OLD and is already load-bearing for three separate things** (catching a stale prompt · governing card supply · resting a seasonal card). That convergence is *some* evidence the model is right — the same mechanism solved three problems that looked unrelated — but it also means a defect in it now has three blast radii, and it has had **one** real run. **Treat its first few runs as evidence, not as settled.** Two things already argue for that posture: (a) it shipped with a latent bug found the same day — a broken `entityRef` read as `season-free`, silently certifying a broken card as fine to serve (fixed `c587cec`, and it was the FOURTH instance of that failure shape in this codebase); (b) the bloom-gated-by-wording path (`q-clematis-variety`) is a **heuristic** and always will be — no rule reliably derives "the observable is the flower" from a prompt, which is why it raises REVIEW and never decides. **Specific things to re-examine after real use:** whether `CLASS_CAP = 2` produces a queue that actually feels varied to her (the number is the agent's, not Paul's); whether the `approvedForServe` batch rhythm fits how he actually works, or just how the design imagined he would; and whether the fail-open posture ever lets something stale through that a human then has to catch. |
| **Phase G — observations as a knowledge layer** | Field notes feed other surfaces ("you noted the laurel opening April 25 last year — watch for it now"); the loop/flywheel's non-assistant surfaces. | Phase E proven **and** observation set ~50+ entries. |
| **Fairway/meadow grass ID — inferred, awaiting ground-truth** | Paul's 7/20 photo walk shows **two distinct grasses** in the fairway/meadow: a coarse clump (likely turf-type tall fescue) + a low spreading one (possibly crabgrass). Recorded on `fairway-meadow` as `observedGrasses` (`inferred`, `needs-confirmation`) + a visible note on the meadow card. **Draft** Mom question `q-fairway-grass-seedheads` staged (`active:false`) — flip it **in August** when seed-heads emerge (the clincher). Paul's steer: onus on Mom to confirm with her own pictures down the road. Ties to A7 (the crabgrass entry) + W4b (her own photo on a card). | Staged; serve the question in Aug. Photos: Paul's library, 2026-07-20 (not yet staged into repo). |

## A4 · Don't overwhelm her — the solicitation-stack IA

*Intent: hold the line on input surfaces. The cure for sprawl is the defer-affordances doctrine, not more items. **Run the IA pass on A1 signal — not before.***

| # | Item | Status |
|---|---|---|
| **W8** | **⭐ Justify the whole top-of-app solicitation STACK** (Paul, 2026-07-17 — raised as v1 of the front-door card was going in). We are about to have THREE+ stacked solicitation surfaces at the top: the new **pinned front-door card** (W3) + the **carousel confirm cards** (Mama's Perspective) + the **free-response boxes** (composer + general feedback, W5). Each shipped for a good local reason; together they risk a wall of "give us input" for a reader who reads with difficulty. **Do NOT let this accrete silently** — at a deliberate point (once the front-door card is live + we have a little signal), run an **intentional IA pass**: does each surface earn its place, what's the hierarchy, can any consolidate? Pairs with **W5** (the boxes) + **W7** (confirm-card buttons) — this is the umbrella they both sit under. Design/IA, not a quick fix. | IDEATION — umbrella over W5 + W7; revisit once front-door has signal |
| **W5** | **The three boxes.** All three live in one `<section class="unified-input">`; the note + general field are the **same CSS class** 120px apart; composer and feedback toggle ask **the same sentence**. A label can't carry it for someone who reads with difficulty — **the disambiguator is a person**: *"Something to tell Paul about the app?"* **↑ Paul re-raised 2026-07-17 (direct-from-Mom, past feedback he's carried):** the general app-feedback box must be **clearly labeled and impossible to lose** — explore making it a **persistent side / expand-out button** rather than a third stacked box, so it never gets buried under the confirm cards. Pairs with **W7**. **↑ ux-expert review 2026-07-20** (`.ux-reviews/2026-07-20-persistent-general-feedback-affordance.json`): **BUILD a focused v1** — clears both gates (Mom's direct ask = the defer-affordances signal; and done right it *shrinks* the W8 stack by **replacing** the buried in-carousel `kind:"open"` box, not adding). Recommended form: **a journal "bookmark ribbon" pinned to the right edge, lower-middle — NOT a chat-style FAB bubble** (reads as a SaaS support widget + hits the iOS home indicator). Signify with **Paul's face** (person-to-person, read pre-literately) or fallback pen/✉ + "Tell Paul"; static, no badge/pulse; slides out a note field on the **existing `/api/feedback` verbatim path** (no new backend, honest 3-state ack); `position:fixed`, reduced-motion honored, 56–64px target, touches only the margin. **One quick Mom-check on the signifier** (face vs glyph) before committing the icon. 2 candidate principles logged. **↑ v1 BUILT 2026-07-20** (`FeedbackRibbon` in viewer.html): persistent right-edge "General feedback" tab (Paul's call: label reads "General feedback", **no face** per his 7/20 note), `position:fixed`, taps open a verbatim note panel on `MomQueue.submitGeneralNote` → the existing `/api/feedback` path + honest 3-state ack; **retired the in-carousel `buildGeneral` foot-line** (one home). Screenshot-verified; 0 console errors; reduced-motion + aria + focus-return done. **OPEN (the ux-expert's Mom-check + a known tradeoff):** the full label makes the tab ~150px wide, so on a **phone it covers ~40% of one horizontal reading band** (worse in large-text mode; trimmed but inherent to an always-visible label). Options if it bothers Mom: icon-only rest state that expands to the label on tap (less coverage, less immediately labeled), or accept it. Needs the 30-sec Mom-check on signifier/placement before it's "done." | v1 BUILT 2026-07-20 — pending Mom-check + Paul's deploy call. |
| **W8·a** | **⚡ W8 IS NOW TRIGGERED, AND PAUL HAS SCOPED IT (2026-07-29).** The umbrella above said "run the IA pass on A1 signal — not before." The signal arrived: **Mom's device fired `card_expanded` 4 times in 30 days across 15 sessions** (Paul's device: 47). She reads the glance and rarely opens the second layer — which is where the radar she told Paul she likes *but "didn't know how to access,"* the by-day rainfall strip she re-asked for 3 days after it shipped, and the Journal she wants to look back at all live. Her in-person ask for **"tabs across the top"** (naming *vehicles · equipment · house systems · gardening · wildlife*) is her own diagnosis of the same thing. **Paul's call: reinforce the existing cards as doors FIRST, and ask her to confirm the categories before restructuring** — `q-top-categories` is live and leads the queue. **✅ Cards-as-doors SHIPPED 2026-07-29** (`94d9302`): the 14px `#9bbf9b` "▼" became a labelled **"Open ▼"** pill flipping to "Close ▲", header is a real `role="button"` with `aria-expanded` + keyboard, `body.text-lg` scaled. **NOW SCOPED — a UX review of the input stack, MOBILE-FIRST** (Paul, 2026-07-29): *"there's not always a clean delineation between boxes — ask-the-Almanac vs the cards vs the text you can click to open a box."* **Go interact with every feedback-collection path**, then propose: (i) possibly **kill the click-to-expand line** that opens a text box; (ii) possibly give **each input source its own card / labeled input section** so separateness is structural, not typographic. ⚠️ **Mobile is the primary interaction source — do this review in mobile viewport, not desktop.** Live evidence: a 390px screenshot shows **five** input surfaces stacked (ack ribbon w/ Got it + Write back → photo/mic composer → "Save & ask the Almanac" → Mama's Perspective card → the floating "General feedback" tab, which *overlaps the card text*). | ✅ **SHIPPED 2026-07-30** — see the resolution note directly below this table. Supersedes W8's "revisit later" |
| **W8·b** | **Typographic hierarchy + coherence pass** (Paul, 2026-07-29): *"a general review of hierarchy, text size, font size, italicizing, bolding, etcetera, so we have a more coherent and unified appearance."* Runs as the second half of the same **mobile-first** review as W8·a. **Two concrete inputs already in hand:** ① the 7/26 audit's deferred finding — the **rainfall card renders the regional ERA5 model at 22px and Mom's own station gauge at 13.5–14px**, and the by-day strip wears the *model's* blue while the gauge block is green: **her measured data is smaller than the model's and in the model's colours**, which inverts the source hierarchy the whole project runs on. *(Deliberately NOT patched as a one-off on 2026-07-29 — a spot fix is exactly what this pass would undo. Fold it in.)* ② the **`✓` in the ack ribbon** — Paul, 2026-07-29: *"why is it a check mark?"* A check reads as *task complete*, which is task-manager grammar in a field journal. The **layout** complaint underneath it (mark orphaned on its own line above its sentence) is **✅ fixed 2026-07-29**; whether `✓` is the right mark at all is this pass's call. | 🎯 READY — pairs with W8·a, same review |
| **W8·c** | **Rainfall range: a month and a year.** Mom, 2026-07-29 (`fb-946dp0qk-ms639ds6`): *"a daily rainfall count for each of the last 5-7 days, showing date or day of week, and then perhaps only the past month and year?"* The **last-7-days half already shipped** 7/26 and she reached it (`momack_followed` from her device, 12:28 PM ET 7/28) — so this row is **only the range extension**, month + year, which does not exist. Station history is already local (`weather-history.json`), so this is presentation, not plumbing. Acknowledged to her honestly in the ribbon as *"not built yet."* | OPEN — her direct ask, small |
| **W8·d** | **Zone-audio walkthrough ⬇️ DEMOTED 2026-07-29** (Paul's call, evidence-backed). It was the standing front door, rendered first and unconditionally. 30 days: **launcher offered 60 / viewed 42 / tapped 2 — and both taps were Paul's device.** All 3 zone recordings that exist are from Paul's device on 7/18, **5–6 seconds each** start-to-save: bench tests. **Mom usage is zero.** Now renders **below** the confirm queue — demoted, not deleted; the door stays open. **Revisit if a tap ever arrives from her device.** *(Related and still open: zone-audio has no per-item lifecycle — a recording can still be captured and never listened to, which is the "capture is not a loop" rule one layer over.)* | ✅ DEMOTED — watch for a Mom tap |
| **W7** | **Confirm-card button layout + the per-card "Add a note" question** (Paul, 2026-07-17, from the live screenshot). The confirm card stacks **Looks right / Not quite** then **Ask me later** then a dashed **+ Add a note** — Paul: the **button positioning is awkward**, and there are **too many text-entry surfaces** scattered across the app. **Open question:** does a **per-card "Add a note"** earn its complexity for a reader who reads with difficulty, or should note-adding funnel to the ONE general box (see W5) so we don't have notes-per-card *and* a general field competing? Design-only, no build; **feeds the position-1-card work** (it lives in this same carousel + input stack). | IDEATION — pairs with W5 |

### ✅ W8·a resolution — the input stack is four cards (2026-07-30)

Shipped `ca6939f` → `4145385`, live on Pages. The review ran **mobile-first at 390px** as the row
demanded (ux-expert, findings at `.ux-reviews/2026-07-30-input-stack-four-card-standardization.json`).

**What Paul decided, and it settles (i) and (ii) above:** *"make it very clear that there's four
separate things here, four separate main cards."* So (ii) — each input source gets its own card,
separateness structural rather than typographic — **is the answer**, and (i) (killing the
click-to-expand line) was **not needed**: the ambiguity it was meant to fix came from the container,
not from that line.

- `.unified-input` stopped being a card and became a transparent wrapper — the "tiles within tiles"
  Paul named. The group now reads as a group by **shade** (cream vs the white dashboard cards); its
  width already matched `.main-card`, so the four land pixel-flush with the column below.
- One shared shell (`.ic-card`) + compact header (`.ic-head`) — identity square + Crimson title, the
  grammar the six cards below already teach. **No chevron:** these four are always open.
- **The composer and the Guru thread are ONE card** (Paul: *"one interface to have a conversation"*).
  Load-bearing, not cosmetic: the thread is `hidden` until it exists, so splitting them would render
  **three** cards at rest and four mid-conversation, in a different order — "four" would never be a
  fact she could learn. Fused, the count and page order are invariant in every state.
- **Mama's Perspective now renders always.** It used to vanish when the queue emptied, which is the
  same failure in a different clothing.
- The launcher lost its rail, gradient and oversized head; its CTA adopts `.ui-action-btn` geometry.
  Its **fill is deliberately unchanged** — see the time-box note under Tier-3.
- Fixed in passing: `.ui-conversation`'s `display:flex` overrode its `hidden` attribute, so an empty
  thread rendered a dashed rule + ~24px of padding between her textarea and the Save button.

**Two things that came out of the same session and are NOT part of W8·a:**
1. **One name.** Garden Guru is retired from every Mom-facing string; the write surface, the record
   card and the voice are all **Fernwood Almanac**. This overrides her own word (`q-almanac-name`,
   answered Yes 7/29) — Paul is taking that to her in conversation, **not** via a card. Tier-3 #8.
2. **"Look back ›"** in the header of cards 1 and 2 → opens the record card. Answers her standing
   7/29 ask (*"is there a way to look back at these?"*) with a door where she already is.

**Still open from the row's own evidence:** the floating **"General feedback" tab still overlaps card
content** at 390px (`position: fixed`, `bottom: 40px` — `84px` under the mobile media query, z-index 900).
It was named in W8·a's live evidence and this pass did not touch it. Not a regression — the same overlap
as before.

> **⚠️ MEASURED 2026-08-01, and it is worse than "overlaps card content" — it occludes two live
> controls.** Measured in a real 390px viewport, not read off the CSS. The ribbon renders 82×63 at
> x=308, i.e. **hard against the right edge of a 390px screen**, and:
> - it covers **16% of `.ui-action-btn.ui-save-btn` — "Save & consult the Almanac"** (210×52 at x=153),
>   clipping its right end. That is **Mom's primary capture action**, sitting under a floating tab.
> - a hit-test at `.fn-sync-btn`'s own centre lands on **`button.feedback-ribbon`**, not the sync
>   button — so that control is **entirely unreachable** where it renders. This was confirmed present
>   in the unmodified HEAD build, so it is pre-existing, not introduced by the target sweep.
>
> So this is not a cosmetic overlap and not only a text-legibility issue: **a fixed-position element is
> eating taps meant for two buttons, one of them the main thing we ask her to do.** That reframes the
> priority — but **the fix SHAPE is still a design call on her surface and is Paul's**, because every
> option trades against something the ribbon was deliberately given: it is labelled (not a chat-bubble
> FAB) precisely so it is *"impossible to lose (Mom's ask)"*, and it is pinned low-right for thumb reach
> (Paul, 7/20). Options, none obviously right: **(a) dock it** to the bottom edge as a full-width strip —
> ends all overlap, biggest change to her surface; **(b) shrink it** to icon-only — reduces but does not
> eliminate the overlap, and spends the label that was the point; **(c) move it below the stack** so it
> stops floating — ends the overlap and the always-reachable property together; **(d) reserve a right
> gutter** so content never renders beneath it — 82px is 21% of a 390px reading column, too expensive;
> **(e) collapse it to a thin edge tab while any control is under it** — keeps both properties, most
> complexity.
>
> ### ⚡ DECIDED 2026-08-01 (Paul) — **(e)**, with a narrower trigger. Build is unblocked.
>
> His call: **collapse it to a thin edge tab when something *actionable* is underneath it.** His
> reasoning settles the alternatives at the same time — *"I think we can assume that mom knows that she
> can scroll up and down to uncover things if that general feedback tab is kind of pinned in its place
> on the side."*
>
> Three things that decision fixes, and they are what to build against:
> 1. **The tab stays pinned and stays labelled.** (a) dock-to-bottom and (c) move-below-the-stack are
>    **rejected** — both spend the always-in-the-same-place property he is explicitly relying on.
> 2. **Overlapping static prose is acceptable**, because she can scroll it out from under the tab.
>    This is the half of the original row's framing ("overlaps card *content*") that turns out **not**
>    to be the defect at all.
> 3. **The trigger is narrower than "any overlap" — it is "something actionable."** That is a sharper
>    rule than option (e) as written, which said *any control*. Prose scrolls; a button she cannot
>    reach does not, because scrolling to uncover it also scrolls it away from her thumb.
>
> Implementation note, not yet decided: "actionable" needs a definition that cannot silently rot — the
> honest version is a live hit-test against the ribbon's own rect (the same probe used to measure this),
> not a hand-kept list of selectors, which would go stale the first time a new button lands.

## A5 · Light privacy — hard to find, PII local, never at the cost of Mom's access

*Intent: LIGHT, least-restrictive security only. **Mom's seamless phone access + the self-contained loop dominate** — taking it offline for privacy defeats the whole project. **Recalibrated 2026-07-17 (Paul):** privacy is a light overlay, not a mandate; the concern is "honestly minor"; the heavy "make it private + serve from the Worker" migration is **downgraded** (it risked breaking her access).*

| # | Item | Status |
|---|---|---|
| **Light discovery-hardening + PII-local** | **Recalibrated 2026-07-17 (Paul): privacy is LIGHT and subordinate to Mom's seamless access + the loop.** The old *"🚨 MUST make the repo private + serve `viewer.html` from the Worker"* is **DOWNGRADED** — that migration (different origins, origin-bound localStorage, a paid Pages plan) risked destroying her data / taking Fernwood dark, which defeats the project's purpose. Chosen **light** measures: **(1) hard to find** — `noindex, nofollow` on `index.html` + `viewer.html` so search engines don't surface her (✅ done 2026-07-17); **(2) NO password** — a real one needs the Worker-serve migration (too complex; a client-side password on public Pages is theater — it can't gate bytes GitHub already hands out); **(3) PII stays local-only** — no NEW hard PII in tracked files, existing PII lives in gitignored `.private/` (retained so Paul can pull it via Claude on his machine); swept 2026-07-17, scrubbed one leaked plate from the archive. **Hard rule kept:** never put plates / policy# / full VINs in tracked files. **Context (unchanged facts, now accepted under the light posture):** 146 tracked files name her; the served `viewer.html` mentions her ~99× in comments; past public exposure is SHA-reachable and doesn't unring the bell. | **✅ LIGHT MEASURES DONE 2026-07-17** (noindex + PII sweep); heavy migration not pursued |
| **Password / Worker-serve migration** | The "super simple password" idea (7/16) + the repo-private + Worker-serve migration. **NOT pursued (Paul, 2026-07-17)** — a real password requires serving from the Worker, and that migration risks Mom's seamless access, which the whole project depends on. If ever revisited, the only acceptable shape is one **Mom meets ~never** (long-lived signed cookie / magic link, never a token she types). The on-disk Esri z19 basemap (which needed Worker-serve to be usable publicly) stays parked with it. | **NOT PURSUED — optional; Mom-never-types only** |

## A6 · Guru & capture infra

*Intent: keep the assistant + capture path healthy. **Tool-use migration is the nearest real trigger — the digest is AT the ceiling now.***

| Item | What it is | Gate |
|---|---|---|
| **Tool-use migration** | Move Garden Guru off system-prompt digest-stuffing to tool-use. | digest >80K **or** observations >50. **De-urgent-ed 2026-07-17:** excluding vehicles/equipment from the digest (Paul — Guru is Mom's garden assistant, not the fleet tracker) dropped it **~82K → ~71K tokens**, back under the ceiling with headroom. Still the durable fix eventually, but no longer near-triggering. *(Machine Q&A via Guru is intentionally gone; Paul uses Claude-in-terminal for that.)* **⚠️ The de-urgent-ing has expired — see the redesign row directly below: the digest is now 26% OVER the 80K gate.** |
| **⭐⭐ THE VISION (Paul, 2026-07-28) — one input box, everyone, everything** | *"One input box for conversation with the Garden Guru that serves both me and Mom and anyone else that has interest down the road, and any of this information."* **This is now the north star for the assistant, and it changes two standing decisions:** the 2026-07-17 "Guru is MOM's garden assistant, not the fleet tracker" scoping is **reversed** (vehicles are back in the digest as of `a73afbd`), and the 80K digest gate stops being the trigger for anything — it was about cost, it was never enforced, and it measured the wrong thing. **What it does NOT mean:** a rush to rebuild. Paul's explicit instruction is that *how* to evolve Guru gets its own worked backlog item (below) rather than a snap architecture call. | **SET.** First step shipped 2026-07-28: weeds added, vehicles restored, count-free language. Digest ~98.7K. |
| **⭐ Where Guru is still limited — the audit (2026-07-28, Paul: *"let's make sure we don't limit Guru unintentionally"*)** | Every cap, sorted by whether anyone chose it. **DELIBERATE, leave alone:** the 600-token reply cap (`worker.js:1165` — a field-journal answer should be short) · the 6-turn conversation cap (`viewer.html:16090`) · excluding `questions.json`, `feedback-log.json`, `plants.draft.json`, `sources.json`, `service-records.manifest.json`, `events.json` (working state and provenance, not things to answer *from*) · excluding `weather-history.json` / `weather.json` / `sun-horizon.json` / `weather-bias.json` (live state already rides with each turn — putting the history in the digest would duplicate it stale). **⚠️ ACCIDENTAL — and two of these are loose ends created by the 07-28 weeds add:** ① **`zones.json` (10 zones, ~3.7K tok)** — the weeds carry `observedZones: fairway / fairway-fringe / woodland-edge`, so Guru now reads zone ids **it cannot resolve**, and Mom's zone voice-walks and her map are built on exactly these. ② **`turf.json` (8 entries, ~1.4K tok)** — crabgrass's entire combat advice is *"the real defense is a thick fescue turf — mow high 3–3.5″, overseed every September"*; the weeds text names turf/fescue/overseed **5 times** and Guru cannot see the turf record behind any of it. ③ `candidates.json` (23 plants-to-consider, ~7.6K) and `devices.json` (~0.6K) — plausible, lower value, not urgent. ④ `references.json` (~13.4K) is the prose library — **that one is the RAG case, not a digest case; do not stuff it.** | **① and ② are the real ones and they are cheap (~5.1K tokens combined → digest ~104K).** ⚠️ That crosses the ~100K retrieval-degradation note, so it is **not** a free add — it is the first concrete thing the architecture question below has to answer. Recorded here so the gap is visible rather than discovered later by a wrong answer. |
| **⭐⭐ How to evolve Guru's capability — the worked question** (Paul, 2026-07-28: *"we need to look at this super holistically with the team and best practices... its own backlog item that we really sink our teeth in and don't try to figure out in a rush right now"*) | **The question:** what architecture gets from today's digest-stuffing to the vision above — Mom, Paul, and later others, asking about anything in Fernwood? **Do NOT answer it by building.** The scoping artifact `research/2026-07-28-garden-guru-scope.md` is the evidence base; what it establishes is that the honest constraint is **not cost** ($9.29/yr from a tool-use migration, ~0.54 turns/day) and **not the context window** (Haiku 4.5 is 200K; we sit at ~98.7K), but **retrieval degradation** — the model getting worse at finding a fact as the context grows. There is already one local instance: the 2,800 ft error was the model reconstructing a plausible wrong number *while the correct one sat in its context*. **The real inputs to bring:** ① tool-use vs. RAG is not a versus — split the substrate (structured canon → tool-use `get_plant`/`list_weeds`; the ~85-resource prose library → retrieval; **don't vectorise the digest**, and remember retrieval solves volume, not confusability, which is why it was rejected as the 2,800 ft fix). ② **A test harness does not exist and is the true prerequisite** — probing `/api/chat` forges a Mom-input signal, so Guru currently cannot be regression-tested without corrupting the engagement measurement. ③ Tool-use adds a *worse* failure mode (the model doesn't call the tool and answers from nothing) on a surface where trust is load-bearing. ④ It costs latency — 2+ round trips on rural LTE — so streaming likely ships with it, not after. ⑤ The closed-world "not one we tend" negative needs an always-loaded names index; a per-record lookup can't ground a negative. ⑥ Four fence flows + the promote-species drafter depend on digest-exact naming and get rewritten. | **⏳ PAUL'S — to be worked properly with the expert team (ai-advisor + engineering-partner + ux-expert), not decided in a session.** Deliberately parked from building. When picked up, the first deliverable is a *design*, and the first build is almost certainly the **test harness**, since nothing else can be changed safely without it. |
| **⭐ Garden Guru redesign — the fork (SCOPED 2026-07-28, `ff855db`; full scope `research/2026-07-28-garden-guru-scope.md`)** ⚖️ **SUPERSEDED 2026-07-28 by the two rows above** — Paul took the growth branch and split the work: the cheap content fixes shipped (`a73afbd`), the architecture question is now its own worked item. Kept for its reasoning. | The evidence forces a **fork**, not an answer, and the fork is a product question: **is Guru meant to stay capped, or to grow?** ① **If capped stays capped** (600 tokens, 6 turns, 8 domains), the tool-use migration above **saves $9.29/YEAR**, fixes **none** of the four broken things, makes latency *worse*, and adds a failure mode with **no safe test harness** — so do the cheap fixes instead and either re-set or retire the 80K gate, which is currently a tripwire nobody acts on. ② **If Guru is meant to GROW**, the migration is a **prerequisite, not an optimisation**: the digest is already **26% over its own gate** and has grown **59% in nine weeks**, and *every ceiling event so far was paid for by REMOVING capability from her assistant* — 16 machines dropped 7/17, 5 weeds never added. That is the real cost and it is invisible in the token number. | **Paul's call — it is a product decision about what Guru is for, not an engineering one.** Nothing should be built off this row until he picks a branch; the sub-work under each branch is enumerated in the scope doc. |
| **Naming card `q-almanac-name` — live, and it CANNOT self-clear** (2026-07-28, `4878994`) | Asks Mom whether **"Journal"** fits better than **"The Almanac"** — she called it the journal on 07-26, and the standing rule is *adopt her words, never improve them*, so we asked rather than renaming for her. "Something else" opens a note so a third word of her own can land. ⚠️ **Placed FIRST among the confirms deliberately:** `outstanding()` slices the first 5 in file order, and appended it sat 6th — it would have rendered to **nobody**, and we would have concluded she ignored it. It displaces `q-weed-stiltgrass` below the cap. | **AWAITING HER ANSWER, then a HAND-RETIRE.** A reflective card has no `_foldTarget`, so canon can never mark it handled and it **holds the feedback watermark** until someone sets `active:false` + `resolvedAt` (see the `unprobeable` rule in `CLAUDE.md`). Wording already Paul-approved. |
| **Streaming responses** | Stream Guru replies (~30 lines client). | If turns feel laggy on LTE. |
| **Conversation browse UI** ⭐ **SIGNAL FIRED — Mom asked for it unprompted (2026-07-26)** | UI to browse KV-stored conversations. **Her words, 9:01 AM ET, minutes after asking Guru two questions: _"Is there a way to look back at these, eg in the 'journal'?"_** | **PROMOTE — this is no longer a v2 want.** Note what it is and isn't: **the capability already exists** — conversations are stored and readable (that is how her 7/26 questions were read back). So this is a **findability failure on a shipped feature, not a missing feature** — likely the cheapest real win in the whole batch. Honours `feedback_defer_affordances_pending_signal`: the signal arrived, from the user, unprompted. **↻ SCOPED 2026-07-28** (`research/2026-07-28-conversation-browse-scope.md`). Her question was already answered by the app — badly. **Two defects fixed + live**: large-text never reached the Almanac (`body.text-lg .fn-entry-text` targeted a class that does not exist, so her saved words stayed 13.5px while the live copy scaled to 17px), and a conversation froze at its first save (6 turns in KV, 4 in the Almanac — the boxwoods exchange was never going to arrive). **Third defect fixed**: the log-first entry and the conversation entry duplicated every ask; the conversation now ABSORBS the inline copy, but only after reading itself back from the store, so her words are never in zero places. **The reframe that matters: the Almanac is already the most-opened card in the app** (41 of 139 expansions; 113 of 249 entry-revisits hit conversation entries over 18 days) — browse is not a new behaviour to introduce, it is her most-used read behaviour sitting 8th under a name she did not recognise. So the naming card `q-almanac-name` is live rather than new copy; the composer return-line stays deferred per defer-affordances. |
| **Durable photo-in-note** | Persist photos in saved notes (stripped for iOS quota). | Needs own scoping; mirror the audio_ref server-blob pattern. |
| **Transcribe Mom's voice captures → text (off-device, Paul-side)** ⭐ (Paul, 2026-07-18) | Convert the staged zone-audio `.webm` recordings into usable text files so what she says is **searchable + can feed the fold-to-canon step** (her spoken plant/zone knowledge → the record). **⚠️ AI-BOUNDARY (load-bearing):** transcription IS a model read of HER VOICE — sanctioned off-device seat only: **Paul's machine**, **POST-storage**, **Paul-facing** (never reaches Mom, never auto-folds), transcript is a **hypothesis until Paul checks it against the audio**. The **CHARACTERIZE middle layer** for audio, per [[feedback_no_ai_on_capture]]. | **✅ BUILT 2026-07-25** — `tools/transcribe-mom-zone-audio.py` (companion to read-mom-zone-audio.py): staged `.webm` → 16k wav (ffmpeg) → `whisper-cli` (whisper.cpp, `ggml-base.en` at `~/.local/share/whisper-models/`) → `<name>.transcript.txt` in `.private/` (gitignored), stamped **`[transcript-UNVERIFIED]`** with a provenance + AI-boundary header; deterministic, skips already-done, `--force`/`--model` flags. **Pipeline proven end-to-end on the 5 staged captures:** 2 were empty (7/17 failed taps), 3 transcribed cleanly — all **Paul's ship-day test recordings** ("This is Paul testing on the fairway", etc.), **no real Mom ground-truth yet** (she hadn't used the front door). Ready for real captures whenever she records. (Fallback still: OpenAI Whisper API — needs `OPENAI_API_KEY`.) |
| **⭐ Curate the research corpus with real depth → the RAG use case** (Paul, 2026-07-26) | **The ask:** stop leaving verification as a one-off. Today the pond-filter advice was checked against real sources ([Pond Informer](https://pondinformer.com/watering-plants-with-pond-water/) + an irrigation guide) so it could go into the Almanac as fact rather than as an unverified model claim — and that research **left no durable artifact behind**. Fold it, and the sources that follow it, into the **existing** research library (`research-resources.md`, ~85 verified resources, 7 categories) with genuine depth per topic. **This is an extension of a structure that exists, not a new one.** First topic: pond water — filter-water nutrient content, dilution ratios, edible-crop cautions, algae dynamics. | **The strategic point, and why this is one thread with the tool-use migration above, not two:** a well-curated corpus is the substrate for **RAG (retrieval-augmented generation)**, and Fernwood already *has* the problem RAG solves. Guru is fed by **system-prompt digest-stuffing**; the digest has run **~71–85K tokens** and hit its ceiling once already; and **today's 2,800 ft error is exactly a grounding failure** — the correct elevation was in the context (`property.json` carries 2,959 ft *and* an explicit note that the wrong figure came from Lake Sequoyah) and the model reconstructed a plausible wrong number anyway across 345 KB. **Stuffing more context makes that worse; retrieval makes it better.** So: curating the corpus is not a nice-to-have parallel to the tool-use migration — it is the thing that makes retrieval possible, and the migration is what makes the corpus payable. Scope the two together when either is picked up. |
| **🐛 Guru told Mom something wrong about her own property** (2026-07-26) | Its algae answer states the pond *"stays reasonably warm even at **2,800 ft**"* and *"at 2,800 ft decomposition is slower."* **The property is 2,959 ft. 2,800 ft is Lake Sequoyah.** The digest holds that figure **correctly** (12 occurrences, all fishing-scoped), so this is Guru pulling the lake's elevation onto the property — **the original Fernwood error class returning** (the retired 1,750 ft figure came from exactly this lake-vs-property confusion; see `CLAUDE.md` elevation section). On a surface Mom reads, and *trust is the load-bearing emotion*. | Decide the fix altitude: disambiguate lake-vs-property elevation in the digest's framing, or a system-prompt guard. **n=1 — check a few more Guru answers for the same conflation before designing anything.** |
| **Phase H — audio identification** | Bird/sound ID; built end-to-end then hidden (👂 button `hidden`). | A mature free/single-vendor audio-ID path **and** a Mom-usage signal. |
| ~~**Guru re-inline verification (root-cause fix)**~~ | ✅ **ROOT-CAUSED + FIXED 2026-07-16** (`2adab8d`). The cause was the **GitHub Contents API's 1 MB cliff**: above 1 MB it returns HTTP 200, `encoding:"none"`, and an **empty** content string — no error — so `ghGetFile` handed back a successful-looking `""` and every re-inline found nothing to replace. **`viewer.html` crossed 1 MB on 2026-07-02 in `23ac94f` — "Garden Guru Phase 3: add and remove a plant from conversation."** The commit that shipped the add/remove write path is the commit that broke it; the feature disabled itself on arrival. **Lizard's Tail (7/05) was this.** The 7/06 verification (`d1da306`) detects the symptom but could never succeed — its verify read is the same broken call. Blast radius: all four viewer re-inline sites — promote-species, its verify, remove-species, zone-save — i.e. **Guru's whole write-to-canon path was dead for two weeks.** Fix: fall back to the **Blob API** (100 MB ceiling) when content is empty but size > 0. Verified live: contents → 0 bytes; blob → all 1,235,806; zone-save now `{"ok":true}`. ⚠️ **Watch:** `viewer.html` grows every session — the Blob API's ceiling is 100 MB, so there's headroom, but this is the second ceiling this file has silently hit. | **DONE** |

**Track-A KILLED (don't revive without new evidence):** the **24-row editable plant table** — all five experts rejected it as an **invalid instrument** (a null result can't distinguish "doesn't want ownership" from "can't read a table," but would read as the first and retire her one unprompted ask on a rigged test; ⭐ precedent: 0 uses / 104 sessions). The gap view is **Paul's** `tools/` script, not her surface. Also killed: **EXIF→zone auto-placement** (no georeference; and don't automate the highest-ownership moment in the plan); **baking the God-token** into the public deploy; **AI-for-drudgery** (AI-at-scale is what *produced* the 18 stock photos — "24 is your number, not hers").

**Doctrine amendments forced (proposed, unapplied):** retire **"open feedback → DON'T BUILD"** (she asked for it unprompted; her direct ask outranks the 7/13 panel inference) · the **AI boundary should be a *provenance* rule, not a model rule** (the stock photos + generic guides came from **zero AI calls** — the harm arrived by a route the rule doesn't cover) · `feedback_defer_affordances_pending_signal` — **this is the signal** that gate was waiting for.

## A7 · Weeds — a new domain in the field journal (v1 SHIPPED 2026-07-20)

*Intent: give the field journal a weeds domain — not just what Mom is cultivating, but what she's fighting. Same glance → repository → loop shape as plants, in field-journal voice (never a task-manager "N weeds due" — [[project_tate_tracker_tone]]).*

| Item | What it is | Status |
|---|---|---|
| **Weeds card** | ✅ **v1 SHIPPED 2026-07-20** — a new **Weeds** card (`card-weeds`, `weeds.json` → `WEEDS_DATA`, `renderWeeds`), layered like every rich domain: **glance** — a calm "worth getting after this before it seeds" read; **repository** — per-weed *how to handle it* held in the card, organic-first; **loop** — each entry wears an honest **"our read from a photo · needs confirming"** chip + a *"Mom will settle it"* line, and links to a Mama's-Perspective question. Registered in `check-data-inline` SOURCES; screenshot-verified. **First entry: Crabgrass** (`Digitaria`) — the inferred "second grass" from the fairway/meadow photos (7/20), recorded `confidence:inferred`, `status:needs-confirmation`. **NOT built yet:** a top-of-app *glance* trigger (see next row); an *own-photo* on a weed entry (renders "taken here" — gated on the same W4b add-a-photo affordance). **Authored content:** AI may draft combat advice behind Paul's gate; Mom's surface + capture stay AI-free ([[feedback_no_ai_on_capture]]). | ✅ v1 DONE 2026-07-20 |
| **Populate from Paul's weed photos** | ✅ **First pass DONE 2026-07-20.** Paul sent 10 weed photos (HEIC from his library); read **four** woodland-edge weeds off them + staged one own-photo each into `images/weeds/` (renders **"Taken here on the property"** via the new `renderWeeds` photo block): **Japanese stiltgrass** (`Microstegium` — dominant), **beggar's lice** (`Desmodium`), **Virginia creeper** (native; not poison ivy), **wild violet** (lower-confidence). All `inferred`/`needs-confirmation`. Two DRAFT Mom questions staged (`q-weed-stiltgrass`, `q-weed-beggars-lice`, `active:false`). As Mom answers, fold `inferred→verified` and let it **seed more entries + cards** (Paul's loop). | ✅ 4 weeds seeded 2026-07-20; ID = model-read, awaiting ground-truth. |
| **"Before it seeds" timely nudge** | The weed-glance's concrete trigger — surface a weed at the moment it's about to set seed (highest-leverage time to pull it). Phenological like the bloom windows, framed as a field-journal observation (*"the crabgrass is about to seed — worth getting after it"*), never a deadline/alert. The per-weed `seedTiming` window is already in `weeds.json`; this promotes it into a top-of-app glance. | IDEATION — after a couple of real weeds exist. |

## A8 · ChatGPT-mine use-cases — added 2026-07-22 (rounds 1+2)

*Source: OpenAI ChatGPT conversation mine — `~/Developer/openai-data-archive/reports/use-case-routing.md` §A + `…-round2.md` (fuller detail). All care/wildlife facts from chat are model-read → verify on the ground.*

**Real property EVENTS → wildlife/device layer:**
| Item | What it is | Status |
|---|---|---|
| **Wildlife nuisance flags → `mammals.json`** | ✅ **DONE 2026-07-25.** Authored: **bear** — 2025 pond-liner damage → Paul's electric fence around the pond; **squirrel** — abundant to nuisance (digging beds/containers); **deer + cottontail** — pond-edge browse (cardinal flower stripped Aug 2025); **nine-banded armadillo** — NEW entry, confirmed burrowing pest (Paul, range-expansion); **feral hog** — NEW entry, framed as an *area* concern (Jasper–Ellijay landscaping damage), NOT asserted on-property. ⚠️ **Beaver CORRECTED (Paul 2026-07-25): they're at the LAKE, not the pond** — the mine mis-attributed it (same lake→pond slip as the amphibian note); beaver entry clarified to "Lake Sequoyah nearby (not the property pond)." | ✅ authored |
| ~~**Amphibian-decline observation → `amphibians.json`**~~ | **KILLED 2026-07-25 (Paul):** don't author it — it's hearsay with no real data, AND it referred to the nearby **lake**, not our pond. **Our pond is healthy and full of frogs.** The ChatGPT-mine capture mis-attributed a lake remark to the pond. | 🪦 KILLED — don't author |
| **Pond infrastructure → the rich-pond-zone** (was → `devices.json`) | Bear **electric fence**; **pump shed** (30×22×18 plywood, sloped roof); second gutter+drain on the existing shed; pond **pump + filter → waterfall**; EPDM 40-mil liner ~22 ft × 4.5 ft. **Decision 2026-07-25: do NOT author into the Nest-sourced `devices.json`** — these are pond builds and belong in the **"zones as rich content-containers"** item (A2), where the pond zone will hold its koi + pump/filter + plants. The bear fence is already captured on the `black-bear` mammal entry. The full spec lives here + in the rich-zone item until that zone is built. | ⏸ folds into the rich-pond-zone (A2), not devices.json |
| **`guides/` hunting & game reference** | Pickens County seasons/limits + squirrel/hog handling & meat safety. ⚠️ regs are year-dated — annual-refresh flag. | IDEATION |

**The pond (densest thread, no data-model home yet):**
| Item | What it is | Status |
|---|---|---|
| **Pond/koi record → folds into "the pond as a rich zone"** ⭐ | EPDM 40-mil liner, ~22 ft across, 4.5 ft deep; koi stocked; the koi+*Gambusia*-vs-frog/tadpole tradeoff (fish-free cover zones); pump + filter. **Paul's steer 2026-07-25: don't invent a standalone koi record — eventually build the POND as a rich ZONE** that references its contents: the koi, the pump/filter and what it's used for, and all the plants planted in it (the 7 pond plants + 3 irises now carry `zoneId: pond-area`, so the plant→zone links already exist). This is the concrete seed for making **zones content-containers**, not just map polygons — a genuine "makes the app more useful" direction. **Explore later** (Paul: "a backlog item that we explore later"). | IDEATION — the flagship example for the rich-zone concept below |
| **Pitcher plants → `plants.json`** | ✅ **RESOLVED 2026-07-25 (Paul):** they DO have Sarracenia pitcher plant(s) in the pond — just **NOT the purple species** (S. purpurea). Re-authored as `pitcher-plant` = **Sarracenia sp.** (genus level, honest-uncertainty like the `Plethodon sp.`/`Eurycea sp.` pattern), zoneId pond-area; exact species to confirm on the ground when it pitchers/blooms (colour helps ID). Consistent with the 7/22 memory (3 bought spring 2026, in the pond — which never claimed "purple"). | ✅ authored (Sarracenia sp.) |
| **Add purchased pond plants → `plants.json`** | Pike Nurseries receipt. ✅ **DONE 2026-07-25**: `pickerelweed`, `bowles-golden-sedge`, `dwarf-papyrus` (⚠️ tender — overwinter call ~Oct), `corkscrew-rush`, and (Paul-ID'd 2026-07-25) the **variegated Japanese iris** = `iris-japanese-variegated` (Iris ensata 'Variegata', web-verified: variegated leaves + purple gold-eyed flowers, water marginal, Zn 4-9 — the THIRD pond iris beyond blue/yellow flag). All zoneId pond-area; species IDs marked model-read/inferred; care honest-not-fabricated per the hyperlocalization-audit lesson. | ✅ authored (3 pond irises now) |
| **Cattail entry (⚠️ monitor containment)** + **Cardinal flower** (pond-bog candidate) | ✅ **DONE 2026-07-25** — `cattail` (Typha sp., ⚠️ aggressive-spreader framing, propagate=don't) + `cardinal-flower` (Lobelia cardinalis, on-site find, Aug-2025 browse event recorded). Both zoneId pond-area. | ✅ authored |

**Existing-plant field history + reference:**
| Item | What it is | Status |
|---|---|---|
| **'Orangeola' Japanese-maple field history** → existing Japanese Maple entry | ✅ **DONE 2026-07-25** — appended to the `japanese-maple` guide: the 'Orangeola' struggled in deep shade (heat + powdery mildew from low airflow), recovered after a move to a brighter spot; the "shade-tolerant ≠ deep-shade-happy" lesson. | ✅ authored |
| **Mountain-laurel regrowth / boxwood self-layering / white-pine prop** notes | ✅ **DONE 2026-07-25** — appended to guides: `mountain-laurel` (regrowing from cut stumps under the power line), `boxwood` (self-layering = free propagation, how-to), `white-pine` (prop interest — honest note that pines don't root from cuttings; seed/graft are the real paths). | ✅ authored |
| **Heritage cherry tree** | ✅ **DONE 2026-07-25** — authored into `candidates.json` as `heritage-cherry` (a "worth considering" entry, since it's not planted yet). Framed as Paul asked: a summary of **what cherries work here and what don't**, highlighting the pick. Web-verified vs UGA B992: the 2,959 ft elevation meets the 1,000–2,000 chill hrs lowland GA can't → cherries feasible here; **sour/tart cherry is the better bet AND the most Bavarian** (Morello/Sauerkirsche), self-fertile, later-blooming; sweet cherry possible but harder (pollinizer, earliest bloom, disease); humid-SE disease + frost caveats noted. Exact cultivar = Paul's to lock (Montmorency easiest/UGA-backed; true Morello most heritage-faithful). | ✅ authored (candidate) |
| **"Home in Jasper Summary" (2025-07-19)** | ✅ **WALKED THROUGH 2026-07-25 (Paul asked what it is).** It's a ChatGPT-generated property-context brief Paul had it write as GPT "instructions." **Verdict: do NOT fold wholesale — little NEW verified fact, several traps.** (1) Most content is already in Fernwood (property/plants/wildlife/fishing/vehicles/pond). (2) Carries the SAME errors we've been fixing — pond/lake conflation (its "pond has bass/crappie/bream" is the LAKE, not the koi pond), the killed amphibian-decline hearsay, and pitcher plants (which Paul says they don't have). (3) Contains things that must stay OUT of public canon: family/brother detail (needs Paul's curation, see "Our Tate story") and firearms/shooting (ratified private). (4) Its elevation figure **2,865 ft** is another estimate BELOW property.json's "confirmed" 2,959 — corroborates the audit's elevation-confidence flag (SRTM ~2,888). (5) Some lines are ChatGPT guesses ("assume rural utility challenges," "off-grid-adjacent"). ~~The one genuinely useful OPEN fact: well vs municipal water is unknown.~~ ✅ **RESOLVED 2026-07-25 (Paul):** the house water is on a **community pump system serving part of Tate Mountain Estates** — neither a private well nor standard municipal. Recorded in `property.json` → `utilities.water`. | ✅ assessed — don't fold; water source now recorded |
| **Lake Sequoyah fishing reference** | 2025-03 lake-specific fishing intel. **Paul's steer 2026-07-25:** eventually summarize it into the **fishing-card** area, but figure out the right shape first — **pair it with mining the fishing photos we have for data**. Keep on backlog for now; don't author into `fishing.json` yet. | IDEATION — bundle with fishing-photo mining |
| **Japanese-maple seed-propagation program** | ✅ **DONE 2026-07-25** — folded into the `japanese-maple` `care.propagate` as a history log (Paul's real method: collect samaras around Grant Park → ~3-mo fridge cold-strat → soak → sow at Tate in spring; a few survivors scattered on the property, ~3 tiny ones in this year's crop; annual). Propagate months set to the annual cycle (collect Sep–Oct / sow spring) so the app can nudge it. | ✅ authored (history log) |

**✅ Reconcile flags resolved (Paul, 2026-07-22):**
- **Sarracenia/pitcher plants — they ARE in the pond at Tate.** Capture item added above; memory's earlier "Atlanta-only, too cold for Tate" note superseded.
- **Address: Fernwood is canonical.** The lone "Wildcat Road" mention in a 2025 chat thread was wrong.
- **Japanese maples → Fernwood** (being planted here) — item added above.
- **Pest-control facts stay in Fernwood; the hunting/firearms detail stays private** (kept out of this public repo — see the mine's round-2 report §E; cross-link only in a private surface). Ratified.

**⚠️ Reconcile learning (Paul, 2026-07-25) — the mine conflates the nearby LAKE with the property POND.** Two mine-sourced water items were mis-attributed to the pond when they belong to Lake Sequoyah: the **amphibian decline** (KILLED — hearsay, and it's the lake) and the **beaver** (they fell trees at the lake, not the pond — the property pond has no beaver and is healthy, full of frogs). **On any mine-sourced water/aquatic item, verify pond-vs-lake with Paul before authoring** — the two are 0.3 mi apart and the source text blurs them. (Bear-at-the-pond-liner and the deer/rabbit pond-edge browse were confirmed as genuinely the property pond.)

---

# TRACK B — Fleet & equipment (Paul-facing)

*Intent: a precise, deadline-aware record of Paul's vehicles + equipment — what's owed, what's been done, what's still unknown. Different user (Paul), tone (utilitarian, high-precision), and cadence than Track A. Began as one "Vehicles card"; now a co-equal system.*

## B1 · Live obligations (deadline-bearing — the only clock-bearing items in the whole backlog)

*Intent: things with a real-world deadline. **Surfaced into the backlog 2026-07-17 from the session log** (they were only in the pickup-point trail + the session anchor) so archiving the log doesn't bury them. Owner: Paul.*

| Item | What it is |
|---|---|
| ~~**GTI + Bolores registrations — OVERDUE**~~ | ✅ **RESOLVED 2026-07-22** — Paul renewed BOTH at a GA MVD kiosk (Fulton Co Kroger Glenwood): GTI $25, Bronco $60 (each incl. the $5 late plate penalty), both expire **2027-06-03** (JUN 27 decals). Receipt photo + details in `.private/service-records/`. Residual [CONFIRM]: when/where the GTI's 2026 emissions test was run (kiosk renewal implies compliance was on file; card's `lastEmissions` still shows 2025). |
| **GTI service bundle + coolant verify** | Coolant reframed to "verify at next shop visit" (no verdict). Bundle the coolant dye/pressure check + brake-pad check into the next **Autobahn** or spare-key **dealer** trip — no special trip. **⚠️ NEW 7/21: mention the cone-strike understrike** (front grill + underbody panels, DIY-repaired 7/21 — see `gti-2016` serviceHistory) so the shop eyeballs the radiator/condenser area + mounts on the lift. *(NB 7/22: the car is a MANUAL — the old "DSG overdue at 82,698" framing is void; manual gear oil is long-life, no DSG service exists to book.)* |
| **GTI spare key — dealer booking** | Dealer job (~**$450–500** all-in; MQB Comfort-Access, locksmiths can't). Spec confirmed off Paul's own key: **FCC NBGFS12P01 / VW 5G0 959 752 BE**. Phone-vet + book; bundle with the **Jim Ellis Chamblee** recall trip. |

## B2 · The record

*Intent: the accumulating history + running needs per machine.*

| Item | What it is | Gate / next |
|---|---|---|
| **Maintenance-values confidence audit** ⭐ | Re-verify **every** `maintenance` value on each fleet asset against its factory manual in `manuals/text/`, and honestly re-rate each `confidence` flag — today nearly all are stamped `verified` regardless of provenance, so the badge means little (a value marked `inferred` sits next to model-read guesses also marked `verified`). **Worked example that surfaced this (DR-Z400, 2026-07-22):** card read `oil: "Capacity ~1.4 qt with filter", confidence:"verified"` — but the factory manual (`drz400s-2001-service`, spec table) says **1.8 L / 1.9 US qt with filter** (1.7 L / 1.8 qt without; 1.9 L / 2.0 qt dry/overhaul), dry-sump = **two** drain plugs (crankcase + frame). The card was low by ½ qt *and* falsely "verified"; the ChatGPT-sourced 1.9 qt was actually right. **Next suspect: DR200 oil = `1.0 qt` (same too-low pattern).** Method: grep each `manuals/text/*` for the spec, correct the value, set `source:` to the manual, downgrade any flag the manual can't confirm to `inferred`/`researched`. | **✅ MANUAL-CHECKABLE PASS DONE 2026-07-25** (5 parallel agents, all 16 assets × their factory manuals). **Result: the fleet data held up — no WRONG values across ~120 maintenance keys** (the DR-Z400/DR200 fixes had already set the bar). Corrections applied (`vehicles.json` re-inlined): F-150 `transmissionFluid` `inferred→verified` (manual explicitly specifies **MERCON V**, not "unconfirmed"); Tiguan oil dropped the diesel-only "509.00" spec (508.00 is the petrol one); golf-cart `brakeFluid` line **deleted** (mechanical drum brakes, zero hydraulics in the manual); Kobalt battery flag resolved to **one battery** ("DUAL"=dual-blade) + `inferred→verified`; golf-cart `oilFilter` softened (dropped an "internal screen" over-claim); F-150 spark-plug note credits the manual-confirmed 0.054" gap. **Every part number correctly stays `inferred`** (P/Ns live in parts catalogs, not service text) — no provenance over-claims found. **Manuals-corpus note:** `husqvarna-mower-yth24v54.txt` is the WRONG chassis manual on disk (a YTH24V54 tractor, not the confirmed Z254F) — its engine specs (vs the FR691V manual) are still right; only the deck-belt P/N needs the Z254F parts fiche. Agent-drivable; surfaced by the OpenAI ChatGPT mine ([[project_openai_data_archive]]). |
| **Vehicle records — rest of the fleet** | GTI pile ✅ (7/09 + remaining batch 7/23), Bronco pile ✅ (7/20) — both fully ingested. Remaining: any future paper for the rest of the fleet (Tiguan/F-150/bikes have little on file). | Agent-can-drive when paper shows up. |
| **✅ Bolores eyeball queue — WORKED 2026-07-29; 2 items remain** | Paul worked the 17-item queue against the physical documents in one sitting. **Outcomes: the VIN is CLEARED** (`1FMEU15H5KL…`, four independent sources — GA title + City of Memphis inspection both read by Paul, plus the door-jamb label and a 2016 invoice), so **the Carfax is now safe to run**; the standing *"11th character reads A"* flag was **our own counting error** (position 11 is `L`). **Flags F-6 (Phil Trenary / Memphis provenance) and F-7 (the lift is Memphis 2011 @ 46,815, not GA 2016) are CLOSED.** Nine odometers Paul-verified. Corrections folded to `vehicles.json` + deployed (`5d2ff8d`, Worker `8a63ace2`): **transmission AOD→C6** (door-tag code `K` + a vacuum modulator the AOD does not have + *"remove… overhaul… REINSTALL"* = never swapped), **paint `2D`/`9T`** (the researched `2H` was wrong and ChatGPT's `85` was invented), the phantom interior code **deleted**, an aftermarket **BBK 3503 throttle body** added, and the engine identified as **"motor #2" (~20k mi, not 97k)**. **Downgrades, deliberately:** tire diameter **RESOLVED — she runs 35s** (Paul-verified off the actual tires 2026-07-29; DOT 3220 = 2020 rubber). 🚨 The old "33" was **laundered ChatGPT output** wearing a false "read off the actual sidewalls" provenance — see `_chatgptProvenanceWarning`: **four** wrong card values trace to two `_chatgpt-intake` threads, *"dual"* exhaust briefly removed then **restored and confirmed at the truck (two tips)**, and the 2018 emissions phase split — **A/C removal FALSIFIED (the truck's A/C works)**, air pump strong, **EGR unsupported** (its only document is a procurement diary). Two staging bugs found and fixed (one wrong source document, two un-rotated pages). **Still staged:** `05_EMISSIONS-notes-page`, `06_BELT-air-pump-proof`. Full trail: `.private/service-records/bronco-1989/EYEBALL-VERDICTS.md`; open queue: `EYEBALL-QUEUE.md`. | Owner: **Paul** — 2 crops + one trip to the truck (see the new physical-checks row below). |
| **Bronco door-panel repair** | Paul's stated "next big project"; guide + verified buy-list ready. **✅ [CONFIRM] SETTLED 2026-07-29 — Paul: "The driver's door panel is on. It's attached to the driver door right now."** So the BACKLOG line claiming "panel is out of the truck" was **wrong**, and `vehicles.json` + `guides/bolores-door-panel-repair.md` were **right** — both plan the panel coming off in a Phase-2 teardown. The STEP-6 one-removal protocol therefore governs **both doors from the start**, with neither door's removal already spent. *(The carpet being stripped had made "panel is out" look plausible; it was correctly left unconfirmed rather than inferred.)* | Physical work, owner: Paul. |
| **⭐ Bolores — the one-trip physical checks (2026-07-29)** | Seven questions that the TRUCK answers faster than any document, grouped because they are **one trip with a light**. Under the hood: **is there an EGR valve?** (the 2018 delete is unsupported — its only source is a shopping list) · **a smog/Thermactor air pump?** (removal is well-supported). Underneath: **catalytic converters?** (open hypothesis for the illegible word on the belt package — the A/C reading was falsified when Paul pointed out the A/C works) · **rear main seal weeping?** (flagged 2022, never shown fixed; an engine-or-trans-out job alone, nearly free if the drivetrain is ever apart — **so it bears on the paint-window decision**) · **transfer-case leak?** (flagged 2016) · **frame crack at the steering box?** (flag F-2, the load-bearing one on a lifted 351). From the seat: **shifter quadrant** (`P R N D 2 1` confirms the C6). ~~At the back: count the tailpipe tips~~ ✅ **ANSWERED 2026-07-29 — Paul counted TWO TIPS. She is DUAL**, so the record's original wording was right and the mid-day downgrade was an over-correction. ~~One fresh close-up of a tire sidewall~~ ✅ **DONE 2026-07-29 — 35X12.50R15LT confirmed.** Full list with provenance: `vehicles.json` → `bronco-1989.openMechanicalItems`. | Owner: **Paul** — one trip. Agent folds the verdicts. |
| **⭐ Bolores bed-rust corner — SCOPE IT (2026-07-27)** | Interior carpet is **already stripped**, so the corner is bare and scopeable now. **Next action is one test:** scrape + wire-brush the corner, then tap and poke with a screwdriver — sound metal rings and resists, perforated metal gives. That picks the repair scenario and feeds the Phase-1 walk-around. **Estimate (full detail on the card):** three things make this cheap — the bed floor isn't frame-structural (bonded patch, no welding), it lives under carpet, and the truck is being resprayed anyway, so the bar is *sound and sealed*, not invisible. **A** surface scale → an afternoon, $40–80, DIY. **B** localized perforation → a weekend, $100–180 DIY bonded, or $300–700 shop-welded. **C** wider spread → $800–2,000 shop (repro panels are cheap; labour is the cost). **Most likely A or a small B.** ⚠️ Scope *after* grinding to clean metal — a bubble is the tip, not the extent. **This gates the repaint**, so the real risk is calendar, not dollars. | Owner: **Paul** — the poke test. Agent folds the verdict. |
| **⭐ Bronco interior finish — carpet · headliner · deadening (2026-07-27)** | Full research + decisions folded into the `bronco-1989` card. **DECIDED:** molded ACC complete kit (~$339 Bronco Graveyard), mass backing declined, cargo area carpeted over a fresh factory-colour respray with **nothing bonded** back there. **Paint scope locked:** full respray of everything in factory colours, bed included, plastic trim in chestnut. **New:** `guides/bolores-shop-shortlist.md` (Tate→Atlanta corridor; verified vs directory-only vs ruled-out) and `guides/bolores-door-panel-repair.md` **STEP 6** (the one-removal protocol). **OPEN — Paul's:** headliner recover-vs-ABS (deliberately deferred until the boards are down and their condition is visible); carpet + headliner **colour**, which needs swatches judged against the aftermarket leather before ordering — ACC charges a 30% restocking fee, so there's one shot. **Blocked on conversations, not research:** Larry (doors to bare metal or scuff-and-spray? front floor pan in scope? how long is she with you?), Super Upholstery quote (carpet + both headliner panels + the seat tear as one package), All Classic Car Service (~2-month list, call early), and the **Canton-area shop already named in her paperwork** — the best lead nobody has followed. | Owner: **Paul** — decisions + calls. Agent folds outcomes. |
| **⭐⭐ Bolores COLOUR + CARRIER session (NEW 2026-08-04)** | **Colour is SOLVED and written as one map** on the `bronco-1989` card (`paint.⭐_COLOUR_MAP`), keyed to the PART each colour goes on so "chestnut" can never be ambiguous again: upper body **Scarlet Red 2D/6236** · lower accent **Light Chestnut 9T/6190** · **hardtop OUTSIDE = 9T too** (Paul was right — the brochure shows the fiberglass roof came in exactly four colours) · interior trim **Chestnut = SEM 4168** · **hardtop INSIDE = SEM 4168, same product**. ⚠️ **THERE ARE TWO CHESTNUTS** — exterior *Light Chestnut* (9T) vs interior *Chestnut* (SEM 4168); always say which. **Two corrections landed:** 2D is **NOT Cabernet Red** (that is 2H/6156, a real and different 1989 colour — the name was orphaned when the code was corrected 2H→2D on 7/29 and read as verified for six days); and the 7/29 deletion of "tan/saddle" went one step too far — MPV was a misread, the *colour* never depended on that label and the factory brochure now confirms it. **DECIDED:** factory colour over a spectrophotometer scan (a scan reproduces 37 years of fade; a factory colour is *reproducible* — SEM 4168 still buys the same thing in ten years). ⚠️ **The carpet order and the trim colour are ONE decision** and they sit in two different rows. **CARRIER:** the **factory** carrier is the root cause of the tailgate cracks (it latches to the tailgate; 33s overload it) — sequencing gate now cross-linked into bodywork/tailgate/repaint: remove factory carrier → bodywork incl. its quarter-panel holes → fit the bumper carrier → **paint last**. Its swing-out hinges are **pin-and-barrel, unworn, and need NO parts** — clean, grease, reassemble (buy only 4× ½" USS washers). **NEW FILES** in `.private/service-records/bronco-1989/`: `REFERENCE.md` (the Bronco reference guide + **where the corpus is**), `SOURCES.md` (evidence tiers A/B/C), `tire-carrier-hinge/` (17 photos + spec), `_assets/` 11 MB incl. the cert label, the factory brochure page and SEM's 1989 trim-code sheet. | Owner: **Paul** — the physical work + the carpet/trim colour call. Agent has folded all research. |
| **✅ Bolores corpus — ALL FIVE LEGS CLOSED (4 gaps 08-05 · links 08-04/08-06, re-verified 08-07)** | ⚠️ **This row read fully open until 2026-08-06 and was stale by one day** — commit `acf14f0` (08-05 13:20, *"Mine the LMC catalogue, make the assets greppable, index the fleet sweep"*) closed every one of ①–④ and no row was updated after it. Re-verified against the world 2026-08-06, not against the checkbox: ① ✅ `tools/extract_assets_text.py` exists and `_assets/` now holds **11 `.txt` extractions** — the SEM trim-code sheet, the brochure page, the cert label and all three wiring refs are greppable. ② ✅ `tools/mine_parts_catalog.py` + `PARTS-SHORTLIST.md` (regenerates clean today, rc=0, 206 lines): **tailgate 18 · door-panel 21 · tire-carrier 4 · body-mount 3** candidates, each double-filtered on application-names-Bronco **and** year-range-covers-89, with the filtered-out counts printed so the filter is auditable. ③ ✅ all five `guides/bolores-*.md` indexed in REFERENCE.md §194–198, and its own to-do #275 is struck through. ④ ✅ `tire-carrier-hinge/` is in the SOURCES.md tier table as **A + B** with an explicit does-NOT-prove column. **The lesson is the row, not the work:** this is [[feedback_unchecked_box_is_not_open_work]] firing exactly as described — it over-reported open work, in the safe-looking direction, and one `ls` settled it. *(Original text follows for the record.)* ① **`_assets/` is NOT queryable** — the cert label, brochure page, SEM sheet and 3 wiring refs are images/PDF with **zero text extraction**, so the corpus's own "`text/` is the queryable substrate" rule was never applied to it; SEM 4168 and the paint codes exist only as pixels. ② ⭐ **The LMC FD.88 catalogue is ingested but UNMINED** — 1.46 MB of text already on disk, no network needed, hitting the live items directly: **tailgate 187 · door panel 60 · tire carrier 13 · body mount 12**. Highest value per effort on the whole Bolores side. ③ The five `guides/bolores-*.md` (60 KB incl. the 21 KB door-panel guide) are **not indexed** in REFERENCE.md — two doors into the same knowledge. ④ `tire-carrier-hinge/` sits outside `_assets/` and is not in the SOURCES tier table. 🚨 ~~**AND THE LINKS ARE DECAYING**: of 27 Bronco bookmarks, **11 (41%) are no longer agent-retrievable** — 7 fullsizebronco threads now return a tollbit paywall handshake (the transition was observed live on 08-04), 3 hosts 403 (LMC, eBay, OEM Parts Online), 1 500s; 4 more are YouTube where a link is the only holdable form. **Nothing behind any link is archived.**~~ **← FALSIFIED 2026-08-05, kept struck so nobody re-derives it.** Paul caught it from memory (*"if you open those eleven in Chrome, something will come up — I thought we did that"*) and was right on both halves: the 403s were **bot-blocks, not rot**, and **8 threads had already been archived on 08-04** into `_assets/forum-threads-archived-2026-08-04.md`. Correction trail: `REFERENCE.md` §264-270 + `BOOKMARK-SWEEP.md` L40/L114. **`curl` returning 403 is a fact about the client, not the page.** ⚠️ **Also 2026-08-04: a 76 MB duplicate of FD.88 was created in `_assets/` when it had been in `manuals/` since 08-03** — byte-identical, trashed. The rule now written in REFERENCE.md: *manual/catalogue → `manuals/` + a row in `INDEX.md`; vehicle-specific artifact → `_assets/` + a row in `SOURCES.md`; never both — and check `INDEX.md` before saving anything.* | Owner: **agent — ①②③④ DONE, and the fifth leg is DONE TOO.** 🚨 **The 08-06 rewrite of this row re-propagated a number that had been falsified the day before** — it said "one leg genuinely remains: nothing behind any link is archived, 11 of 27 unretrievable." Re-verified 2026-08-07 by probing, not by reading: **8 threads archived 08-04**; **images recovered 08-06** (surveyed all 8 threads — only 3 carry a content image; all 3 fetched first try with plain `curl`, so the images were never paywalled, only the HTML); `ocautocarpets` goes **403 → 200 on a User-Agent swap alone**, which is the bot-block proven in one command. Of the 30 links in REFERENCE.md today: **1 hard rot** — `www.sourceresearch.com` is **NXDOMAIN** (the domain is gone, not the page; ⚠️ NEW, it was not in the 08-04 count) · `justgivemethedamnmanual.com` still 500s · 5 hosts 403 to a bot and load in Chrome · 7 fullsizebronco answer 202 (tollbit) but are already archived · 1 URL 404s because **the stored link is truncated** (`…Weatherstrip-Kit/produc`, REFERENCE.md L142) — a transcription defect, not decay. 🔵 **Residual, both small and neither this row's:** (a) the 3 images recovered 08-06 have **no `.txt` sidecar** — `python3 tools/extract_assets_text.py --check` exits 1 and names them, incl. the ⭐ fuse-detail payload; one re-run closes it. (b) `manuals/LINKS.md` L136-139 — a **tracked** file — still states the falsified 11/41% verbatim, and **contradicts its own next section** (L148-151: *"403s are bot-blocks, not dead pages… an automated check must not be read as link rot"*). ⚠️ Still true regardless: the links/bookmarks layer belongs to the bookmark-sweep thread (`~/.claude/handoff/BOOKMARK-SWEEP.md`); **do not build a second link index.** |
| **Receipt-mining residuals** | ✅ **[CONFIRM] flags ALL CLEARED 2026-07-22** — Paul batch-resolved the eBay install statuses (8 items), plug/filter, drain plug, LIFMOCER, chainsaw receipt in one pass; receipts stream is folded (card rows queued behind the 7/22 maintenance audit). What remains of `CANDIDATE-ROWS.md` is **decision/inspection work, not verification** (12V-outlet approach, RetroSound head-unit call, carrier bushings) — surfaces at Paul's next wrench session, not agent-drivable. | Residual: Paul's wrench-session decisions only. |
| **Off-machine backup target (R2 vs Google Drive)** | The only unbuilt piece of service-records durability. | Paul's decision; Apple Photos is the interim second copy. |
| **⭐ Generac 7000EXL — recommissioning sweep (NEW 2026-08-04) 🍂 seasonal** | **The machine was missing from the fleet entirely** until 2026-08-04 — `vehicles.json` tracked 17 assets down to the trimmers and had zero hits for "generac" or "generator". Surfaced by the bookmark-export analysis (`~/Desktop/Claude/bookmarks-analysis-2026-08-04.md`): five saved sources from Aug 2025, four of them "won't start after sitting" walkthroughs, plus the manual scan. **Paul, 2026-08-04: it is decommissioned and fully drained of anything and everything** — so this is a storage state, not neglect, and the open question is whether it comes back. Card added with every `maintenance` value honestly stamped `tbd`. **Three pieces of work:** ① **ingest the manual** (`archive.org/details/manualsbase-id-609002` → `manuals/pdf/` + `manuals/text/generac-7000exl.txt` via `download.sh`, then `INDEX.md` row with a confidence rating) and fill all six `tbd` values; ② **produce a recommissioning sequence** — a drained unit needs fluids, a carburetor that has sat dry, and almost certainly a **new starting battery** (it's electric-start); the four bookmarked walkthroughs are the informal half, the manual is the authoritative half, and where they disagree the manual wins; ③ **a parts/consumables buy-list** so the first start attempt isn't three trips. ⚠️ **Confirm the model off the unit's data plate before ordering anything** — `7000EXL` currently comes from Paul's bookmark titles, not from a read of the machine ([[feedback_verify_scanned_image_inferences]] applies: a bookmark title is not a data plate). | **🍂 The gate is the weather, and it's the only real clock in Track B.** Paul: *"a good thing to test before it gets cold."* A generator first tested during the outage it was bought for is a generator you don't have — so the physical start attempt wants to land **well before first frost (~Oct 17 at Tate)**, with slack for parts. **Agent-drivable now:** ① and ③, plus drafting ②. **Paul's:** the data-plate read and the start attempt. |
| **📥 Manuals-corpus ingestion queue (NEW 2026-08-04)** | **`manuals/LINKS.md` now exists** — the fleet's second authority file, for references we do *not* hold as files, and the discriminator against `INDEX.md` is mechanical: **is there a file on disk?** Yes → INDEX, no → LINKS. So the two cannot both claim a document, which is the FD.88 duplicate prevented structurally rather than by remembering. **Four rows are queued 📥 for ingestion:** the **Generac 7000EXL manual** (archive.org, title API-verified — belongs to the Generac row above), the **Yamaha G22-AX2 2006 manual** (compare against the G22A manual already held before ingesting — it may add nothing), and **two cmsnl parts fiches** (DR-Z400 2004 K4, DR200SE 2009 K9) which are the complementary document to the two Suzuki *service* manuals already on disk. 🔴 Also: the **G22A wiring diagram is a hard 404** and needs re-sourcing — it was the only wiring diagram found for the cart. Each ingestion = `download.sh` → `pdf/` + `text/` → an `INDEX.md` row with page count, match confidence and source, and **delete the row from LINKS.md** (the migration *is* the ingestion). ⚠️ Confirm year-drift applicability per assembly on both fiches — 2004/2009 documents against 2001/2017 machines. | 🔵 **Agent-drivable now.** No Paul input needed for any of the four. The re-source of the wiring diagram is a search, also agent-drivable. |
| **⚠️ The manuals QUERY GAP — one gap, TWO builds, and only one of them is open** *(reconciled 2026-08-04)* | A concurrent session scoped *"a local index over `manuals/text/` for the whole fleet — the obvious next build"* and correctly did not start it. **It is not homeless, and taken as one build it would silently re-open something Paul parked.** It splits cleanly: <br><br>**① The deterministic door — OPEN, and it is Track B.** A fleet-wide search over `manuals/text/` that Paul can run **without invoking a model**. Today querying is `grep -rin`, which works but doesn't span filenames, doesn't rank, and doesn't tell you *which machine* a hit belongs to. This is squarely [[feedback_non_ai_door]] `[paul-stated 2026-08-02]` — *"anything deterministic must be reachable without invoking a model"* — and its falsifier applies almost word for word: **if the only way to learn what your manual says is to ask Guru, this is broken.** Nothing about it is AI work; it needs no test harness and no expert panel. <br><br>**② Guru reading the manuals — PARKED, and not ours to start.** That is the retrieval/tool-use thread at **§A6, the "How to evolve Guru's capability" row** — ⏳ **Paul's**, deliberately held for the expert team. Two findings there already constrain it: ai-advisor's 2026-07-26 correction that **retrieval solves volume, not confusability**, and the harder one — **a test harness is the true prerequisite**, because probing `/api/chat` forges a Mom-input signal and corrupts the engagement measurement. <br><br>**The failure mode this row exists to prevent:** building ① and describing it as progress on ②. They share a substrate and nothing else. | **① 🔵 agent-drivable, unblocked.** **② ⏳ stays parked — do not start it from here.** The `text/` corpus is held out of the digest on purpose to protect its ~57K/80K budget; that stays true either way. |
| **Per-vehicle mileage/hours + last-service anchors** | Add anchors to all 15 assets. | Needs Paul's odometer/service readings. |
| **Tiguan / F-150 profile enrichment** | Fill the near-empty vehicle profiles. | Needs Paul's history input. |
| **Guru-machines deferred bits** | On-card per-vehicle input; "which one?" disambiguation chip; notes-lister CLI. | ask-then-log made them unneeded; revisit on signal. |

## B3 · Data collection (was CLAUDE.md "Outstanding for Paul")

*Intent: specific facts to read/collect off the machines. **Folded here 2026-07-17** from CLAUDE.md's "Outstanding for Paul" list — read + update it here now.*

1. ~~**Husqvarna riding mower:** model sticker~~ ✅ **RESOLVED 2026-07-12 — it's a Husqvarna Z254F zero-turn (54"), read off the deck sticker; consistent with the Kawasaki FR691V engine already on file.** (Still open: the primary mower-belt P/N — first digits were worn illegible.)
2. ~~**Homelite trimmer:** confirm UT33650A vs UT33550A~~ ✅ **RESOLVED 2026-07-27 — it is the UT33650A (straight shaft).** Two independent legs closed this and neither needed the smudged digit: (a) `manuals/INDEX.md:58` had already established that **UT33550A is not a real model**, so the alternative this row asked Paul to rule out never existed — the real pair is curved `UT33600A` / straight `UT33650A`; (b) **Paul read the shaft off the machine — it is straight.** A direct look at the object beats an inference from an ambiguous sticker (see `feedback_absence_of_records_is_weak_evidence`, ratified 2026-07-27). ⚠️ Note this row and `manuals/INDEX.md` disagreed for ~10 days, with the INDEX carrying the answer the BACKLOG was still asking for — a self-contradiction, which per doctrine is our own bookkeeping error before it is anything about the world.
3. **Homelite blower/vac:** no model sticker found on the unit. Maintenance specs are inferred from the trimmer's engine family (HHCPS.0264AT). Acceptable for at-a-store reference.
4. **Annual: NASA SVS Dial-a-Moon visualization ID** — when SVS publishes the 2027 visualization (usually Dec/Jan), update the `DIAL_A_MOON_VIZ` constant in viewer.html (`year`, `parent` bucket, `id`). Find the new ID at svs.gsfc.nasa.gov/gallery/moonphase. Until refreshed, the moon hero hides cleanly once the year flips.
5. ~~**Bolores paint codes — door-label photos**~~ ✅ **RESOLVED 2026-07-29 — and it needed no new photo.**
   The label photo already existed, buried in the OpenAI/ChatGPT export; it was recovered, preserved to
   `.private/service-records/bronco-1989/_assets/`, and **read in full by Paul**. **EXTERIOR PAINT COLORS = `2D`
   (upper) + `9T` (lower).** The researched candidate `2H/M6156` was **wrong**; `9T` was **right**. The ChatGPT read
   of **`85` was fabricated** — it appears nowhere on the label (it looks to have taken `U154`, the TYPE-GVW field).
   The claimed interior code **`MPV` was deleted, not corrected** — `MPV` is the label's **TYPE** field
   (Multi-Purpose Vehicle); there is no interior trim field on this label at all. ⭐ **This gated the respray** —
   mixing off `85` or `2H` would have put the wrong red on a truck that gets painted once. Residual: the *code* is
   verified but the *colour name* is not — have the supplier cross-reference `2D` against a 1989 Ford chip chart
   before mixing. **The VIN came off the same label** and is verified on four sources; see the eyeball row above.
6. **GTI — confirm exact mileage:** anchored at ~81k from Paul's estimate (2026-06-28). Read the odometer next drive and update `gti-2016` `mileage` + the spark-plug/DSG/carbon framing if it's off. *(NB: 82,698 recorded 7/11 from the Express Oil invoice — reconcile.)*
7. **Tiguan paint color code:** read the VW data sticker (spare-wheel well under the trunk floor, or driver door jamb) before buying touch-up paint for the new "Paint touch-up" step — the touch-up has to match.
8. ~~**GTI spare key — own-key spec**~~ ✅ **RESOLVED 2026-07-14 (and it CORRECTED the assumption).** Paul read the FCC ID off his own working fob → **FCC NBGFS12P01 / VW 5G0 959 752 BE family / 315 MHz / MQB48 / HU66 / CR2025** (2015-19 MK7 GTI Comfort-Access flip key; web-verified). This is a **different, newer generation** than the card's prior `5K0 837 202 AK` / `NBG010206T` spec (which came from the Facebook Marketplace keys — the WRONG generation for this car). Card corrected; source any spare to **NBGFS12P01 / 5G0 959 752**, not the 5K0.
9. **Marietta dealer name:** verify it's still "Volkswagen of Marietta" when calling — the old "Jim Ellis VW Marietta" listing reads closed (possible rebrand/ownership change). Recorded under the current name in `gti-2016` `serviceContacts`.
10. ~~**⭐ Bolores transmission — identify the actual unit**~~ ✅ **RESOLVED 2026-07-29 — she is a C6 3-speed.**
    The card's "AOD 4-speed" was wrong and is corrected. Four independent legs, three of them Paul-verified:
    (1) **door-tag `TRANS: K`** = C6, read off the label; (2) the C6 is the factory automatic behind the **5.8L**
    (the AOD went behind the 5.0L); (3) ⭐ **the 2010 Broadus overhaul replaced a VACUUM MODULATOR and its hose** —
    C6 hardware the **AOD does not have** (it uses a TV cable), so this is evidence about the physical object, not a
    code decode; (4) that invoice reads *"remove transmission, disassemble and inspect, overhaul transmission
    complete, **REINSTALL** transmission"* — the same unit went back in, and a full chronological scan found **no
    swap anywhere 2010–2025** (only seals and linkage since). ⚠️ **Stated limit:** there is no documentation at all
    for 1989–2010 (~46,000 mi), so the pre-2010 history cannot be cleared from paper — but the unit present in Dec
    2010 was vacuum-modulated and was rebuilt rather than replaced. `specs.transmission` + `transmissionFluid` both
    rewritten (the old fluid note reasoned from the wrong gearbox; Broadus actually filled it with **Dexron III**).
    ChatGPT's "E4OD" was wrong twice over. **Free confirmation, not adjudication:** a C6 is a THREE-speed — the
    quadrant `P R N D 2 1` closes it; an overdrive position would mean a swap. Folded into the physical-checks row.
11. ~~**DR-Z400SM — NGK #1275 plug + APE fuel filter, installed or on-hand?**~~ ✅ **RESOLVED 2026-07-22: Paul — "both should be installed."** The 7/12 "in the shop" remark is superseded. Both fold as DR-Z serviceHistory rows (purchases export-verified: APE filter 2025-06-25 $10.58, NGK CR8E 2025-07-16 $6.42; install date unrecorded — logged as "by Jul 2026"). Card rows queued behind the 7/22 maintenance audit.
12. ~~**Magnetic drain plugs (golf-cart trials) — which one was kept?**~~ ✅ **RESOLVED 2026-07-22 (Paul + purchase math): the cart's installed plug is the M12.1×1.5 oversize piggyback repair plug** (April 2026, the spring stripped-thread fix — Paul independently recalled "twelve point one by one point five"). The magnetic plugs were trials: one returned, one shelf spare — which one is deliberately not-worth-chasing (Paul). The Oct-2025 ChatGPT "M14×1.5 confirmed" read is contradicted by the hardware. Cart maintenance row queued.
13. **SEALIGHT 194 LED interior bulbs (2700K warm — dome/map lights, NOT headlights; row wording corrected 7/22) — purchase provenance gap.** Install is Paul-confirmed (kept warm, returned both white sets) but the purchase exists only in the 7/12 order-history screenshots (truncated title, no price/order#) — absent from the Amazon data export AND both Gmail mailboxes. Paul 7/22: "not sure where those came from." Stays install-verified / purchase-inferred; settle only if wanted via the Amazon app around the Oct 17 2025 order date.
14. ~~**LIFMOCER jump starter — refunded order, disposition unknown.**~~ ✅ **RESOLVED 2026-07-22: Paul — bought it, too small for the Bronco, returned it.** No replacement; the AVAPOW 1500A (2022) remains the kit's jump starter. Row closed as returned. (Items 11–14 all surfaced by the Amazon deterministic pass 2026-07-22; 11/12/14 batch-resolved by Paul same day — see `.private/service-records/bronco-1989/VERIFICATION-2026-07-22.md` §Amazon.)
15. ~~**GTI — CarMax acquisition data (owner-history).**~~ ✅ **RESOLVED 2026-07-23 — the paper pile beat the data pull.** The remaining-GTI-paper ingest (29 scans) carried the full purchase packet: $21,998 base / $25,126.87 OTD @ 43,101 mi, **the $4,000 CarMax payment = the down payment** (order #633679 matches), financed 3.45% and paid off by 6/2021 (two lien releases). Folded onto `gti-2016.acquired` + serviceHistory; full detail in `.private/service-records/gti-2016/EXTRACTED.md` ("## 2026-07-23"). The CarMax data pull is now optional corroboration, not a blocker.
16. **Maintenance-audit physical reads (from the 7/22 manuals audit).** ⚡ **Mostly RESOLVED FROM THE MANUALS by the 2026-07-25 5-agent audit:** (a) ~~F-150 ATF type~~ ✅ **MERCON V** — the manual specifies it explicitly (no dipstick read needed; card now `verified`); (c) ~~golf cart brakes~~ ✅ **mechanical drums, zero hydraulics** — `brakeFluid` line deleted; (d) ~~Kobalt two batteries?~~ ✅ **one battery** (the "DUAL" = dual-blade); (e) ~~DR200 oil~~ ✅ already handled (`inferred`, manual self-contradiction captured). **Still needs a PHYSICAL read — only (b):** MS 290 chain/bar **gauge** stamped on the bar before buying chain (intl manual says 0.063", US Farm Boss saws often 0.050" — card stays `inferred` until Paul reads the bar stamp). Also still open (manual can't resolve): GTI manual-gearbox oil spec (G 052 527 A2 — not on disk) + Tiguan US-spec 8-speed trans fluid (on-disk manual is the intl DSG edition); and F-150 `specs.transmission` reads "4R70W" but the 2006 manual calls it "4R70E/4R75E" (minor designation drift, fluid MERCON V either way).

## B4 · Photo layer

| Item | What it is |
|---|---|
| **Photo-library vehicle/repair-photo miner** | Mine the ~50K-photo library for per-vehicle machine + teardown shots; propose-then-confirm. ~~now its own project (`~/Developer/photo-miner/`)~~ **photo-miner was RETIRED into `photo-organizer` (its P10 housekeeping); the live successor is the VISUAL-JOURNEYS thread there `[paul-stated 2026-08-03]`**: serviceHistory ids minted fleet-wide (56 entries), photo-organizer reads `vehicles.json` in place, candidate join = date-window × probe × labels × in-progress verdicts → Paul curates + describes, mappings accrete as `service:<vehicle-id>:<sr-id>` tags. See CLAUDE.md "Cross-project linkage". The `photo-seed.json` (68 Bolores truck images) remains the standing seed. |

## B5 · Smart-home systems (Nest) — inventory + live feed (idea, 2026-07-20)

*Seeded from the Google-Takeout Nest export (`~/Developer/google-takeout-archive`). The inventory leg is DONE; the live-feed leg is an idea pending signal + a real access decision.*

- **✅ Landed — device inventory → `devices.json`.** The Tate property's smart devices, captured deterministically from Google's Nest export: **1 Nest Learning Thermostat (3rd gen)**, Family Room, serial `REDACTED-S19`, added to the "Tate" structure 2025-11-10. Key property fact surfaced: **heat is propane (LP) forced-air** (backup heat + cooling electric) → implies an on-property **propane tank** to monitor/refill (a genuine maintenance item — candidate for a Track B1 obligation once tank size / refill cadence is known). Unwired data file for now (not inlined into `viewer.html`).
- **💡 Idea — a Nest live feed, like the weather station.** Fernwood already renders one live signal (the on-site Ambient Weather station). The cabin thermostat could be a second: **current indoor temp + heat/cool state at Tate** as a "glance" tile ("the cabin is 58° right now") — useful remotely (Paul away; is it holding heat?) and calm enough for Mom's surface. Fits the deterministic-capture rule (raw device data, no AI). Would follow the same live-fetch pattern as `renderWeather()`.
- **⚠️ Access reality (why this is an idea, not a task).** The **Takeout export is a one-time dump, not a feed** — same lesson as the Google Photos API lock. A live Nest feed needs the **Google Smart Device Management (SDM) API**: a Google Cloud project + **Device Access registration (one-time $5 fee)** + OAuth consent. Non-trivial plumbing and a small cost. Decide whether the "is the cabin holding heat?" glance is worth that before building. Cheaper interim: the propane/HVAC facts already captured are static and need no API.
- **Owner:** Paul's call on whether to pursue the live feed. Inventory data is agent-maintainable.

## B6 · Household systems — a third `group`, not a new domain (Mom's ask + Paul's shape, 2026-07-26) ⭐

*She proposed this unprompted, in one run of texts, and derived the taxonomy herself. Recorded in her words because the wording is evidence, not decoration.*

**What she asked for (9:04–9:06 AM ET):** the most important systems *within the house* — furnace, hot water heater — carrying make / model / age, with **receipts or service orders attached**, and per-system practical notes (her example: filter size for the furnace). She named the structure herself: **_"there are vehicles there is equipment and there are household systems."_** Full wording in `.private/mom-feedback-2026-07-26.md`.

| Item | What it is | Gate / next |
|---|---|---|
| **✅ SHAPE DECIDED (Paul, 2026-07-26) — one new enum value, not a new file** | `vehicles.json` **already carries a `group` field with exactly two values** — `vehicle` (7) and `equipment` (9). **Mom named the third.** Add `group: "household-system"` and it inherits everything: every entry already has `specs` (make / model / serial / manufacture date = her *"make model age"*), `maintenance` (per-item specs = her *"filter size for the furnace"*), and 7 carry `serviceHistory` (= her *"receipts or service orders"*). The card already renders grouped maintenance blocks. **She did not invent a category — she noticed a slot that was already shaped and empty.** *(Rejected: a separate `systems.json` + its own card — duplicates the schema, the render logic and the receipts pipeline, and splits the record of the place in two.)* | **Build it.** Two cheap accelerants: **B5's Nest export already holds the furnace** (propane LP forced-air + thermostat make/model/serial/install date) — that is entry #1, already captured; and `vehicles.json` is **excluded from Guru's digest** by design, so this costs **nothing** against a context budget already at its ceiling. |
| **Receipts / service orders — the pattern already exists one card over** | Her *"would be fantastic if I could attach Receipts or Service orders"* is **exactly** the vehicle service-records pipeline (receipts → `serviceHistory[]` + a private backup for the raw document). Extending it to household systems is a **known shape, not a new build**. The real user problem is in how she put it — she doubts she still *has* the receipt for the new hot water heater. The record is worth having **precisely because the paper is lost.** | Reuse the vehicle service-records path. Note the repo is **public** — raw receipts stay private-side, same split as vehicles. |
| **Naming pass — dispatched to content-steward 2026-07-26** | With a third sibling, "Vehicles & Equipment" no longer covers a truck, a cordless mower and a furnace. Paul's ask: rename the card and the higher categories for consistency now that the contents have drifted. **Hard constraint: "household systems" is HER coinage and is ratified — if anything gets levelled it is the inherited two, never hers.** The internal `group` values and the `vehicles.json` filename are advisory only (referenced by `VEHICLES_DATA`, `check-data-inline.py`, `build-digest.py` + others — real churn). | Awaiting content-steward's proposal + churn verdict. |
| **Cross-ref: B5 already holds half the data** | **B5 (Nest) already captured that the cabin's heat is propane (LP) forced-air**, plus the thermostat's make/model/serial and install date, from the Google Takeout export — i.e. **the furnace she is asking about is partly already in the record**, just not on a surface she can see. The propane tank noted there as a candidate maintenance obligation belongs to this domain too. | Fold B5's device inventory into this domain rather than standing up a parallel one. |
| **Register note (for the researcher pass)** | Even while designing the thing herself she hedged about her own wording. Same not-wanting-to-be-wrong reflex as the confirm cards (A3). Worth reading as one behaviour, not two. | Evidence for the A3 finding. |

## B7 · Interactive 3D Bolores — clickable repair-story showpiece (idea, 2026-08-02)

*Paul's idea (2026-08-02): a 3D model of a vehicle you can spin, with clickable parts that open the
repair stories — a portfolio showpiece weaving in the documented work, and a learning project.
Feasibility studied same day; filed IDEATION — "lots of other stuff to do."*

- **Vehicle: Bolores (the '89 Bronco), decided.** She has 10× the material of any other vehicle: 18
  public `serviceHistory` rows, the provenance chain back to the 2011 Memphis lift build, both engine
  swaps dated, 35 mined diagnostic threads, and the 68-photo seed (`photo-seed.json`, B4). A clickable
  model is only as good as what's behind the clicks. (Also practical: dirt bikes photogrammetry-scan
  poorly — thin tubes and spokes; a big boxy truck scans well.)
- **Interaction layer is a solved pattern, no game engine:** Google's `<model-viewer>` web component
  gives rotate/zoom + clickable **hotspots** anchored to model points, each opening a detail panel fed
  by the existing `serviceHistory` data + photos. Static page, weekend-buildable. Upgrade path if
  wanted: Three.js (camera fly-tos, part highlighting, exploded views).
- **Asset path — Paul's constraint: it must read as HER (lifted '89, her stance/paint), so:**
  - **Primary: photogrammetry-scan the actual truck** (Polycam or similar, iPhone, ~30–45 min walk-around;
    overcast light, hood popped so the engine bay captures; underside won't scan — no hotspots live
    there). Captures lift/tires/paint exactly *because it is her*. Output = GLB, drops into the viewer.
  - **Placeholder to build against now:** free correct-generation (bricknose, not '66–'77) stock model
    exists — Sketchfab "Ford Bronco 1989" (EGGJUICE, 39dc64e2), more on TurboSquid / 3D CAD Browser.
    Build the whole experience against it; swap one file when the scan lands. Nothing wasted.
  - **Fallback: Blender-modify the stock model** (raise body vs. axles, scale tires, repaint material) —
    modest, scriptable, a legit learning detour, but always an approximation. Only if the scan disappoints.
- **Privacy rail:** hotspot content draws ONLY from the public `serviceHistory[]` card data (already
  PII-scrubbed) — never `.private/…/EXTRACTED.md` (costs, VINs, prior-owner detail).
- **Where it lives:** its own standalone page, not Mom's surface — different audience (portfolio
  viewers). The Fernwood vehicle card can link to it.
- **Portfolio framing (the career tie-in):** the 3D page is the hook; the deeper story is the chain
  behind it — receipts + photos mined through the verify-before-publish pipeline into structured data
  now feeding a 3D front-end. Cross-ref `~/Developer/Career` when it ships.
- **Status: IDEATION — feasibility CLEARED, deliberately parked.** Gate: Paul says go. First mover
  when picked up: prototype `<model-viewer>` + stock GLB + 2–3 hotspots wired to real
  `serviceHistory` rows; the scan is a separate nice-day errand.
- **↻ 2026-08-03 — the substrate this needs started building (B7 itself stays parked).** Paul's
  "marrying more threads" session: every `serviceHistory` entry fleet-wide now carries a stable
  `id` (hotspots can reference `service:bronco-1989:<sr-id>` durably), and photo-organizer is
  growing the photo↔entry mapping via Paul's curate-and-describe review. When B7 unparks, the
  clicks open onto mapped photos, not just card text — and the part-level tags that review
  produces are exactly the hotspot vocabulary. Nothing here changes the gate.

---

---

# TRACK C — Cross-cutting / infra / doctrine

*Intent: things that serve both products or the meta-stack.*

| Item | What it is | Gate |
|---|---|---|
| **Worker deploy automation — arm the secret** | GitHub Action `.github/workflows/deploy-worker.yml` is built + runs green, skipping the deploy until armed. **One-time: add repo secret `CLOUDFLARE_API_TOKEN`** (Cloudflare → API Tokens → "Edit Cloudflare Workers"; GitHub → repo Settings → Secrets → Actions). After that, Worker changes self-deploy on push. | **⚡ NOT A GATE ON ANYTHING — proven by command 2026-07-28, not by prose.** `test -x tools/deploy-worker.sh` succeeds: `19aa3aa` made deploys **agent-runnable without the token** (run it with the Bash sandbox disabled — see `CLAUDE.md`). Arming the secret is a convenience that moves deploys onto push; it is Paul's (external account) and **optional**. Anything previously recorded as "blocked on the Cloudflare token" was blocked on nothing. |
| **🔑 Rotate the Ambient Weather API key** (found 2026-07-25) | The Ambient `applicationKey` + `apiKey` are hardcoded as fallbacks in `tools/record-daily-rollup.mjs` AND embedded in `viewer.html` — served world-readable on the public Pages site (confirmed raw-fetch 200). Moderate blast radius (read access to the station + rate-limit burn, nothing financial). **Rotate at ambientweather.net;** then Claude will move the new key to a GitHub Actions secret + strip the hardcoded fallbacks from both files (one pass). The rain backfill ran on the current (exposed) key at Paul's direction 2026-07-25. | **WAITING ON PAUL** (secret) — say "rotated" → Claude does the de-embed cleanup. **🔴 RE-VERIFIED STILL LIVE 2026-07-28** against the *public* raw file: both 64-char hex literals are retrievable from `raw.githubusercontent.com` right now. ⚠️ **Pointer corrected — they sit at `viewer.html:6451-6452`, NOT 6389-6390**; that day's edits shifted them, and a stale line number on a security item is exactly how it gets read as already handled. Also present in `tools/record-daily-rollup.mjs` and `.github/workflows/record-weather.yml`. Exposed since `99f0f07` (2026-05-05) = **84 days**. Blast radius stays small (read access to one weather station), which is why deferring is still reasonable — but this is **deferred, not done**. **Unpark trigger (Paul's call 2026-07-27):** *"we'll do the ambient de-embed as part of a Fernwood working session"* — not as a standalone fix. |
| **✅ ONE shared entity-resolution map — "assumed plants" hit THREE times in one day** (filed 2026-07-26 · **SHIPPED 2026-07-27**) | Every place that resolved a card's `entityRef` to a record re-implemented the lookup, and each was written assuming plants — all three shipped broken and none failed loudly: `fold-answer.py` degraded weed cards to *"entity not found in plants.json"*; `read-mom-feedback.py`'s probe did the same; and `buildCard` gated on `eref.type === "plant"`, so **`q-weed-stiltgrass` was served for six days with a photo Mom took rendering nothing**. **Now collapsed:** `momlib.ENTITY_SOURCES` is the ONE declaration (carrying file + list key + viewer const); `fold-answer.py` binds straight to it and `check-cards.py` **reads** `buildCard`'s binding via `momlib.viewer_entity_map()` instead of re-typing a `RENDERABLE` set. JavaScript cannot look a `const` up by name, so `buildCard`'s `ENTITY_DATA` is the one irreducible copy — but it is no longer agreed by hand: `momlib.entity_map_divergence()` derives the comparison, `check-cards.py` reports it once up front, and `test-feedback-cycle.py` has a RESOLVE leg that fails on a missing type, a wrong const, and an unreadable binding. Behaviour proved unchanged: a 16-card snapshot (state · entity hit · photo · probe target · the exact fold edit) is byte-identical pre/post. | **Adding a domain = `momlib.ENTITY_SOURCES` + `buildCard`, nothing else** — the test names exactly what is missing. ⚠️ Still open and **Paul's call, deliberately not touched:** `harvest-questions.py` is the one remaining plants-only site, and it is a *producer*, not a resolver — it reads `plants.json` only and knows only the `variety`/`bloom` marker shapes, not the weeds' top-level `confidence`. Handed the weed records it drafts **zero** candidates, so `crabgrass`, `virginia-creeper` and `wild-violet` (all `confidence: inferred` + `status: needs-confirmation`) can never be harvested while `japanese-stiltgrass` and `beggars-lice` got hand-authored cards. Wiring it to the shared map would put **new cards in front of Mom** — a Mom-facing behaviour change, not a refactor. |
| **⭐ How should the record be ORGANIZED, holistically? (Paul, 2026-07-28)** | Raised off the `harvest-questions.py` weed gap above, and it reframes that item: *"technically, weeds are plants, right? I think we need a holistic view of how to organize all the information."* The gap is a **symptom of an unanswered taxonomy question**, not a wiring bug — every domain so far was added by accretion (plants → wildlife → weeds → vehicles → equipment → household systems B6), each with its own JSON file, its own card, and its own implicit shape, and `ENTITY_SOURCES` now makes that list explicit **without ever deciding what the list should be**. Live tensions already on the board: weeds carry a top-level `confidence`/`status` while plants carry `variety`/`bloom` markers (the exact mismatch that makes weeds unharvestable); Mom herself derived the vehicles/equipment/household-systems split unprompted (B6) — evidence the *domains* are right even if the *schema* isn't; and the field-journal framing may want "things I tend / things I fight / things that visit / things that run the place" where the data model wants shared fields. **Two ways to answer it and they're not exclusive:** ask the **team** (engineering-partner on the data model + ux-expert on whether the domain split is how Mom navigates, and user-researcher on whether her mental model matches the file layout), and/or **ask Mom** — she has already proven she'll propose structure unprompted, and this is the one kind of question that asks no verdict of her about a plant she might get wrong. **Do not answer this by refactoring first** — the answer decides whether `harvest-questions.py` gets wired to the shared map or the shape underneath it changes. | **📄 ANSWERED 2026-08-02 — recommendation on the table, Paul's call.** Full analysis: `.engineering/2026-08-02-record-organization.md` (field inventory of all 11 domain files, measured not recalled). **Finding: the domains are already right and a reorganization is the expensive wrong move.** A universal spine already exists unplanned (`id`/`name`/`scientificName`/`emoji`/`photo`/`attribution`/`notes` in every domain); the five wildlife files are one schema wearing five filenames; the ONLY axis that actually diverges is **honesty** — weeds carry top-level `confidence`+`status`+observation provenance (the best design in the repo), plants carry it nested and partial (3/36 variety, 24/36 bloom, 8/36 `_provenance`, 0/178 seasonNotes), **wildlife carries none at all — 0 of 64 records**, and vehicles use a fifth shape. **And the harvester is worse than 'plants-only': it hardcodes the `variety` and `bloom` FIELD SHAPES**, so repointing it at `weeds.json` would find zero candidates — the weeds are marked askable in their own vocabulary, which it cannot read. **Answer to 'weeds are plants': biologically yes, but this record's split has never been biological — it is what you DO (tend vs fight), which is the axis Mom derived herself with vehicles/equipment/household-systems. Biology is a PROPERTY, not a folder.** Recommended, dependency-ordered: **M1** one uncertainty contract in every domain + a marker-agnostic harvester (the whole unblock) · **M2** a `momlib` temporal accessor, NOT a rename of 64+ records · **M3** declare the action group in each `_meta`. ⚠️ **The risk is supply, not schema:** a harvester that can see four domains puts new cards in front of Mom, and the 5-slot cap binds immediately with 8 already on the bench. |
| **Citizen-science scaffolding** | Dormant code in viewer.html. | Paul's call: re-enable / drop / leave dormant. |
| **Batch document-mining playbook** | Generalize the triage→characterize→verify→fold receipt-mining pattern cross-project. | Until a 2nd project needs it. |
| **Expert-proposed principles (candidates)** | "reuse the mechanism, not the semantics"; "match structure to the reader's unit of meaning"; "widen the ask → implied the log". | Paul demote/keep call. |

---

# SHARED REFERENCE

## Recently shipped

**2026-07-30 — The input stack is four cards, everything is the Fernwood Almanac, and the two blind channels now record a device.** Three threads in one session. (1) **W8·a shipped** — `.unified-input` is a transparent wrapper, four cards on one shell (acknowledgment · Fernwood Almanac · Mama's Perspective · exploring and defining Fernwood's zones), composer+thread fused so the count and order are invariant in every state, Mama's Perspective always renders, the launcher demoted to visual parity. Full resolution note under A4/W8·a. (2) **One name** — Garden Guru retired from every Mom-facing string; the record card renamed Journal → Fernwood Almanac, knowingly overriding her own 7/29 answer, with Paul taking it to her in conversation (Tier-3 #8). The ack ribbon's naming sentence was removed rather than rewritten, and its link re-pointed to the same card. **"Look back ›"** added to cards 1 and 2. (3) **Capture plumbing** — `/api/observations` and `/api/conversations` recorded **no device at all**, so `tools/people.json` could only ever be applied to `/api/feedback`; the two channels `check-mom-ack` flags most often were the two that could never answer "whose was it?" Now stamped at `ObservationStore.save()` (the lowest write helper, first-writer-wins) and via `device_id` → `persistConversation` (sticky). Verified end-to-end with an `origin:"probe"` call; Worker `805ec695`. **Not backfillable** — pre-07-30 records on those channels have no device and never will. Commits `ca6939f`→`0ee548e`.

**2026-07-25 — Hyperlocalization audit shipped + deployed** (full trail: `.engineering/2026-07-23-hyperlocalization-audit.md`). Five-domain audit (fishing/plants/wildlife/weather/voice) + a DEM terrain-solar computation. Landed & live on Pages + Worker: (1) **wildlife/sky corrections** — snakebite limb position, timber-rattlesnake legal claim, Cope's Gray Treefrog rename **+ correct call audio**, two-lined salamander, deer-rut week, turkey season, the non-visible Aug-12 eclipse card removed, stale plant count; (2) **rain-rollup TZ bug** fixed + **history recomputed** (12 dup-rain days gone, +78%→+25% precip delta, believable orographic); (3) **soil provenance** in property.json (dropped impossible Cecil/Pacolet); (4) **#12 terrain-horizon sun times** (fishing windows anchored on the lake's ridge, dashboard "last light here" for the house — sun-horizon.json + SUN_HORIZON_DATA, browser-verified); (5) **#9 lake-temp model** rewritten to climatology + bounded nudge (kills the "still warming at peak" / 26°F-water absurdities). Commits `421b289`→`500eea2`. **Open follow-ups → see the two rows below + W9.**

**2026-07-27 — Tips & tricks on the machines + the bikes' seasonal rhythm.** A `techniques[]` field had sat on the Bronco since 6/20 (the screw-boss rebuild) with **no renderer at all** — five weeks of dark data. Now a **tips & tricks** panel between "what she needs" and "what she's had done", warm sand so it reads as lore rather than a job list. What belongs in it: a quirk that is normal and wants no repair, or a repair method proven on that machine. First quirk entry — **Blue Thunder seeps fuel from the carb overflow parked at a steep lean and stops on flat ground** (diagnosed on the property; not a bad seal, no part to buy). Both bikes also moved to a **fall put-away + spring wake-up** rhythm rather than one spring service — oil goes in *before* the winter (fresh oil overwinters better than used) and a drained float bowl *prevents* the spring carb clean instead of scheduling it, which is exactly what cost both bikes their last recommissioning. Recurring calendar reminder set, first Saturday of November. Also inlined the **house zone** (Paul-confirmed), clearing the last `check-data-inline` drift. Commits `8ccc9c0`→`4156f74`, live on Pages + Worker `c3b85667`.

**2026-07-17 — Zone-journey front door v1.** Position-1 launcher card + one-zone voice walk (leads with Eastern Patio) + the `flowId` funnel. Live (`b52ce03`).

**2026-07-14 — Plant look-fors → Plants tile.** The day's top *plant* look-fors lead the always-visible Plants tile as tappable "👀 Worth a look" rows; tapping routes to the composer pre-filled to log what she sees. Top-two, priority-led (narrow bloom/prune windows lead; routine turf only when nothing more urgent). One engine (`gatherPlantLookForCandidates`). Also: unified-input close-outs — button relabeled "Save & ask the Almanac"; fixed the conversation-open layout shuffle (Mama's Perspective stays pinned, composer + button stay together).

**2026-07-13 — Worker deploy automation** (built, awaiting the `CLOUDFLARE_API_TOKEN` secret — now Track C). **Save/Ask → ONE button "Save & ask the Almanac"** (log-first, then Garden Guru; `bc2cfff`) — writes her verbatim entry first (deterministic, AI-free, always succeeds), then fires Guru best-effort. Closes the 7/3 failure; **resolves the 2026-07-05 "Save/Ask two-button split" question** (hierarchy → one log-first button). Preserves [[feedback_no_ai_on_capture]].

## SHIPPED (the built base these build on)

Mama's Perspective (Mom confirm queue, 2026-07-13) · Unified input — one "Save & ask the Almanac" button, log-first (7/13; collapsed the 5/20 Save/Ask split; relabeled 7/14) · The Almanac card (5/21) · Garden Guru Phase E conversational layer (5/19) + redesign Phases 1–3 (7/02) + into-the-machines (7/07) · Phase D capture rebuild (5/19) · Phase F image input → auto-promote (5/21) · Concept A "Today + Reference Drawer" / computeLookFors (7/05) · structured `peakDates` (7/06) · Fishing granular + dynamic, own card (7/06) · Plants bloom-time + Hydrangea hub (7/12) · **Property zone map** in the Property card (interactive, 5/28) · Vehicles "what she needs" restoration list (6/12) + "what she's had done" service-records pipeline (7/09) + per-step tap-to-call contacts (6/28) + registration reminders (7/11) + manuals corpus (7/08) · Metrics capture (5/20) · analyze-fernwood.py (5/21) · Sources card (5/21) · Worth Considering candidates card (5/26) · drift-check tools + deploy-worker.sh · cross-device zone sync fix (5/28) · storage-quota / sanitize-at-boundary fix (5/26) · Mom no-glasses a11y · W1 capture-that-cannot-lie (7/16) · W0 NAIP basemap (7/16) · W2 zones drawn + reconciled to 9 (7/17) · W2-SCHEMA WGS84 vertices (7/16) · W3 zone-audio capture (7/17) · zone-journey front door v1 (7/17).

## KILLED / SUPERSEDED

| Item | Resolution |
|---|---|
| **⭐ "this matters" star** | KILL — 0 uses / 104 revisits; revisit frequency *is* her curation. (Still in code; retire on next touch.) |
| **Seeded prompts** | Deprecate — 0 usage; a standing control she doesn't operate. |
| **🚩 open-feedback / standing "leave feedback" box** | DON'T BUILD — "the star all over again." General feedback lives as the one quiet foot-line in Mama's Perspective + out-of-band to Paul. *(See W5/W8 — Mom's direct ask for a clearly-labeled feedback field is reopening this; her ask outranks the panel inference.)* |
| **Emailed Mom discovery interview** | DEAD — sent 5/29, refreshed 6/20+6/21, never returned; device + usage replaces it (the reframe behind Mama's Perspective). |
| **"prompt Mom for input" weed seed** | SUBSUMED into Mama's Perspective. |
| **Comprehensive UI/UX overhaul** | Dropped — let evidence commission targeted passes, not a speculative overhaul. |
| **Phase D classify-on-save** | Removed (kept dormant) — no-AI-on-capture principle. |
| **Classifier for machine-spec routing** | Rejected — the fused real message argues against routing. |
| **Weather Underground PWS (KGAJASPE279)** | Killed as a data source — the on-site Ambient Weather station is the sole source. Don't reintroduce. |
| **Two-box architecture (separate Field Notes + Garden Guru)** | Superseded by the unified input surface. |
| **Name "When you're out there"** | Superseded → **"Mama's Perspective"** (Paul's steer). |
| **Text-path plant-add (standing button)** | Don't ship — Paul-want, no Mom-signal; if ever built, funnel back to the photo path. |
| **Save/Ask two-button split (7/05 open question)** | RESOLVED 2026-07-13 — collapsed to one log-first "Save & ask the Almanac" button (hierarchy, not removal). |

## Reconciliation notes

**2026-07-17 (the two-track rationalization):**
- **Fernwood is two products in one repo** (Mom's journal + Paul's fleet tracker) — split into Track A / B so the Mom arc reads clearly and the fleet sub-system isn't interleaved through it. Spec: `.plans/2026-07-17-backlog-rationalization.md`.
- **Folded in:** CLAUDE.md's "Outstanding for Paul" (9 items) → **B3**; the CLAUDE.md "Backlog — raised 2026-07-05" fragment (all shipped/resolved) → pointer; "Backlog — Mom engagement SHIPPED" → pointer.
- **Merged:** the three loop items (bloom "is it open yet?" + bloom-in-glance + Fairway/change-reactions) + Phase G → **A3** (one intent).
- **Elevated:** Tool-use migration (digest AT the 80K ceiling now) → visible in **A6**; the GTI/Bolores live obligations (only deadline-bearing items) → **B1** (surfaced from the session log before archiving it).
- **Archived:** the dated pickup-point trail + the Phases-D/E/F/G roadmap prose → `PICKUP-LOG-ARCHIVE.md`.

**2026-07-13 (first consolidation):**
- **Mama's Perspective shipped, but three "authoritative" docs still said HELD / not-a-queue.** Paul steered the panel's "single confirm probe" into a navigable, continuously-populatable **queue** and it shipped the same day (git `a888ebb`). **Ground truth = code + RELEASE_NOTES + git HEAD.** CLAUDE.md + the memory index updated to SHIPPED; the dated design docs are left as historical point-in-time artifacts.
- **Property map** — code check confirmed it's SHIPPED (live in the Property card), not "paused." Only a zone-naming completeness pass remains (A2 above).
- **Photo-library miner** — CLAUDE.md said "not started"; it was since prototyped and moved to its own repo (`~/Developer/photo-miner/`). Tracked there now (B4).

**2026-07-26 (the legibility rationalization — Paul's ask: *"the most effective, efficient way"*):**
- **Added `▶️ NEXT` at the top** — a pointer table, not a second tracker. It names the row and its section; **status text stays in the row**, so there is nothing to keep in sync. (Rejected: a separate `NEXT.md` — that is the third-tracker trap the 7/17 State Contract warns about, and a status written twice drifts.)
- **Added an owner signal.** Before this pass, **only 21 of 101 rows** said whose they were, so "what needs Paul?" was unanswerable without reading all 342 lines. Owner now rides in the NEXT table (👤 Paul / 🤖 agent-drivable / 👥 both).
- **Closed-but-reads-open, fixed in place** (struck, not deleted): the A1 *"Mama's Perspective — validation gate"* row still stated the **retired** Grow/Kill gate as live, contradicting its own section header three rows above; the A1 *"Re-examine the gate itself"* row still asked for a decision Paul made the same day.
- **Duplicate/adjacent rows folded:** `soilNotes W9 sweep` → merged into **W9** (it was a row whose entire content was "folds into W9") · the three pond rows (A2 rich-zone + A8 pond-infrastructure + A8 pond/koi) now point at **one** canonical row instead of restating each other · the two A3 bloom rows merged into one (they share a single fix — the template in `harvest-questions.py`).
- **Structural bug fixed:** B3 items **15–16 were stranded at the very bottom of the file**, orphaned below `## Reconciliation notes` and invisible as fleet work. Returned to B3.
- **Not done, deliberately:** no row was deleted and no history was compressed. The narrative in the status cells *is* the decision record — it is why this project does not relitigate — so the fix was to make the **next action** findable above it, not to thin it out.
