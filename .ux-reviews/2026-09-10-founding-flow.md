# /ux-sweep — 2026-09-10 — the FOUNDING FLOW at qa (candidate `318416a`)

Trigger: the release loop's lap 6 — `check-ux-sweep.py` says the two-pass sweep is OWED (last 2026-08-31;
121 commits to `viewer.html` against the 20 limit), and the founding-flow design window's brief makes it
the window's **first act** `[paul-ruled 2026-09-10: "Go on the design lane/window"]`. Scope, as graded by
the coordination window: **the whole cold-founder path from the bare door through the app**.

## Method note

- **Target: QA — `https://fernwood-qa.pages.dev`**, the candidate. The bare door is `/` → meta-refresh to
  `/estate/` (the sign-in door); account creation lives at `/onboarding/`. Pass 1 starts at `/` — a person
  who was texted the address and has nothing else — not at `/onboarding/` where the synthetic seats started.
  No launcher exists for this product; the URL is the door.
- **Freshness:** `GET /qa-build.json` at launch — `{"short":"318416a","branch":"staging","env":"qa",
  "builtAt":"2026-09-10T17:55:14-0400"}`. `qa-behind.py`: *qa serves 318416a — 29 commits behind HEAD
  (no app surface changed)*. `git log 318416a..HEAD -- onboarding/index.html homes/index.html`: empty.
  HEAD at launch `05a8a8a`. The review is of exactly the candidate five seats walked.
- **Conditions:** viewport **414×848** · **A+ text** (the served default for a new device at qa; the
  reviewer confirms the toggle reads A+ on the first app screen and says so). No `prefers-color-scheme`
  in any of the three pages (grep: 0 · 0 · 0), so no scheme emulation.
- **Safety rule for THIS run — a deliberate departure from the production rule, recorded.** The founding
  journey cannot be reviewed without founding, so pass 1 signs up and founds ONE house at qa under a
  synthetic identity (`syn-sweep-0910` · `syn-sweep-0910@synthetic.invalid` · place name and a public,
  non-residential fixture address — nobody's home). Given verbatim to both agents:
  never leave `fernwood-qa.pages.dev` · never type a real person's name, address, email or phone ·
  never tap **"Save & consult the Almanac"** or send any Almanac / Guru message (a paid model call) ·
  never send a free-text note · never record audio · the confirm card's own two buttons MAY be tapped
  on the house the reviewer founded, because that house is the reviewer's own and what happens after
  the tap is part of the product · everything else on that house (open, close, expand, tabs, scroll,
  the shelf, settings) is fine. **The founded estate id is recorded here the moment it is known and
  sent to the coordination window for the chronicle's teardown list** — the seventh synthetic house at
  qa, the seats' class, never Paul's (`PAK`).
- **Telemetry pollution (accepted cost, to exclude):** one new device bucket at qa, one `found` event,
  onboarding-metrics for one session.

## Pass 1 — fresh eyes (un-primed) — VERBATIM, returned 2026-09-10 ~9:25 PM ET

Agent: general-purpose, un-primed (told nothing of the seats, the register, Paul's walk or any suspicion). Started at `/`.
Founded **Bramble Hill** (`syn-sweep-0910`, 2025 Baxter St, Athens, GA 30606 — a public library, nobody's home). 42 frames at
`.private/ux-sweeps/2026-09-10-founding-flow/sweep-01…42-*.png`. Console across the run: 3× "password field not in a form" (verbose), 1× 404 `/api/profile` during onboarding, no JS errors.

### 1. Screen-by-screen log

| # | URL / screen | What it asked | What I did | What I saw |
|---|---|---|---|---|
| 1 | `/` → redirected to `/estate/` · title **"My Home"** | Nothing. | Read it. | Green header with "‹ Your homes" and "Settings"; a card **"Early days — Nothing's been built on it yet — that's my job. / Open your invitation link and your place will be here. / [Open your place ›] / Empty so far."** No sign-in, no sign-up, no explanation of what this is. Storage and cookies were empty — this is what a stranger gets. |
| 2 | `/homes/` "Your homes" | Nothing. | Tapped "＋ Add a home". | "Open your invitation link and your homes will be here." Tapping **Add a home** did NOT add a home — it opened a feedback box: **"One account, one home — for now. More than one is the next thing I'm building. What would you add? [Optional] Goes to Paul, nobody else. [✓ Send]"**. (Did not type.) |
| 3 | `/settings/account/` "Your account" | How to reach you (By email ✓ / By phone / Please don't), Your colour (7 swatches), Username **"—"**, Save. | Read, scrolled. | A settings page for nobody: username is a dash, "By email" is pre-checked with no email known. Native blue radio buttons. |
| 4 | `/viewer` (cold, no account) | The Almanac composer at top. | Read, full-page shot. | A complete "My Home" almanac for nobody: A / **A+ (pressed)** toggle; "My Home Almanac" appears **four times** on one screen (chip, composer title, jump-strip row, card); cards Weather / Vehicles / Equipment / Household / Gardening / Wildlife / Sky & Stars / Almanac (LOCAL ONLY) / What you told me / Reference, all "Nothing here yet." |
| 5 | `/settings/place/` "Settings" | What you call this home (empty), What you call the Almanac (placeholder "Almanac"), Its colour, "How I reach you lives in your account ›", Save. | Read. | Settings for a home that doesn't exist. |
| 6 | `/onboarding/` — **found only by guessing the URL** | Username · Password · Password again · "How should Paul reach you?" (Email me ✓ / Call or text me / Please don't) · Email · Phone (optional) · colour · "✓ Create my account" · "If you get stuck, email paul.kirschenbauer@gmail.com." | Filled the synthetic identity, picked Pine. | Header here is **slate/Stone**, every other page is Pine green. Live validation: "syn-sweep-0910" is free. / These match. Picking Pine recoloured the button — nice. |
| 7 | `/onboarding/` step 2 | **"Nothing in it yet — it gets built from what you tell me. What do you call it?"** | Typed Bramble Hill → "✓ That's it". | No confirmation that an account was created; "it" has no antecedent. Header still "My Home". |
| 8 | `/onboarding/` step 3 · header now **"Bramble Hill"** | "Where is your place?" Street / Apt / City / State / ZIP + **"…You can fix it before saving, and check it on the next screen."** | Filled the library address → "✓ That's it". | Console: one 404 on `/api/profile`. |
| 9 | step 4 | **"Got it — that's the address down. [Next ›]"** | Tapped Next. | The promised "check it on the next screen" did not happen here — an interstitial with no address on it. |
| 10 | step 5 (one long card) | Address shown · "See it on Google Maps ›" · "Nothing goes to Google unless you tap." · "Call it something else ›" · **"Does that look right? [✓ Yes, that's it] [Not quite]"** · then "What would you like to spend time on at your place?" 11 tiles (5 marked *an idea — not built yet*) · "✓ Save these" · "Open Bramble Hill". | Tapped **Yes, that's it**. | The Yes/No block was **replaced by "Anything else to add? [Optional] [✓ Send]"** — a text box appeared; I left it alone. No "thanks/got it" line. |
| 11 | same | Interests. | Tapped Gardening (1), Watching what's around (2), Keeping the household systems running (3) → "✓ Save these". | Numbered green badges, order preserved. After save: button relabels **"✓ Update these"** and a line **"Got it — your place is below."** appears. |
| 12 | `/estate/` · "Bramble Hill · Signed in as syn-sweep-0910" | Nothing. | Read; tapped "That's not right ›" and "Change the order ›". | "Early days — Your place is set up. Weather and sky are already in there." Then WHERE IT IS / HOW TO REACH YOU ("By email. The default — change it whenever you like.") / WHAT I'LL BUILD FIRST (1-2-3). **All three "change" links open the same generic box at the very bottom of the page: "What should it say instead?" [✓ Send]** — none changes anything. |
| 13 | `/viewer` · "Bramble Hill" · **first app screen** | Composer. | Read, opened every card, radar, data-source disclosure. | **Text size: "A+" rendered pressed/selected (dark pill) on first load, A muted** — persisted on reload and even after I cleared storage, so it's the served default. Weather and Sky populated for Athens. Cards: Bramble Hill (address + "Local Weather Stations ↗"), Weather, Sky & Stars, What you told me. **None of my three chosen interests has a card.** |
| 14 | `/homes/` signed in | — | Read. | "Bramble Hill · Athens, GA ›" with Pine edge. Works. |
| 15 | `/settings/account/`, `/settings/place/` signed in | — | Read. | Username now shows; place name "Bramble Hill"; Almanac placeholder "Bramble Hill Almanac". |
| 16 | `/onboarding/` while signed in | — | — | Redirects to `/estate/`. Sensible. |
| 17 | Sign-in (cleared storage → `/onboarding/` → "Sign in instead ›") | "Welcome back. Sign in and your place is where you left it." username / password / "Can't remember your password? Ask Paul…" | Signed in. | Landed on **`/viewer`** (whereas finishing setup landed on `/estate/`). A+ pressed again. |

### 2. Ranked findings

**F1 · There is no front door — a stranger cannot find sign-up · blocker.** `/` drops you on `/estate/` "My Home / Early days / Open your invitation link and your place will be here." There is no "Create an account", no "Sign in", nothing that says what this is. "Open your place ›" opens an empty almanac; "Add a home" opens a suggestion box. I only reached `/onboarding/` by guessing the path. A person texted this address by a friend hits a dead end in three taps. *Best-in-class:* a single first screen that says what this is in one sentence and offers exactly two doors — "Set up my place" / "I've been here before".

**F2 · Every "change it" affordance is a message to Paul, not a control · major.** The estate summary says "The default — change it whenever you like" and onboarding said "Reorder and save again as often as you like." But **"That's not right ›", "Change that ›" and "Change the order ›" all open one identical box — "What should it say instead?" — pinned at the bottom of the page**, far from the thing tapped, with the same wording for an address, a contact preference and a ranked list. For a reader with big text, the box appears off-screen; nothing visibly happened. *Best-in-class:* the link opens the relevant editor inline (address form, the three radios, the tile picker), or is honestly labelled "Tell Paul it's wrong ›".

**F3 · The interests I chose vanished from the app · major.** Onboarding: "Your order tells me what to build next — nothing is switched off or hidden because you left it out." The cold `/viewer` shows Gardening, Wildlife, Household systems, Vehicles, Equipment cards (empty). After I picked Gardening / Watching what's around / Household systems, **`/viewer` shows none of them** — only Bramble Hill, Weather, Sky & Stars, What you told me. The estate card says "The rest fills in as you add it" but there is no place to add anything. The one card that mentions my choices is a read-back list. *Best-in-class:* the three chosen domains appear as calm empty cards with the single first thing to tell it ("What's planted? Name one plant.").

**F4 · Promise/order-of-operations break on the address · major.** Step 3 says "You can fix it before saving, and check it on the next screen." The next screen is "Got it — that's the address down. [Next ›]" — it has already been saved and there is nothing to check. The check comes a screen later. It also acted (geocoded, stored, 404'd `/api/profile`) before asking. *Best-in-class:* one screen: address, then "Is this right?" with the rendered address, then save.

**F5 · "Yes, that's it" answered with another ask · major (register).** Tapping the affirmative on "Does that look right?" gives no acknowledgement — the block is simply replaced by "Anything else to add?" plus a Send button. For an older reader, the tap looks like it produced a form to fill in. Meanwhile "Save these" *does* say "Got it". Two confirmations, two grammars. *Best-in-class:* "✓ That's the address." (with an inline "Not quite ›"), and no new text box unless they chose "Not quite".

**F6 · One name, four times on one screen · major (calm).** On `/viewer` the almanac name is rendered as a jump chip, the composer title, a jump-strip row (cold view), and a card. **The chip scrolls nowhere** — it highlights itself and stays put (there is no Almanac card in the signed-in build). Weather and Sky & Stars each appear three times (chip, summary tile, card). It reads as a page assembled from parts that each brought their own header.

**F7 · Two different "home" screens depending on how you arrived · major.** Finishing setup → `/estate/` (green summary page). Signing in later → `/viewer` (the almanac). "‹ Your homes" from the almanac goes to `/homes/`, whose row goes to `/estate/`, whose button goes to `/viewer`. Three tiers of "home" with three different headers (green flat / green flat / green gradient serif). The reader will not build a mental map of this.

**F8 · The onboarding chrome is a different product · major (intentionality).** Every page is Pine green with a sans headline; `/onboarding/` is slate-grey with a serif "My Home", native form styling, blue focus rings, and the maker's personal Gmail address in the footer. The default colour is announced as **Stone** ("Your place opens in this colour — Stone unless you pick another") but every other page already shows **Pine** pre-selected. Swatches wrap 5+2 here and 4+3 in Settings.

**F9 · The floating "Tell me" bubble covers controls · minor.** At 414×A+ the 💬 button sits over the "Open ▼ / Close ▲" buttons of the Vehicles/Bramble Hill/Weather cards and over the "How I reach you lives in your account ›" line in Settings. It is present on every single page, including the sign-in screen, before you have anything to tell anyone about.

**F10 · Numbers and phrases that read false or technical for a place made 60 seconds ago · minor.** "📡 **the weather station** — No station here — regional readings" (lowercase label for a thing I don't own) · "**CLIMATE · LOADING ERA5 ACTUALS…**" stays on screen permanently as a label · "Region · 7 Days · 0.00" · Very Dry · **0th percentile**" · Sky: "Tonight: **Closed out**", "162 **km** vis" in an otherwise-Fahrenheit app, "Sun 18° below horizon", "NWS dark-window cloud · 38% avg · 28–49% range · 8h" · "Hot day ahead." floats as a lone heading inside the Weather body, repeating the card subtitle · Event titles truncate at A+: "Saturn at Opposition — Rings T…", "Geminid Meteor Show…".

**F11 · "What you told me" body has no card padding · polish.** Its text runs flush to the page edge (x≈14px) while every other card body indents to the card. It also says "The default — change it whenever you like." with no change link (the estate page has one).

**F12 · "I" vs "we" vs "Paul" · polish.** "That's my job" / "I'm building" / "I'll build first" / "Only Paul sees it" / "**We** work out your weather" / "Goes to Paul, nobody else" / "Ask Paul — he can reset it". The same author is I, we, and a third person on adjacent screens.

**F13 · No sign-out anywhere · polish.** Account page has no sign-out. Minor for the target reader, but it means a shared phone cannot switch people.

**F14 · "Add a home" is a decoy · polish.** A dashed "＋ Add a home" button that opens "One account, one home — for now" should not be a "+" button. Either drop it or label it honestly.

### 3. What genuinely works
1. **The header becomes the place's name the moment you name it** ("My Home" → "Bramble Hill") and the almanac becomes "Bramble Hill Almanac" — the product visibly takes what you told it.
2. **The address confirmation card** — clean rendered address, "See it on Google Maps ›", "Nothing goes to Google unless you tap." Exactly the right privacy sentence at the right moment.
3. **The interest picker's numbered badges** — tap-to-rank with "tap again to remove" is learnable in one try, and honest "*an idea — not built yet*" labels are refreshingly candid.
4. **Weather and Sky actually work for a brand-new address** — real forecast, radar centred on Athens, moon image, dark window. "Weather and sky are already in there" was true.
5. **The tone of the empty states** — "Nothing here yet. What shows up at your place, and when." is the field-journal voice the brief asks for; no alerts, no counts, no chores.

### 4. Gut read
It feels like three builds stitched together: a slate-grey onboarding form, a flat-green account/homes/settings layer, and a rich green-gradient almanac — each with its own header style, its own vocabulary for the same things, and its own idea of where "home" is. The almanac itself is intentional and often lovely; the seams around it are not. The reader described in the brief would **not get in at all** from the texted URL (F1). If someone sat with her and typed `/onboarding/`, she would get through setup — the steps are short and the buttons are big — but she would come out the other side slightly puzzled: she said "Gardening" and there is no gardening; she tapped "Change the order" and a box asking "What should it say instead?" appeared somewhere below the fold; a name she recognises is printed four times on the first screen. She would feel it was made for her by a person (the voice is warm) and that it isn't finished — which the copy itself keeps admitting ("Early days", "not built yet", "for now").

### 5. Exit state
- **Ended on:** `/viewer` as `syn-sweep-0910`, place **Bramble Hill** at 2025 Baxter St, Athens, GA 30606, interests saved 1 Gardening · 2 Watching what's around · 3 Keeping the household systems running, colour Pine. Signed in a second time via "Sign in instead" after clearing local storage (the account persisted server-side).
- **Confirm card:** tapped **"Yes, that's it"** once on the onboarding address card. A text box ("Anything else to add?") appeared; I did not type in it or send. Never tapped "Not quite".
- **Text-size control:** **A+ rendered as selected** on the first app screen, on reload, and on a cleared browser — it is the served default, not something I chose.
- **Did not do (by rule):** never tapped "Save & consult the Almanac"; never sent any text into any box; never recorded audio; never left the domain; the three "change" links were tapped only to see what they opened.
- **Could not do:** reach sign-up from the landing page without guessing a URL; edit the address, contact preference or interest order from the estate page; find any of my chosen domains in the almanac; sign out.

## Pass 2 — doctrine (adjudication, re-verification, punch list)

Agent: doctrine pass, primed with every file in `~/.claude/design-principles/` (fernwood.md, cross-project.md + all of
`cross-project/*.md` incl. candidates.md), the three working distillations (seats · plans-and-principles · bundle-rows),
and the five CLAUDE.md-only Paul-stated rules handed in by the parent. Returned 2026-09-10 ~9:20 PM ET.
**Build before and after: `GET /qa-build.json` → `318416a` · builtAt 2026-09-10T17:55:14-0400 (both reads).** HEAD at
launch `4c594c2`; `git log 318416a..HEAD -- onboarding/ homes/ estate/ settings/ viewer.html engine/viewer.template.html
worker/worker.js` is empty, so the surface reviewed is the candidate. Viewport 414×848 throughout.
Signed in as the sweep's own account (`syn-sweep-0910`) via `/onboarding/` → "Sign in instead ›" for the signed-in
surfaces; storage cleared for the cold ones; then signed in again the same way (which is itself the F7 reproduction).

**Safety rule, kept:** never left `fernwood-qa.pages.dev`; typed only the sweep account's own username/password; never
tapped "Save & consult the Almanac", "Not quite", any Send, the mic, or created an account. Card open/close, tabs, the
"change" link (opened, never sent), the almanac chip and the masthead links were tapped.

**Telemetry pollution from THIS pass (for the chronicle's exclusion list):** at qa, one more `session`/sign-in for
`syn-sweep-0910`, card expansions and one `text_size_served` from one device bucket, and a `door_failed`-free walk.
⚠️ **And two records at the LEGACY Worker that are mine, not a person's:** two probes guessed the host as
`fernwood.paul-kirschenbauer.workers.dev` (the pages derive it from the `pages.dev` label, so qa's is
`fernwood-qa.…`); the legacy Worker answered 404 `unknown-or-other-estate` and, per `worker.js:4463–4470`, wrote two
`door_failed` records (`serverSide:true`, ~9:08 PM ET 2026-09-10, `deviceId:null`). Exclude them from any door reading
of production. The correct-host re-probe returned `whoami` 200 for `est-gndlvf`.

**Screenshots:** `browser_take_screenshot` timed out (5s) on `/viewer` and `/homes/` after fonts loaded, so this pass
carries ONE new frame (`pass2-03-estate-after-not-right-tap.png`) and otherwise cites pass 1's 41 frames plus measured
DOM. ⚠️ Two files `pass2-01-estate-not-right-box.png` / `pass2-02-viewer-first-screen.png` already existed at
18:46–18:47 — ~3 hours before this pass launched; they are not this pass's and were not used.

### A · Re-verification results

Every measurement below is `getBoundingClientRect()` (doc y = viewport y + scrollY) or `innerText`, read off the live
DOM at 414×848 — the rendered geometry, not the declaration (measurement-and-evidence.md, "the CSS you wrote is not
the CSS that ran").

| pass-1 claim | reproduced? | measurement | RE-VERIFIED |
|---|---|---|---|
| **F1** `/` gives a stranger no sign-in/sign-up | **YES** | cold browser (`localStorage` keys `[]`): `/` → `/estate/`, title "My Home", h1 "My Home"; header links `‹ Your homes` (/homes/) and `Settings` (/settings/place/); body text exactly *"Everything here gets built from what you tell me. Early days Nothing's been built on it yet — that's my job. Open your invitation link and your place will be here. Open your place › Empty so far."*; visible CTAs = those two header links, `Open your place ›` (/viewer), `💬`. No control names sign-in or sign-up. | RE-VERIFIED: evaluate on `https://fernwood-qa.pages.dev/` after `localStorage.clear()` — 2026-09-10 |
| **F2** the three "change" links open one box at the bottom, off-screen at big text | **PARTLY** | signed-in `/estate/`: links at doc y=424 (`That's not right ›`), 582 (`Change that ›`), 764 (`Change the order ›`), all `href="#"`. Tapping the first: `#fbnote` textarea appears at doc y=964 (540px below the link) with placeholder swapped to *"What should it say instead?"* — and the page **auto-scrolls to scrollY=358 and focuses it**, so the box IS on screen (viewport y≈606, `pass2-03`). The tapped link is then at viewport y≈66 with two other sections between. Mechanism: `estate/index.html:612–617` `openFb(about)` — the **same** `#fbbox` the 💬 bubble opens (`fbopen` → `openFb(null)`, `:618`); the three links differ only in `pendingAbout`, sent as `id: "estate-<label>"` (`:632`). "At A+" does not apply: this page has **no text-size mode** (body 16px; `grep text-lg estate/ homes/ settings/` = 0). | RE-VERIFIED: evaluate + real click on `/estate/` signed in — 2026-09-10 |
| **F3** the chosen interests have no card on `/viewer` | **YES** | signed-in: visible `.main-card` = `card-property` ("🏡 Bramble Hill"), `card-weather`, `card-celestial`, `card-told` — **4**; `card-plants`, `card-wildlife`, `card-household` are `display:none`. Cold (no account): **10** visible incl. those three. The record holds the picks: `whoami.ranked` = garden · wildlife · house-systems. Mechanism: `viewer.html` `renderEmptyCards()` — *"SUPERSEDED 2026-09-07 `[paul-stated, beat 3…]`: Let's not show any of the empty modules… at the start"* → `if (household && moduleState(mod)==="empty") card.style.display="none"`. | RE-VERIFIED: evaluate on `/viewer` signed in and cold; `GET /api/grant/whoami` at `fernwood-qa.…workers.dev` — 2026-09-10 |
| **F4** "fix it before saving / check it on the next screen" vs the interstitial | **YES (source + frames; not walked)** | `onboarding/index.html:604` carries the promise; `:2128–2160` POSTs `/api/estate {verb:"found"}` on the address step's own button; `#s3` (`:622–629`) is *"Got it — that's the address down. [Next ›]"* with no address on it; the confirm is `#s4` (`:727–729`). Pass 1 frames `sweep-15/16/17`. Not re-walked: the rule forbids a second account. | RE-VERIFIED: source lines cited; parent's confirmation of `:2130–2160` — 2026-09-10 |
| **F5** "Yes, that's it" is answered with another ask | **YES (frame + source; not tapped)** | `sweep-17`: after `#ok1`, the block becomes *"Anything else to add? [Optional] [✓ Send]"* — a filled-green ✓ primary; `:2019` `"Anything else to add?"`, `#s4ask` = textarea + `#sendNote`. No receipt line. | RE-VERIFIED: `sweep-17-after-yes.png` + `onboarding/index.html:730–735, 2019` — 2026-09-10 |
| **F6** the almanac name four times; the chip scrolls nowhere | **PARTLY** | signed-in first screen: "Bramble Hill **Almanac**" renders **2×** (jump chip `.js-label` y=234; composer `.ic-head-title` y=309); the **place** name "Bramble Hill" renders 2 more (h1 y=89; `card-property` title y=819) → four *"Bramble Hill"* strings, two of them the almanac. Cold: "My Home Almanac" **5×** (chip · composer · `dash-cell-label` y=470 · `card-fieldnotes` title · `fn-intro`). Weather 3× and Sky & Stars 3× (chip · dash tile · card) — signed-in and cold. **Chip:** click → scrollY 0→0, `location.hash` "", `card-fieldnotes` `display:none` (household mode). The chip label wraps to 2 lines (line-clamp 2; chip 54px tall vs 44 for its siblings). | RE-VERIFIED: TreeWalker over visible text nodes + click on `/viewer`, both states — 2026-09-10 |
| **F7** three landings, three headers | **YES** | sign-in → `/viewer` (this pass, `si-go` click); setup → `/estate/` (pass 1, `sweep-20`). Headers: `/estate/`, `/homes/`, `/settings/*` = flat `rgb(47,93,58)` + system-ui h1 24px/600; `/viewer` = `linear-gradient(--hdr-1..3)` + Crimson Text 30px/600; `/onboarding/` = `linear-gradient(135deg, #212a36, #33404f 55%, #47576a)` + Georgia 27.52px/600. | RE-VERIFIED: computed styles on all six pages — 2026-09-10 |
| **F8** onboarding is a different product; Stone vs Pine; 5+2 vs 4+3 | **YES** | `--accent: #3f5266` (Stone, `onboarding:69` *"the ONE token an instance overrides"*) vs `--accent:#2f5d3a` (Pine) **hardcoded** in `:root` of `estate:50`, `homes:37`, `settings/account:36`, `settings/place:31`. Cold onboarding: Stone pressed; cold settings: Pine pressed (pass 1). Swatches: onboarding rows y=1124 (**5**) / 1209 (**2**); settings/account rows 372/452 (**4+3**); settings/place 583/663 (4+3). Radios: onboarding native with `accent-color: rgb(60,90,114)` (slate — themed, not "native blue"); settings/account native, `accent-color: auto` (system blue). Body type: onboarding **19px**; the three green pages **16px**; viewer `body.text-lg`. | RE-VERIFIED: computed styles + swatch geometry on `/onboarding/`, `/settings/account/`, `/settings/place/` — 2026-09-10 |
| **F9** the 💬 covers Open/Close and the settings line; present on sign-in | **PARTLY** | **Cold `/viewer`** (what pass 1 shot): ribbon is the *extended* pill `💬 Tell me` 68×54 at viewport (346,710); Vehicles `Open ▼` at (296,726) 87×32 → **37×32px overlap at rest**. **Signed-in `/viewer`**: `.feedback-ribbon.is-corner.is-slim` 44×46 at (358,788) — toggles span x 294–382 → 24px x-overlap whenever a card header scrolls through y 788–834 (momentary). **`/settings/place/`**: bubble 356–402 × 788–834; *"How I reach you lives in your account ›"* paragraph at y 837–860 → **3px clear at rest**, covered on a 3–46px scroll; the tappable `your account ›` spans x 179–277 and is never under it. **`/onboarding/` and the sign-in screen: NOT the bubble** — an inline text link `#fbopen.fblink` *"Something not right? Tell me."* `position:static` at page bottom (y=1460). | RE-VERIFIED: geometry on `/viewer` (cold + signed in), `/settings/place/`, `/onboarding/` — 2026-09-10 |
| **F10** false/technical strings | **YES, with two time-dependent deltas** | present verbatim on signed-in `/viewer` (all cards open): *"📡 the weather station · No station here — regional readings"* · *"CLIMATE LOADING ERA5 ACTUALS…"* still on screen **with the monthly table already rendered beneath it** (Jan 53°…) · *"REGION · 7 DAYS 0.00" Very Dry 0th percentile"* · *"Sun 18° below horizon"* · *"🛰️ NWS dark-window cloud · 38% avg 28–49% range · 8h"* · *"Hot day ahead."* as card subtitle **and** again as a body heading. Changed since pass 1: *"Tonight: Excellent"* (was "Closed out"), *"110 km vis"* (was 162). Truncation at A+: *"🪐 Saturn at Opposition — Rings Tilted"* scrollWidth 232 > clientWidth 215 (ellipsis); *"☄️ Geminid Meteor Shower — Best of the Year"* 277 > 165. | RE-VERIFIED: `innerText` + scroll/client widths on `/viewer` signed in — 2026-09-10 |
| **F11** "What you told me" body flush to the page edge | **YES** | `card-told` at x=12 (390 wide); its `.main-card-body` x=14, `padding-left: 0`; `.told-list` x=14; first text *"Where it is"* x=**14** — i.e. 2px inside the card border. Sibling: `card-property` body's first text x=**49**; `card-weather`'s first content is a centred control. `.told-label` = 11px uppercase `#7a8a72`. | RE-VERIFIED: geometry with the three cards expanded — 2026-09-10 |
| **F12** I / we / Paul | **YES (source)** | first person: onboarding builder lines, homes *"I build the rest from there"* / *"I'm building"*, estate *"that's my job"*, account *"email me and I'll sort it"*. Third person: onboarding *"How should Paul reach you?"*, *"goes no further than Paul, who built this"*, *"Only Paul sees it"*, *"Ask Paul — he can reset it"*; homes *"Goes to Paul, nobody else"*. **"We": exactly one string** — `onboarding:604` *"We work out your weather…"*. The file states its own rule at `:791–793`: *"The builder voice stays first-person… the person stays named."* | RE-VERIFIED: grep across the five pages — 2026-09-10 |
| **F13** no sign-out | **YES** | `/settings/account/` innerText contains neither "sign out" nor "log out"; no `@` anywhere on the page (no email shown back). | RE-VERIFIED: innerText on `/settings/account/` signed in — 2026-09-10 |
| **F14** "＋ Add a home" is a decoy | **YES, and deliberate** | `#addbtn` dashed 1.5px `rgb(198,214,187)`, 378×60, opens `#addcard` *"One account, one home — for now."*; `homes/index.html:117–122` comment: *"THE `+` IS PRESENT AND HONEST… opens a card that says what is true."* | RE-VERIFIED: geometry + source — 2026-09-10 |
| **TIER 2·19** password never confirms | **YES** | `#uword`: no `minlength`; **no `#uwordstate` element exists** (`#unamestate` and `#uword2state` do); hint *"Eight characters or more."*; length is checked only at submit (`:1708`). | RE-VERIFIED: DOM on `/onboarding/` cold — 2026-09-10 |
| **TIER 2·21** masthead bar aligned to nothing | **MEASURED** | viewer `.hh-utility`: `display:flex; gap:16px; justify-content:normal`, 14px, **no `text-lg` rule**; links at x=18 (w84) · 118 (w115) · 249 (w53) → they occupy 18–302 of a 378px band, **76px empty on the right**; h1 below at x=18. Sibling headers: estate = `‹ Your homes` x=18 **and** `Settings` right-anchored x=339–396 (space-between); account = `‹ Your homes` only; place = **`‹ Back` → `/estate/`**; homes = **no header links** (`Your account ›` is a footlink 100px below the add button). | RE-VERIFIED: geometry on all five headers — 2026-09-10 |
| **TIER 2·25** sentence-case regex | **YES (source); not on this account** | `viewer.html:18481` lowercases only `/^(A|An|The) /`. The interest labels are now verbs: the 5 `soon` rows are *Keeping the paperwork straight · Marking where things are · Drawing your own map · Asking questions about your place · Getting it ready to hand over* — none matches, so **5 of 5** would render *"You put Keeping the paperwork straight first."* This account ranked no `soon` item, so no idea card and no "You put" line renders (`youPut = []`). | RE-VERIFIED: `onboarding:1016–1068` + `viewer.html:18481–18484`; page scan — 2026-09-10 |
| **TIER 2·18** lifecycle: sign out · recover password · recover username · email shown back | **MEASURED** | sign-in screen: *"Never set one up? Create your account ›"* · *"Can't remember your password? Ask Paul — he can reset it, and it only takes him a second."* — **no username-recovery sentence**; no sign-out on any page; the email appears **nowhere** (account page, estate receipt *"By email."*, viewer receipt *"By email."*). `whoami` (200, qa) keys: personId · estateId · capability · relationship · entry · vault · name · accent · address · addressParts · ranked · contactPref · profileAccent · coordinates · hasAccount — **no email**. `GET /api/profile` = **403** at qa. | RE-VERIFIED: fetches from the page against `fernwood-qa.paul-kirschenbauer.workers.dev` — 2026-09-10 |
| **(given)** `POST /api/session` lists `est-qa0001` for a person whose place is `est-gndlvf` | carried, not re-derived | `worker.js:1008` builds `estates` from `scope.id`; `whoami` for the same grant says `estateId: est-gndlvf`, `relationship: ["owner"]`, `capability: member`, placed (census, 2026-09-10T22:33Z). | cited from the parent's confirmation + this pass's whoami read — 2026-09-10 |

### B · Adjudication (verdict first)

**F1 · no front door — ALREADY-RULED.** setup-journey PLAN §2 D2 *"a front door for everyone at the bare origin"*, B2 (one screen, both doors, exactly one account-creation surface) and B3 — ruled, unbuilt. The walk proves the cost the ruling names: a texted URL dead-ends in three taps. Canon behind it: *Meet the user at the action* (an empty state that explains emptiness without the action that fills it) and *A CTA's label must promise what the destination delivers* (`Open your place ›` opens a nobody's almanac). What the ruling now requires of the surface: `/` must not resolve to `/estate/`'s "open your invitation link" for a credential-less device (B4c: *the no-grant empty names the door, not an invitation link*). Build item, not punch.

**F2 · every "change" is a message — PARTLY, and half ALREADY-RULED.** Stands: the three links write to the feedback channel and edit nothing; the estate page says *"change it whenever you like"* of a preference it cannot change here — **rule 5 (EVERYTHING IS CHANGEABLE)** is violated on its own caveat: *"never call a thing changeable and then make changing it costly."* Also *A CTA's label must promise what the destination delivers* (canon): "Change the order ›" delivers a note box. Killed: "off-screen; nothing visibly happened" — the page scrolls and focuses the box (measured). Ruled: D3 *"TRANSFER the receipts into What you told me in the app, then retire /estate/"* — the editors belong to that transfer. Until then the honest punch is the label, not the mechanism.

**F3 · interests vanished — DELIBERATE-PER-DOCTRINE on the hide, CONFIRMED-VIOLATION on the promise.** The hide is Paul's own 09-07 ruling in the source (*"only the almanac and the my-home view"*). But the onboarding line still says *"nothing is switched off or hidden because you left it out"* — and now the modules ARE hidden, whether or not you left them out; its own comment says *"If that ever changes, this sentence changes with it or it becomes a lie."* It changed. And the code's *"THEIR #1 OPENS ALREADY OPEN"* branch (`renderEmptyCards`) is defeated by the hide that follows it — a second engine over the same inputs (*One engine, one verdict*, canon). Canon also strained: *Meet the user at the action* — the first screen offers no place to add the first plant except a composer captioned *"Stays on this phone for now"*. → NEEDS-PAUL (F §1). Design item.

**F4 · promise / order of operations — ALREADY-RULED.** *The confirm is a GATE* `[paul-ruled 2026-09-10]`: founding waits for the tap; "Not quite" edits before anything is written; PO-box refusal said at submit; one card on the address step. The ruling makes `:604`'s *"before saving"* true again and retires `#s3`'s addressless interstitial. Rides the gate exhibit.

**F5 · "Yes" answered with an ask — CONFIRMED-VIOLATION (candidate) + rides the gate.** *Receipt and consequence are two different acknowledgments* (candidate): the tap gets no receipt, only a new field. *Four tiers of a card action* (candidate) is honoured on placement (SAY MORE after the answer) and broken on weight: the Send is a full filled-green ✓ primary — **standing rule 1**'s affirmative grammar spent on an optional note, so the screen holds three filled primaries (seats: *"three chances to wonder which one is the real one"*). The gate rework re-draws this card; the receipt line and Send demotion are punch-sized on their own.

**F6 · one name, four times — PARTLY: two halves, two verdicts.** Weather ×3 and Sky ×3 (chip · tile · card) are **DELIBERATE-PER-DOCTRINE** — *Strip teases, card holds* `[paul-stated]`: *"duplication between strip and card is by design"*. The almanac name ×2 signed-in (chip + composer) is within *One number, one altitude*'s own carve-out for strip↔card. What stands: the **chip scrolls nowhere** — a jump-strip chip to a hidden card fails *Icons/Ornament earn their place* and *A CTA's label must promise what the destination delivers* (canon). Cold view ×5 is the no-account surface B2 retires. Pass 1's "four" conflated the place name and the almanac name — see C10 (TIER 2·20).

**F7 · two home screens — CONFIRMED-VIOLATION (Tone-coherence across all chrome, canon) and partly ALREADY-RULED.** Three header grammars measured (A). D3 retires `/estate/` and r3 *"the shelf is the way in"* `[paul-ruled 2026-09-08]` — once both land there is one landing (the shelf → the app). The remaining seam is homes/settings (flat, system-ui) vs the app (gradient, Crimson) — D4 *names before pixels*; exhibit-sized.

**F8 · onboarding is another product — PARTLY.** The slate is the engine default (`:69`) and a person has no colour yet at s0 — DELIBERATE. What is not: Pine (`#2f5d3a`, Fernwood's hue) hardcoded in four engine pages' `:root`, so cold defaults disagree (Stone vs Pine) and an instance value sits in engine files (C4). Swatch wrap 5+2 vs 4+3 and the Georgia/Crimson split are *Tone-coherence* leaks; the maker's Gmail in the footer is a register question for content-steward. Colour noun: ALREADY-RULED TIER 1·29 (copy only) — but see C4: the copy fix names a field the product holds twice.

**F9 · the bubble — DELIBERATE-PER-DOCTRINE, one half stands.** `[paul-stated 2026-08-03]` *"the chat bubble stays as-is; momentary cover-ups accepted as natural… do not re-propose"* — the signed-in 24px scroll-through overlap is exactly that. What stands: (a) the **cold** extended pill overlaps a control **at rest on first paint** — the 08-31 sweep's own verdict for this case was *"protect the bubble, fix the clearance"*; (b) pass 1's *"present on the sign-in screen"* is a **false positive** — that screen has an inline link, not the bubble; and TIER 1·28 rules the bubble *should* appear from the moment an account exists, so the finding inverts: the feedback door on onboarding s1–s4 is the wrong shape, not present too early.

**F10 · false/technical strings — CONFIRMED-VIOLATION (several canon).** *"LOADING ERA5 ACTUALS…"* standing over a rendered table: *A render's own age is part of its denominator* / *Static visuals lie on dynamic surfaces*. *"Hot day ahead."* twice: *One number, one altitude* (candidate). *"0th percentile"*, *"km vis"*, *"18° below horizon"*, *"NWS dark-window cloud · 38% avg"*: *Tone-coherence* + *Make every surface read at half-engagement* (canon). *"the weather station — No station here"* lowercase label naming a thing the household doesn't own: *Register is carried by chrome* (candidate). Event-title truncation at A+: the A+-allowlist candidate. "Closed out" and "162 km" were time-dependent — NEEDS-PAUL? No: the *class* reproduces with different values.

**F11 · told-list padding — CONFIRMED-VIOLATION (Content first; form follows — and then form has to be excellent, canon).** 2px from the card border, measured.

**F12 · I / we / Paul — DELIBERATE-PER-DOCTRINE except one string.** The I-for-builder / Paul-for-privacy split is the page's own stated rule (`:791–793`) and content-steward's; the single "We" at `:604` is the drift. Punch.

**F13 · no sign-out — ALREADY-RULED.** TIER 2·18 pulled into the bundle `[paul-ruled 2026-09-10]`: design now (sign out · recover password · recover username · email shown back). Note C5: email shown back is blocked on the Worker, not the page.

**F14 · Add a home decoy — PARTLY.** Deliberate (the comment) and honest once opened; but a dashed **＋** control that adds nothing fails the button-glyph sharpening clause `[paul-ratified 08-02]` (＋ carries neither decision nor topic — the same ＋ retired from "＋ Add a note") and *A CTA's label must promise what the destination delivers*. Punch: label, not mechanism. D5 (multiple homes, this lap) will eventually make it true.

**Seats' converged (A)–(F):** (A) confirm order-of-ops → ALREADY-RULED (gate). (B) three filled primaries on F09 → CONFIRMED-VIOLATION of standing rule 1 as applied by *Shape is the role* (candidate); rides the gate exhibit; the Send demotion is punch-sized. (C) empty shelf two doors → PARTLY: *"Set up my first home"* and *"＋ Add a home"* do different things (route to onboarding vs message) wearing peer chrome — *Identical form is a promise of identical function* (candidate); D5/B2 territory. (D) PO box → ALREADY-RULED (said at submit; refusal never after the record exists). (E) voice/receipts → as F12/F5; *email never shown back* → C5. (F) composer on an empty place → NEEDS-PAUL (F §1, with F3).

**Paul's walk items.** *Bubble from account creation* → ALREADY-RULED; the surface fact is C1 (three shapes). *Colour noun* → ALREADY-RULED copy fix, with C4's field question attached. *Unit number, don't force it* → CONFORMS today: `Apt` is present, optional, unlabelled as required (`addressParts.line2:""` accepted). *"Formatting of Your homes / What you told me / Settings is off"* — measured, three candidate meanings each (C13).

**TIER 2·20 (what the Almanac IS)** → NEEDS-PAUL/content-steward, evidence in C10. **TIER 2·21** → measured in A; CONFIRMED-VIOLATION of *Ordering first, then type in service of it* (three destinations of three kinds at one weight) and of the sibling pages' own header grammar. **TIER 2·25** → CONFIRMED-VIOLATION reproduced by source (5 of 5 now); remedy is sentence-case at the slot regardless of wording.

### C · Coherence findings (what the cold pass cannot see)

- **C1 · One control, four doors.** The feedback channel renders as (i) an extended pill *"💬 Tell me"* (cold viewer), (ii) a 46px corner circle (household viewer, homes, settings ×2, estate), (iii) an inline text link *"Something not right? Tell me."* (onboarding, sign-in), and (iv) the estate page's three "change" links, which open **the same box** with a swapped placeholder. Same job, four signifiers; the seats route by shape. RE-VERIFIED: `.feedback-ribbon` vs `.fbbubble` vs `#fbopen.fblink` vs `openFb(about)` — 2026-09-10.
- **C2 · Three header grammars** (A/F7): flat Pine + system-ui · green gradient + Crimson · slate gradient + Georgia. *Tone-coherence across all chrome* (canon). Names-before-pixels (D4) then one grammar.
- **C3 · One back-link, three wordings, two destinations.** `‹ Your homes` → `/homes/` (estate, account) · `‹ Back` → `/estate/` (place) · none (homes) · a three-link bar (viewer). *A CTA's label must promise what the destination delivers*. RE-VERIFIED: header link hrefs on five pages — 2026-09-10.
- **C4 · The colour is two fields with three writers.** Signup POSTs `accent` (`onboarding:1746`) → `acct.accent` (`worker.js:804`); settings/place POSTs `accent` → **both** `grow.accent` and `acct.accent` (`:4187`, `:4225`); settings/account POSTs `profileAccent` (`settings/account:241`) → `acct.profileAccent` (`:4229`). `whoami` for this person: `accent: #2F5D3A`, `profileAccent: null`. So the account page's picker edits a field signup never wrote, while the place page edits the one it did; cold defaults are Stone (onboarding) vs Pine (the other four, hardcoded). TIER 1·29's copy fix names *"your account's colour"* — the surface will say one thing and the record will hold two. Two engines, one noun. RE-VERIFIED: source lines + whoami — 2026-09-10.
- **C5 · "Email on file" reads a route that does not carry it.** `settings/account:212–226` fetches `/api/grant/whoami` and renders `d.email` — the response has no `email` key (it reads the GRANT row; email lives on the account). `#contactvalue` is `hidden` with empty text on this account. The ruled *"email shown back"* is an engineering item first. RE-VERIFIED: whoami keys + DOM — 2026-09-10.
- **C6 · A+ does not travel, and inside the app it is an allowlist.** Viewer serves `text-lg` (A+ pressed, persisted across sign-out/in); estate/homes/settings have no text-size mode (body 16px, zero `text-lg` rules); onboarding is 19px on its own. Inside the viewer, `.told-label` (11px) and `.hh-utility` (14px) — both founding-flow surfaces — have no `.text-lg` rule. Candidate *"An accessibility text mode built as an allowlist silently excludes every surface added after it"* — second occurrence on the same product. RE-VERIFIED: grep + computed sizes under `body.text-lg` — 2026-09-10.
- **C7 · The receipt is rendered by two engines.** `estate/index.html` rows and `viewer.html:19944 renderTold()` (*"Ported from estate/index.html's own rows"*) build the same three rows from the same keys with copied logic — one concept, two code paths, kept aligned by hand (*One engine, one verdict*, canon — the render-layer twin of single-source-of-truth). D3's transfer-then-retire is the merge; until then they can drift (today the estate rows carry an `act` link each; the viewer rows carry none — so the receipt reads "change it whenever you like" in the app with nothing to tap, pass 1's F11 second half).
- **C8 · The masthead's "What you told me" opens a card the reader cannot see.** Click → `card-told.expanded` true, scrollY 0→0, card top at doc y=1177 on an 848px viewport. Nothing moves; at half-engagement the tap did nothing. *Save-success at the surface, not at the destination* / *Meet the user at the action*. RE-VERIFIED: click on `[data-open-told]` — 2026-09-10.
- **C9 · The receipt labels are uppercase in all four layers** (`estate .row .label` 13.12px, settings `label` 13.12px, viewer `.told-label` 11px). Consistent — and consistently the treatment *Tone-coherence* names as CMS chrome on a journal surface. Judgement (F §3).
- **C10 · The place name and the almanac name share a screen and a settings page separates them.** First screen: h1 "Bramble Hill", chip "Bramble Hill Almanac", composer "Bramble Hill Almanac", card "🏡 Bramble Hill". `/settings/place/` asks *"What you call this home"* and, separately, *"What you call the Almanac"* (placeholder "Bramble Hill Almanac") with the definition *"The record of this place — where notes, questions and what grows here live."* That definition exists only on a settings page; the first screen shows the noun four times and defines it nowhere. This is TIER 2·20's evidence.
- **C11 · Two locality claims on one screen.** Composer caption *"Stays on this phone for now — nobody else sees it"* sits 300px above a receipt card whose content came from the server and a masthead link to it; the seats' handover reading (*"tells a handover founder not to start"*) stands. Honest-surfaces class (*provenance shown as honestly as the value*).
- **C12 · "Signed in as syn-sweep-0910"** on estate and homes; absent on viewer and settings. One fact, two of four layers.
- **C13 · What "formatting off" could mean, measured — Paul's TIER 1·30 note, three surfaces:**
  *Your homes* — (a) type from no scale: name 18.4px/600, city 14.72px, footlink 15.2px, add 16px, all system-ui — four fractional sizes on four text elements; (b) the row (white, 89px) and the add button (dashed, 60px) are stacked peers of different heights and different meanings; (c) `Your account ›` is a lone footlink 100px below, and the header carries no utility row at all while every other page does.
  *What you told me* — (a) body text 2px inside the card border (F11); (b) labels 11px uppercase not scaled at A+ (C6); (c) *"The default — change it whenever you like."* in italic with no control (C7).
  *Settings (account)* — (a) uppercase 13px labels over 16px values, system-blue native radios on a green page (accent `auto`); (b) swatches wrap 4+3 with no fourth column; (c) `✓ Save` is a 97px-wide left-anchored button under a 378px-wide form, and `‹ Your homes` is the only header link (place has `‹ Back`).
  RE-VERIFIED: geometry on the three pages — 2026-09-10.

### D · Punch list (hours, not days; ranked by intentionality-restored ÷ effort)

1. **`"We work out your weather…"` → `"I work out your weather…"`** → the page's own voice rule (`:791`) + Tone-coherence → F12, seats' *who is "me"* → `onboarding/index.html:604`.
2. **Colour line → `"Your colour — Stone unless you pick another. It follows you to every home you're part of."`** (content-steward to word; the noun is ruled) → TIER 1·29 ALREADY-RULED; *never call a thing something it isn't* → F8 → `onboarding/index.html:479`. ⚠️ Ships with C4 flagged, not silently.
3. **After `expandCard("card-told","nav")`, `scrollIntoView({block:"start"})`** → Save-success at the surface / Meet the user at the action → C8, TIER 2·21 half → `viewer.html:19934`.
4. **`.told-list { padding: 0 14px }`** (or the inner-inset the property card uses) → Content first, form follows — form has to be excellent → F11, TIER 1·30 → `viewer.html:4236`.
5. **`.text-lg .told-label{font-size:13px} .text-lg .hh-utility{font-size:16px}`** → the A+-allowlist candidate; 414×A+ is the standard `[paul-stated 09-03]` → C6, TIER 1·30 → `viewer.html` text-lg block (~`:23128`).
6. **Hide the almanac jump chip when `card-fieldnotes` is hidden** (household mode) — or point it at the composer → Ornament earns its place; CTA promise → F6 → jump-strip build in `viewer.html` (search `.js-label` / `card-fieldnotes`).
7. **Add `#uwordstate` beside the first password: `"That'll do."` at ≥8 chars, silent below** → TIER 2·19 ruled (confirmation, never a gate) → `onboarding/index.html:420` + the `checkMatch` block `:1536`.
8. **Receipt line after `#ok1`: `"✓ That's the address."` and demote `#sendNote` to the outlined secondary** → standing rule 1 (one filled affirmative per moment) + *receipt and consequence* → F5, seats (B) → `onboarding/index.html:2019`, `#s4ask`.
9. **`"＋ Add a home"` → `"Another home? Tell me ›"`** (glyph dropped) → button-glyph clause `[paul-ratified 08-02]` + CTA promise → F14, seats (C) → `homes/index.html:123`.
10. **`"That's not right ›" / "Change that ›" / "Change the order ›"` → `"Tell Paul it's wrong ›"` (one wording)** until D3's editors land → CTA promise; rule 5's caveat (don't call it changeable while it costs a message) → F2 → `estate/index.html:373, 396, 431`.
11. **Replace `"CLIMATE · LOADING ERA5 ACTUALS…"` with `"CLIMATE · 25-YEAR ACTUALS (ERA5)"` once the table renders** → A render's own age / Static visuals lie → F10 → the climate-table renderer in `viewer.html` (search `LOADING ERA5`).
12. **Drop the body-level `"Hot day ahead."` repeat** (keep the card subtitle) → One number, one altitude → F10 → weather body renderer (search the today-line slot).
13. **`‹ Back` on settings/place → `‹ Your homes`** (href `/homes/`, matching account) — or name the place — → one wording per destination; CTA promise → C3 → `settings/place/index.html` header.
14. **Sign-in copy: `"Can't remember your username or password? Ask Paul…"`** → a correct "no" still owes a next move; TIER 2·18 interim while recovery is designed → `onboarding/index.html:357`.
15. **Event titles: allow two lines at A+** (`white-space:normal` under `.text-lg` for the event title cell) → the A+-allowlist candidate → F10.

### E · Not punch-sized → design exhibit or backlog

- **Confirm-as-gate** (F4/F5/seats A, B, D) — ruled; the exhibit mocks the GATE first. Design.
- **The front door** (F1; B2/B3) — ruled, unbuilt. Build.
- **Account lifecycle** (F13, TIER 2·18) — ruled into the bundle. Design + the C5 Worker change (whoami or a profile GET must carry the email before any page can show it).
- **Interests-vanished** (F3, seats F) — NEEDS-PAUL first (F §1), then a design exhibit: what the ranking DOES on the first screen.
- **Self-serve editors for the receipts** (F2) — D3's transfer-then-retire; the editors are the transfer.
- **One header grammar across the four layers** (F7/F8/C2) — D4 names-before-pixels, then an exhibit.
- **The colour field** (C4) — engineering + Paul (F §2) before any further copy.
- **A+ for the three sibling pages** (C6) — needs a shared text-size mechanism; not a CSS line.
- **TIER 2·20 / 2·21** — content-steward + ux-expert with D4; C10 and the A-table geometry are their evidence.
- **`/api/session.estates` from `scope.id`** — engineering; carried, not fixed here.
- **The two-engine receipt** (C7) — merges when D3 lands; do not patch both.

### F · Framed trades / NEEDS-PAUL

1. **Hidden-when-empty vs "nothing is switched off or hidden."** *Question:* the 09-07 ruling hides every empty module, so the person's three picks produce no card; the onboarding promise now says the opposite, and the app's own "#1 opens already open" branch is dead. Keep the hide? *Recommendation:* keep it (his ruling, and ten empty cards read worse) — but (a) reword the promise to what is true (*"Your order tells me what to build next. Nothing you leave out is switched off."*), and (b) put the #1 pick's single first ask on the first screen, where the composer is — *"You put Gardening first — name one plant and I'll start there."* — so *Meet the user at the action* holds. *Alternatives:* show only the #1 ranked card, open, with its one invitation (the branch already written); or show all ranked picks as quiet empty cards (pass 1's ask; the seats counted that as "six asks on one screen").
2. **Which field is THE colour?** *Question:* the record holds `accent` (signup + place page) and `profileAccent` (account page), and the ruled copy fix says "your account's colour." *Recommendation:* one field, `accent`, on the account; the place page stops offering a colour (a person has a colour; a home does not — his own 09-10 reading); retire `profileAccent`. *Alternatives:* keep both and name them honestly on both pages; or make the place page's picker write `profileAccent` too and leave the model.
3. **Uppercase receipt labels.** *Question:* WHERE IT IS / HOW TO REACH YOU / WHAT I'LL BUILD FIRST are caps on all four layers — the treatment *Tone-coherence* names as admin chrome. *Recommendation:* sentence-case the receipt labels (a receipt is prose she is reading back), keep caps for numeric meta (REGION · 7 DAYS). *Alternative:* keep caps everywhere as the app's meta-label convention and accept the register.
4. **The cold bubble's clearance.** *Question:* the 08-03 ruling protects the bubble as-is; the 08-31 verdict was "fix the clearance"; the cold extended pill sits on Vehicles' Open at first paint, and the household mode already uses a corner circle. *Recommendation:* one shape — the corner circle — everywhere once an account exists (TIER 1·28), which removes the at-rest overlap without touching the ruled bubble; leave the cold surface to B2. *Alternative:* keep extended-at-top and add bottom padding on the last card.
5. **Stone or Pine as the cold default?** *Question:* engine pages hardcode Fernwood's Pine; onboarding uses the engine's Stone. *Recommendation:* Stone (the engine default) on every page until the record says otherwise — it also takes an instance hue out of engine files. *Alternative:* Pine everywhere including onboarding (matches what Paul and Mom see today).

### G · Process verdict

**Fresh eyes caught, unprompted and real:** the dead almanac chip (F6 second half — nobody had measured a scroll); the told-list flush edge (F11); the ERA5 "LOADING" label standing over a rendered table (F10); "Add a home" as a ＋ that adds nothing (F14); the three "change" links being one box (F2 mechanism — pass 1 read it from behaviour; source confirms it is literally the bubble's box); and the whole-picture gut read ("three builds stitched together"), which the seats — each walking one journey — could not say.

**Adjudication killed or reduced:** F2's "off-screen, nothing happened" (auto-scroll + focus, measured); F6's "four times" (two are the place name; strip↔card duplication is the ratified contract); F8's "native blue radios" on onboarding (themed slate; blue is on settings/account); F9's "on the sign-in screen" (an inline link, not the bubble — and the ruling wants the bubble earlier, not later); F12's I/Paul (the page's stated rule; one "We" drifts); F3 as a *bug* (Paul's 09-07 ruling — the finding relocates to the promise copy and the first screen's ask).

**False positives, with mechanism:** "at A+ the box appears off-screen" — the estate page has no A+ mode and the box is scrolled into view; "Closed out" / "162 km" — time-dependent values (the class reproduces); "present on every page including sign-in" — three different controls share one label.

**What this pass adds that neither pass 1 nor five seats could:** C4 (two colour fields), C5 (email unreachable by the route the page reads), C6 (A+ is viewer-only and an allowlist inside it), C7 (two receipt engines), C8 (the masthead link opens a card off-screen) — all found by reading rendered geometry against source, which is the doctrine pass's job.

**The instrument's own defects, reported with the findings:** one evaluate returned 873K characters because a `/You put/` scan matched the inline `<script>` — re-scoped, not re-run; two probes hit the legacy Worker by a guessed host and wrote two `door_failed` records (flagged above); screenshots timed out on the heavy pages, so this pass has one frame.

## Synthesis — the parent session checks the checker (written before filing, per `[paul-ruled 2026-09-04]`)

Pass 2's four load-bearing code claims, re-verified by the parent against the working tree at `aed6dec` (surfaces unchanged since `318416a`):

| pass-2 claim | re-verified | RE-VERIFIED line |
|---|---|---|
| F3: ranked-but-empty modules are hidden; the "#1 opens already open" branch is dead | **CONFIRMED** — `viewer.html:18565` expands the top-ranked card when `moduleState === "empty"`, and `:18592` then sets `display:none` on every empty module for a household (`:18601` for invited ones). The expand runs, the hide wins | RE-VERIFIED: `grep -n 'moduleState(mod) === "empty"' viewer.html` + `sed -n 18556,18605p viewer.html` — 2026-09-10 |
| C4: the colour is two fields with three writers | **CONFIRMED** — onboarding posts `accent` (`onboarding/index.html:1746`); the account page reads and writes `profileAccent` (`settings/account/index.html:228–241`); the Worker names both (`worker/worker.js:943`) | RE-VERIFIED: `grep -n profileAccent worker/worker.js settings/account/index.html; grep -n 'accent:' onboarding/index.html` — 2026-09-10 |
| C5: "email shown back" cannot render from whoami | **CORROBORATED, not independently executed** — the session response carries `email` (`worker/worker.js:988`, inside `handleSession`); the Worker's own comment at `:1449` says `/api/grant/whoami` reads the GRANT row, which holds no email. The account page reads whoami. QUESTION left open: whether a later field was added to the whoami payload — the parent did not diff the handler's return object | RE-VERIFIED: `grep -n whoami worker/worker.js` (`:934–937`, `:1449`) — 2026-09-10 |
| F2: the three "change" links are the feedback box with a swapped placeholder | **CONFIRMED** — `estate/index.html:456–458` binds every row's link to `openFb(r.label)`; `:612–617` shows the same `#fbbox` and sets the placeholder to *"What should it say instead?"* | RE-VERIFIED: `sed -n 444,460p estate/index.html; sed -n 612,617p estate/index.html` — 2026-09-10 |

Pass 2's one measured correction of pass 1 stands on its own evidence (the box IS scrolled to and focused — pass 1's "off-screen" was its own reading of a static frame). The founded estate (`est-gndlvf`) was read by the parent via `GET /api/grant/whoami` as the person, the same instrument the gate uses — RE-VERIFIED: curl `/api/session` → `/api/grant/whoami` at the qa Worker — 2026-09-10.

`ux-trail-check.py --path` at filing: 3 of 4 flagged lines are pass 2 restating the seats' converged findings and the steward's evidence pointers, not fresh counts — read, not repaired.

### Process verdict
Fresh eyes caught, unprompted and from the bare door: the front door's absence as a BLOCKER (the seats started at `/onboarding/` and could not see it) · the change links opening one box for three different things · the ranked interests vanishing from the app · the almanac name four times on one screen · three visual layers reading as three builds. Adjudication killed or corrected: F1/F4/F13 are ALREADY-RULED (cost proven, nothing new) · F2's "off-screen" (the page scrolls and focuses) · F9's "on the sign-in screen" (an inline link, not the bubble) and the bubble itself DELIBERATE per 08-03 · F3's cause is Paul's own hidden-when-empty rule, so the copy is the false half. What only the doctrine pass could find: the colour held in two fields by three writers (C4) · whoami carrying no email so "shown back" is a Worker change first (C5) · A+ being viewer-only with two viewer classes unscaled (C6) · two dead taps on the first screen (C8) · the feedback door in four shapes (C1) · `/api/session.estates` naming the deployment's estate. The content read (TIER 1 · 31, `.content/2026-09-10-founding-flow-copy-REVIEW.md`) added the two findings neither pass could: "Almanac" minted by arithmetic after the Journal ruling, and a "stays on this phone" caption over a button that sends to a model.
