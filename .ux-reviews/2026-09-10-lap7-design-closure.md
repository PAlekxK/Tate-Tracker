# Lap 7 — design closure on the ONE-candidate apply list

- **HEAD at writing:** `b57ca71` · **candidate surfaces frozen at** `318416a` (the sha the sweep, the seats and the content read all walked)
- **Mode:** review · **level:** flow + screen-component · **conditions for every layout answer:** 414 × 848 × **A+**
- **Subject:** `.plans/2026-09-10-founding-flow-design-PLAN.md` §4 THE APPLY LIST (steps 1–10), read against the Beat 6 commit table (`cycle/release/CYCLE-LOG.md:2497–2509`)
- **Read:** the design plan §1/§2/§2a/§4/§5/§6 · CYCLE-LOG §Lap 7 (Beat 6 rows D·C·B·A·E and the eleven rulings) · `.ux-reviews/2026-09-10-founding-flow.md` (pass 1, pass 2 §A–§G) · `.content/2026-09-10-founding-flow-copy-REVIEW.md` (§A–§F, open questions) · `BACKLOG.md` TIER 2 · 18 and TIER 1 · 41/42/45/46 · the six exhibit mocks (`.private/ux-sweeps/2026-09-10-founding-flow/exhibits/{gate,shelf,receipt,lifecycle}.js` + `staging/*.html`) · live surfaces `onboarding/index.html`, `estate/index.html`, `homes/index.html`, `settings/account/index.html`, `viewer.html` (masthead + `renderTold`), `tools/pages-deploy.py`
- **User context.** Primary user: a cold founder on a phone who was texted a URL and has nothing else in hand — and, behind every line, Mom at the condo (reading difficulty, measured depth-1, served A+, whose documented fear is getting it wrong). Job: *get my place set up, and be able to get back into it from any phone.* Context of use: one-handed, 414 wide, A+ text, often outside the house on a network that comes and goes. `user_context_confidence: high` — five seat reads, one un-primed pass, one doctrine pass and a content read at the same build.
- **Scope discipline.** Nothing below re-opens a ruling. Where a review's own recommendation was **overruled** by Paul, I say so in the row, because following the review instead of the ruling is the likeliest way this build goes wrong (rows 34 and 38 are the two live traps).
- **Marking.** Everything is read from source or from the cited artifact unless the cell says **INFERRED**.

---

## The closure table

| # | apply step | open decision | CLOSED as (builder's instruction) | basis | status |
|---|---|---|---|---|---|
| 1 | 1 · front door B2 | What `/` does today, and what it does after | The generated bare-origin file (`tools/pages-deploy.py:214–219`, today a meta-refresh to `estate/`) becomes a two-line decision on **local state only**: if `localStorage["fw-grant"]` is present → `location.replace("viewer.html")`; if not → `location.replace("onboarding/")`, which paints the door. No fetch, no validation, no round trip before first paint. | setup-journey D2 (iii) + its own owed reconciliation — *"a credentialed device passes straight through and only an un-credentialed visitor meets the door"* (`:71`); ux F1a | **CLOSED** |
| 2 | 1 | What the door view IS, concretely | A new `#s-door` section **inside `onboarding/index.html`** (never a new page, never a second form): one sentence saying what this is, then two full-width controls — **`Set up my place`** filled primary → reveals `#s0`; **`I've been here before`** outlined secondary → reveals `#s-nolink` (`:325`). Both already exist; this is a third view over them. | B2's constraint *"exactly one account-creation surface"* (`setup-journey:120–121`); pass 1 F1 blocker | **CLOSED** |
| 3 | 1 | Where a successful sign-in lands (pass 1 F7: setup → `/estate/`, sign-in → `/viewer`) | **Branch on the home count the session response already carries.** 0 homes → `/homes/` empty shelf (the one founding control, row 22). Exactly 1 → `/viewer`. 2+ → `/homes/`. The bare origin (row 1) does **not** branch — it knows nothing without a round trip, and a credential alone means *go to the app*. | B2's falsifier *"sign in and land in your place"* vs r3 *"the shelf is the way in"*; the bug lane's own shape, FINDINGS §4.3(4) *"branch on count after sign-in"* | **CLOSED** |
| 4 | 2 · bubble (R6) | Which screens, which corner, at 414 × A+ | The **46 px corner circle**, `position:fixed; right:12px; bottom:14px` — byte-identical to the four pages that already carry it (`estate:145`, `homes:102`, `settings/*`) — on onboarding **s1 · s2 · s3 · s4 only**. On onboarding it opens the page's **own** `#fbbox` (`:853`), not a link to `/estate/`. | pass 2 Q4 ruled *one shape, the corner circle, once an account exists*; TIER 1 · 28 | **CLOSED** |
| 5 | 2 | Does it collide at 414 × A+ | Yes: onboarding's commit buttons are `width:100%` inside `main{max-width:560px;padding:22px 18px}` (`:116`, `:171–173`), so at 414 they span x 38–376 and the circle occupies x 356–402 / y 788–834 — a **20 px overlap for any button that comes to REST there**. Fix the clearance, never the bubble: `main` bottom padding `40px → 96px` on onboarding; `.wrap` bottom padding ≥ 72 px on estate/homes/settings. Scroll-through overlap stays and is accepted. | `[paul-stated 2026-08-03]` *the bubble stays as-is, momentary cover-ups accepted*; the 08-31 verdict *"protect the bubble, fix the clearance"* | **CLOSED** |
| 6 | 2 | One channel, how many doors per screen | **One door per screen.** On s1–s4 the inline `#fbopen.fblink` (`:852`) is hidden and the circle is the door. On `#s-door`, `#s0` and `#s-nolink` (pre-account) the inline link stays and the circle does **not** render. ⛔ Do not refactor the other four pages' bubble-as-link this lap — it is not on the apply list and it works. | C1 *one control, four doors*; R6 says *from the moment an account exists* — not before | **CLOSED** |
| 7 | 3 · account receipt at naming | Where the one line sits on s1 | First prose in `#s1`, **above** `:508`'s *"Nothing in it yet…"*, carrying the ✓ glyph as a receipt mark (not a button). It is the only ✓ on that screen until `go1`. | exhibit 2b **R**, ruled; `shelf.js` `RECEIPT` places it there; strict Finding 5 (*"it" has no antecedent*) | **CLOSED** |
| 8 | 4 · the gate card | Is the gate a new screen or the same card | **Same card, in place.** Tapping `#go2` swaps `#s2`'s field group for the read-back block; the card does not scroll, the header does not change, `#s3`'s addressless interstitial (`:622–630`) is **deleted**, and `#s4`'s `#confirm` block (`:714–730`) moves here. Nothing is written until the affirmative. | R1 the confirm is a GATE; exhibit 1 **A**; `gate.js` opt `A` | **CLOSED** |
| 9 | 4 | Where the optional unit line sits, and is it a second ask | The existing `#a2` field (`:554`, `autocomplete="address-line2"`) **stays in state 1** — it is the autofill-capable ask and must not be lost. In state 2 the quiet **`Add an apartment or unit number ›`** renders **only when `a2` is empty**, **directly under the rendered address and ABOVE the Google-Maps line and both buttons** — it edits the value being confirmed, so it cannot sit below the control that confirms it. Tapping it reveals one field in place; leaving it alone is a complete answer. ⚠️ This moves the link from where `gate.js:21` puts it (after the buttons); the reason is the same measured one that moved the disclosure above `#go2` in the first place (`:578–584`). | R7 *"don't force it"*; the 09-05 measurement at 414 × A+ | **CLOSED** |
| 10 | 4 | Where the PO-box refusal renders, what pattern fires it, who is the authority | Renders in **`#trouble` (`:609`)** — already directly above `#go2`, the measured-correct slot. **BLOCKING**: state 2 does not render, focus returns to `#a1`, nothing is written. The page's cheap check **reuses the regex that already ships** — `/\b(p\.?\s?o\.?\s*box|post\s*office\s*box|postal\s*box)\b/i` (`viewer.html:19957`) — and must **not** be widened to bare `Box 12` (the rural "Box 12, Route 3" false positive the content read names). The **Worker is the authority**: `POST /api/estate` refuses a box too, so the block cannot be bypassed, and the page prints **one** sentence whichever refused. | Beat 6 row A *"blocking at submit"*; exhibit 1 **PO2** copy; D1a *"(ii) as the authority, (i) as the courtesy"*; *One engine, one verdict* | **CLOSED** |
| 10a | 4 | What a genuinely box-only household does after a block | Nothing new is built. The feedback circle (row 4) is on that screen and is the escape; a blocked person has a door. ⛔ Do not add a "continue anyway" control — it would make the block decorative. | *a correct "no" still owes a next move* (punch 14's principle), satisfied by an existing control | **CLOSED** |
| 11 | 4 | What "Not quite" does | Returns the **same card** to state 1 with every typed value intact, focus on `#a1`, one quiet line above the fields saying the values are still there and re-tapping "That's it" re-checks. ⛔ It is an outlined neutral, carries **no ✓**, and opens **no note box**. Nothing has been written, so there is nothing to undo. | `:704–713` (*"Not quite" is not the error path*); owner F07 | **CLOSED** |
| 12 | 4 | What the affirmative shows, and where the free-text box goes | On `#ok1`: print the receipt line in place (content's words), then advance. The **`#s4ask` textarea + `#sendNote` (`:731–735`) no longer render on the affirmative at all** — they render only after "Not quite", if at all. If a Send survives anywhere on this card it is the **outlined secondary**, never a filled ✓. | standing rule 1 (one filled affirmative per moment); *receipt and consequence are two acknowledgments*; pass 2 punch 8 | **CLOSED** |
| 13 | 5 · ranking | Which control is the one filled commit | A two-state machine on the one screen. **Before the ranking is saved:** `#go5` = filled ✓ `Save these`; `#gohome` = **outlined** (transparent, 1.5 px accent border). **After a successful save:** `#go5` relabels to `Update these` **and loses its fill and its ✓** (outlined secondary); `#gohome` becomes the filled `bigbtn`. Exactly one filled control at every moment, and the route control never wears a ✓. | D3; standing rule 1; `homes/index.html:85–90` — a route control correctly declines the affirmative grammar | **CLOSED** |
| 14 | 5 | Which is filled when the person picks nothing | The same machine, unchanged: `Save these` is still the filled commit at zero picks, because **choosing nothing is a real answer that gets recorded** (`:745`), and the receipt card correctly renders no row for it (`viewer.html:19942`). ⚠️ *Hidden-when-empty* is a ruling about **viewer cards**, not about this screen — it changes nothing here. | `[paul-ruled 2026-09-07]` scope; `:745` *answering nothing is a legitimate answer* | **CLOSED** |
| 15 | 5 | Open-without-Save (untapped today) | Tapping `Open <name>` with **unsaved** picks **saves first, then routes** — no dialog, no warning, no loss. The post-save receipt line is skipped (she is leaving). If the save fails, the existing *"That didn't go through — your note is still here"* grammar applies and the route does not happen. | *capture must not lie* (`estate:648`); rule 5's caveat — never make the changeable thing expensive | **CLOSED** |
| 16 | 6 · receipt editors | Inline or step-return, and one answer for all? | **One label for all three — `Edit` — and the destination differs because what exists differs.** *Where it is* → step-return to the **one** address screen (onboarding `#s2` in edit mode, her values pre-filled, same gate card, same refusal). *How to reach you* → step-return to `/settings/account/` (the working self-serve control, `settings/account:100–104`, `:236`). *What I'll build first* → **inline** on the receipt: the tile picker re-rendered in place, re-save supported. | exhibit 4 (**Edit** editors, ruled); D5's A-for-ranking/B-for-address split resolved by `homes:226` — *"there is exactly one place in the product that asks for an address… a second screen would MANUFACTURE one"* | **CLOSED** |
| 17 | 6 | What makes three unlike mechanisms read as one affordance | **The invariant, and it is the whole of the answer:** every `Edit` ends back on the receipt with that row rewritten to the new value, and **no `Edit` ever opens a note box**. A reader never has to model navigate-vs-inline; they have to see the row change. | *Identical form is a promise of identical function* (candidate) — honoured at the level of outcome, not mechanism; rule 5's caveat | **CLOSED** |
| 18 | 6 | What happens to the derived facts when the address is edited | The edit commits through the **same gate**, so the refusal fires **before** the write on this path too. On the affirmative, one line names what moved with it (weather and sky) — the person must not gain derived facts silently. If the new address does not place, the address she gave is kept (the address is hers), the coordinates are not silently retained from the old one, and the place card's existing unplaced copy explains the blank (`viewer.html:18379/18384`). | `elicitation-lens`'s **derived-silently** reading; R1 (refuse before writing, never after) | **CLOSED** |
| 19 | 6 | Email shown back — display-only, or see-correct-remove | Shown back under **How to reach you** in `settings/account` card 1, as a **value with no affordance** (three states: the address · *none on file* · *unknown* when the record could not be read — never absent). ⛔ Do **not** ship a link beside it that only sends a message — that is the exact violation row 16 is removing. **NEEDS-PAUL on scope:** Beat 6 row B commits *"email shown back"* and not *editable*, but three seats across two builds say a person who mistyped their email can never find out, and the recovery path runs through it. **Recommendation:** display-only this lap, the editor in lap 8, and say *"not in this build"* on the release note rather than leaving it unsaid. | Beat 6 row B; setup-journey B6; BACKLOG TIER 2 · 18 (*see it · correct it · remove it is the floor*); C5 — it is a Worker change first | **NEEDS-PAUL** |
| 20 | 6 | Sentence-case receipt — where, and how | **At the slot, not in CSS.** Author `Where it is` · `How to reach you` · `What I'll build first` in sentence case and drop `text-transform:uppercase` from `estate .row .label` and `viewer .told-label`. Keep caps for numeric meta (`REGION · 7 DAYS`). ⛔ Do not lowercase with a regex — `viewer.html:18481`'s `/^(A|An|The) /` is the defect this ruling closes, not the tool for it. | pass 2 Q3 ruled sentence-case; TIER 2 · 25 | **CLOSED** |
| 21 | 6 | The receipt exists twice (estate rows + `renderTold`) — which one gets the editors | **The app's card (`viewer.html:19944–19989`) is the one that gets `Edit`.** `/estate/` is on its way out (D3/B5) and a second set of editors would have to be kept in step by hand. Until `/estate/` retires, its three links get the honest interim label, **not** editors. ⛔ And the app card's *"The default — change it whenever you like."* (`:19966`) may not ship without its control — either the `Edit` lands or that line goes. | C7 *two receipt engines*; §E *"do not patch both"*; CLAUDE.md rule 5 | **CLOSED** |
| 22 | 7 · shelf | One control when empty | `#addbtn` (`homes:123`) and `#addcard` are **not rendered while the shelf is empty**; the only control is the existing `Set up my first home` `bigbtn` (`:229–233`). Both return the moment a home exists. | exhibit 2 **A**, ruled; seats (C) *two founding-looking controls* | **CLOSED** |
| 23 | 7 | What the `＋` becomes once a home exists | The dashed `＋` add-affordance is retired as a shape: a quiet row-styled link, no glyph, no dashed box. ⛔ The card behind it keeps its copy exactly (`:126–131`) — three seats read it without complaint. | button-glyph clause `[paul-ratified 2026-08-02]`; content B6 (*the label is the problem, not the card*) | **CLOSED** |
| 24 | 7 | Shelf row content | Unchanged and re-affirmed: **name + town/state only** (`townState`, `:175–181`), one tap, one destination, no street number, no counts, no badges. The row wears the **place's** colour as a rail; the page wears the **person's**. | setup-journey B4 constraints; `homes:166–169` `[paul-ruled 2026-09-06]` | **CLOSED** |
| 25 | 8 · lifecycle | Exact placement of "Sign out of this phone" | A **fourth card** on `/settings/account/`, inserted **after** `#save` and its `#trouble` line (`:119–120`): a short label, the full-width **outlined** button, one quiet line about what it does to this device. Placing it above Save would put a device action in front of the commit for the three cards above it. | exhibit 3 **as drafted**, ruled; `lifecycle.js` ACCOUNT | **CLOSED** |
| 26 | 8 | Does sign-out confirm, and what does it clear | **Two taps, inline in the same card** (`✓ Sign out` filled / `Cancel` outlined) — a mis-tap costs a phone call to a human, because the administrator is the reset path. It clears **identity keys only**: `fw-grant`, `fw-username`, `fw-onboard-*` (step, name, addr, parts, owner, interests, contact, coords), `fw-accent`, `fw-profile-accent`, `fw-journal-name`. It **keeps** device preferences that are not identity — above all the **A+ text size**, which is an accessibility setting and must survive. Every key touched is rostered (`check-storage-keys.py`). | D1 (the administrator resets); C4 2b (an unrostered key is one she loses); `[paul-stated 2026-09-03]` A+ is the standard | **CLOSED** |
| 27 | 8 | Where sign-out lands, and what the door says | Lands on the door in **sign-in view** with a signed-out lede. ⛔ It must **not** inherit `#si-lede`'s default *"This link isn't working."* (`:331`) — that sentence is a lie on this route and alarming on the screen where a person has just chosen to leave. Route it with an explicit flag (`?from=signout`) the way the page already selects among its three ledes (`:326–330`). | `:326–330`'s own three-routes rule; *a correct action must not render as a fault* | **CLOSED** |
| 28 | 8 | Where recovery lives on the door — below the form, or behind a link | **Below the form, adjacent to the failure**, in this order under `#si-go`: `#si-trouble` (the constant refusal) → **one** entry point `Can't get in?` → the existing `Never set one up? Create your account ›`. **One control covers both username and password** — a locked-out person usually cannot say which they have forgotten, and D1 makes both the same act. Tapping reveals the block **inline**; no new page. | D1 (the administrator does both); punch 14 (the copy must name username recovery); *don't make the reader diagnose their own failure* | **CLOSED** |
| 29 | 8 | What the recovery block contains and answers | One field — the email address — one `✓ Send`, and a **constant** response that is byte-identical whether or not the address is on file. ⛔ No "we found your account", no different timing, no different field state. ⚠️ It must **not** share `ratelimit:feedback:<ip>:<bucket>` (`worker.js:4106/4377/4412`) — spending a person's feedback quota on a recovery attempt is measured, not hypothetical (TIER 1 · 42). Copy is content-steward's; the security read is owed before it ships. | Beat 6 row B *"without an existence oracle"*; security-steward owed (design plan §row seats) | **CLOSED** (copy + security read owed, named) |
| 30 | 8 | Recovery for someone who is signed in | The `If you get locked out` card on `/settings/account/` as drafted: the explanatory line plus **`Ask Paul to reset my password ›`**, which sends the request naming the username, never the password. This is the signed-in twin of row 28 and is a different reader (they can still get in). | exhibit 3 as drafted; `lifecycle.js` ACCOUNT | **CLOSED** |
| 31 | 8 | What the house-named refusal is anchored to on screen | **`#si-trouble` (`:343`)** — already hidden by default, already directly under the sign-in button, already the slot the page owns for this. **One constant string**, identical for wrong password · unknown username · another house's credential · revoked; it names the **door**, never the credential. ⛔ Exhibit 3b's drafted sentence (*"that username isn't set up at this house"*) is **REJECTED** and may not ship. | `[paul-ruled 2026-09-10]` rejecting 3b; SECURITY L1(b); the byte-identical 404 stays | **CLOSED** |
| 32 | 8 | The house in the lede — what supplies the name | Ruled and stands, with one condition a builder must not guess past: the lede may name the house **only from a build-time instance value** (the same substitution mechanism `viewer.template.html` uses). It may **not** read `localStorage` `fw-onboard-name` — a clean device has none and the lede would read *"Welcome back to My Home."* **If no instance value is wired this lap, the lede ships nameless** (*"Welcome back."*) rather than wrong; that is not a blocker on anything else in row 8. | exhibit 3b's *house-in-the-lede half stands*; measured: `#head` defaults to `My Home` on a cold device (`:304`) | **CLOSED** (conditional, condition named) |
| 33 | 8 | What quarantine looks like on screen | On a **refused** credential: keep the cached rows, **drop `Signed in as <user>`** (`homes:173`, `estate #who`), swap each row's `Open ›` for **`Sign in ›`** routing to the door, and print the refusal state above the list. ⛔ Never clear storage on a refusal — the no-signal premise makes clear-on-failure destructive. **Refused and unreachable are two different sentences and neither may render as the other**; the third state (*verified empty*) keeps its own. | Beat 6 commit *"`/estate/` tells refused from unreachable"*; B1's `ok · empty · unknown` resolver; row 41 | **CLOSED** |
| 34 | 6 · 9 | **TWO colours** — which field, which surface, so a builder places each once | **Account colour = `profileAccent`.** Written by signup (`onboarding:1746` must change from `accent` → `profileAccent`) and by `/settings/account/` (`:241`, already correct). Paints the shelf, the account page, and anything that is not a place. **Place colour = `accent`.** Written by `/settings/place/` **only** — and that page must **stop writing `acct.accent`** (`worker.js:4187`, `:4225`). Paints the app masthead, the place page and the shelf row's rail. Two pickers own two fields; the onboarding s0 picker **is** the account picker. ⛔⛔ **TRAP: pass 2's Q2 recommended ONE field and Paul ruled TWO. Do not implement Q2.** | `[paul-ruled 2026-09-10]` two colours, two honest labels; C4 (two fields, three writers) | **CLOSED** |
| 35 | 9 | Stone as the cold default — where | Remove the hardcoded Fernwood Pine `--accent:#2f5d3a` from the four engine pages' `:root` (`estate:50`, `homes:37`, `settings/account:36`, `settings/place:31`) and default them to Stone `#3F5266`, matching `onboarding:69` (*"the ONE token an instance overrides"*). Fernwood keeps Pine because **its instance supplies it**, not because an engine file names it. | pass 2 Q5 ruled **Stone**; F8/C4; estate-neutrality (an instance hue in engine files) | **CLOSED** |
| 36 | 9 | What colour a newly founded place opens in | The place's `accent` is **seeded from the person's `profileAccent`** at founding, so the place page's picker never opens unset and the masthead never renders a colour no swatch claims. After that the two move independently. | `settings/account:157–165` (*a picker that shows no selection cannot tell her what colour her place already is*) | **CLOSED** |
| 37 | 9 · D8 | Which punch items are affordance/layout (mine) vs copy (content's) | **Mine, ship them:** 3 `scrollIntoView({block:"start"})` after `expandCard("card-told","nav")` (`viewer.html:19934`) · 4 `.told-list{padding:0 14px}` · 5 `.text-lg .told-label{13px}` / `.text-lg .hh-utility{16px}` · 6 hide the almanac jump chip when `card-fieldnotes` is hidden · 7 add the missing `#uwordstate` element and fire it at ≥8 chars, silent below · 8 demote `#sendNote` (row 12) · 9 drop the `＋` glyph and the dashed box (row 23) · 11 swap the ERA5 label **on the render event**, not on a timer · 12 drop the body-level duplicate heading · 13 (row 39) · 15 allow two lines under `.text-lg` for event titles. **Content's, not mine:** 1, 2, and the words of 7, 8, 11. | pass 2 §D; C6 (the A+ allowlist, second occurrence) | **CLOSED** |
| 38 | 9 · D8 | Punch 10 — relabel the change links | ⛔⛔ **DO NOT APPLY.** Punch 10 (*"Tell Paul it's wrong ›"*, one wording) was the honest interim **for a world where the editors do not land**. Exhibit 4 ruled the editors in, so applying both would ship a message-link labelled as a message beside an `Edit` that edits. Apply row 16 instead; punch 10 survives only on `/estate/` until it retires (row 21). | exhibit 4 ruled `[paul-ruled 2026-09-10]`, superseding pass 2 §D·10 | **CLOSED** |
| 39 | 9 · D8 | `settings/place` header `‹ Back` | **`‹ <place name>`**, falling back to `‹ My Home` — its destination is `/estate/`, which *is* the place, and the page's own comment demands a destination word (`:15–18`). ⛔ Not `‹ Your homes` (punch 13's first option) — that would be a wrong destination. `/settings/account/`'s `‹ Your homes` is correct and does not move. | C3 *one back-link, three wordings, two destinations*; content A6; *a CTA's label must promise what the destination delivers* | **CLOSED** |
| 40 | 9 · D8 | TIER 2 · 21 — three unlike destinations at one weight in the viewer masthead | Two navigations are anchored like the sibling pages — `‹ Your homes` left, `Settings` right, `justify-content:space-between` (matching `estate:168–171`, which kills the measured 76 px dead band). The **in-page jump** `What you told me` renders at the same size but **without a chevron and at the lighter utility weight**, because it goes nowhere — and it must actually scroll (row 37 item 3). | pass 2 §A TIER 2 · 21 measurement; C8 (the tap that does nothing); *ordering first, then type in service of it* | **CLOSED** |
| 41 | 4 · 9 | The gate's PO-box sentence is now said twice | Once the refusal is said at submit, `estate:371`'s row-level repeat is a **second telling**. Keep the **place-card** one (`viewer.html:18379/18384` — it explains a blank weather card, which is a different job) and cut the estate-row one. | content C2's flagged consequence (*four tellings is how a kind sentence becomes a scold*) | **CLOSED** |
| 42 | — | `Save & consult the Almanac` — the composer's split control (content B1, escalated to me) | **Named, and OUT of this apply.** It is not in §4 and it touches the AI boundary surface. The design for the record: **two controls, not one** — a primary that saves locally and a secondary that sends, each captioned by its own consequence; the who-sees-it line belongs to the **button**, not the box above it. Lap 7 ships the caption fix only (content's). | content B1 + its proposed cross-project candidate *"a caption belongs to the control, not the space above it"*; §4 scope | **OUT — design named** |
| 43 | 10 | D6 (ranked-but-empty cards) and D9 (glance consolidation) | **Not in this candidate**, and said so on the release note in the person's own terms rather than left unsaid. D6 waits on Paul's Q1 and a user-researcher read; D9 waits on G6. ⚠️ Consequence the builder must not paper over: `onboarding:777`'s promise is false while the hide stands — if the reworded promise (Q1's remedy (a)) is not shipped, the sentence must be withdrawn, not left standing. | design plan §4 step 10; content B7 (*the build must catch up to the copy*) | **CLOSED** |
| 44 | 9 | `Almanac → Journal` (five engine sites) | Copy/vocabulary — content-steward's and Paul's (their open question 2). **My half only:** wherever the noun changes, the jump chip, the composer title and the card title must all change **from the one writer** (`viewer.html:19991`) — a fifth site minted by arithmetic somewhere else is how this came back. | content A1; `VOCABULARY.md:430` | **OUT of my lane — one constraint stated** |

---

## The lifecycle journey, as taps

For the harness author. Preconditions: one account, one **placed** home, signed in on the device, 414 × 848, A+. Stops are named `L01…` so they do not collide with `U01…`/`F01…`.

| stop | screen | tap / assertion |
|---|---|---|
| **L01** | `/viewer` (household) | masthead `‹ Your homes` → `/homes/` |
| **L02** | `/homes/` | footlink `Your account ›` (`:137`) → `/settings/account/` |
| **L03** | `/settings/account/` | the email renders under **How to reach you** in one of its three states (row 19) — assert it is not simply absent |
| **L04** | `/settings/account/` | scroll to the **This phone** card (after `#save`); assert the sign-out button is not covered at rest (row 5) |
| **L05** | | tap `Sign out of this phone` → the inline confirm appears in the same card |
| **L06** | | tap `✓ Sign out` |
| **L07** | the door | assert: identity keys gone, **A+ still set**, no `Signed in as` on any surface, and the lede is the signed-out one — **not** *"This link isn't working."* (row 27) |
| **L08** | `/` (cold, storage cleared) | assert the bare origin paints the **door**, not `/estate/`'s invitation-link empty (row 1) |
| **L09** | `#s-door` | assert **two** named doors; tap `I've been here before` |
| **L10** | `#s-nolink` | type a **wrong** password → assert `#si-trouble` shows the **one constant string**, and that it is byte-identical to the string shown for an unknown username (row 31); assert a `door_failed` record with **no** `personId` |
| **L11** | `#s-nolink` | tap `Can't get in?` → the recovery block reveals inline (row 28) |
| **L12** | recovery | submit a **known** email → record the response string; submit an **unknown** email → assert the response is **byte-identical and equally timed** (row 29). ⛔ This is the journey's last machine-checkable stop |
| **L13** | *(human)* | the administrator resets — **out of the harness by D1's own design**. The walk must say so rather than scoring it walked |
| **L14** | `#s-nolink` | sign in with the good credential → assert the landing branches on count (row 3): 1 home → `/viewer` |
| **L15** | `/viewer` | assert the masthead carries the place's name, the receipt card holds `Where it is` · `How to reach you` · `What I'll build first`, and each row's `Edit` returns to the receipt with the row rewritten (row 17) |

**Two harness notes.** (1) Unlike J1 and J2, this journey does **not** consume its entry state — the account survives a sign-out, so the fixture is durable and reusable. (2) L13 is a genuine coverage boundary, not a gap to be closed by a mock: a walk that scores it "passed" would be asserting a human did something.

---

## What I did NOT decide

- **All copy.** Every sentence above is a slot, not words. The receipt line, the refusal constant, the recovery response, the signed-out lede, the colour labels, the definition of the Journal and the `Almanac → Journal` substitution are content-steward's, all DRAFT, all human-confirmed before they reach a person.
- **Anything already ruled.** The eleven rulings stand untouched. Where a review recommendation conflicts with one, I flagged the ruling and marked the review's version a trap (rows 34, 38).
- **Backend and data model.** `PAGES_WORKERS` / the Worker map (row D), G6's events and their reader (row C), whether `whoami` or a profile GET carries the email (C5), `POST /api/session.estates` reading `scope.id` (`worker.js:1008`), the `POST /api/estate` refusal shape, the rate-limit bucket split, and the single sign-in door (**lap 8**) are engineering-partner's and Paul's.
- **Security wording and mechanism** for username recovery — security-steward is owed before that field is built; I closed the *shape* (one constant, never confirms), not the crypto or the route.
- **D6 and D9** — not designed here; named as out on the release note (row 43).
- **The composer split** (row 42) — designed for the record, deliberately not scoped into this candidate.
- **The estate page's feedback-bubble-as-link** on homes/settings/place — works, not on the apply list, left alone this lap on purpose.

**One NEEDS-PAUL, total:** row 19 — is *email shown back* display-only this lap, or does it get its editor? My recommendation: display-only, editor in lap 8, named as *not in this build* on the release note.
