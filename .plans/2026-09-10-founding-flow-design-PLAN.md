# The founding-flow design window → the LAP 7 UX BUNDLE — one decision set, one candidate

- row: proposed — `BACKLOG.md` TIER 1 · 26 (the window) carrying TIER 1 · 27 (the lap-7 UX bundle, by pointer); founding rows 19 · 28 · 29 · 30 · 31 · 41; TIER 2 · 10 (anchor) · 13 · 18 · 19 · 20 · 21 · 25; the 08-31 leftovers (Track A · A4); cards `fernwood-11` · `fernwood-8`
- objective: O3
- class: engine · declared (every surface here serves every household; nothing names Fernwood)
- seats: **ux-sweep pass 1** → `.ux-reviews/2026-09-10-founding-flow.md` (filed, un-primed) · **pass 2** → same file (running) · **content-steward** → `.content/2026-09-10-founding-flow-copy-REVIEW.md` (TIER 1 · 31, **filed**; §C drafts the gate card, the PO-box refusal in both forms, the receipt line, the colour noun — every exhibit's copy is its, all DRAFT until human-confirmed) · **engineering-partner** → **owed** on D1 (the gate's write path: where the PO-box check runs and what `POST /api/estate` refuses) and D5 (the change links' edit path) · **security-steward** → **owed** on D4 before any username-recovery field is built (a username-exists check is an enumeration oracle; § 🤝 INVITE & JOIN already names this) · **user-researcher** → waived for the confirm card and shelf (five seat reads + one un-primed pass are the evidence); **owed** before D6 ships (which empty cards a person expects after ranking) · **ai-advisor** → waived (no model on any path here)
- depends-on: `.plans/2026-09-08-setup-journey-PLAN.md` (D2 front door · B2/B3/B4 · B9 · D3 receipts) · `.plans/2026-09-10-interests-as-activities-PROPOSAL.md` (the ranking screen's words; not redesigned here) · `.plans/2026-09-07-backlog-grooming-SCAN.md` §11 (GL-1…GL-13) · `.user-research/2026-09-07-glance-measurement-procedure.md` §5.1 (G1–G6) · `.plans/2026-09-01-preglance-stack-ledger.md` (D4 ledger, RUN) · `handoff/handoff-cross-device-signin.md` (row 41's bug lane; cited, not re-derived)
- ready: **agent-proposed 2026-09-10 — Paul rules.** ⛔ Nothing here is applied. `onboarding/index.html` and `homes/index.html` are frozen at `318416a` until the coordinator lifts the hold; every proposal below is a patch, an exhibit or a sentence.
- stage: concept
- stage-note: 2026-09-10 — written in the design window while pass 2 and the content read were in flight; §2 (exhibits) and §5 (punch list) are filled as they land. Sweep target: qa at `318416a`, 414×848, A+. The sweep founded `est-gndlvf` (recorded for teardown).

> ### ⭐ THE ONE PARAGRAPH
> Five seats and one un-primed pass agree on what the founding path does wrong, and Paul has already ruled the two
> biggest halves: **the confirm is a GATE** and **the account lifecycle is in**. What is left to decide is smaller than
> it looks — where the PO-box check runs, which single control the empty shelf keeps, what a "change it" link must
> honestly do, and what a person who ranked Gardening should see on an app with no garden. Everything lands as **ONE
> candidate** `[paul-ruled 2026-09-10]`, walked once by the full battery, then by Paul.

---

## 1 · What Paul has already ruled (cited; nothing below re-opens them)

| ruling | words | what the surface must now do |
|---|---|---|
| R1 **the confirm is a GATE** `[paul-ruled 2026-09-10: "I'm good with your recommendation on one"]` | founding waits for the tap; "Not quite" edits before anything is written; the PO-box refusal and the confirm become one card on the address step; **a refusal never fires after the record exists** | `POST /api/estate` is not called until "Yes, that's it"; "Not quite" returns to the fields, not to a note box; the promise *"check it on the next screen"* becomes true or is retired |
| R2 **the PO-box refusal is said AT SUBMIT** `[paul-ruled 2026-09-10]` | *"when they hit submit, if we can check whether it's a PO Box… just say we don't accept PO Box"* | the address step says so before advancing — sub-question D1a below |
| R3 **account lifecycle is IN** `[paul-ruled 2026-09-10: "let's pull it in"]` | *"let's have the UX come up with some designs while they're in the context because those are big gaps that we need to fill"* | sign out · recover password (D1 of setup-journey: the administrator is the reset path) · recover username · the email shown back — designed here, built on the bundle's candidate |
| R4 **colour = the one-line noun fix only** `[paul-stated 2026-09-10]` | *"let's just focus on the one line copy fix and put the other item in the backlog"* | TIER 1 · 29 rides; TIER 2 · 17 stays out |
| R5 **ONE candidate** `[paul-ruled 2026-09-10]` | all applied design lands together, walked once by the full battery, then him | §4 is the apply list; nothing ships in slices |
| R6 the feedback bubble from the moment an account exists (TIER 1 · 28) | *"so even on the account screen or home selection screen you can have feedback"* | `fbbubble` on every post-signup screen (naming · address · confirm · receipt) |
| R7 unit number `[paul-stated 2026-09-10]` | *"eventually ask 'do you want to provide a unit number?' — don't force it"* | an optional ask on the gate card, never a required field |
| R8 the front door for everyone at the bare origin (setup-journey §2 D2, ruled; B2/B3 unbuilt) | one screen offering both *sign in* and *set up*; exactly one account-creation surface | pass 1's F1 is this gap found independently from the bare door |
| R9 the glance consolidation GL-1…GL-13 (TIER 2 · 10) — **G6 first** | *"instrumentation lands BEFORE the adaptive order"* | the strip work in this bundle is design-only until G6 + card-face opens land (a build lane's; the coordinator has been told) |
| R10 content-steward reads every walk (TIER 1 · 31) | *"consistent language and ensuring that things are accurate"* | its review gates every exhibit's copy |

## 2 · The decision set — each with the seats' reads, the falsifier, and the ruling it still needs

### D1 · The gate card on the address step (R1 + R2 + R7) — EXHIBIT 1
**What it is.** One card: the fields · the optional unit line · the promise sentence · "That's it" → the address rendered back with a subject line ("WHERE IT IS", reused from the receipt — setup-journey B9(b)) · "Does that look right?" · ✓ Yes, that's it (founds) · Not quite (returns to the fields, nothing written).
**Seats' reads (verbatim, `seat-distillation`):** owner — *"nothing was worked out from my address until I said it was right"* is what it expected; strict — *"either the question is load-bearing and the two buttons below it are a bypass, or it is not"*; wide-eyed — *"a courtesy dressed as a gate"*; pass 1 F4/F5 — *"it has already been saved and there is nothing to check"*, *"Yes, that's it" answered with another ask*.
**D1 copy:** review §C1 (question with a subject: *"Your address — have I got it right?"* · *✓ Yes, that's it* unchanged · *"Not quite — let me fix it"* returns the fields with her values · the unit number as a quiet link that never forces · on the affirmative *"Got it — that's the address down."*, never a new ask). **D1a — where the PO-box check runs (OPEN; engineering-partner owed; the steward drafted BOTH forms, §C2, and will not pick — blocking vs non-blocking is Paul's).** Two honest forms, both exhibited: **(i) detect before `found`** — the page reads the parts at submit (a box-number pattern) and says *we don't accept PO boxes* with the fields still editable; nothing is minted. **(ii) the Worker refuses** — `POST /api/estate` answers a named refusal for a box, the page says so, nothing is minted (the response already carries `geocodeWhy: refused:<reason>` for placement; founding would have to refuse too). ⚠️ (i) is a pattern and can be wrong (a rural "Box 12, Route 3"); (ii) is authoritative but costs a round trip before the person is told. **Recommendation: (ii) as the authority, (i) as the courtesy** — the page warns on the pattern, the Worker decides; one message either way. R1's clause *"a refusal never fires after the record exists"* is satisfied by both only if the Worker refuses BEFORE minting.
**Falsifier.** A strict-seat walk with a PO box: the refusal is on the address step, before any tap on the confirm, and `walk-founding` reads NO new `est-` for that walk. An owner walk: `est-` is minted only after `#ok1`; the transcript shows no `/api/estate` call before it. A "Not quite" walk: the fields regain focus with the typed values intact and no feedback POST fires.
**Seat owed:** content-steward (the card's words, §C of its review) · engineering-partner (D1a).

### D2 · The empty shelf — EXHIBIT 2
**What it is.** `homes/index.html`'s empty state: today *Set up my first home* (routes to onboarding) + dashed *＋ Add a home* (opens a "one home for now" message card). Four seats: two founding-looking controls; the shelf is off a cold founder's path (signup → naming; the shelf is reached only by URL).
**Options.** **(A) one control** — keep *Set up my first home*; the `＋` is not rendered while the shelf is empty (it returns when a home exists, where "one home for now" is true and useful). **(B) on-path** — after signup the product lands on the shelf (the account-exists receipt strict called *"the best screen in the walk"*), then one tap into naming. **(C) off-path, receipt at naming** — keep signup → naming, add strict's one-line account-exists receipt above *"What do you call it?"* so "it" has an antecedent.
**Constraints inherited:** setup-journey §1 r3 *the shelf is the way in* `[paul-ruled 2026-09-08]`; B4 — a row does ONE thing, never the full street address, no counts/badges; the empty state renders only on a VERIFIED empty (B1). Row 30: Paul's own note that the shelf's formatting is *off* — the exhibit shows him the shelf so he can say what.
**Recommendation:** A + C — one control on the empty shelf, and the receipt line at naming; B changes the ruled journey and adds a tap for every founder to fix a screen most never see.
**Falsifier.** A cold walk's F04 (if reached by URL) shows exactly one founding control; F03's naming screen carries the receipt line; a placed house's shelf row shows name + town and never the street.

### D3 · Three same-styled filled ✓ commits on one screen (mom, strict, wide-eyed; pass 1 F5)
With R1, *Yes, that's it* moves onto the address step, which leaves *Save these* and *Open <place>* on the ranking screen. **Recommendation:** *Save these* is the one filled ✓ commit; *Open <place>* becomes the outlined secondary until the ranking is saved, then the primary (the interests proposal §5(b) already has `go5` relabel to *Update these*). ONE affirmative grammar `[paul-stated 2026-07-29]`. Untapped today: Open-without-Save — the exhibit's note says what it does.

### D4 · The account lifecycle (R3) — EXHIBIT 3 (appearance-and-affordance; the build is the bundle's)
Four screens, one rule each — designed against TIER 2 · 18's own guard (*"do NOT let it become a checklist of missing buttons"*):
1. **Sign out** — on *Your account*, below Save, outlined secondary: *Sign out of this phone* — the copy says what it does to THIS device only.
2. **Email shown back** — *Your account* shows the address under HOW TO REACH YOU (setup-journey B6: shown, and shown as *unknown* rather than absent when the record cannot be read); the same line on the receipt.
3. **Recover a password** — the door's *"Can't remember your password? Ask Paul"* becomes an ACTION (a one-tap message to the administrator that names the username, never the password), because D1 of setup-journey rules the administrator IS the reset path; the copy stays honest about who resets.
4. **Recover a username** — a field that asks for the email and answers *"if that address is on file, it has been sent"* — never confirms existence (security-steward owed: an enumeration oracle).
5. ⭐ **This is house X / you are not at this house** (row 41) — the sign-in refusal distinguishes *wrong password* from *no account at this house*; the door names the house it is the door TO. Cited from `handoff/handoff-cross-device-signin.md` (on disk, generated at `8e93f88`): §2·1 *accounts live per deployment namespace… `pkirsch` at `fernwood-home` cannot resolve* · §2·4 *"didn't accept any of my credentials" is the wrong sentence for what happened and the product produced it: the page cannot tell wrong password from this account is not at this house.* The bug lane owns the model (§3 engineering-partner: a person-level index); this window designs only the two sentences and where they render.
**Falsifier (TIER 2 · 18's own):** a seat creates an account, signs out, returns on a clean device, recovers a forgotten password AND username, and reaches its place without asking a human except where D1 says so.

### D5 · Every "change it" link is a message, not a control (pass 1 F2) — with EVERYTHING IS CHANGEABLE beside it
`estate/index.html`'s *That's not right ›* · *Change that ›* · *Change the order ›* open one *"What should it say instead?"* box at the page bottom. The rule `[paul-stated 2026-08-04]` is that changeable must be TRUE. **Options:** (A) each link opens its own inline editor (address fields · the three radios · the tile picker — the components exist in onboarding); (B) the links are relabelled honestly — *Tell Paul it's wrong ›* — and the box appears beside the tapped item. **Recommendation:** A for the ranking (the picker exists and re-saving is already supported), B for address and contact until a self-serve edit exists (the interests proposal §8 says not to promise more than a self-serve edit can keep). Engineering-partner owed on A's write path.

### D6 · The chosen interests vanish from the app (pass 1 F3) — NEEDS-PAUL, user-researcher owed
After ranking Gardening · Wildlife · Household systems, `/viewer` renders none of them. Setup promised *"nothing is switched off or hidden because you left it out"*. **Question:** should a ranked domain render as a calm empty card with its single first ask (*"What's planted? Name one plant."*), or stay absent until it has content? **Recommendation:** render the ranked ones as empty cards — it is the standing principle (capture what personalizes) made visible, and it answers the seats' *"the Almanac composer is the primary control on an empty place"* (D) by giving the person a first thing to say. **Alternative:** keep absent, and say so on the receipt. Not exhibited until Paul answers; the build is not small.

### D7 · The front door (pass 1 F1; setup-journey D2 ruled; B2/B3 unbuilt) — ALREADY RULED, build owed
The sweep found it independently from the bare door. Not re-designed here; the bundle's candidate should carry B2 (*sign in* and *set up* on one screen, one account-creation surface) or the F1 blocker stands for every cold arrival.

### D8 · Small ruled or near-ruled items riding the same candidate (punch-list class)
TIER 1 · 28 bubble on every post-signup screen (R6) · TIER 1 · 29 colour noun (R4; content-steward's line) · TIER 1 · 30 the three formatting notes (exhibited so Paul can say what *off* means) · TIER 2 · 19 password confirmation in the voice of *These match* · TIER 2 · 20 the Almanac defined before it is named (content-steward §D) · TIER 2 · 21 the masthead bar's three unlike destinations rendered alike (sequenced with D4 naming) · TIER 2 · 25 sentence-case at the slot · the (F) repeats: *what grows there* tense · the shelf drops the house number · email never shown back (D4·2) · coordinates on no screen.

### D9 · The glance consolidation (TIER 2 · 10, the anchor) — design here, build after G6
Pass 1 F6 (one name four times; the Almanac chip scrolls nowhere; Weather and Sky three times each) is the consolidation's own finding restated by fresh eyes. The shape is fully ruled (GL-1…GL-13). ⛔ Its apply waits on **G6 + the card-face open events** (a build lane); this window designs the strip's six-cap, the second row, and the synthesis line, and names the co-requisite. The D4 stack ledger (RUN, `3,196 px` to the first card at A+) is the measurement any trim proposes from.

### D10 · Two CRITICAL copy findings from the content read (TIER 1 · 31) — carried to the register
1. **"Almanac" is minted by arithmetic on every engine surface** (`<name> + " Almanac"`) after `VOCABULARY.md:430` retired it as a portable noun `[paul-ruled 2026-09-10: "let's just call it the Journal"]` — verified on the file. It produced *"the condo Almanac"* (mom seat) and *"Home Almanac"* on a PO-box household. One substitution, five sites; Fernwood keeps its own supplied name. TIER 2 · 20's definition is therefore written for **the Journal** (review §D); the steward recommends against both offered names (*"Your input"* loses the attribution *"you told me"* carries; *"property"* is a rejected word, `VOCABULARY.md:423`).
2. **"Stays on this phone for now — nobody else sees it."** (`viewer.html:19995`) captions a composer whose button *Save & consult the Almanac* POSTs the words to `/api/chat` (`:22939–22992`). True of the note, false of the button. Capture must not lie, on the AI-boundary surface. Handover's ruling (2) and strict's F8 are the same finding from the person's side.
**Also from the read, folding into D1/D5/D8:** the narrator introduction ruled 2026-09-05 was never written (`onboarding:387–391` is a comment describing an absent sentence) — one new line resolves the I/we/Paul drift, and the existing split (first person for intent, the name for who-sees-it) stays · *"check it on the next screen"*: only *next* is false · *Change that ›* should route to `/settings/account/` (already works); *Change the order ›* is true only on the onboarding screen; the address link is honestly a note — so D5's option B applies to the address only · *"nothing is switched off or hidden"* is false on this build: the build catches up to the copy (D6), not the reverse · `＋ Add a home`: fix the glyph/label, keep the card's copy.

## 3 · Engineering finding carried, not fixed (for the register)
`POST /api/session` reports `estates: [{estateId: scope.id …}]` — the DEPLOYMENT's estate — to a person whose own estate is elsewhere (`worker/worker.js:1008`, read 2026-09-10). The sweep's account founded `est-gndlvf` (confirmed via `GET /api/grant/whoami` as the person) while its sign-in response said `est-qa0001`. Nothing on a page misreads it today; the row-41 bug lane and the M1+M2 route work should know.

## 4 · The ONE candidate — the apply list (R5), in the order the walk meets it
1. Front door B2 (D7) · 2. bubble on post-signup screens (R6) · 3. account-exists receipt at naming (D2·C) · 4. the gate card with the PO-box refusal and the optional unit line (D1) · 5. ranking screen: one filled commit (D3) · 6. receipt: email shown back; change links honest or live (D4·2, D5) · 7. shelf: one control when empty; name + town on a row (D2·A) · 8. account: sign out, recovery actions, house-named refusals (D4) · 9. the D8 punch items · 10. D6 and D9 only if ruled and built in time; otherwise named as NOT in this candidate on the release note.
Walked once by the five seats at the new sha (J0 + J2 + J3 + a sign-out/return journey the harness does not yet have — `journey-walk` needs a stop for D4, the build lane's), then Paul.

## 5 · What this file does NOT claim
No exhibit has been staged at this draft (§2 marks them EXHIBIT 1–3; paths land here when they exist). Pass 2's adjudication and punch list, and the content read, are not yet folded in. Nothing has been applied, deployed, or walked. The rulings in §1 are quoted from the register at `6f98f9d` and later; a ruling not on the register is not in force.
