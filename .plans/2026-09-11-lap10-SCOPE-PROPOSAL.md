# Lap 10 — "The place: zones v1 plus the capture write path" — the commitment, PROPOSED for Paul's beat-6 pick

- row: proposed — feeds `BACKLOG.md` § ⏭ THE NEXT TWO LAPS; the pick lands in `cycle/release/CYCLE-LOG.md`'s beat-6 table when lap 10 opens, and the committed-by-ruling rung reads it from there `[paul-ruled 2026-09-11 ~1:20 AM ET, the THEME: "The place: zones v1 plus the capture write path" — Mom's ask by name, over coordination's recommendation of INVITE & JOIN]`
- objective: O3
- class: engine · declared
- stage: draft
- seats: engineering-partner → **owed at lap 10's commit phase** (the ruled commit-phase rule: it audits the pick and writes the lap-10 build plan; nothing here is that plan) · ux-expert · content-steward · user-researcher · security-steward → declared per row below where a design pass or scoping stands between a row and READY · practice-steward → waived: this proposes a commitment in the loop's own beat-6 shape
- depends-on: .plans/2026-09-10-lap8-9-SCOPE-PROPOSAL.md
- depends-on: .plans/2026-09-07-zones-PLAN.md
- depends-on: .plans/2026-09-07-capture-write-path-PLAN.md
- depends-on: .plans/2026-09-10-zones-automation-ASSESSMENT.md
- depends-on: handoff/handoff-zones-session.md
- depends-on: .plans/2026-09-11-lap8-build-PLAN.md
- depends-on: .plans/2026-09-11-lap9-READINESS.md
- depends-note: the READINESS read landed at `80b41cd` after the first draft; row B's placement is finalised against it (its own window during lap 9, after lap 8 · A15)
- ready: agent-proposed 2026-09-11 — **Paul commits at lap 10's beat 6. This file proposes, never commits.** ✅ Reviewed by the coordination window against the loop's dependencies 2026-09-11 ~2:25 AM ET, approved with three edits (applied: row B's placement confirmed by the READINESS read, PROPOSED-confirmed and still his · row A's sequence stated plainly · Q-10·1 carries coordination's recommendation). **Held for Paul's pick at lap 10's groom; he decides whether to walk its five questions before then.**
- stage-note: 2026-09-11 ~2:05 AM ET — drafted by the backlog-refinement window (`tate-tracker-2a`) at HEAD `7c7e4ae`, in the shape of the laps 8/9 proposal. Every line marked RULED / PROPOSED / MEASURED.

> **How to read every line.** **RULED** = Paul's words place it, cited. **PROPOSED** = this window's reading, his to overrule. **MEASURED** = read from a file or a tool at the sha named. *unruled — his* means exactly that.

> ⛔ **Not a build plan** (engineering-partner writes that after the pick) · **not a ranking within the lap** · **not a third register** — every item cites the row that holds its status.

---

## 0 · THE SITE'S PHYSICAL PREMISE — a design constraint on EVERY row, stated once `[paul-stated 2026-08-31, CLAUDE.md § THE SITE'S PHYSICAL PREMISE]`

No cell reception; Wi-Fi radiates from the house and falls off with distance — *the places worth walking to are exactly the places with no network*; heavy canopy costs GPS accuracy (3–11 m under closed cover). **PERMANENT.** Consequences that bind rows A and B alike:
- **Capture must be entirely local in the field, sync deferred** until the person walks back into range. A zone confirmed or a plant placed while standing in it must land in a local outbox first.
- **Capture must not lie** — the outbox class (TIER 1 · 42's *"Saved on your phone ✓"* against a dead endpoint is the instance). A Send that has not reached the Worker says so.
- **A ±30 ft record is never presented as a locate** (zones plan §8·7, REFUSED) — canopy makes the geometry approximate by construction; the primitive is a NAMED PLACE, geometry optional.
- Every row's *done means* includes an offline stop: walked with the network off, the record intact on reconnect.

---

## 1 · LAP 10 — proposed beat-6 table

**The shape, PROPOSED:** lap 10 is **Mom's ask by name** — she has asked Paul for the zone layout repeatedly (*"I'm breaking out the fertilizer — what plants? I don't wanna miss any. What zones have what plants that need the fertilizer?"*, CLAUDE.md § THE RULE). Row A gives her the places; row B is the plumbing without which no place she names can be written to her own household. ⚠️ **Both depend on lap 8 · A (estate-as-row)** — a per-estate zone write has no meaning while a household is a deployment.

| | committed item | the row it cites | the ruling that places it | depends on, from laps 8/9, by step | what a design pass / scoping needs before READY | done means |
|---|---|---|---|---|---|---|
| **A** | **Zones v1 — zones × plants, zones defined first; Mom's Fernwood on the cleaned 23** | TIER 2 · 7 (`→ READY · .plans/2026-09-07-zones-PLAN.md`, stage `design`, stamped 09-07 *for the stage, not the build*) | **RULED the THEME** `[paul-ruled 2026-09-11 ~1:20 AM ET]` · **RULED the SUBJECT** `[paul-ruled 2026-09-11 ~1:20 AM ET: "Zones v1 will be Mom's Fernwood, and we have kind of a draft version of the zones to work with."]` · the thirteen Z-rulings and R-Z1…R-Z6 in the plan §3 and §9 (Z-10 zones first, the plant record does not travel · Z-11 the cascade · Z-13 preload-then-confirm) · **MEASURED:** the cleaned 23 is the *leading candidate, labelled and watched*; `zones.json` stays the raw trace and is **NOT replaced** (plan stage-note 09-10) | **lap 8 · A** (estate-as-row — the zone set is written under `est-<id>:`) · **lap 8 · B** (her account row at the production origin) · **row B of this lap** (the write path — same-commit co-requisite, TIER 2 · 8) · ⛔ **the PRELOAD (Z-13) into her house waits on HER founding and readiness** — a disposition, never a commitment (9·3). **Plainly: v1 is BUILT and WALKED at qa against the cleaned 23 and the frozen instance's answer key BEFORE Mom founds** (the ruled sequence — design and build proceed against the draft), **and reaches HER house only through row C's disposition after lap 8 · B. A slip in lap 8 · B delays C, not A.** | the zones window is CLOSED (`handoff/handoff-zones-session.md`); its **§5 open items are still Paul's**: R-A1…R-A6 (the frame rule · the parcel off qPublic · the 175 m² building at the driveway branch · anchors written at founding as `inferred` · whether R-A4 is feature work under G1) · **the answer key is ambiguous** (plan §9a·1: R-Z4 says *the twenty-three*, the frozen instance serves **18** — three copies of the set exist and disagree; say which, every time) · a copy contradiction in the plan §0·② (is the completeness gap normal or a defect) · **content-steward DRAFTING owed** (the confirm/correct prompt, the place-naming ask, the "everything is changeable" clause — declared owed in the plan header) · seats otherwise trailed (user-researcher · engineering-partner · ai-advisor · ux-expert) · **the stage gate to build is a sha on QA** | the plan's own v1 (§6): a household's named places exist as records under its own estate; a plant can be placed in a zone and the zone lists its plants; **her fertilizer question is answerable from the app** — *which zones have plants that want fertilizer this month* (Q-10·2 asks whether that is THE acceptance walk); the Z-13 preload at her house shows the cleaned 23 with her sixteen names, each confirm/correct-able; offline stop per §0; ⛔ no ±30 ft geometry presented as a locate |
| **B** | **The per-estate capture write path — LEG 0** — move `handleZoneSave`'s 503 gate below the KV write; gate the read (R-Z6·B) in the same commit; prove the five capture handlers per-estate | TIER 2 · 8 (`→ READY · .plans/2026-09-07-capture-write-path-PLAN.md`, stage `concept`, stamped 09-07) | **RULED the THEME** (with A) · **MEASURED on the row:** five capture handlers touch no git and work at `home`; **exactly one create is broken** — `handleZoneSave`'s 503 sits at the top, blocking a KV write ~80 lines below; **R-Z6(B) is a same-commit co-requisite** — without gating the read, a first map load at an empty house serves Fernwood's zones (*"The bank"*) · two pre-registered refutation checks **unrun**: POST the five endpoints against `home`; `GET /api/zones` against `home` | **lap 8 · A** (the write lands under the household's own prefix, or it is Fernwood's write path with a different label) · **its DESIGN PASS — see the placement question below** | ⭐ **THE DESIGN PASS: PROPOSED for LAP 9, in its own window, beside the weather card** — not lap 8 (lap 8 is credential-path work and its own rule is *behind them, not across them*; a second Worker write-path change in the same lap doubles the credential lap's surface) and not lap 10 (a design pass inside the build lap is the *"concept → build with no design"* shape the ladder forbids). The lap-8 build plan §6 already places it *"a lap-9 candidate"* (MEASURED, `.plans/2026-09-11-lap8-build-PLAN.md:856`); ✅ **engineering-partner's READINESS read agrees and sharpens it** (`.plans/2026-09-11-lap9-READINESS.md` §6–7 at `80b41cd`): **its own window DURING LAP 9, after lap 8 · A15** — lap 8 too early, lap 10 too late; §6 names THE PLACE seats and carries the site premise; §7 puts INVITE & JOIN's scoping → lap 11. Seats for the pass: engineering-partner (the gate move and the read gate, per handler) · security-steward (a per-estate write is a per-estate isolation claim — the within-estate cross-person gap, OPEN-ITEMS ⑤·1, is adjacent) · ux-expert waived (no surface) · content-steward waived (no copy) | the two unrun refutation checks run and pass at qa: the five capture POSTs land under the caller's estate prefix; `GET /api/zones` at an empty household returns **empty, never Fernwood's**; `handleZoneSave` writes KV at a household with no `GITHUB_TOKEN`; the falsifier-tenancy set (C1–C5) still green; offline stop per §0 |
| **C** | **Zones preload (Z-13) into Mom's house** | TIER 2 · 7 (Z-13) | **RULED gated** — *not until Mom has created her Fernwood and is ready*; **RULED a disposition, never a commitment** (9·3) | her act, after lap 8 · B | none — ⛔ **listed only so the gate is visible; NEVER pickable at beat 6**. It becomes a beat-2 disposition the day she founds | her Fernwood opens with the cleaned 23 preloaded and her sixteen names on them, each confirm/correct-able (preload-then-confirm, Z-13) |

**Riders, PROPOSED:** the four register edits the closed zones window still owes to TIER 2 · 7 and the derived-first-draft plan (its §7: the *"awaiting stamp"* and *"ten rulings"* sentences are stale; `LAND-SOURCES.md` promotes footprints and OSM driveways to VERIFIED; the EXPERIMENT §8 step 1 becomes *fetch the anchors*) — register hygiene this window applies on its own authority before lap 10 opens, no candidate touched.

### Excluded from lap 10, each with its ruling or reason

| out | why |
|---|---|
| **§ INVITE & JOIN — the build** | **RULED** off lap 9's critical path; its five-seat scoping runs in lap 8 (9·4); coordination proposed it for lap 10 and **Paul chose the place instead** → **a lap-11 candidate with its scoping standing** |
| **§ ADDRESS VALIDATION** | security-steward's roster ruling first, then a scoping — two steps before any build |
| **Personalization** (§ THE STANDING PRINCIPLE — describe-or-infer) | the open question is his and no plan exists; the ask-design window (§ A-ASK) is where its ask half is being designed |
| **C8 · the condo build-out with his images** | *"at his pace"*, instance content |
| **Process B — the semi-automated zone definition pipeline** (TIER 2 · 9, `derived-first-draft`, stage `concept`) | the assessment's own sequence puts it on the OPERATOR track (fetch the anchors; propose only what no download covers); it feeds v1's draft and is not v1. Two things already ruled OUT: the CHM without a PDAL install, any ML segmentation |
| **Lines and points · plant instances (W6) · the illustrated map · the place field on the other nine domains** | the plan §8's named deferrals — a decision, not an omission; do not re-open without him |
| **An acknowledgment surface for the zone work · a householder drawing tool as the primary path · a survey register · anything presenting a ±30 ft record as a locate** | plan §8·7 — **REFUSED**, not deferred |

### Questions Paul answers to commit lap 10

- **Q-10·1 — Whose zones are v1's FIRST REAL SUBJECT?** ✅ **RULED: Mom's Fernwood, on the cleaned 23** (*"we have kind of a draft version of the zones to work with"*). What follows and is still his: the C7 *"no garden"* falsifier — is the condo walked as v1's **negative control** (a household with no zones renders no zone affordance and no Fernwood zone), even though it is not the subject? **Coordination's recommendation, marked as such: YES** — a seat's walk, not a design; the check is `check-estate-neutral.py --url` on the condo's app after v1 lands (a no-zones household renders no zone affordance and none of Fernwood's zones). His word makes it a ruling.
- **Q-10·2 — Is Mom's fertilizer question THE acceptance walk?** *"What zones have what plants that need the fertilizer?"* is the one thing she has asked for by name. **Recommendation: yes** — a seat, at qa, on the cleaned 23 with the frozen instance's plant↔place answer key, asks it and gets a list; if v1 cannot answer it, v1 is not done for her. ⚠️ Z-10 says the plant record does not travel — the walk uses the answer key, never migrates it. **Paul's first read, 2026-09-11 ~3:20 AM ET, verbatim:** *"I don't really understand question ten to the acceptance walk. With fertilizer, I don't think so."* — read as a lean to **NO**, with the question re-put in plain words (an *acceptance walk* = the one scripted walk a seat does at qa that decides whether v1 is done); **not yet ruled** — what decides v1 is done, if not her question, is the plan's own v1: a named place exists under her household, a plant can be placed in it, the zone lists its plants.
- **Q-10·3 — The write-path design pass: lap 9, in its own window?** Recommendation above (row B), now **corroborated by the READINESS read** (its own window during lap 9, after lap 8 · A15). The lap-8 alternative is argued against by that read; **his word at lap 9's open makes it a ruling.**
- **Q-10·4 — Which zone set is the answer key?** R-Z4 says the twenty-three; the frozen instance serves 18; three copies disagree. **Recommendation: the cleaned 23 is the key for v1's build and the served 18 is a separate, unasked act** (the plan's own words) — say which, every time. ✅ **RULED** `[paul-ruled 2026-09-11 ~3:20 AM ET, verbatim: "The clean twenty three is the right zone count and version to go with."]` — the cleaned 23 is both the count and the version; the served 18 stays a separate, unasked act.
- **Q-10·5 — The zones window's R-A1…R-A6** (the frame rule · the parcel · the 175 m² building · anchors as `inferred` at founding · R-A4 under G1): ten minutes of facts only he holds. Rule them before lap 10's build plan is written, or the plan carries them as unknowns.

---

## 2 · What lap 10 depends on from laps 8 and 9, by step — so a slip is visible here

| earlier step | if it slips | what moves here |
|---|---|---|
| **lap 8 · A** estate-as-row (`scopeFor` at every call site; the lab falsifier) | no per-estate prefix for a zone write | rows A and B cannot start; lap 10 slips a lap |
| **lap 8 · B** her account row at the production origin | her house does not exist to preload into | row C's gate cannot open; A and B still build and prove at qa |
| **lap 9 · the write-path DESIGN PASS** (proposed placement) | B enters lap 10 at `concept` | the *"concept → build with no design"* shape; B slips or the pass runs at lap 10's open and delays the build |
| **lap 9 · A** the weather card | none on zones directly | none — but the ask-design window's playbook (the weather card's intro ask) is the template the place-naming ask should follow (Z-11's cascade of asks); if the playbook slips, v1's asks are authored without it |

---

## 3 · Falsifier

If Paul's beat-6 pick for lap 10 contains an item in neither §1's table nor its exclusions, this proposal's derivation was wrong — fix the ⏭ rung or the register, never widen this table. If any line marked RULED cannot be traced to his words at the sha it cites, strike it; do not soften it to PROPOSED. And if the lap-9 READINESS read places the write-path design pass elsewhere, its reason replaces row B's placement line — the line says so on its face.

## 4 · QA

`python3 tools/check-backlog-ready.py --ladder` (zones at `design`, capture write path at `concept`, both stamped) · `grep -n '^## Lap 10' cycle/release/CYCLE-LOG.md` (the commitment, once he makes it) · `python3 tools/qa-divergence.py --check` and `python3 tools/check-estate-neutral.py --url <qa>` after any zone write at qa (a household naming another household's zones is the leak this lap can create).
