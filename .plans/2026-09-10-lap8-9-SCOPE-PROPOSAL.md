# Laps 8 and 9 — the commitment, PROPOSED for Paul's beat-6 pick

- row: proposed — `BACKLOG.md` § ⏭ THE NEXT TWO LAPS is the derived queue this file feeds; the pick lands in `cycle/release/CYCLE-LOG.md`'s beat-6 table for each lap, and the committed-by-ruling rung reads it from there `[paul-commissioned 2026-09-10 ~11:35 PM ET: "I'd like to go ahead and start, at the next convenient moment, setting the scope for laps eight and nine — the commitment."]`
- objective: O3
- class: engine · declared
- stage: draft
- seats: practice-steward → waived: this proposes a commitment table, the loop's own beat-6 shape, and changes no process · engineering-partner → **owed at each lap's commit phase** (the ruled commit-phase rule: it audits the pick and writes `.plans/<date>-lap<N>-build-PLAN.md`; nothing here is that plan) · ux-expert · content-steward · user-researcher · security-steward → **declared per row below**, where a scoping is what stands between a row and READY
- depends-on: cycle/release/CYCLE-LOG.md
- depends-on: .plans/2026-09-10-lap7-build-PLAN.md
- depends-on: .plans/2026-09-10-multi-tenancy-PLAN.md
- depends-on: VOCABULARY.md
- depends-on: .plans/2026-09-10-teardown-REPORT.md
- depends-note: the chronicle's lap 7 · Beat 6 and its AMENDED rulings (`ef03acc`); `VOCABULARY.md` §3i
- ready: agent-proposed 2026-09-10 — **Paul commits at each lap's beat 6. This file proposes, never commits.** ✅ **EVERY QUESTION RULED** `[paul-ruled 2026-09-11 ~12:10 AM ET, walked in the coordination window: "Let's go on laps eight and nine… let's walk through it."]` — the commitment lives in `cycle/release/CYCLE-LOG.md` § *Laps 8 and 9 — SCOPE COMMITTED BY RULING* (`767242c`) and is transcribed into each lap's beat-6 table at its open; the committed-by-ruling rung reads it from there. The rulings are carried onto each question below, cited never restated.
- stage-note: 2026-09-11 ~12:20 AM ET — status lines below marked RULED from `767242c` by the refinement window; nothing here opens lap 8 (lap 7 is the open lap).
- stage-note: 2026-09-11 ~2:20 AM ET — **the lap-8 BUILD PLAN exists** (`.plans/2026-09-11-lap8-build-PLAN.md`, draft, `80b41cd`, engineering-partner): its **seven recommended commitment changes** (the ⬆️ CHANGE sections — A2 the multi-tenancy plan stale in three of four changes · A3 "the 60 call sites" is a moving number · A6 the harness cannot walk one person on two devices · …) **and its questions Q0–Q8 are Paul's AT LAP 8's OPEN**, not tonight — this file points at them and does not restate them. Also landed: `.plans/2026-09-11-lap9-READINESS.md` (the write-path design pass = its own window during lap 9, after lap 8 · A15; INVITE & JOIN scoping → lap 11) and the lap-10 draft.
- stage-note: 2026-09-10 ~11:50 PM ET — drafted by the backlog-refinement window (`tate-tracker-2a`) at HEAD `cea1ce1` while the lap-7 build window builds; reviewed by the coordination window against the release loop's dependencies before it reaches Paul. Sources: `check-backlog-ready.py --ladder` (the rungs) · the register's rows by number · the chronicle. `product-steward.py` T1's 18 uncarried ruling lines were read and **not swept in** — all predate lap 6 and none names a lap-8/9 item.

> **How to read every line.** **RULED** = Paul's words place it, cited. **PROPOSED** = this window's reading, his to overrule. **MEASURED** = read from a file or a tool at the sha named. A row that says *unruled — his* has no ruling and is not being smuggled in as one.

> ⛔ **What this is not.** Not a build plan (engineering-partner writes that after the pick). Not a ranking within a lap (beat 6 is his). Not a third register — every item cites the row that already holds its status.

---

## 1 · LAP 8 — proposed beat-6 table

**The shape, PROPOSED:** lap 8 is **the door and what the door makes possible**. Its spine is one already-ruled item (A) that everything else in the lap either depends on or rides beside. ⚠️ The multi-tenancy plan's own timing warning applies to the whole lap: *"Step 1 touches the credential path — the single thing that must not fail while two first-time users are walking through the door… do step 1 behind them, not across them."* Who is walking during lap 8 is question Q-8·1 below.

| | committed item | the row it cites | the ruling that places it | depends on, from lap 7 | what a scoping needs before READY | done means |
|---|---|---|---|---|---|---|
| **A** | **The single-origin sign-in door** — one sign-in page for the account, routing to every house held; production becomes ONE origin, estate-as-row, `scopeFor(request)` at every call site | TIER 1 · 41 / 46 | **RULED** `[paul-ruled 2026-09-10: "a single sign-in page that redirects to everywhere it needs to go, not individual sign-in pages"]` · **RULED LAP 8** (lap 7 · beat 6 exclusions; TIER 1 · 46 "SEQUENCED") · `VOCABULARY.md` §3i step 3 | **B1** (`/api/session` reports the person's estate, not the deployment's — A3 depends on it) · **B2** (the revoke at the right scope) · **B3** (`handleSession` through `putAccount`) — all three are lap 7 Worker steps and all three are preconditions the FINDINGS §5 names *before the M2 backfill* | `.plans/2026-09-10-multi-tenancy-PLAN.md` **has no header** (MEASURED: `--ladder` reads it `absent`) — it needs `row:` (TIER 1 · 46) · `stage:` · `seats:` engineering-partner (the 60 call sites, batches) · **security-steward, named explicitly** (the door is a credential path; the existence oracle at `GET /api/account/available` that TIER 1 · 46 carries already publishes username existence unauthenticated, so the sign-in's byte-identical 404 guards a secret a sibling route gives away — the seat rules what the door may reveal before it is built; and no redirect door that names which house a username lives at — FINDINGS §5 constraint) · ux-expert (the shelf after sign-in, `/homes/` picker) · content-steward (every word on a door read by someone who thinks they are locked out — the sign-in-door proposal's own clause) · user-researcher (the uncovered walk shape *multi-household person, cold device*, TIER 1 · 46 P-list) · Paul's stamp | the multi-tenancy plan's falsifier, verbatim: *"Two accounts on one deployment, each having created their own estate, where every read one makes for the other's estateId returns 404 — and a grant presented for estate A cannot name estate B by any route"* — proven at **lab** first (sequence step 2), then one sign-in at the production origin reaches every house `whoami` lists; sequence steps 1–4 done, **step 5 is row B**. **And Paul's "synced" ruling as acceptance clauses, each one a seat can walk** `[paul-ruled 2026-09-10 ~10:05 PM ET; CYCLE-LOG lap 7 · Beat 6, "synced — RULED"]`: **(s1)** a journal entry, a feedback note or a Guru query written on one device is readable on another by the same person · **(s2)** it is scoped to the household and the account, never the browser · **(s3)** the owner sees a member's contribution · **(s4)** the administrator sees it from any of his devices. **That is the destination the door exists for** — the falsifier is not "can sign in" but "what I wrote is here when I come back on another device" |
| **B** | **`fernwood-home`'s one account row MIGRATES into `myhome-paul`, and the `home` deployment retires** — a MIGRATION with a verified copy, never a delete | TIER 1 · 46 (Q7) · 41 · `VOCABULARY.md` §3i step 3–4 | **RULED** `[paul-ruled 2026-09-10 ~10:58 PM ET, Q7: "yes to all" — `myhome-paul` becomes THE production origin; `ef03acc`]` | **A** (there is one origin to migrate into) · lap 7 **D** (the production origin's app has its Worker) | `household-import.py` is **unwritten** (MEASURED: `.plans/2026-09-10-OPEN-ITEMS.md` ④·4 — *"Export is fixed. Nothing in Phase C runs until both exist"*) · seats: engineering-partner (the copy and its verification, the tombstone) · **security-steward, named explicitly** (a real person's credential row moves between namespaces — `marguerite`'s; the seat rules the copy's handling, the proof-before-delete, and what the retired deployment may still answer) · ⛔ **Paul's call at the act**: the multi-tenancy plan's *"Step 5 is the only irreversible step and it is Paul's call, not a consequence of 1–4"* | `marguerite`'s account row present at the production origin with a verified byte-for-byte copy in `.private/` before the source is touched; `home` retired with a `wrangler.toml` tombstone and `est-e6696a` **kept** (it is her estate id, not a fixture's); `watch-accounts.py` reads her at the production origin and nowhere else; ⛔ **the release note promises nothing about `home`** (Q7) |
| **C** | **The email editor** — the edit affordance behind lap 7's display-only *How to reach you* | TIER 2 · 18 (Q1) | **RULED** `[paul-ruled 2026-09-10 ~10:58 PM ET, Q1: display-only this lap; the editor is lap 8; named "not in this build" on lap 7's note]` | lap 7 **B10** (the three-state shown-back value) · **B6** (recovery reads the email — editing the recovery address is a takeover vector, so the editor's write path must not be weaker than sign-in) | small; seats: content-steward (the editor's copy) · **security-steward** (a change to the recovery address confirmed how? — the one design question) · ux-expert waived unless the closure's row 19 shape does not hold | the lifecycle journey (`journey_lifecycle`, lap 7 H3) gains an editor stop and passes; a changed address is what `POST /api/recover` answers against |
| **D** | **D9 · the glance consolidation — a DESIGN pass, not a build** — jump strip · tile row · cards, three layers repeating one thing | TIER 2 · 10 (GL-1…GL-13) | **RULED shape** `[paul-ruled 2026-09-07, GL-1…13]` · **RULED sequence**: *"design after G6 lands, not this candidate"* (lap 7 · beat 6 exclusions; closure row 43) | lap 7 **C** — G6's events **and a reading window** with `read-glance-order.py` output (C6/C7); a design pass with no reading is the thing the exclusion forbids | seats: ux-expert (the design) · user-researcher (what the reading says a person actually opens) · content-steward (the strip's words, TIER 2 · 20's *Almanac* question owns whether the word may be dropped) | a design proposal citing G6 evidence by sha, with the build as a lap-9 candidate at the earliest. ⛔ **CONDITIONAL, with a slip rule written here so a slip is a rule and not a failure:** G6 ships at lap 7's production deploy and needs days of real sessions; **if `read-glance-order.py` reads fewer than N sessions across the real households at lap 8's beat 6 (N proposed = 10, his to set), row D moves to lap 9 by rule** and the pass does not design from zero |
| **E** | **The Midtown scratch instance retired** — repoint `check-condo-falsifier.py` and `place-claims.py` to `instance/paul.json` | TIER 1 · 49 (teardown report §2) | **RULED the instance goes** `[paul-ruled 2026-09-10, § C7: "and the scratch instance goes with it"]`; the repoint is the teardown lane's MEASURED shape of *how* | nothing | small; engineering-partner in the build plan; no other seat | both tools green against `instance/paul.json`; no file under `instance/` names Midtown; `check-estate-neutral.py` reads the removal as a fix |
| **F** | **The fixture stamp reaches J0 seats and founding carries it forward** — so every house a synthetic founds from here is tool-deletable | TIER 1 · 48 (teardown report §5) | **PROPOSED** — unruled; his. The rule it serves is his (*a tool that deletes should be safe before it is useful*; *delete only what is PROVABLY a fixture*) | nothing from lap 7; a Worker change on `qa`/`lab` only | seats: engineering-partner · **security-steward** (a deployment-decided stamp that production never sets — the var must not exist at the production origin, and the report's own proposal says so) | `household-fixtures.py --teardown` at qa deletes a house founded by a seat **after** the change with zero REFUSED rows, and the same run at the production origin finds zero stamped rows by construction |
| **G** | **A per-instance seam for the acknowledgment ribbon** — `MOM_ACK_DATA` stops being a literal in the engine template and reads from the instance, so a household is acknowledged with its own lines or with none | TIER 1 · 50 (the L4 step's finding, `60e439d`; MEASURED at `cea1ce1`: `instance/paul.json` lacks `ack` + `questions` in its absent list; the literal sits at `engine/viewer.template.html:12022`) | **PROPOSED** — unruled; the ribbon's own contract is his (`[paul-stated 2026-08-04]`: attribution for feedback SHE gave, refreshed on her events, never ours) and **cannot be honoured at any household but Fernwood without the seam** | lap 7's **P-step** (the absent list gains `ack` + `questions`, so the condo stops rendering Mom's ribbon at all — containment first); lap 7 **D** (a household has a Worker to be acknowledged through) | small; seats: engineering-partner (the `build-viewer.py` substitution, the same contract C4 5b ratified for identity) · content-steward (what an empty ribbon says, and whether it renders at all — the empty *Your Perspective* card of TIER 1 · 43 is the same question one card down) · ⛔ **never** write Paul's line into the literal — that inverts the leak | `check-estate-neutral.py`'s gap named on TIER 1 · 50 has a reader: a build at any non-Fernwood instance renders **no** Fernwood ribbon text (a grep of the built page for `MOM_ACK_DATA`'s Fernwood phrases returns nothing), and Fernwood's own build is byte-identical to before |

**Riders, PROPOSED, each small and each an instrumentation debt the ruled rule names** (*an event with no reader is not instrumentation*): TIER 1 · **37** (`/api/onboarding-metrics` has no GET, so the position-vs-preference series has no reader) · TIER 1 · **36** (stamp every ranking with the wording it was captured under — lap 7 ships without it, so every lap-7 ranking is under today's labels by omission). Both are readers or stamps, not surfaces; neither moves the candidate's design. **Third rider, RULED** `[paul-ruled 2026-09-11 ~1:50 AM ET: "I say go on both"]`: **the ASK LEDGER reader, `tools/ask-ledger.py`** — every ask the product has made · served · answered as counts · folded into what · which feature row it links to; exit 3 UNREADABLE. Its SPEC comes from the ask-design window (`handoff/handoff-ask-design.md` → `.plans/2026-09-11-ask-design-PLAN.md` §9, which wins over this rider where they disagree); a reader, no surface (§ A-ASK). **Spec, stage-note 2026-09-11 ~3:40 AM ET:** reports **per-ask per-ENV only, never per-estate at any size** (`legacy`/`home`/`paul` are n=1 — a per-estate count is a person's answer); **served = DISTINCT asks, never exposures**.

### Excluded from lap 8, each with its ruling or its reason

| out | why |
|---|---|
| **§ INVITE & JOIN — the build** | **RULED** *"not this round… just owners setting up houses"* for the QA lap; **PROPOSED**: its **scoping session runs IN lap 8** (five declared seats: user-researcher · engineering-partner · security-steward · ux-expert · content-steward) so a build is possible in lap 9 — a scoping is not a build and does not touch the candidate. ⚠️ **It also decides Bob (lap 9 · B)** |
| **Zones preload (Z-13)** | **RULED** gated on Mom founding her own Fernwood (TIER 2 · 7; lap 7 exclusions) — a person's act, not a lap's |
| **§ ADDRESS VALIDATION** | **RULED** *not this lap* on its own row; **security-steward roster mode rules first** whether an address may leave the estate at all |
| **The product name** | a RULING, not a build (`.plans/2026-09-03-product-name-PLAN.md`, `ready`, unstamped); ⚠️ see Q-8·4 — row B writes a name into Mom's link |
| **The weather card from an address** (TIER 2 · 11) | **RULED a priority** `[paul-ruled 2026-09-07: "the top priority of what we need to get ready to implement in the next lap"]` and **never placed in a lap since** — it has competed with the founding bundle twice. ⛔ **Not excluded by a ruling; excluded here by capacity, and that is his call** — Q-8·3 |
| **The capture write path** (TIER 2 · 8) | stamped at `concept`; a design pass is owed before any build (the ⏭ rung) — a lap-9 candidate |
| **G3 (a close signal)** | left out of lap 7 *unless Paul wants it* (build plan §2 A2); still unasked |

### Questions Paul answers to commit lap 8 — ✅ ALL RULED `[paul-ruled 2026-09-11 ~12:10 AM ET; CYCLE-LOG `767242c`]`

| q | RULED |
|---|---|
| 8·1 | **Mom waits until B is proven** — nobody real walks the door while A's steps land at lab; she founds once, at the production origin |
| 8·2 | **B after A's isolation falsifier passes at lab** |
| 8·3 | **The weather card = lap 9, first row** |
| 8·4 | **Rule the product name BEFORE B** — `.plans/2026-09-03-product-name-PLAN.md` is now a lap-8 prerequisite, flagged AWAITING on its header. ✅ **The ADDRESS half RULED 2026-09-11 ~12:40 AM ET** `[paul-ruled: "we got the domain myhome.place, right? We should use that."]`: **the production origin is `myhome.place`** — the link Mom receives; B gains a build step (bind the Pages project to the apex, write its DNS — zero records today — and add it to `pages-deploy.py`'s `ORIGIN` map). ✅ **The NAME RULED 2026-09-11 ~3:05 AM ET: "My Home Place", Home emphasized (My *Home* Place), said "my home dot place"** — 8·4 is fully met; B gains the name strings (two doors' title/h1, the shelf fallback, the top-bar rule, the apex page) as build steps beside the apex binding |
| 8·5 | **One candidate A + C + E + F; B its own gate on his word at the act** |
| 8·6 | **The fixture stamp = lap 8 rider** (qa/lab only; production never sets the var) |
| D | **threshold = 10 sessions**; row G the ribbon seam **accepted by inclusion** |

*(The questions as asked, kept for the record:)*

- **Q-8·1 — Who is walking while the credential path moves?** The plan's timing rule is *behind them, not across them*. If Mom is to found her Fernwood during lap 8 (she has an account, no house), the door work and her founding collide. **Recommendation:** her founding waits for row B's migration to be proven, and she founds at the production origin once; nobody real walks the door while A's steps 1–3 land at lab.
- **Q-8·2 — Order of A and B.** Migrate `home` before or after A's falsifier passes at lab? **Recommendation: after** — a migration into an origin whose isolation is unproven is the one irreversible act done on inference.
- **Q-8·3 — The weather card (TIER 2 · 11).** His 09-07 top priority has now missed two laps. In lap 8 beside the door, or lap 9 first? **Recommendation: lap 9, first row** — lap 8 is credential work and should stay small; the card is an engine feature every household sees and deserves its own candidate.
- **Q-8·4 — The production origin's name before Mom's link moves.** Row B puts `myhome-paul` into the link she will use. Rule the product name first (the plan is `ready`, unstamped), or accept the label and rename on its own clock as §3i says? **Recommendation:** rule the name before B; a link she receives is the one thing that should not be renamed under her.
- **Q-8·5 — Is lap 8 one candidate again?** Lap 7 was ruled *one candidate* for a design bundle. Lap 8's spine is Worker work with a lab falsifier and one irreversible act. **Recommendation:** one candidate for A + C + E + F, with **B as its own gate** on his word at the act.
- **Q-8·6 — Rider F (the fixture stamp):** in lap 8 as a rider, or its own row later? Unruled.

---

## 2 · LAP 9 — proposed beat-6 table

**The shape, PROPOSED:** lap 9 is **the second person and the first feature** — what the door makes possible (a second person in a house) and the engine feature that has waited longest. ⛔ **Nothing in this table is ruled into lap 9.** Every row is a candidate with its dependency stated; the pick is his at that lap's beat 6, and a lap-8 outcome can move any of them.

| | candidate item | the row it cites | the ruling that already places it | depends on | what a scoping needs before READY | done means |
|---|---|---|---|---|---|---|
| **A** | **The weather card from an address** — base card from the address, two opt-ins (radar · the household's own station) | TIER 2 · 11 | **RULED a priority** `[paul-ruled 2026-09-07]`; **unplaced in a lap — his** (Q-8·3) | lap 8 **A** (one origin; every household's card reads one address model) · TIER 2 · 12's canon store seam for the station opt-in | its plan exists → the row's pointer; seats per its header; a `design` pass if the opt-in copy is not yet content-steward's. **PROPOSED reorder** (ask-design's lead seat, 2026-09-11): **the visible receipt — the card reading the answer — is built FIRST, the card-intro ask second**; no ask on this product has yet been shown to change what the answerer sees. His to confirm at lap 9's open | every household at the production origin renders a weather card from its own address with the honest *REGION* labelling; the station opt-in only where a station is declared |
| **B** | **§ INVITE & JOIN — the build** — an owner invites an existing account by username; the invitee sees the notification naming the house and the inviter; accepts; comes out a member with the conferred role; **no new `est-` id minted** | § 🤝 INVITE & JOIN · TIER 1 · 19 (`conferred*` never read by founding) | **unruled — his**; the scoping (lap 8) precedes it; his three roles (read-only · member read-and-write · owner) are his answer to *what "role type" means*, recorded verbatim on the section | lap 8 **A** (the grant edge `grant:<personId>:<estateId>`, `route: → {personId}` — M1+M2, *conforming to a ruling already made*) · the lap-8 scoping session's PLAN with five seat trails | the scoping itself: what the pending invitation is as a record and where it lives · role type against the two ratified axes (relationship SET · capability SINGLE) · whether an owner may confer `administrator` · what a username lookup may reveal (the enumeration oracle, security-steward) · **and the one instrument gap it makes structural**: nothing can see within-estate, cross-person (OPEN-ITEMS ⑤·1) — a falsifier for that must exist before two people share a house | the section's falsifier verbatim: *"a synthetic owner at qa invites a second synthetic by username; the second signs in, sees the notification naming the house and the inviter, accepts, and `whoami` lists that estate with the conferred role — with no new `est-` id minted"* |
| **C** | **Bob — the first real second person** | § INVITE & JOIN · TIER 1 · 19 · teardown report §1 (`bob` DESTROYED, invite died with it) | **unruled — his.** ⚠️ Two different journeys hide under "Bob": **J0** (Bob signs up at the production origin's open door and founds his own house — needs no invite and no row B) or **J1** (Bob is invited into an existing house — needs row B). *"Bob's two houses"* reads as J0 twice, or J0 + J1; **which is his to say** | ✅ **RULED J0 × 2** (`767242c`): lap 8 **A** (estate-as-row) **plus a founding-surface step for the second house — the *add another place* path, TIER 1 · 19, carried here as C's dependency**; no invite, no re-mint | none beyond the journey he names; a gate-kit walk (TIER 1 · 25) for a real person is *his* walk, not a seat's | Bob reaches his house(s) from his own device; every read he makes for another estateId is 404 (the multi-tenancy falsifier, now with a real person in it) |
| **D** | **The capture write path** — LEG 0, the co-requisite of zones | TIER 2 · 8 | stamped at `concept` `[paul-approved]`; **the ⏭ rung says a design pass is owed first**; unplaced | a design pass (lap 8 or beside it — its own window, per the ⏭ note that zones' design work is not a lap slot) | the design pass with its declared seats | `handleZoneSave`'s KV write lands at a household with no `GITHUB_TOKEN` (the one broken create, MEASURED on the row) and every capture handler is proven per-estate |
| **E** | **D9 — the glance consolidation, the BUILD** | TIER 2 · 10 | ruled shape (GL-1…13); the build waits on lap 8 · D's design | lap 8 **D** (the design with G6 evidence) | none beyond D | the strip and tile row stop repeating the cards; the ranked order the G6 reader shows is what renders |
| **F** | **Zones preload (Z-13)** | TIER 2 · 7 | **RULED gated**: *not until Mom has created her Fernwood and is ready* | **Mom's act**, after lap 8 · B; ⛔ **a person's act cannot be committed to a lap** — it is listed so the gate is visible, not so it can be picked | its plan is at `design`, stamped; the stage gate to build is a sha on QA | her Fernwood, founded by her, opens with the cleaned 23 zones preloaded and her sixteen names on them |

### Excluded from lap 9 as things stand

| out | why |
|---|---|
| **§ ADDRESS VALIDATION** | the security-steward roster ruling comes first, then a scoping — two steps before a build; a lap-10 candidate at the earliest, **PROPOSED** |
| **Houseplants** (§ 🪴 HOUSEPLANTS) | **RULED a research item, not a build** `[paul-stated 2026-09-10 ~11:00 PM ET]`; the research runs beside laps, its output is a row with a plan pointer |
| **C8 · the condo build-out with his images** | *"at his pace"* on its own row; instance content, not engine |
| **The invite-flow surface for Mom to invite Paul** (multi-tenancy plan § *How Paul gets in*) | this IS lap 9 · B seen from Mom's side — one build, not two; listed so it is not minted twice |

### Questions Paul answers to commit lap 9 — ✅ ALL RULED `[paul-ruled 2026-09-11 ~12:10 AM ET; CYCLE-LOG `767242c`]`

| q | RULED |
|---|---|
| 9·1 | **Bob founds his own, TWICE (J0 × 2)** — no invite; INVITE & JOIN is **off lap 9's critical path**. ⚠️ **Row C gains a dependency:** a second house needs the *add another place* path (TIER 1 · 19) — lap 8's estate-as-row **plus a founding-surface step** |
| 9·2 | **The weather card leads lap 9** (follows 8·3) |
| 9·3 | **Mom's founding is a disposition when it happens, never a commitment** — the loop rests; her input fires it |
| 9·4 | **INVITE & JOIN's five-seat scoping convenes IN lap 8**, so a lap-10 build is possible |

*(The questions as asked, kept for the record:)*

- **Q-9·1 — Bob: J0 or J1, and how many houses?** See row C. The answer decides whether row B is on lap 9's critical path or can slip.
- **Q-9·2 — Does the weather card lead lap 9?** If Q-8·3 puts it in lap 8, lap 9 opens with INVITE & JOIN instead.
- **Q-9·3 — Is Mom's founding a lap event?** Row F and the whole zones thread hang on her act. **Recommendation:** treat it as a beat-2 disposition when it happens, never a beat-6 commitment — the loop rests; her input fires it.
- **Q-9·4 — The INVITE & JOIN scoping's five seats — convene them in lap 8?** Without that, row B cannot be READY for lap 9 and this table has no second-person row.

---

## 3 · What lap 8 and lap 9 depend on from lap 7, by row — so a lap-7 slip is visible here

| lap 7 row | if it slips | what moves here |
|---|---|---|
| **B1** `/api/session` reports the person's estate | lap 8 · A cannot start step 3 (the call sites) honestly — A3's landing branch would route on the deployment's estate | lap 8 · A slips a lap |
| **B2 / B3** revoke scope · `putAccount` | the FINDINGS §5 preconditions *before the M2 backfill* are unmet | lap 8 · A step 1 is unsafe |
| **B10** email shown back | nothing to put an editor behind | lap 8 · C slips |
| **C** G6 events + `read-glance-order.py` | no reading window | lap 8 · D cannot design from evidence; lap 9 · E slips two laps |
| **D** the Worker map | the production origin's app has no backend | lap 8 · B migrates a person into a house with no Worker — **must not happen** |
| **H3** `journey_lifecycle` | no journey to add the editor stop to | lap 8 · C has no falsifier |

---

## 4 · Falsifier

If Paul's beat-6 pick for lap 8 contains an item that appears in neither §1's table nor its exclusions, this proposal's derivation was wrong — the ⏭ rung or the register is missing a row, and the fix is there, not in a wider table here. The same for lap 9. And if any line marked RULED cannot be traced to his words at the sha it cites, strike the line; do not soften it to PROPOSED.

## 5 · QA

`python3 tools/check-backlog-ready.py --ladder` (the rungs this reads from) · `grep -n '^## Lap 8' cycle/release/CYCLE-LOG.md` (the commitment, once he makes it — the only place it is written) · `python3 tools/product-steward.py` T1 (any ruling this file should carry and does not).
