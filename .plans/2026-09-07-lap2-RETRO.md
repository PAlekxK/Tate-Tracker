# LAP 2 — RETROSPECTIVE ON THE LOOP, not on the product

- row: process (no BACKLOG row — same posture as the flex-point AUDIT and the lap-boundary PROCESS)
- objective: O5 (the loops are the artifact)
- kind: retro
- seats: practice-steward (this file, sole author) ·
        engineering-partner → owed at C-2, C-4 and C-6 only; **nothing here is designed** ·
        ux-expert · content-steward · user-researcher · ai-advisor → **waived**: no surface, no copy,
        no person and no model sits on any path in this document
- depends-on: `.plans/2026-09-07-lap2-CLOSE-HANDOVER.md` · `.plans/2026-09-07-lap3-paul-feedback-CAPTURE.md`
  · `.plans/2026-09-07-lap3-paul-feedback-CAPTURE-2.md` · `.plans/2026-09-07-lap-boundary-PROCESS.md`
  · `.plans/2026-09-07-pipeline-flex-point-AUDIT.md` · `.plans/2026-09-07-feedback-consolidation-lap2-PRACTICE.md`
  · `cycle/release/CYCLE-MAP.md` · `cycle/release/CYCLE-LOG.md` · `~/.claude/rituals/CYCLE-SPINE.md`
- ready: agent-proposed 2026-09-07 — **Paul rules**
- gate: ⛔ **NOTHING IN THIS FILE EXECUTES.** Read-only. **No item is ranked and no product call is
  made** — every ordering below is dependency and sequence. Where a call is Paul's, it says so and stops.
- stage-note: 2026-09-07 evening — written at `92be1bf` (tree clean, HEAD not deployed; QA serves
  `fbd5072`). Every number was executed tonight against the walk corpus, git, the live origins and the
  production KV; nothing is quoted from a status line. Grades: `measured` = I ran or read it ·
  `inferred` · `unverified`.

---

## 0 · THE STOPPING PROPERTY, APPLIED TO THIS FILE FIRST

My own rule from this morning: *does this audit's output ADD rows or SETTLE rows?* Disposal was
measured at 1 of 12 yesterday and is **1 of 25 tonight** (`measured`: 25 files under `.plans/` carry
`- ready: agent-proposed`, 1 carries a `- ready: paul-` stamp).

**So this retro is scored against itself.** It settles **nine** open things and raises **two** new
ones, both process-level, both stated with why they could not wait.

| settles | how |
|---|---|
| `instrumented-counted` | **DISCHARGED — yes**, §5 |
| `second-viewport` | **cannot be discharged, and the reason is structural** — §5 says exactly what would |
| **R-E** (derive the lap from the chronicle) | re-answered **yes**, and the case is now stronger than this morning — §7 C-1 |
| **R-F** (audit budget) | re-answered **yes**, unchanged — §7 |
| `strict` immunity + the Open-Meteo degradation | **collapse into ONE statement** with a zero-overlap coverage hole — §2.1 |
| F3's "missing stage ladder" | **it is not missing** — `check-backlog-ready.py:51` + `wip-exception` already are it — §7 C-5 |
| "11 of 34 walk runs never read" | re-measured with a stated predicate — §2.2 |
| "why did four seats pass a build that could not let Paul in" | **answered, bounded, cheap to close** — §3.0 |
| the seven methods | sorted into procedure vs person, with an attachment point each — §1 |

| raises | why it could not wait |
|---|---|
| **N1 — the returning journey has never been walked, and cannot be** | it is the exact journey F4 broke, and lap 3's first item (`4a3a61b`) is in the same file. §3.0 |
| **N2 — the certifying battery ran 60 seconds after the commit it certified** | it bears on whether gate ① means what the map says it means, and lap 3 will run the same procedure tomorrow. §4.3 |

---

## 1 · WHAT WORKED — and the only question that matters is *procedure or person?*

The handover names seven methods. **Six of the seven happened because a person thought of them that
round.** The distinction is the whole value of this section: a method that lives in a paragraph is a
method the next lap may or may not have.

| # | method | procedure or person? | evidence | where it would have to attach to become procedure |
|---|---|---|---|---|
| **M1** | **Verify by SUBSTITUTION, not absence** — the garden gate credited because two seats found the same rule firing with only the instruction changed | ⚠️ **PERSON**, but the machinery exists and is unreached | `measured` — `tools/check-condo-falsifier.py` is exactly a substitution harness ("can the engine render a plantless estate without a fork"), and it is named in **`MOM-CYCLE-MAP.md` only** — **zero mentions in `cycle/release/CYCLE-MAP.md`, the release `CYCLE-LOG`, `CLAUDE.md`'s pickup block, any skill or any command** (grep across all six surfaces) | `cycle/release/CYCLE-MAP.md` § GATE ①, as a **sixth clause** on any build touching `engine/`. This is the fifth instance of *a capability the loop cannot reach by running its own procedure is not a capability the loop has* |
| **M2** | **RECOMPUTE rather than pattern-match** — 17/17 forecast values, sunset to the minute, coordinates to 11 decimals, against a Fernwood counterfactual wrong by 55 min | ⛔ **PERSON, wholly** | `measured` — no tool in `tools/` recomputes a served value against an independent source; `grep -l "recompute\|counterfactual" tools/*.py` returns 4 files, none of which does this | The flex-point audit already named the attachment: **`product-steward --round` re-reads every report and re-resolves every citation once per round. Give that same act one more duty — re-derive any number a round states as `measured`.** Unbuilt. `.plans/2026-09-07-pipeline-flex-point-AUDIT.md` § "Where the cheap one attaches" |
| **M3** | **Allow-list of good states beats deny-list of bad ones** | ✅ **PROCEDURE — enacted, once** | `measured` — `tools/walk-integrity.py:188` carries the flip in its own text ("…SILENTLY STOPS MATCHING. Flipped to the allow-list, which also aligns the two tools"); `release-gate.py:171` `CLAUSES` is the allow-list twin | ✅ Done **for these two tools**. ⚠️ **Nothing checks a NEW check for deny-list shape** — the lesson is enacted, not generalised. Attachment if wanted: a line in the check-authoring convention, not a tool |
| **M4** | **A word list only finds badness someone already imagined** | ⚠️ **DISCLOSED, not fixed** | `measured` — `tools/check-estate-neutral.py:316` now prints *"AND IT TESTS FOR NAMES"* on every pass. That is an honest boundary, not a remedy: the gauge leak was numbers and possessive pronouns and would read ✅ again tonight | Nowhere, honestly. **A word list cannot be fixed by a longer word list** — the remedy is M1/M2 (substitute, recompute), which is why those two are the ones worth attaching |
| **M5** | **Grep for the GUARD, not the symptom** — found 12 garden sites where the report named 1, and the second `ZONES_DATA` writer | ⛔ **PERSON, wholly** | `measured` — commit `65ff6f9` 16:28 *"the zones fetch defeated the ground gate — fourth instance, one assumption"* and `56735d3` *"both writers of ZONES_DATA gated; the sweep found the second"*. No tool enumerates guard sites | **`tools/check-place-claims.py` is the nearest existing home** (`place-claims.py --check`, already in the pickup block, already red-while-unclassified). A guard-site roster is the same shape: enumerate the writers of a guarded value, assert each is gated. → engineering-partner |
| **M6** | **A harness that under-serves the origin cannot see a whole class** — twice in one day (`weather-history.json`, then `zones.json`) | ⚠️ **PERSON — and there is a stronger version of this finding below** | `measured` — and §3.0 shows the same shape at the *browser-state* level, not the fixture level, which is where it cost the most | `tools/journey-view.py` — the harness must serve what the origin serves **and hold what a returning device holds**. §3.0, §7 C-2 |
| **M7** | **Pre-register the expected diff before looking** | ⛔ **PERSON, wholly** | `measured` — `cycle/release/cycle-state.json` `pre_registered[]` holds **2** items while `CYCLE-LOG.md:826-846` lists **9** for lap 2; nothing derives one from the other (B3 of the consolidation design, still true tonight) | The **lane brief**, not a tool: a lane that states its expected diff before it edits. Cheapest possible attachment — one line in the handoff brief template |

⭐ **The read:** the loop's *checks* are strong and its *methods* are oral tradition. M1 and M2 are the
two that caught the biggest defects of the lap, and **both are one attachment away from being steps
the loop runs on its own** — M1 into gate ①, M2 into `product-steward --round`. Those are C-3 and C-4
in §7.

**Falsifier for this whole section:** if lap 3 runs M1 and M2 without anyone naming them, they were
procedure already and I have mis-sorted them. Cheap test — at lap 3's close, grep the lane commits for
a substitution or a recomputation nobody was asked for.

---

## 2 · WHAT DIDN'T — process only

### 2.1 ⭐ THE ONE THAT SETTLES TWO CANDIDATES INTO ONE STATEMENT

> **The certifying battery had zero overlap between "can test a placed household" and "had undegraded
> data." Gate ① reported 4 of 4.**

`measured` — the battery at `1e2748d`, the build now in production, run 17:16–17:21 ET:

| seat | its address (`.private/walk-answers/<seat>.json`) | 429s from `archive-api.open-meteo.com` | can it test a placed household? |
|---|---|---|---|
| `mom` | 38 Hill St Apt 3B, Roswell GA | **2** | yes |
| `owner` | 2949 Hwy 52 E, Dahlonega GA | **2** | yes |
| `wide-eyed` | 73 Harlow St, Bangor ME | **2** | yes |
| `strict` | **PO Box 417**, Cartersville GA | **0** | ⛔ **no** — a mail drop; its own answer file says *"no physical location for the weather-and-what-grows promise to be built from"* |

**The only seat with clean weather data is the only seat that cannot see a weather defect.** Both halves
were already known separately — the handover states each — and neither is interesting alone. Together
they mean the build Paul cleared was certified by **three degraded observations and one irrelevant one**,
and the gate's own arithmetic said 4 of 4.

⚠️ **This is not a defect in the gate's design.** `release-gate.py:149` deliberately grades a
third-party 429 as `third-party-throttled` — *"a caveat, not a refusal"* — and prints it. That call is
right: refusing on someone else's rate limit would install a permanently-red control. **The defect is
that the caveat and the immunity are never read together**, and nothing computes the intersection.

`measured`, lap 2 overall: **25 of 39 runs carry `rateLimited: true`** — 64%, every one on the same two
`archive-api` URLs. Paul has already approved the Open-Meteo proxy (`BACKLOG.md`, `534bdc0` 15:09 ET),
which is the fix for the supply. **It is not the fix for the arithmetic**, which is C-3.

### 2.2 Unread walks — re-measured, with the predicate stated

`measured`: **12 of 39** lap-2 runs were never read.
**Predicate:** run folders under `.private/synthetic-walks/{mom,owner,strict,wide-eyed}/` timestamped
`2026-09-07T` after `115000` (Paul cleared lap 1 at 11:50 ET); *never read* = `REPORT.md` absent or
still carrying the `WALK-REPORT-UNWRITTEN` marker.

⚠️ **This is a different number from the handover's "11 of 34" and both are true** — that count was
taken at ~13:00 ET over a window that included lap 1's morning. **Neither is comparable to the other
and the corpus figure is different again** (72 of 163 across all seats and all dates,
`measured`). A count without its predicate is not a count.

⭐ **7 of the 12 are `mom`.** `inferred`, and the cheap test is in §6 P3.

### 2.3 `wide-eyed`'s reason for existing was unreached, and "unreached" was scored as passed

`measured`, **verified by a second method** rather than accepting the handover's number: I scanned
**31** `wide-eyed` `_view.json` files (09-06 and 09-07), **10,756 strings**, for `frost` · `hardiness`
· `growing season` · `zone 5`. **All four: zero.**

⚠️ **The dedup caveat does not weaken this one.** `journey-view.py:123` deduplicates the `text` array,
so every *count* claim off that record is unsound — but dedup can only remove repeats, never
manufacture a zero. An absence measured this way is sound; a frequency is not.

**The process defect is not that the surface is missing.** Paul has ruled the onboarding promise
**held to next lap**. The defect is that the seat walked seven times, its four load-bearing terms were
absent every time, and **`release-gate.py` has no clause that can express *"this seat did not reach
what it exists to reach."*** Its five clauses are all about the *run* (at-sha, watched, countable,
zero failures, not rate-limited) and none about the *coverage*. So a seat that cannot do its job
reports identically to one that did it and found nothing. **`X` and `not-X` produce the same
observation** — the shape this corpus keeps re-deriving.

### 2.4 The state artifact lost the lap — and it is lost right now, as I write

`measured`, at `92be1bf`, `cycle/release/cycle-state.json` on disk, tree clean:

    "generated_at": "2026-09-07T11:34:20-04:00",
    "candidate_sha": "c821051",
    "last_lap": { "lap": 1, "opened": "2026-09-06", "outcome": "cleared", "cleared_sha": "c821051" }

**Lap 2 does not exist in the state artifact.** Paul's clear of `1e2748d` this evening is not in it.
`--cleared 1e2748d` was never run. And the live derivation disagrees with the file:

    $ python3 tools/release-state.py
    release loop — FIRED · beat 2/5 (the synthetic loop) · owner: session · candidate fbd5072 · seats pass: False

`measured` — so the loop's **published state** says *lap 1, cleared*, its **live derivation** says
*lap 2 is mid-synthetic-loop*, and the **chronicle** says lap 2 shipped to production and Paul passed
it. Three registers, three answers.

`measured`, the cause, exactly as R-E predicted this morning and now with the second half proven:
- `tools/release-state.py:49` — `last = prior.get("last_lap") or {"lap": 1, …}`. Once a prior file
  exists, `lap` is **copied forward forever**. No CLI verb opens a lap.
- `momlib.py:1721` — `LAP_HEADING_RX = ^##\s+Lap\s+(\d+)\s+[—-]\s+(\d{4}-\d{2}-\d{2})`. The release
  chronicle's headings are `## Lap 1 — candidate build 6ee2e48 → …` and `## Lap 2 — W0 geocoding, …`
  — **no date after the dash**, so `lap_outcomes()` returns **0** for this loop while returning **8**
  for `MOM-CYCLE-LOG.md` and **3** for `cycle/fleet/CYCLE-LOG.md` (executed tonight).

**R-E is still my answer, and more strongly.** This morning it was a tidiness argument about S4. Tonight
it is the reason a cleared production release is absent from the loop's own record.

### 2.5 The disposal beat exists as a tool and has never disposed of anything

`measured`, executed tonight:

    📥 Feedback watch — 6 environment(s) · 480 record(s) awaiting Paul's disposition · 0 unreadable
       🔔 home · est-e6696a — 10 record(s) · 10 awaiting disposition · 1 of 10 fully labelled · checked 6h ago

`tools/watch-feedback.py` was built at 12:40 ET today (`0a7bbf6`) and works. Beats **F1 (sweep)** and
**F2 (label)** of my own consolidation design are built. **F3 (dispose) is Paul's and has fired zero
times**, so F4/F5/F6 have no input.

⛔ **I am not saying he should have disposed of them** — that is his capacity, which is the binding
constraint and the one thing more machinery cannot increase. I am saying the **loop has no beat for
it**: `cycle/release/CYCLE-MAP.md` § The beats declares **five**, ending at *"Paul clears it."*
**There is no beat 6.** The words a real person leaves after the clear are owned by no beat, which is
why 10 production records have sat undisposed for ~8.5 hours across a lap boundary.

### 2.6 Two knowingly-shipped defects, and a timing the record should carry

`measured` from git and the served build stamp:

| clock | event |
|---|---|
| 17:15 | `1e2748d` — *"the door card: an owner guard, and the app's own predicate"* · `estate/index.html`, +23/−1 |
| 17:16–17:21 | gate ① battery, 4 seats, 4 of 4 pass at `1e2748d` |
| 17:36 | `4a3a61b` — *"the predicate was saved by another file, and the guard reached 4 of 7 reads"* · same file, +24/−4 |
| **17:39:18** | **production deploy of `1e2748d`** (read back from `fernwood-home.pages.dev/qa-build.json`, `builtBy: tools/pages-deploy.py`) |

**The fix existed on `main` three minutes before the deploy of the build it fixes.** Paul ruled *"ship
as is, you're out of time"* and that ruling is his and correct. **The record should carry the timing**,
because *shipped knowingly* and *the fix was on disk and not taken* read differently to a future reader,
and only the second is true. `4a3a61b` has **never been walked by any seat** (`measured` — zero
transcripts carry it as `buildBefore` or `buildAfter`).

---

## 3 · WHAT PAUL HAD TO ASK FOR — the highest-signal section

### 3.0 ⭐⭐ FIRST, THE QUESTION UNDERNEATH IT: could the loop have caught F4?

**Answer: no, not as built — and the gap is small, named, and was already written down as owed.**

Three measurements, each independent:

1. **`measured` — all 39 lap-2 walks ran `fresh: true`.** Not one seat arrived as a person who already
   exists. `cycle/release/CYCLE-MAP.md` § TWO CLASSES OF WALKER already convicted this on 2026-09-06:
   *"Every walk on record uses `--fresh`, so the RETURNING journey has never been walked by anyone —
   the state most of the product's life is spent in."* **Lap 2 did not change it.**

2. **`measured` — the returning mode exists as a flag with no journey behind it.**
   `tools/journey-walk.py:177-190`: `journey(fresh, …)` branches on `fresh` for **exactly one stop**
   (the account screen). Everything after — naming, address, ranking, handoff, settings — is the same
   onboarding script. A token arrival lands past all of it, so every fill and click times out.
   **The only four-seat returning battery ever run** was 09:44–09:52 ET this morning at `ca9161e`:

   | seat | failed actions | report |
   |---|---|---|
   | `mom` 094426 | **13** | ⛔ UNREAD |
   | `owner` 094657 | **14** | ⛔ UNREAD |
   | `strict` 094936 | **12** | ⛔ UNREAD |
   | `wide-eyed` 095158 | **15** | ⛔ UNREAD |

   ✅ It **failed closed** — 12–15 failed actions can never satisfy the `no-failed-actions` clause, so
   it could not have falsely certified anything. ⚠️ **And the build moved past `ca9161e`, so the
   per-sha gate never asked again and 54 failed actions left no trace.** Expiry is right for the
   *gate*; it is not a policy for the *evidence*, which is B5 of my own consolidation design,
   re-measured tonight and still true.

3. **`measured` — the defect class is unreachable by construction.** `tools/journey-view.py:63` calls
   `browser.newContext({…})` — a **fresh, empty browser context every run**. `localStorage` survives
   *within* a run (`:147` says so) and **never across runs**. F4 requires a device holding
   `fw-onboard-owner` written by a *previous* grant. **No walk in the corpus has ever held state from
   a previous walk**, so the state F4 lives in has never existed in the harness.

⭐ **So the honest verdict is not "the seats missed it."** It is: **the loop cannot walk a returning
person, it recorded that it could not on 09-06, it shipped a lap whose headline change is a returning
person's door card, and the one battery that tried failed 54 times unread.** That is a process finding,
it is bounded, and §7 C-2 is a small fix.

⚠️ **And this raises the stake on lap 3's first item.** `4a3a61b` fixes two defects in
`estate/index.html` and is unwalked — in the same file, on the same card, in the same journey the
harness cannot reach.

### 3.1 The four asks from the handover, and one measured twice

| # | what he had to ask for | should the loop have produced it unasked? | smallest mechanism |
|---|---|---|---|
| **A1** | *"tell me when the slate is clean with only my walk outstanding"* | ✅ **yes — and the tool already computes it.** `measured`: `release-state.py` publishes `beat.owner: "paul"`, which is precisely *stopped on you*. Nothing **announces** it; a session must be asked to print it | ⭐ **The post-commit hook already runs `release-state.py --write` (R3, `.git/hooks/post-commit:9`, verified).** Give it one conditional line: when `beat.owner` **transitions** to `paul`, print one line. Not a nag, not a cadence — a **transition**, so it fires once per lap and can never be permanently on |
| **A2** | *"make sure everything we built for this lap is instrumented"* | ✅ **yes, and it was pre-registered to be automatic this lap.** `measured`: `release-gate.py:223` still prints *"instrumented (reported, **counted from lap 2**)"* — and `CLAUSES` at `:171` holds **five** entries, **`instrumented` is not one of them**. The promotion was written into the tool's own text and never made | Promote `instrumented` from a reported line to a **sixth clause**. ⚠️ **Not tonight and not by me** — §7 C-6 states why it is the one change that could install a permanently-red control, and what must be true first |
| **A3** | *"a watcher for new accounts"* | ✅ **yes.** `measured`: Mom's invite went out by text ~12:05 ET and every mom-cycle reader points at the **frozen** estate; nothing watched `est-e6696a`. `tools/watch-accounts.py` was built 12:12 ET (`7282f7e`), **35 minutes after her invite was sent** | ✅ **Built.** The residue is placement — it is in `CLAUDE.md`'s pickup block and in **no cycle map**. §7 C-5 |
| **A4** | *"production feedback needs to be accessible for backlog seeding"* | ✅ **yes.** `measured`: `read-onboarding.py --env home` is UNREADABLE by construction (`.private/fernwood-token-home` does not exist); the 12:10 read was a person with `wrangler` | ✅ **Built** (`watch-feedback.py`, `0a7bbf6`). The residue is the **disposal beat** — §2.5, §7 C-5 |

### 3.2 ⭐ THE ONE I MEASURED TONIGHT: he asked twice, unprompted, in one day

`measured`, from the production store via `tools/watch-feedback.py --env home --all`:

    homes-second-home · 2026-09-07T15:21:16.347Z · p-yjnw9lt41nww · —/homes · second-home-ask · note 232 chars

That is **11:21 ET**, typed into the app during his production walk: *"We want to show roles on this
page — I am the owner for Grant Park and you can see Home members. Down the road I will want to invite
mom to have access to my condo…"*

And **F14 tonight**: *"in the settings… a way to also invite people to the household. It'll show your
role as owner and you can invite people."*

**`F12` is the same shape** — logout, asked pre-walk and again tonight, and the capture file says so
in its own text: *"asking twice in one evening is the signal."*

⭐ **Nothing counts a repeat ask.** The record has one home for what he says while walking
(`.private/synthetic-walks/GATE2-paul-findings.md`, whose "Lap 3" section is still **`*(open)*`** —
`measured`, tonight's findings are not in it) and another for what he types into the app
(`est-e6696a:feedback:*`). **A second unprompted ask crosses those two registers and is therefore
invisible to both**, which is exactly how it happened twice today without anyone noticing until a
human wrote *"second time asked"* by hand.

**Should the loop have produced it unasked?** ⛔ **Not the answer — the *count*.** Whether roles-in-
settings is built is a value call and it is his. **That he asked twice is a fact, and a fact nothing
records is a fact that will be lost.** The smallest mechanism is §7 C-7: one column.

⚠️ `unverified by me`: I read the 11:21 ET record directly from KV; I did not hear tonight's ask and
am relying on `CAPTURE-2.md` F14 for it. The *pattern* claim rests on both.

---

## 4 · THE STEP RECORD — how lap 2 was DESIGNED to run, and how it ACTUALLY ran

⚠️ Reconstructed from the world, not from a status line: 39 walk transcripts, the git log for
2026-09-07 after 11:50 ET, and the served build stamps read back from both origins tonight.

### 4.1 The map's beats against the sequence that occurred

| map beat (`CYCLE-MAP.md`) | owner on the map | what actually happened | verdict |
|---|---|---|---|
| **1** a BUILD exists — a sha deployed to QA, reported by `qa-build.json` | main session | **11 candidate shas were deployed and walked**: `05f1830 · 0014f65 · bebdc7a · 24564e6 · 34cb103 · 6584c9b · 4e2ec87 · 1cd3fb8 · f1813d2 · 56735d3 · 1e2748d` | ✅ ran, **11×** |
| **2** the SYNTHETIC LOOP — gate ① passes, *"it may take many batteries"* | seats | **8 full four-seat batteries** (`bebdc7a · 34cb103 · 6584c9b · 4e2ec87 · 1cd3fb8 · f1813d2 · 56735d3 · 1e2748d`) + **3 partial** (`05f1830` mom×2 · `0014f65` mom · `24564e6` mom+wide-eyed) | ✅ ran, **11×** |
| **3** PAUL WALKS IT | Paul | ran **once**, in the evening — **and could not start until three unmapped repairs were made** (§4.2) | ⚠️ ran, **preceded by steps no map contains** |
| **4** a FAILURE RE-ENTERS beat 2 — ⭐ *never patched under Paul and handed back* | main session → seats | ✅ **honoured on product surface.** `measured`: the only commit during his walk is `21ee3e4` 18:22, touching `tools/grant-mint.py` and a plan file — **no app surface**. F4/F7–F15 were captured, not patched | ✅ **held** |
| **5** PAUL CLEARS IT | Paul | ✅ cleared `1e2748d`, ~18:30 ET, recorded at `92be1bf` 18:33 | ⚠️ **cleared in prose only** — `--cleared 1e2748d` was never run; §2.4 |

### 4.2 ⭐ THE UNMAPPED STEPS — three acts, no beat in any loop

Paul's walk could not begin until his own account was reachable. `measured` from `CAPTURE.md` F4 and
the commit record:

| # | act | against what | is it in any map? |
|---|---|---|---|
| **U1** | **mint a fresh grant** — the browser held a dead one, `/api/grant/whoami` → 404 | **production KV** | ⛔ no |
| **U2** | **build and run `grant-mint.py hydrate`** — the fresh grant was born blank; the 8 place facts ride on at sign-in (`worker.js:595`) and **there is no sign-in door** | **new code, written at 18:22 mid-walk, run against production data** | ⛔ no |
| **U3** | **hand re-stamp `fw-onboard-owner`** in his browser | **his own device's localStorage** | ⛔ no |

⭐ **A lap that needed three unmapped acts to reach its own exit condition must say so in its own
record**, and the release chronicle currently does not — the lap-2 section names the deploy, the
findings and the open items, and **contains no account of how beat 3 was made possible.**
`.plans/2026-09-07-lap3-paul-feedback-CAPTURE-2.md` says it plainly (*"A future reader must not
conclude the product let him in on its own. It did not."*) and that file is a capture, not the
chronicle. **→ `cycle/release/CYCLE-LOG.md` lap-2 section is the document that needs amending.**

⚠️ **U2 is the one to look at twice, and not because a rule was broken.** No rule covers it. Beat 4
forbids patching the *product* under Paul; U2 wrote a *tool* that changed *production data* while the
person certifying production was standing in it. That is a real act on a real environment with no
gate, no beat and no record beyond a commit message. **Whether that needs a gate is Paul's call**, not
mine — it turns on how reversible a hydrate is, which is a fact about the data, not about the process.

### 4.3 ⭐ N2 — a deviation the map cannot express: the certifying battery is younger than the code

`measured`: `1e2748d` committed **17:15**; the four-seat battery ran **17:16–17:21**; production
deployed **17:39:18**.

The gate is **per-sha**, which is right and was hard-won. **It has no clause about the sha's age or
about how much of it any seat covered.** A build one minute old passes on the same terms as one that
has been sat with. And 21 minutes later the same author found two defects in the 23 lines that
battery had just certified.

⛔ **I am not proposing a cooling-off period** — that is a throughput judgement and it is his. **I am
saying the map claims something the procedure does not deliver**: *"the synthetic loop — until it no
longer fails"* reads as an iterative convergence, and at `1e2748d` it was **one pass, one minute after
the write.** Both are true of the same lap: rounds 1–7 converged exactly as the map describes; round 8
did not.

### 4.4 Where the map is now wrong — the documents that need amending

| document | what is wrong | grade |
|---|---|---|
| `cycle/release/CYCLE-MAP.md` § The beats | declares **5 beats**, ending at Paul's clear. **There is no beat for what happens to the feedback his clear produces**, and 10 production records now sit undisposed. My consolidation design proposed six more beats and asked *"one loop with eleven beats, or two loops?"* — **Paul's call, unruled** | `measured` |
| `cycle/release/CYCLE-MAP.md` § Conformance | **S1 ⬜ to build · S4 ⬜ to record · S5 ⬜ · S6 ⬜ to build.** S1 and S6 are **built** (`release-state.py`, `release-gate.py` prints one screen). **S4 is still genuinely unmet** — §2.4. The table under-reports in the *pessimistic* direction, which is the rarer one and still wrong | `measured` |
| `cycle/release/CYCLE-MAP.md` § TWO CLASSES OF WALKER | lists the durable-persona walk mode as owed. Still owed, **and now also demonstrably load-bearing** (§3.0). The section should say the flag exists and the journey behind it does not | `measured` |
| `cycle/release/CYCLE-LOG.md` lap-2 section | **omits the three unmapped repairs** (§4.2), and its round list names **8 shas where 11 were walked** (`4e2ec87` and `f1813d2` walked by four seats each and absent; `c821051` listed but is lap 1's; `4a3a61b` listed but never walked) | `measured` |
| `cycle/release/CYCLE-LOG.md` lap headings | do not match `momlib.LAP_HEADING_RX`, so this loop reports **0 laps** while its siblings report 8 and 3 | `measured` |
| `cycle/release/cycle-state.json` | frozen at 11:34 ET, lap 1, `outcome: "cleared"` — a word **off the spine's enum** (`closed \| open \| abandoned`, `momlib.VALID_LAP_OUTCOMES`) | `measured` |
| `.private/synthetic-walks/GATE2-paul-findings.md` | its "Lap 3" section reads `*(open)*`; tonight's F4–F15 are not in it. It is also **untracked** (`.private/`), so the loop's best instrument for *what a person finds that no seat found* has no backup and no history | `measured` |

⭐ **The pattern across all seven rows is one thing:** lap 2 built a great deal of machinery and
amended almost none of the documents that describe it. That is C-1 in §7.

---

## 5 · DISCHARGING THE TWO LAP-1 PRE-REGISTRATIONS

The two-sided rule (`[[feedback_retro_improvement_closes_a_cycle]]`): a retro must discharge what the
prior lap pre-registered, not only propose new ones.

### `instrumented-counted` — ✅ **DISCHARGED. The answer is YES.**

> *"at the lap-1 candidate sha, does every seat's capture.json show ≥1 app event via grant?"*

`measured`, executed tonight against the run folders, not against the state file:

| seat | run | `capture.json` `app.events` | `app.via` |
|---|---|---|---|
| `mom` | `2026-09-07T110051` | **5** | `["grant"]` |
| `owner` | `2026-09-07T110154` | **5** | `["grant"]` |
| `strict` | `2026-09-07T110257` | **5** | `["grant"]` |
| `wide-eyed` | `2026-09-07T110358` | **5** | `["grant"]` |

All four at `buildBefore == buildAfter == c821051`, the lap-1 candidate. Independently mirrored by
`cycle/release/cycle-state.json` `gate_1.seats.*.instrumented: true`. **4 of 4. Disposition: `closed`,
outcome `yes`.**

⚠️ **And the two-sided half is NOT discharged, which is the more useful finding.** The
pre-registration's own consequence was written into the tool — *"REPORTED THIS LAP, **COUNTED FROM LAP
2**"* (`release-gate.py:157`) — and lap 2 has closed with `instrumented` still absent from `CLAUSES`.
The question was answered; the promotion it was pre-registered to authorise was not made.

⚠️ **A second reading, and it is why the promotion should not be automatic.** The five events are
`session_start · text_size_served · jumpstrip_viewed · card_section_viewed · session_end` — **generic
app telemetry, not W0**. A green `instrumented` clause proves *the app phoned home*, not *this lap's
feature is instrumented*. Paul's actual ask (A2) is the second thing. **Promoting the clause as
written would satisfy the letter and miss the ask.** → §7 C-6.

### `second-viewport` — ⛔ **CANNOT BE DISCHARGED, and the reason is structural, not neglect.**

> *"does a laptop-width walk find what 414 hides (Paul found one on 2026-09-06)?"*

`measured`: `tools/journey-view.py:64` hardcodes `viewport: { width: 414, height: 848 }`,
`deviceScaleFactor: 3, isMobile: true, hasTouch: true`, and **there is no CLI flag to change it**
(`grep` across `journey-view.py` and `journey-walk.py` returns the constant and its defence, no
option). `:53` states the constraint deliberately: *"viewport, deviceScaleFactor, isMobile and
hasTouch are untouched."*

**So zero laptop-width walks exist, and none can be produced by the loop's own tooling.** The
pre-registration asks a question the instrument cannot answer, which means **it would read `open`
forever** — a permanently-red control by another name, and against Paul's own standing rule.

**What would discharge it, and it is small:** a `--viewport WxH` option on `journey-view.py`
(defaulting to 414×848 so nothing changes by accident), one four-seat battery at laptop width against
a single sha, and a diff of findings against the 414 battery at the same sha. **One run, one
comparison, a yes or a no.**

⛔ **Whether that is worth building is Paul's call, and both answers are legitimate.** He may also
**kill it** — 414 × A+ is her measured condition and a laptop is nobody's confirmed device. **What is
not legitimate is leaving it `open` for a third lap**, because an undischargeable pre-registration
teaches the loop that pre-registrations do not have to be answered. → §6 P1 offers the disposition
either way.

---

## 6 · PRE-REGISTERED FOR LAP 3 — five, each falsifiable, each with the evidence that settles it

⚠️ Deliberately five and deliberately cheap. Paul's own rule: *"None — pre-registered metric unmoved"*
is a valid recorded outcome, and manufacturing amendments is a defect generator. Three of these five
can be settled by a command.

| id | question | what settles it | grade at open |
|---|---|---|---|
| **P1** | **`second-viewport` gets a disposition — build the flag, or kill the pre-registration.** Not a third lap open | `cycle-state.json` `pre_registered[]` shows `disposition != "open"` for it, with `evidence` non-null | `measured` open |
| **P2** | **Does the returning journey get walked at all in lap 3?** Not "does it pass" — does one non-`--fresh` run exist at the cleared sha with **< 5 failed actions** | one transcript with `fresh: false`, `failedActions` length < 5, at lap 3's cleared sha. **Today's baseline is 12–15 failures on 4 of 4** | `measured` 0 of 39 |
| **P3** | **Does the unread rate fall below tonight's 12 of 39** on the same predicate, or does the loop instead *dispose* of unread runs (read · superseded-and-kept · discard)? Either counts | re-run tonight's count at lap 3's close, plus a disposition record if the second route is taken. ⚠️ **Same predicate or the number is not comparable** | `measured` 12/39 (31%) |
| **P4** | **Does any production feedback record reach `disposition != null`?** One is enough to prove the beat exists; zero proves it does not | `tools/watch-feedback.py --env home` reports `< 10 awaiting`. Tonight: **10 of 10 awaiting, 0 disposed** | `measured` 0 |
| **P5** | **Does the `ready: agent-proposed` pile stop growing?** The count at lap 3's close against tonight's **25** — a fall OR a flat line both pass; only growth fails | `grep -c` over `.plans/*.md`, same predicate. ⭐ This is the pre-registration on **my own** output, and it is the one that should bind hardest | `measured` 25 (vs 1 `paul-`) |

**Falsifier for the set:** if all five read green at lap 3's close and Paul's walk still needs
out-of-band repairs to start, I pre-registered the wrong five and the retro measured the wrong layer.

---

## 7 · THE PROCESS CHANGES — ranked by (a) settles-something-open · (b) cheap · (c) reversible

⛔ **I have enacted nothing.** This session applies them. Every row states plainly whether it is safe
to enact without Paul's ruling, and **no row ranks a product item.**

### Safe to enact without a ruling — all four are record-repair or already-ruled

| # | change | settles | cost | reversible | why safe |
|---|---|---|---|---|---|
| **C-1** | **Amend the six documents in §4.4 to describe the procedure actually being run**, including R-E's heading rewrite: `## Lap 1 — 2026-09-06 · candidate build 6ee2e48 → c821051` + `<!-- outcome:closed at:<utc> -->`, and the same for lap 2; then read `momlib.lap_outcomes('cycle/release/CYCLE-LOG.md')` at `release-state.py:49` instead of the hardcoded default | R-E · S1 · S4 · §2.4 · §4.4 | **one editing pass, no new tool** | ✅ text and a one-line read | ⭐ **R-E was already ruled by Paul at ~12:45 ET** (*"I go with your recommendations on the other items"*) and has not been applied. Everything else here makes a document match a world that already exists. **Falsifier:** if lap 3's heading never gets written, the borrow fails at lap 4 — check the heading exists before wiring the read |
| **C-2** | **Give the returning journey a step list**, and give each seat a **persistent browser context** so a device can hold what a previous run wrote: `journey()` gets a token branch past stop 2 (sign-in-or-arrive → the app, no onboarding fills); `journey-view.py` gains an opt-in `--carry-state <dir>` | §3.0 · N1 · P2 · the 09-06 durable-persona ruling, owed since | small — one branch, one launch option | ✅ opt-in; default unchanged | Paul **already ruled** the two walker classes on 2026-09-06 and the map lists this as owed. This enacts a standing ruling, it does not make one. → **engineering-partner**. ⚠️ **`--carry-state` must default OFF** — a persistent context that leaks between seats would silently make four seats one |
| **C-3** | **Gate ① prints the INTERSECTION it already computes**: one line naming which seats both (i) can exercise a placed household and (ii) had undegraded data. **Counted, never graded** — no refusal, no threshold, no red | §2.1 — settles `strict`-immunity and the 429 degradation as one statement | ~15 lines in `release-gate.py`'s report | ✅ output only, no clause | It cannot refuse anything, so it cannot install a red control. It is the exact form Paul's own coverage rule prescribes. **Falsifier:** if the intersection is never empty over three laps, the line is noise — delete it |
| **C-4** | **`product-steward --round` re-derives any number the round states as `measured`** before it is written into a doc | M2 → procedure · the flex-point audit's own named attachment · the un-triggered class | it already re-reads every report and re-resolves every citation; this is one more duty on an existing act | ✅ it reports, it never edits | R7 option C is `paul-approved`; the seat is **citation-bound and may not rank**, so it cannot make a judgement call to reverse. ⚠️ The trial is **inconclusive at 1 round** and needs rounds 2 and 3 regardless — this gives round 2 something to measure |

### NOT safe without a ruling — each needs a word from Paul, and the word is named

| # | change | what is Paul's, exactly | my recommendation | falsifier |
|---|---|---|---|---|
| **C-5** | **Give the release loop a beat 6 — DISPOSE** (F1–F6 of the consolidation design), or declare it a second loop | ⭐ **One loop with eleven beats, or two loops?** My own design put the question to him and stopped. It is a governance shape, not a method call. **Also his:** does the mom-cycle FOCUS FREEZE bind her arrivals on `est-e6696a`? The sweep must not settle that by existing | **One loop, beats 6–11.** A second loop is a fourteenth loop for a solo operator, and the state artifact is already shared. ⚠️ `render.py:832` cannot parse `F1 · SWEEP` — renumber 6–11 or accept prose | If beat 6 fires and the undisposed count does not fall, the beat is bookkeeping and the constraint was never the beat |
| **C-6** | **Promote `instrumented` from a reported line to a sixth gate clause** | ⛔ **Do not enact.** `measured`: the four client-side gates built this lap — station · burn · terrain · sky — carry **zero `track(` calls** (60+ event names in the template, none of them a gate outcome). **A clause promoted tonight would be green on generic session telemetry while his actual ask is unmet, or red on every build until those gates are instrumented.** Either way the clause lies | **Instrument the four gates first, then promote.** Sequence, not priority: the clause depends on the instrumentation, never the reverse | If the four gates land instrumented and the clause still reads green on a build with an uninstrumented new feature, the clause is measuring the wrong thing and should stay a reported line forever |
| **C-7** | **Count the repeat ask.** One column — *"asked before? when?"* — in the single register that holds what Paul says while walking, merged from `GATE2-paul-findings.md` and the `est-e6696a` feedback store | ⛔ **It must not become a priority signal without his word.** A repeat ask is a *fact about frequency*; treating it as *importance* is a value call and it is his. And ⭐ `GATE2-paul-findings.md` lives in `.private/`, **untracked** — whether the loop's best instrument should be backed up is his call (it holds a real person's words) | **Record the count; render it beside the row; rank nothing by it.** Merge the two registers so a cross-register repeat is visible at all — today F12 and F14 were caught by a human writing *"second time asked"* by hand | If two laps pass and no repeat ask appears, the column is dead weight — delete it. If repeats appear and he never acts on them, it was never a signal |
| **C-8** | **A gate on U2-class acts** — writing new code that mutates production data while production is being certified | ⛔ **Turns on reversibility of a hydrate, which is a fact about the data, not the process.** His standing rule is that the gate sits on *irreversible* acts and never on work | **Ask before designing.** I will not propose a gate whose necessity I cannot establish. If a hydrate is idempotent and reversible, no gate is owed — only a record | If a future hydrate cannot be undone and no record says one ran, the absence of both is the cost |

⭐ **R-F is unchanged: one corpus-bounded review per cycle, at the disposal beat.** This retro is that
one. It settled nine and raised two, which is the ratio the budget exists to protect. **I would not
run another before lap 3's own disposal beat**, and §6 P5 binds me to it.

---

## 8 · WHAT I DID NOT MEASURE — stated so this file does not read as full coverage

- ⛔ **I did not open a single walk REPORT's contents.** Every "unread" claim is the marker's presence,
  not a judgement of quality. A read report may still be a bad one.
- ⛔ **I did not verify tonight's second roles-and-invite ask myself** — I read the 11:21 ET record from
  KV and took F14 from `CAPTURE-2.md`. `unverified` on that half.
- ⛔ **I did not read the 812-line maps-and-zones PROPOSAL**, deliberately: zones is lap 3's design
  work and reading it here would put product content in a process document.
- ⛔ **I did not re-verify any of the eleven open product defects** in the handover §3. They are
  product; this is method.
- ⛔ **I did not diff the `_view.json` text arrays for anything except term absence**, because
  `journey-view.py:123` deduplicates them and every frequency claim off that record is unsound.
- ⚠️ **One reading I could not settle, and it is a contradiction I am reporting rather than
  resolving:** the chronicle says *"~8 rounds"*; the transcripts show **11 walked shas**. Both were
  written honestly. Which one "a round" means — a deploy, a battery, or a merge — is a definition this
  loop has never fixed, and fixing it is a word, not a measurement.

---

## 9 · THE NEXT LAP

The proposed procedure for lap 3 is a **separate document so it can be ruled separately from these
findings**: `.plans/2026-09-07-lap3-PROCEDURE-PROPOSAL.md`. It reconciles the lap-boundary work
(R-A…R-F), Paul's staged pipeline (F3), the three deterministic sweeps, and the order he stated —
**sweep and prove → consolidate → then build the options list.**
