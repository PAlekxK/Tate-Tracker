# Lap 5 · Beat 5 · GROOM & BUCKET — the proposed rationalization, and the board on two axes

- row: process (no BACKLOG row — same posture as the CHARTER and the CARRY artifact it follows)
- objective: O5 (the loop is the artifact)
- class: engine · declared (process machinery; nothing is ranked, scoped or built here)
- kind: round
- seats: product-steward — the citation-bound carrier, ONE-LAP TRIAL, `[paul-ruled 2026-09-07, R7→C]`
- depends-on: .plans/2026-09-07-product-steward-CHARTER.md
- depends-on: .plans/2026-09-08-lap5-CARRY.md
- depends-on: .plans/2026-09-07-backlog-grooming-SCAN.md
- depends-on: cycle/release/CYCLE-MAP.md § The beats
- ready: n/a — this file records a beat that ran. ⛔ **§1 is a PROPOSAL and starts nothing.**
- gate: ⛔ **NOTHING HERE RANKS.** COMMIT (beat 6) is Paul's and *"no instrument is ever built for it."*
  ⛔ **NOTHING HERE WAS APPLIED.** `BACKLOG.md` was not touched: `git status` shows it unmodified at
  `71119d6`. ⛔ **NO SEVERITY WAS ORIGINATED** — every grade in §2 is carried, with its grader named.
- stage-note: 2026-09-08 — beat 5 of lap 5, the first GROOM & BUCKET ever run as an owned beat
  (`groom` had no owning beat until the 2026-09-08 renumber). **10 buckets named · 1 minted from Paul's
  DISPOSE ruling · head-gap 672 → 181 proposed · 9 live ordinal collisions measured · 6 questions.**

---

## 0 · THE EXIT CONDITION, AND WHETHER IT WAS MET

> *"the board is laid out on **two axes** — kind-shaped buckets it owns, carried severity it cites."*
> — `cycle/release/CYCLE-MAP.md:84`

**Met.** §2 lays every live item on **axis 1 · bucket (kind)** — ten buckets, named and owned by this
beat — and **axis 2 · severity as carried**, each with the seat, tool or ruling that established it.

⛔ **Two things this beat did not do, stated rather than left as an absence.** It did not say one item
matters more than another, inside a bucket or across them. And it did not apply a single character of
the rationalization: §1 is a diff described, not a diff made.

⚠️ **AND THE SEVERITY COLUMN IS NOT A COVERT RANKING.** The grades in §2 come from four kinds of
grader on four scales — Paul's rulings, four synthetic seats, three expert seats, and six tools. A 🔴
from `walk-integrity` and a 🔴 from the `owner` seat are not the same claim and do not sort against
each other. The column says *someone graded this, and here is who*; it does not say *this one first*.

---

# 1 · THE PROPOSED RATIONALIZATION, AS A DIFF

## 1.1 · The measurement — and the fact that decides the argument

`measured` at `71119d6`, `python3 tools/check-backlog-drift.py`:

```
📋 Backlog rationalization is OWED — last run 2026-09-03 (5d ago)
   ⚡ the ranked list is 672 lines below its own head (limit 400) — line 20 -> 692
   · BACKLOG.md is 4257 lines; 0 of 10 sections above the tracks already read as finished
   · 76 commits to BACKLOG.md since that run
```

⭐⭐ **THE FACT THAT MAKES THIS URGENT IS NOT THE NUMBER. IT IS THE SLOPE, AND IT IS THIS LAP'S OWN.**
`measured` by reading `BACKLOG.md` out of each of lap 5's commits and locating its `TIER 1` heading:

| commit | beat | file lines | TIER 1 at | head-gap |
|---|---|---|---|---|
| `dcf99ec` | lap 4 closes | 4,027 | 530 | **510** |
| `20cda10` | lap 5 · **OPEN (1)** | 4,071 | 574 | **554** |
| `1cbbf04` | lap 5 · **DISPOSE (2)** | 4,121 | 624 | **604** |
| `5998f67` | lap 5 · **READ (3)** | 4,121 | 624 | **604** |
| `37ced32` | lap 5 · READ, 2nd write | 4,165 | 668 | **648** |
| `b998b30` | lap 5 · **CARRY (4)** | 4,257 | 692 | **672** |

**Every beat of this lap widened the gap that beat 5 exists to close, by 162 lines in one day.** The
lap 5 log recorded 554 at open and wrote *"lap 4 widened it by filing into it"*; lap 5 then did the
same thing four more times. ⭐ **That is not a criticism of the filing** — DISPOSE and CARRY were
correct to file two themes and twelve citations, and both were told to. **It is the diagnosis:** the
head region is where new material lands by default, so the pointer list drifts away from its own
ranked table *as a direct product of the loop working*. A file whose ordering degrades when the
process succeeds needs a periodic move, not more discipline at the moment of writing.

⭐ **And it is the 07-29 shape re-grown by append, exactly as `check-backlog-drift`'s own doctrine
predicted.** The 07-29 run was commissioned to kill *"the two colliding ▶️ NEXT tables."* Today the
head at line 20 declares a reading order — *"WAITING ON PAUL → open decision cards → Tier 1 / 2 / 3 →
the three lenses → the tracks"* — and **five sections that the order does not name sit between it and
TIER 1**, totalling **560 lines**. The collision is no longer two tables; it is a declared order and
an actual order that disagree.

## 1.2 · MOVE SET R1 — restore the file's own declared reading order

⛔ **Every item below is a MOVE of a whole section.** No row is deleted, no status changed, no row
renumbered, nothing split. That is the 09-02 precedent verbatim (*"every move was a MOVE"*), and the
one thing that run got right in refusing to split is the thing this one refuses too.

**Two new regions are created to receive them**, both placed **after the lenses and before
`# TRACK A` (currently line 911)**:

```
# 🗂 THEMES — groupings over existing rows, not new scope
# 📜 THE RULING REGISTER — rulings in force, and why they are written down
```

| move | section (anchor by heading text, not line number) | lines | from | to |
|---|---|---|---|---|
| **R1-a** | `## 🔓 2026-09-06 — FOUR RULINGS, and the reason this block exists` | **223** | 138–360 | `# 📜 THE RULING REGISTER` |
| **R1-b** | `## ⭐⭐ EVERY ITEM SHIPS WITH AN ASK, A CHECK AND AN ATTRIBUTION` | **39** | 424–462 | the lens region, after `## ⚖️ TRACK A vs TRACK B` (ends 910) — it becomes the **fourth lens** |
| **R1-c** | `## 🗂 THE USER'S OWN RECORD — what they told us, what we ask next…` | **74** | 463–536 | `# 🗂 THEMES` |
| **R1-d** | `## 🧪 SPLIT THE JOURNEY FROM THE READER — a synthetic-testing redesign` | **155** | 537–691 | `# 🗂 THEMES` |

**Total moved out of the head region: 491 lines.**

*(⚠️ **Move sets renamed `M1…M4` → `R1-a…R1-d` on 2026-09-08** by the backlog-refinement window, with
this window's agreement. Reason: `BACKLOG.md` already has rows **M1 · M2 · M3** (`:1921`, `:3164`,
`:3143`), so inside this file `M1` meant a move set at §1.2 and the backlog row
`## ✅ M1 · THE FEEDBACK READER…` at §1.4 — an address collision one day old, in the document that
diagnoses address collisions. Free to fix while nothing cites it. ⛔ The backlog rows were **not**
renumbered; §1.3's do-not-renumber finding applies to this file too.)*

### ⭐ THE HEAD-GAP THIS CLOSES

| | |
|---|---|
| today | **672** (line 20 → 692) · limit 400 · 🔴 |
| after R1 | `TIER 1` lands at **201** → **head-gap 181** · limit 400 · ✅ |
| margin | **219 lines of headroom** — at lap 5's own observed rate of ~162 lines/lap, roughly **1.4 laps** before it is red again |

⚠️ **The margin figure is the honest half.** R1 buys less than a lap and a half at the rate this lap
actually filed. **A move alone does not fix a file that grows at the head** — which is why §1.6
proposes that new themes be filed **into the THEMES region from the start**, and why §3 · Q11 asks
whether the head region should have a declared line budget at all. Without one, beat 5 will re-run
this same move every second lap forever.

### Minimal variant, if Paul wants fewer moves

**R1-a + R1-d only (378 lines) → head-gap 294 · ✅ under the limit.** It clears the check and leaves the
two 2026-09-07/08 contracts (R1-b, R1-c) in the head. `agent-proposed`; the full R1 is the recommendation
because R1-b and R1-c are a standing contract and a theme, and both classes now have a named home.

### Provenance lines the move must carry

Each moved section gains one line at its new site, matching the 09-02 precedent:

```
*(Moved here from the pointer-head region at lap 5's GROOM & BUCKET, 2026-09-08 — reading order only;
no row deleted, no status changed. Proposal: `.plans/2026-09-08-lap5-BOARD.md` §1.2.)*
```

And **the head's own reading-order sentence must be updated in the same commit** — it currently reads
*"WAITING ON PAUL → open decision cards → Tier 1 / 2 / 3 → the three lenses → the tracks."* Proposed:

> **Reading order now:** FOCUS FREEZE → WAITING ON PAUL → open decision cards → Tier 1 / 2 / 3 → the
> **four** lenses → **THEMES** → **THE RULING REGISTER** → the tracks → SEEDS → the archive.

⭐ **Note what that sentence change admits: `## 🧊 FOCUS FREEZE` has sat in the head region since
2026-09-03 and the declared order has never named it.** It is the one head-region section this
proposal does **not** move (it is the scope gate for everything below it), so the order is amended to
match reality rather than the section moved to match the order. See Q11 — the freeze block has a
structural problem of its own that a move would not fix.

## 1.3 · ORDINAL COLLISIONS — **nine live**, and the resolution is not renumbering

`measured` by parsing the three tier tables at `71119d6`:

| tier | live rows | struck rows |
|---|---|---|
| TIER 1 | 6, 11, 14, 15, 16, 18, 19, 19b, 19c, 20 | 12 |
| TIER 2 | 7 … 24 (18 rows) | 6 |
| TIER 3 | R, 1, 2, 5, 6, 7 | 4 |

**Live ordinals that name two different rows:**

| ordinal | TIER 1 | TIER 2 | TIER 3 |
|---|---|---|---|
| **6** | ✓ ack-ribbon checkmark (held open) | — | ✓ W6 instance model |
| **7** | — | ✓ zones epic | ✓ wildlife confidence markers |
| **11** | ✓ the sound pipeline | ✓ the weather card from an address | — |
| **14** | ✓ the `reviewed` field | ✓ one digest, N estates | — |
| **15** | ✓ "behind N" is a bot | ✓ model routes not modular | — |
| **16** | ✓ Guru's hard-facts block | ✓ the ask surface | — |
| **18** | ✓ absent-domain guards | ✓ the account-lifecycle sweep | — |
| **19** | ✓ cannot found a second place | ✓ password confirmation | — |
| **20** | ✓ login→select→arrive journey | ✓ what the Almanac IS | — |

**Nine live collisions.** ⛔ **And one of them has already produced a wrong citation in a tracked
file:** `feedback-dispositions.json`'s `why` for `fb-vurlf77f` read *"FOLDS INTO TIER 1 · 11 (the
weather card from an address)"* — the parenthetical title was right and the tier was wrong, so a
reader following the record landed on a row about bird audio. ✅ **Fixed in `b998b30`** by the session
(not by this seat — the file is outside the charter's write set); the **class is not fixed.**

### ⛔ DO NOT RENUMBER — the cost is measured, and it is in production code

`measured`: **eight** `TIER n · m` citations live outside `BACKLOG.md` in files that are deployed or
executed, plus three unqualified ones:

| file | citation |
|---|---|
| `worker/worker.js` | 5 sites — `:83`, `:1406`, `:1476`, `:1817`, `:2246`, `:2754` all cite `TIER 2 · 14` |
| `viewer.html` · `engine/viewer.template.html` | `:6654` `TIER 2 · 10`, `:6658` `TIER 2 · 14` |
| `CLAUDE.md` | `TIER 2 · 15` |
| `tools/grant-mint.py:213` | ⚠️ **"BACKLOG row 20"** — unqualified; resolves to TIER 1 · 20 only from context |
| `tools/guard-concurrent.py:69` | ⚠️ **"BACKLOG row 15"** — unqualified; resolves to TIER 1 · 15 only from context |
| `cycle/release/CYCLE-LOG.md` | ⚠️ **"row 19's journey work"** ×3 — unqualified |

**Renumbering would silently falsify comments inside a running Worker.** A comment that cites the
wrong row is worse than no comment, and nothing checks it.

### ⭐ THE PROPOSED RESOLUTION — three parts, none of which moves an ordinal

| | proposal | why |
|---|---|---|
| **C1** | **The qualified form `TIER n · m` is the only legal citation of a backlog row**, in prose, in code comments, in `feedback-dispositions.json`, and in every `.plans/` artifact. The three unqualified sites above are corrected in place. | It is the form that already works. The one failure was a *wrong tier*, not a missing one |
| **C2** | **Re-sort each tier table into ordinal order.** `measured`: TIER 1 currently reads `1,2,3,4,5,6,8,9,7,10,11,12,13,14,18,17,16,15,19,19b,19c,20` — **six rows out of place**; TIER 2 reads `…19,21,20,22…` — **one**. ⛔ A pure MOVE of table rows: no number changes, no status changes | A reader looking up "TIER 1 · 16" today scans past 18 and 17 to find it. This is the same defect as the head-gap, one altitude down |
| **C3** | ⚠️ `agent-proposed`, **Paul rules**: a `tools/check-row-citations.py` that fails on an unqualified `row N` reference to `BACKLOG.md` and on a `TIER n · m` whose parenthetical title does not match that row's heading. ⛔ **Not built here.** The 09-07 SCAN's own R-e ruling applies — *the fix is upstream* — and a fourth check may not be worth its own maintenance | Nothing today can catch the exact failure that happened. But a check nobody reads is this repo's most-recorded waste, so this is offered, not assumed |

⭐ **The alternative Paul may prefer, stated so it is a choice and not an omission:** give every row a
**stable slug id** (`T2-11-weather-from-address`) minted once and never reused, with the ordinal
demoted to a display position. It fixes the class permanently and it is the only option that survives
a future re-tiering. **It costs a rewrite of all eleven external citations plus every `.plans/`
reference, and it is a schema change, not a move** — which is why it is Q7 and not a proposal.

## 1.4 · WHAT READS AS FINISHED — archive, do not delete

⚠️ **`check-backlog-drift` reports `0 of 10 sections above the tracks already read as finished`, and
that is correct.** Everything below is **below** TIER 1, so **archiving it closes ZERO head-gap.** It
is proposed for the reader, not for the metric, and saying so is the point.

`measured` — each candidate grepped for live riders (`🔴 🟠 🟡 ⏸ OWED / NOT FIXED / still open`):

| section | lines | riders found | proposal |
|---|---|---|---|
| `## ✅ M1 · THE FEEDBACK READER ONLY LOOKS IN ONE CHANNEL — FIXED 2026-09-01` | 77 | **none** | ✅ archive whole |
| `## ✅ ~~🎗 Z-ACK~~ — CLOSED 2026-09-07` | 34 | ⛔ **REGRADED 2026-09-08** — **three** live things, two of them its own: a standing `[paul-ruled 2026-09-07]` prohibition (*"do not design a ribbon, a card, a message or any surface for it, and do not re-raise the row"*), a conditional that fires **when the zone work ships** (*"the ribbon leads on her sixteen names…"*) — and Paul opened a dedicated zones window the same day — plus the cross-reference originally noted | ⛔ **DO NOT ARCHIVE.** Regrade by the refinement window, `.plans/2026-09-08-lap5-REFINEMENT.md` §10.3; accepted by this window |
| `## ✅ L1 · A LAP THAT OPENS AND CLOSES THE SAME DAY — FIXED 2026-09-01` | 48 | **none** | ✅ archive whole |
| `## ✅ SHIPPED 2026-07-29 — the Tier-1 correctness pass` | 19 | **none** | ✅ archive whole |
| `## ✅ SHIPPED 2026-07-29 (evening)` | 55 | its two `⏸ needs Paul` threads are both **struck and decided 2026-08-02** | ✅ archive whole — **promoted `inferred` → `measured` 2026-09-08**: the re-grep its own note demanded was run (`:3500`, `:3501` both struck **and** `✅ DECIDED 2026-08-02`) and it cleared |
| `## A1 · ✅ PASSED 2026-07-26` | 46 | ⛔ **carries the live R1/R2 monitoring definitions** | ⛔ **DO NOT ARCHIVE.** A live spec under a ✅ heading |
| `## ✅ FLEET LAP 1 · BEAT 6` | 81 | ⛔ `⏸ Emissions hardware STAYS OPEN` + `### 🐛 TWO DEFECTS IN fleet_probe.py — FILED, NOT FIXED` | ⛔ **DO NOT ARCHIVE WHOLE.** Splitting the riders out is a judgment edit — Paul's, not a move |
| `## 🎨 The shape system — SHIPPED 2026-08-02` | 170 | ⛔ carries `🔬 NEXT LAP` and `🟡 MEASURED + PARTLY FIXED` | ⛔ **DO NOT ARCHIVE.** Same shape |

**Clean archive: 144 lines (3 sections), or 199 with the fifth — revised down 2026-09-08 by the Z-ACK
regrade.** ⭐ **The finding worth more than the line count, and the regrade STRENGTHENS it: FOUR of eight
sections carrying a ✅ or SHIPPED heading contain live work.**
That is `[[feedback_unchecked_box_is_not_open_work]]` running in the *other* direction — a section
that reads FINISHED and is not — and it is the more dangerous direction, because the standing rule
tells a reader that these documents over-report open work, never under.

**Destination is Q8** — a bottom `# 🗄 ARCHIVE` region, or a companion `BACKLOG-ARCHIVE.md` on the
`PICKUP-LOG-ARCHIVE.md` precedent.

## 1.5 · THE 09-07 KILL LIST — five applied, two live, five overtaken. **Do not redo the SCAN.**

`.plans/2026-09-07-backlog-grooming-SCAN.md` §4 proposed twelve strikes. `measured` at `71119d6`:

| | proposal | status today |
|---|---|---|
| **K1** | TIER 1 · 10 (`FN_STORAGE_KEY` TDZ) | ✅ **APPLIED** — row reads *"STRUCK 2026-09-07"* |
| **K2** | TIER 1 · 17 (deploy `paths:`) | ✅ **APPLIED** — *"STRUCK 2026-09-07"* |
| **K3** | a `door` reader | ✅ shipped `72b9276`; `watch-door.py` is in the pickup block |
| **K4** | strike the colour-precedence *"which wins"* clause from TIER 1 · 19c | ✅ **APPLIED** — `grep 'which wins' BACKLOG.md` returns **zero**; the phrase survives only in the SCAN and the lap-3 brief |
| **K5** | withdraw the Almanac-naming finding | ✅ withdrawn `75627a2` |
| **K6** | TIER 3 · 8 | ✅ **APPLIED** — *"STRUCK 2026-09-07"* |
| **K7 · K8** | GAP 2, the guided visit | ✅ recorded inside § FOUR RULINGS (`:220`, `:243`, `:246`) — **and R1-a moves that record out of the head region, which is the only thing still owed** |
| **K9** | Z-ACK | ✅ **APPLIED** — section struck and closed 2026-09-07 |
| **K10** | the two lap-1 pre-registrations | ✅ discharged |
| **K11** | `fernwood-5` · `fernwood-6` | ✅ **APPLIED** — both read *"RETIRE 2026-09-08"* in the decision-card block |
| **K12** | `transcript.personId` is a harness field, belongs in harness docs not a row | ⚠️ **OVERTAKEN, NOT APPLIED.** CARRY wrote the same finding into **TIER 2 · 22** at F13 as a member of *the gate's own camera*. ⭐ K12 said *"this is a caution, not work"*; TIER 2 · 22 says *the harness's undocumented fields make seats file false alarms*, which **is** work. **The two disagree and the disagreement is real** — Q12 |

⭐ **Eleven of twelve landed in five days without anyone tracking that they had.** That is the strongest
available evidence that the SCAN's format works and should be extended rather than replaced — which is
what §2 does, per the SCAN's own ruling **R-d**: *"a slate IS beat 5's output, prepared out-of-band and
adopted at the next GROOM & BUCKET. ⛔ Do not mint a second artifact class."*

## 1.6 · WHAT THE DIFF DELIBERATELY DOES NOT DO

- ⛔ **Rank.** Not within a bucket, not across buckets, not by moving anything above anything.
- ⛔ **Renumber a row.** §1.3 explains the measured cost, in a running Worker.
- ⛔ **Split a section.** R1-a–R1-d are whole-section moves. The three ✅-headed sections carrying live
  riders (§1.4) stay exactly where they are until Paul rules on the split.
- ⛔ **Delete anything.** The archive is a move to a named region; the trail survives.
- ⛔ **Touch `check-backlog-drift.py`'s thresholds.** They are a first cut and they fired correctly.
- ⭐ **One forward-looking rule is proposed and it is the only new rule here:** *a new THEME is filed
  into `# 🗂 THEMES` from the start; a new standing CONTRACT is filed into the lens region.* Without
  it, R1 is a move beat 5 repeats every second lap — §1.2's margin arithmetic says so.

## 1.7 · APPLY-ME — the mechanical sequence

```
1. Create `# 🗂 THEMES` and `# 📜 THE RULING REGISTER` immediately before `# TRACK A`.
2. Move R1-a → THE RULING REGISTER.  Move R1-c, R1-d → THEMES.  Move R1-b → after `## ⚖️ TRACK A vs TRACK B`.
3. Add the one-line provenance note (§1.2) at each of the four new sites.
4. Rewrite the head's "Reading order now:" sentence (§1.2).
5. C2: re-sort TIER 1's rows to 6,11,14,15,16,18,19,19b,19c,20 order and TIER 2's 20 before 21.
6. C1: qualify the three unqualified citations — tools/grant-mint.py:213, tools/guard-concurrent.py:69,
   cycle/release/CYCLE-LOG.md (3 sites).
7. Q8's answer decides §1.4: move the 4 clean sections to the chosen archive destination.
8. Mint § UX & DESIGN COHERENCE into THEMES from §2 · B1 below (Paul's DISPOSE ruling; Q2 closes).
9. Re-run `python3 tools/check-backlog-drift.py` — expect head-gap 181 and the marker date advanced.
10. Update BACKLOG.md's head marker to `(rationalized 2026-09-08)` — the clock is read from EVIDENCE,
    and applying the rationalization is what resets it.
```

⚠️ **Step 10 is not bookkeeping.** `check-backlog-drift` reads its clock from the head marker plus
`.plans/` artifacts, newest wins. **If the moves land and the marker does not, the check stays red
over a file that was just groomed** — and a control that is red immediately after the work is the
control this repo has twice recorded nobody reads.

---

# 2 · THE BOARD, ON TWO AXES

**Axis 1 · BUCKET (kind).** Ten buckets. Each names *what its members share* — a surface, a seam, a
journey, a subject, a contract, an audience, an instrument, a library, the loop's own record, or
someone else's ownership. ⛔ **Buckets are not ordered.** They are lettered so they can be referred to.

**Axis 2 · SEVERITY, CARRIED.** Every member cites a grade **someone else established** and names
them. Where nobody has graded it, the cell reads `ungraded` — which is a fact about the record, not a
low grade.

⭐ **This extends the 09-07 SCAN's slate register rather than replacing it**, per ruling **R-d**. Nine
buckets map onto existing slates 1–11 · Z; **three slates are added** (12, 13, 14) for material the
SCAN pre-dates.

| bucket | kind | SCAN slate |
|---|---|---|
| **B1 · 🎨 UX & DESIGN COHERENCE** | one CRAFT LIBRARY | ⭐ **new · slate 12** |
| **B2 · ⚙️ THE LEAK BAND** | one SEAM | slates 2 · 5 · 6 |
| **B3 · 🚪 THE DOOR AND THE PERSON WHO RETURNS** | one JOURNEY | slates 3 · 9 |
| **B4 · 🗂 THE USER'S OWN RECORD** | one SUBJECT | ⭐ **new · slate 13** (minted at DISPOSE 2026-09-08) |
| **B5 · 🏡 WHAT FILLS A PLACE** | one SURFACE | slates 1 · 4 · Z |
| **B6 · 📡 WHAT EMITS AND WHAT READS IT** | one CONTRACT | ⭐ **new · slate 14** |
| **B7 · 🔬 THE INSTRUMENT THAT WATCHES THE BUILD** | the release instrument | slate 7 |
| **B8 · 🛠 THE LOOP'S OWN RECORD** | the loop's own record | slate 10 |
| **B9 · 🔧 A DIFFERENT PERSON IS THE USER** | one AUDIENCE | slate 8 |
| **B10 · 🧊 OWNED ELSEWHERE, OR BY THE WORLD** | not ours to bucket | slate 11 |

⚠️ **A member may appear in two buckets.** Cross-listings are marked `↔` and are named on both sides —
an item that is *both* a colour seam and a design-library question is not made single by picking one.

---

## B1 · 🎨 UX & DESIGN COHERENCE — ⭐ MINTED HERE, ON PAUL'S DISPOSE RULING

> *"I would wanna build up a bucket or theme of just, like, UX and design work… so we continue to
> build a UX design library and do things cohesively."*
> — `[paul-stated 2026-09-08 at DISPOSE]`, `feedback-dispositions.json` key
> `home|est-e6696a|feedback|fb-gu2zv3t9-mtt494f1`

⭐ **This closes queue Q2.** Record `fb-gu2zv3t9` was disposed **`act`** to a theme that did not exist;
CARRY correctly declined to mint it (*"the disposition sites it at GROOM & BUCKET"*). It exists now.

**What makes this a KIND and not a grab-bag:** a member belongs here when **the remedy is a rule in the
design library, adjudicated against the other members as a set** — not when it is merely visual. A
one-surface visual fix with no library consequence stays a row. ⭐ **The library already exists**
(`~/.claude/design-principles/`, 983 lines) and **`/ux-sweep` pass 2 adjudicates every finding against
it automatically**, so this bucket has a working instrument on day one rather than a proposed one.

| member | severity, as carried | who established it |
|---|---|---|
| `.told-row` is a **divider** grammar in an **accent-rail** app (`viewer.html:4236–4240` vs `.br-{type}`) | `act` — *"a formatting question, not a card question"*; mechanism `measured` in source | **Paul** at DISPOSE · verified by **user-researcher** (READ §1·3rd) |
| the first onboarding screen asks **seven things** before the product says what it is for | seat finding, ungraded by Paul | **`mom` seat**, `bfa3f23` battery |
| browser-default **blue radios** in a Stone-and-green product ↔ B2 | seat finding; partly owned by TIER 2 · 17 | **`wide-eyed` seat** |
| **TIER 2 · 21** — the masthead utility bar is not aligned to anything | row is explicitly marked 🎨 **ux-expert's** | **Paul**, `[paul-walked 2026-09-08]` |
| **TIER 2 · 17** — colour has four axes and the engine knows one ↔ B2 | ⚙️ engine · **must-not-diverge** | **Paul**, `[paul-commissioned 2026-09-08]` |
| the 💬 bubble **overlaps type and clips a control** at 414 × A+ | ⚠️ — carried to **A4 · W8·a**, whose 2026-08-01 decision is unbuilt | **`wide-eyed`** + **`owner`** seats |
| the jump-strip chip **truncates a 34-char place name and drops "Almanac"** ↔ B5 | ⚠️ — carried to **TIER 2 · 10**, sequenced with · 20 | **`wide-eyed` seat** |
| `.cel-event-name` **truncates, and A+ makes it much worse** | ⚠️ — sits in § WAITING ON PAUL since lap 4 | **lap 4**, 2026-08-19 |
| **TIER 1 · 6** — the ✓ on the ack ribbon | ⏸ **held open by Paul**, 2026-07-29, *"we can keep that as an option"* | **Paul** |
| ⭐ **the two-pass `/ux-sweep` is OWED** | 🔍 **OWED** — 8 days, **120 commits** to `viewer.html` against a limit of **20** | **`tools/check-ux-sweep.py`**, `measured` today |

⭐ **The sweep line is the bucket's own supply.** As of today's ruling, `/ux-sweep` **runs when due at
OPEN** — so this bucket does not depend on anyone remembering to feed it. Pass 1 browses un-primed;
pass 2 adjudicates against the library. ⛔ **And the standing constraint rides with it:** *a single-seat
review does NOT reset the clock; only a two-pass run does.*

> ⭐ **THE BUCKET'S OWN FALSIFIER, pre-registered:** *if two consecutive laps close and no member of B1
> has produced a line in `~/.claude/design-principles/`, this is a label and not a library — dissolve
> it back into its rows.* The bucket exists to make design work **cohesive**; a container that
> accumulates findings and emits no doctrine has not done that, and should be said to have failed.

---

## B2 · ⚙️ THE LEAK BAND — one estate's content inside shared machinery

**Kind: one SEAM.** Everything here is the same defect on a different substrate — *an instance value
living where the engine lives.* The SCAN grouped slates 2 · 5 · 6 by this key and it still holds.

| member | severity, as carried | who established it |
|---|---|---|
| **TIER 2 · 14** — one digest, N estates | ⚙️ engine · **must-not-diverge**; floor SHIPPED 2026-09-08 | **Paul**, `[paul-ruled 2026-09-08]` |
| **TIER 2 · 15** — the model routes are not modular; **60 hardcoded place literals against 43 derived** | ⚙️ engine · **must-not-diverge**; named in `CLAUDE.md` as **BACKLOG TIER 2 · 15** | **Paul**, `[paul-stated 2026-09-08]` |
| **TIER 2 · 17** — colour has four axes ↔ B1 | ⚙️ engine · **must-not-diverge** | **Paul**, `[paul-commissioned 2026-09-08]` |
| **TIER 2 · 23** — estate-neutrality is one-directional (`pages-deploy.py:264`) | ⚙️ engine · **must-not-diverge** | found at close-out 2026-09-08 |
| **TIER 2 · 12** — the per-estate canon store, one seam four consumers | ⚙️ engine · **must-not-diverge**; ⭐ *"plant capture is the WRONG unit"* | **Paul**, `[paul-ruled 2026-09-07/08]` |
| **TIER 2 · 8** — the per-estate capture write path (LEG 0) | 🔴 blocked on **its own first act** — the R-Z6 probe returned `HTTP 401` = UNCHECKABLE | **09-07 SCAN** §12c |
| slate 5 — six wildlife domains have **no marker path at all**, so they cannot produce a card | ⚠️ blocked on **card SUPPLY**, not schema: 8 benched, none approved, 5-slot cap | **`check-domains.py`** · SCAN §12b |
| `place-claims` — shared engine prose that claims a PLACE | red while any row is unclassified | **Paul**, `[paul-stated 2026-09-04]` |

⚠️ **The measured warning that rides with this whole bucket, from `CLAUDE.md`:** `check-estate-neutral`
read **✅ 311 needles / rendered=0** against the very origin where Fernwood's *"123 days, 30.83 inches,
OUR GAUGE"* rendered at three strangers' houses. **A green here is evidence about names and nothing
else.** Three readers of *"is this estate-neutral"* now exist and only two are instrumented.

---

## B3 · 🚪 THE DOOR AND THE PERSON WHO RETURNS

**Kind: one JOURNEY.** Everything a person meets between a link and their own place — including the
second time, which is the half nothing had ever walked.

| member | severity, as carried | who established it |
|---|---|---|
| **returning-recognition** — a device-local `fw-username` decides recognition and `onboarding/index.html:1982` returns **before** `/api/grant/whoami` at `:2032` | 🔴 | **`owner` seat**, RETURNING walk @ `bfa3f23`; carried to **C6 § the sign-in door** |
| the **finished-setup redirect is unwalked at any build** — a *fixture* property, not an oversight ↔ B7 | ⚠️ | **`owner` seat** + lap 4; carried to **TIER 2 · 22** |
| **the email a person gave appears on no screen** | ⚠️ — **three seats, two builds** | **`mom`** · **`wide-eyed`** @ `bfa3f23`, **`strict`** @ `95b8559`; carried to **TIER 2 · 18** |
| **TIER 2 · 18** — the account-lifecycle sweep (sign out · recover a password · recover a username) | ⚙️ engine; *"named, deliberately not scoped"* on Paul's own instruction | **Paul**, `[paul-stated 2026-09-08]` |
| **TIER 2 · 19** — the password field never says it is long enough | ⚙️ engine · small | **Paul**, `[paul-stated 2026-09-08, walking the door]` |
| **TIER 1 · 19 / 19b / 19c** — a person cannot found a second place; the places menu; settings on both surfaces | 🔴 and *"BOTH real users are about to want one"* | **Paul**, `[paul-stated 2026-09-06]` |
| **TIER 1 · 20** — login → SELECT → arrive: designed, recommended, **never built**, scattered across six artifacts | 🟠 | **Paul**, `[paul-stated 2026-09-06]` |
| **C9** — the invite flow | ⚠️ its shape is safe *only while Fernwood is the only estate with people in it*; re-rule **before instance 2** | **09-07 SCAN** §3 · B-9 |
| the **PO-box admission arrives three taps after the sell** | standing finding (b), **both builds** | **`strict` seat**; owned by `.plans/2026-09-08-setup-journey-PLAN.md` **B9** |
| ⚠️ **the setup-journey PLAN owns B2/B2i/B6/B7/B9/B10 and no ranked row cites it** ↔ B8 | `measured` — no `BACKLOG.md` or `PRODUCT-ENGINE.md` line names the file | **CARRY**, queue **Q3** — still open |

---

## B4 · 🗂 THE USER'S OWN RECORD — *already minted at DISPOSE, 2026-09-08*

**Kind: one SUBJECT.** ⛔ **Not minted here** — it exists at `BACKLOG.md:463` and this beat only moves
it (R1-c) into the THEMES region and records its membership. Paul's framing: *"'What you told me'
becomes its own kind of theme of just tracking, in general, what information we've been provided by
the user, what we wanna ask next, how we can use all that information."*

| member | severity, as carried | who established it |
|---|---|---|
| **TIER 2 · 16** — the ask surface, a standing epic | `objective: O1` | **Paul**, `[paul-ruled 2026-09-08]` |
| **TIER 2 · 18** — see it · correct it · remove it ↔ B3 | ⚙️ engine | **Paul** |
| **TIER 2 · 20** — what the Almanac IS, incl. *"maybe we change 'What you told me' to Your input"* | ⚙️ engine · **content-steward's** | **Paul**, `[paul-stated 2026-09-08]` |
| **TIER 2 · 21** — the masthead bar is why the record is unreachable ↔ B1 | 🎨 **ux-expert's**; the `mom` seat **never found the link** | **Paul** + **`mom` seat** |
| ⭐ **where does CORRECTION live** — a question neither framing asks; **18 is UPSTREAM of the card question, not parallel** | in-lane correction, `measured` against the shipped build | **user-researcher**, READ §1·2nd; carried at CARRY |
| record 3 · *should "What you told me" be a card at all* | disposed **OPERATOR INSPECTION**, ⛔ never *"what customers want"* | **Paul** at DISPOSE · provenance ruling by **user-researcher** |

---

## B5 · 🏡 WHAT FILLS A PLACE

**Kind: one SURFACE.** What a household actually sees on its own place, and where that content comes
from when the household is not Fernwood.

| member | severity, as carried | who established it |
|---|---|---|
| **TIER 2 · 11** — the weather card from an address | ⚙️ engine · declared; *"the top priority of what we need to get ready to implement in the next lap"* | **Paul**, `[paul-ruled 2026-09-07]` |
| record 2 · **the station indicator at a household with no station** — right words, **wrong component**; `viewer.html:16736` gates on runtime liveness and ignores the declaration; **4 of 5 instances** | **CUSTOMER EVIDENCE** — a fact-of-household claim, the one class the builder bias cannot reach | **Paul** (production report) · mechanism corrected by **user-researcher** at READ |
| **F1b** — the class is wider than the station: any Fernwood-derived string phrased as a **fault state** reads elsewhere as *"your home is broken"* | `inferred` — **a search instruction, not a census** | **user-researcher**, READ §2.1 |
| **TIER 2 · 10** — the glance consolidation (jump strip · tile row · cards) | ⚙️ engine · declared; shape fully ruled `GL-1…GL-13`; ⛔ **instrument G1–G6 FIRST** | **Paul**, `[paul-ruled 2026-09-07]` · sequencing by **09-07 SCAN** §12a |
| **"what grows there" is sold and not built** — Gardening and Wildlife are the only unbadged ranking items | ⚠️ — **three seats independently** | **`wide-eyed`** · **`mom`** · **`strict`**; carried to **TIER 2 · 16** |
| **TIER 2 · 7** — zones as a feature (the epic) | rulings `Z-1…Z-10` + decisions `R-Z1…R-Z6`; blocked on **LEG 0** | **Paul**, lap 3 design lane 2026-09-07 |
| **TIER 2 · 9** — Process B, the derived first draft from an address | 🔭 long-horizon | **Paul**, `[paul-ruled 2026-09-07]` |
| slate 1 — the four number defects on the weather and sky card | **build-ready**, estate-independent, *"no ruling, no person, no data about anyone"* | **09-07 SCAN** §12a, verified at HEAD |

⛔ **The standing hazard this bucket inherits, `measured` and unchanged:** *the geocoder has never run
outside QA.* `home`, `paul`, `bob`, `lab` all read *"the geocoder was not asked."* **Every weather
claim built on an address is built on a population of zero** until a retry fires. (09-07 SCAN §12g · 1)

---

## B6 · 📡 WHAT EMITS AND WHAT READS IT

**Kind: one CONTRACT.** ⛔ Its clause is Paul's own: **an event with no reader is not instrumentation.**

| member | severity, as carried | who established it |
|---|---|---|
| **TIER 2 · 13** — the telemetry census: v1 is a **CENSUS, not a build** | ⚙️ engine · declared | **Paul**, `[paul-ruled 2026-09-07, GL-8]` — *"we keep uncovering uninstrumented stuff, and we need to instrument it all really"* |
| § **EVERY ITEM SHIPS WITH AN ASK, A CHECK AND AN ATTRIBUTION** — four fields verifiable at the build commit (moved by **R1-b** to the lens region) | a standing contract, in force | **Paul**, `[paul-ruled 2026-09-07]` |
| **no activity sweep for production accounts** — `read-mom-engagement.py` has no `--env`, hardcoded to Mom's device on `legacy` | ⚠️ | **Paul**, `[paul-raised 2026-09-08]`; carried to **TIER 2 · 13** |
| `/api/onboarding-metrics` **has no GET route** — all three fresh seats report `UNREADABLE` | ⚠️ | the `bfa3f23` battery; carried to **TIER 2 · 13** |
| **345 `⚡ DIVERGENT` rows at qa**, and the tool *"has said `diverg` seven times per run since it was built and this block never named it"* | ⛔ named in `CLAUDE.md` as **the fifth instance of a capability the loop cannot reach** | **`watch-accounts.py`**, `measured` 2026-09-08 |
| the **geocode record nothing read** — `grep -n geocode tools/*.py` returned **ZERO** before `read-geocodes.py` was built | ⛔ *"a record nothing read"* | built 2026-09-07 to repair it |
| ⛔ **the backfill is ruled, and its constraint with it** | *"instrument OUTCOMES, never people"* — `watch-door.py`'s rule is the model | **Paul**, `[paul-ruled 2026-09-07]` |

---

## B7 · 🔬 THE INSTRUMENT THAT WATCHES THE BUILD

**Kind: the release instrument.** Everything whose subject is *the walk*, not the product.

| member | severity, as carried | who established it |
|---|---|---|
| **gate ① keeps the best-scoring run per seat** (`release-gate.py:233-240`) — so a failing returning walk is **invisible to the gate** | 🔴 · ⛔ **do NOT patch with `(seat, journey-kind)` first** — the three-axis redesign dissolves the unit | **lap 4**; ordering ruled in § SPLIT THE JOURNEY |
| **TIER 2 · 22** — the gate's own camera cannot see the stop it was built for; **two of four seats REFUSED TO TESTIFY** and were right | ⚙️ engine · **release instrument** | **`mom` + `strict` seats**, gate ① battery @ `95b8559` |
| `personId` ≠ `signedInAs` in the transcript, by design and **undocumented** — two seats tripped, one nearly filed a false alarm ↔ B8 | ⚠️ | **`strict`** · **`wide-eyed`**; carried to **TIER 2 · 22**. ⚠️ **The 09-07 SCAN's K12 says this is a caution, not work — Q12** |
| § **SPLIT THE JOURNEY FROM THE READER** — the three-axis redesign (journey · reader · **properties**), moved by **R1-d** | `[paul-proposed 2026-09-08]` — ⛔ **not in force**; *"it multiplies, and Paul named the mitigation in the same breath"* | **Paul** |
| **11 runs belong to no round** — `no-transcript` 6 · `contaminated` 3 · `build-unrecorded` 2 | refused by the instrument's own keys | **`walk-integrity.py`**, `measured` today |
| ⛔ **the evidence is QA.** Gate ① green at `qa` proves nothing about production `home` | necessary and **not sufficient**; *"PAUL WALKS PRODUCTION HIMSELF"* | the ratified cascade; printed by `product-steward --round` |
| **the 09-07/08 rounds ran through four HTTP 429s from the ERA5 archive**; one panel still loading when a walk ended | ⚠️ **do not read this lap's weather figures as typical** | two seats, `bfa3f23` battery |
| 🔬 **PROPOSED BEAT 10b · the customer-journey update** — *"the next battery's seat briefs cite it"* | ⛔ **NOT IN FORCE** — deliberately proposed beside the beat table | **Paul**, `[paul-stated 2026-09-08]` |

---

## B8 · 🛠 THE LOOP'S OWN RECORD — ⭐ *the bucket Paul's process-first steer needs in order to be actionable*

> *"if there were backlog and process related items in the backlog, let's consolidate those and
> implement them first, so we don't wind up back in a situation where we don't enact the fix… because
> the project can't see the inbox."* — `[paul-ruled 2026-09-08]`

⛔ **THIS BEAT MAY NOT PUT THEM FIRST. That is ranking, and it is Paul's at COMMIT.** What this beat
owes is that they be **findable as one set**, and that is what B8 is. ⚠️ **And the honest caveat: three
buckets could each be read as "process."** B8 is the loop's record of itself; **B7** is the release
instrument; **B6** is instrumentation of the product. Which of the three Paul's *"process related"*
means is **Q13** — and picking for him would be ranking by definition.

| member | severity, as carried | who established it |
|---|---|---|
| ⭐ **the rationalization itself** — §1 of this file | 📋 **OWED**, head-gap **672** against a limit of 400, widened **162 lines by this lap's own beats** | **`check-backlog-drift.py`**, `measured` |
| **26 `.plans/` documents no `BACKLOG.md` row cites** — plus **1 uncited seat trail** (`.user-research/2026-09-08-localized-feed-and-property-type.md`) | 🔴 141 readiness flags across 38 plans; *"orphan"* | **`check-backlog-ready.py`** · **`product-steward --triggers` T2**, `measured` today |
| **20 ruling lines in the chronicles that nothing in the register carries**, + 8 UNCHECKABLE | 🔴 T1 | **`product-steward.py`**, `measured` today. ⚠️ CARRY carried **none** of them — a stated gap, not a clean sweep |
| 🔴 **`--record` has been dead since 43 minutes after round 1** (`tools/product-steward.py:1135` calls a `cmd_record` that `890efd5` deleted) — **and `--selftest` is green over it**, 24 clauses, none exercising `--record` | 🔴 · *"a trial that is not instrumented is renewed by inertia"* | **CARRY**, found by running the charter's own command — queue **Q6**, open |
| **nothing checks that a disposition's named destination row exists** — `fb-vurlf77f`'s `why` cited **TIER 1 · 11** and folded into **TIER 2 · 11**; ✅ instance fixed `b998b30`, **class open** ↔ §1.3 | a defect **with a location** | **CARRY**, queue **Q1** — the instance closed, the class did not |
| **nine live ordinal collisions** across the three tier tables ↔ §1.3 | `measured` today; produced one real mis-citation | this beat |
| **the freeze register was designed and never built** — `freeze.json` and `tools/freeze.py` do not exist; `git grep 'FOCUS FREEZE' -- '*.py'` returns **zero** | ⛔ *"a rule whose mechanism is never made is the thing this rule exists to prevent"* | named at ratification, `[paul-approved 2026-09-07]` |
| **TIER 2 · 24** — two red session-start checks no lap has ever read, and **the lap that created their subjects is this one** | ⚙️ engine | found at close-out 2026-09-08 |
| **L2** — the concurrency guard has one state slot and two laps can share a repo | 🟠 | 2026-09-01 |
| **C2** — there is no feature-development process, and the prototyping tool has run **once** | ⚙️ engine | **Paul**, `[paul-raised 2026-09-02]` |
| **C3** — the trace is a query, not a file; the founding leak is located | ⚙️ engine | **practice-steward**, 2026-09-02 |
| ⛔ **a seat's RULING-SHAPED output has no route to a decision** — `54e3b57` named the beat-2/beat-12 deadlock on 09-07; **it sat a day** and Paul reached it independently | ⭐ *"the process finding is not the deadlock"* | **practice-steward** 09-07 · recorded in `CYCLE-MAP.md` beat 2 |
| ⚠️ **`release-state.py` reports `beat 9/12 (Paul walks it)` while this lap is running beat 5** | `inferred` — the derived beat may be reading a *previous* build's evidence as this lap's position; `check-release-docs.py` is ✅ | this beat — **Q10** |
| **the 09-07 grooming SCAN's kill list: 11 of 12 applied in 5 days, with nothing tracking that they had** ↔ §1.5 | `measured` today | this beat |

---

## B9 · 🔧 A DIFFERENT PERSON IS THE USER — Track B, and Paul's own door

**Kind: one AUDIENCE.** ⚠️ Its rows are not blocked technically: **WORK is lifted, PUSH is frozen**
`[paul-stated 2026-09-06]` — it may be built and may not ship to her.

| member | severity, as carried | who established it |
|---|---|---|
| **P1 · P2** — the Husqvarna and Homelite manuals are for **different machines** | ⛔ *"not a naming quibble"* | fleet lap 1, 2026-09-01 |
| **P3 · P4 · P5** — `DR200SE` vs `DR200S`; the handwritten Bronco parts list, still unread; three Bronco checks past 60 days | ❓ / 📄 / 🔩 — **only Paul can close them** | fleet lap 1 |
| **P6 · P8 · P9** — the water heater record vs a 2025 photograph; a possible second rear-window switch; a 5/8" bolt rounded off and seized, **nothing knows about it** | 🚿 / 🪟 / 🪛 | fleet lap 1 beat 3, reopened |
| **B0** — Track B has **no ask loop**, and that is the only thing the two tracks do not share | ⛔ *"the one real asymmetry is not domain — it is AUDIENCE"* | `measured` 2026-09-01 |
| **V1 · V2 · V4** — 8 of 22 machines carry an identifier, 14 carry none; the masking protected nothing and broke the pointer | 📊 / ⛔ / ▶️ | V-series, `measured` 2026-09-01 at Paul's ask |
| **B1 · B3 · B4 · B5 · B6 · B7** — live obligations · data collection · photo layer · Nest · household systems · 3D Bolores | mixed; **no live deadline**, next hard date 2027-06-03 | `BACKLOG.md` Track B |
| slate 9 — **Paul's own door**, the capture surface he does not have | grouped with slate 8 by audience | **09-07 SCAN** |
| ⛔ **the standing rule** — *do not resurface old Bolores issues from the old documentation* | in force | **Paul**, `[paul-stated 2026-09-01]` |

---

## B10 · 🧊 OWNED ELSEWHERE, OR BY THE WORLD

**Kind: not ours to bucket.** ⭐ Named rather than groomed, so nobody re-grooms them.

| member | who owns it | what would move it |
|---|---|---|
| slate 11 — **Mom's frozen instance and the catch-up** | **the world** | Mom having a real account on production. Her invite **`p-b91e4d` is minted and, at last reading, unspent** |
| **Mom's held feedback** | ⛔ **Paul's word, and only that** — *"I will say when to lift the freeze"* | not a shipping condition; a named human act |
| **C8** — the condo build-out | a compound gate stated in the row: C4 · C5 · C7 shipped **and** the freeze lifted | *"a fresh request by construction when the gate opens"* |
| ⭐ **Mom has not asked for anything lately** — *"She's been very busy"* | **Paul**, asked at READ this lap | ⛔ **record that as a REASON, not as silence.** The next lap must not re-read this window as disengagement |

---

# 3 · WHAT I COULD NOT BUCKET — six questions

⛔ Each is an OPEN question. It is not a finding, a recommendation, or a decision, and **this seat may
not close one.** Format follows the queue's own template; these are **Q7 … Q12**, continuing from
CARRY's Q1–Q6.

### Q7 · Nine live ordinal collisions. Mandate the qualified form, or mint stable slug ids?
- **the finding:** `measured` — TIER 1 ∩ TIER 2 collide on **11, 14, 15, 16, 18, 19, 20**; TIER 1 ∩
  TIER 3 on **6**; TIER 2 ∩ TIER 3 on **7**. One has already produced a wrong citation in a tracked
  file (`feedback-dispositions.json`, fixed `b998b30`).
- **why it could not be bucketed:** ⛔ **renumbering is not available** — eleven citations live outside
  `BACKLOG.md`, six of them **inside `worker/worker.js`**, and a renumber would silently falsify
  comments in a running Worker. So the choice is between **C1** (mandate `TIER n · m`, cheap, does not
  fix the class) and **stable slugs** (fixes the class permanently, is a schema change and a rewrite of
  every external citation). **A seat that picked would be choosing the file's identity scheme.**
- **what would close it:** Paul's word on C1 vs slugs, and on whether **C3**'s check is worth building.

### Q8 · Where does the archive go — a bottom region, or `BACKLOG-ARCHIVE.md`?
- **the finding:** 178 lines across four sections read finished with **no live rider** (`measured`),
  233 with the `inferred` fifth. ⚠️ **Archiving them closes ZERO head-gap** — they all sit below TIER 1.
- **why it could not be bucketed:** the precedent cuts both ways. `PICKUP-LOG-ARCHIVE.md` says a
  companion file; `## KILLED / SUPERSEDED` at `:3735` says a bottom region, and both are live in this
  same file today. **Two right answers is not a thing a seat resolves.**
- **what would close it:** Paul's word. ⚠️ Whichever he picks, **three of eight ✅-headed sections carry
  live work** (§1.4) and must not be swept in with them.

### Q9 · 26 plans and 1 seat trail that no ranked row cites — is a plan required to have a row?
- **the finding:** `check-backlog-ready.py` flags **26 orphans**; `product-steward --triggers` T2 flags
  `.user-research/2026-09-08-localized-feed-and-property-type.md` as uncited. This is **Q3
  generalized** — Q3 asks it of one plan; the census says it is a property of the corpus.
- **why it could not be bucketed:** ⛔ **the predicate may be wrong, and that changes the answer
  entirely.** A `-PROPOSAL` that was considered and set down *should* have no row. If "orphan" means
  *considered and closed*, 26 is healthy and the check is over-reporting; if it means *unreachable
  work*, 26 is the largest single reachability gap on this board. **Nothing distinguishes them today.**
- **what would close it:** a ruling on what an orphan means — or a `closed:` header key that lets a plan
  say it is finished, which would make the count read honestly either way.

### Q10 · `release-state.py` says beat 9/12 while the lap is running beat 5. Which number is the lap's position?
- **the finding:** `measured` today — `release-state.py` prints *"FIRED · beat 9/12 (Paul walks it) ·
  owner: paul · candidate bfa3f23 · seats pass: True"* while lap 5 is executing **GROOM & BUCKET (5)**
  and has not reached COMMIT. `check-release-docs.py` is **✅ green** — the map and the code agree on
  the beat *list*, so this is not the drift that check looks for.
- **why it could not be bucketed:** `inferred`. The derived beat may be reading **evidence state** (a
  gate that passed on a previous build) where a reader takes it for **lap position** — or the lap's own
  reckoning may be the thing that is wrong. **Which one is the defect is a judgement**, and
  `check-release-docs`'s own rule is *flags, never edits* — *"which of the two is right is a judgement,
  and it has gone both ways."*
- **what would close it:** Paul's word, or a ruling that `release-state` publishes both readings
  separately. ⚠️ It matters because **`beat.owner: paul` is how this loop says a human gate is open**,
  and it is currently saying so at the wrong gate.

### Q11 · Does the head region get a declared LINE BUDGET — and what happens to § FOCUS FREEZE?
- **the finding:** R1 buys **219 lines of headroom**; this lap alone spent **162**. `measured`: at lap
  5's own rate the check is red again in **~1.4 laps**, and beat 5 re-runs the identical move forever.
  ⭐ Separately, § FOCUS FREEZE (69 lines) is the one head section this proposal keeps, and it has the
  same defect one altitude down: **five successive amendments** (09-03 → 09-04) each partly superseding
  the last, so the operative state is derivable only by reading all five in order.
- **why it could not be bucketed:** a budget is a **process rule** and a freeze rewrite is a **judgment
  edit** — the 09-02 precedent is explicit that splitting is not a move. Both are Paul's.
- **what would close it:** a ruling on a head budget (and whether `check-backlog-drift` should measure
  it), and separately on whether the freeze block is rewritten to state its current position once.

### Q12 · K12 and TIER 2 · 22 disagree about `transcript.personId`. Which is right?
- **the finding:** the 09-07 SCAN killed it — *"a finding correctly recorded and then filed as a row…
  it has no v1, no falsifier and nothing to build… that belongs in the walk harness's own
  documentation, next to the field."* CARRY then wrote the **same finding** into **TIER 2 · 22** at F13
  as a member of *the gate's own camera*, on the strength of **two seats tripping on it and one nearly
  filing a false alarm**.
- **why it could not be bucketed:** ⛔ **both are defensible and they imply opposite acts.** K12 says
  write a comment; TIER 2 · 22 says the harness's undocumented fields cost seat-hours and produce false
  findings, which is work. **Neither reading is stale** — K12 is one day old and F13 is hours old.
- **what would close it:** Paul's word, or a citation showing the two are about different scopes (the
  field's documentation vs. the class of undocumented harness fields), in which case **both stand**.

### ⚠️ Q13 · Which of B6, B7, B8 does *"process related items"* mean?
- **the finding:** Paul's steer — *"consolidate those and implement them first"* — names a set this
  beat must make legible. Three buckets each answer to the name: **B8** (the loop's record of itself),
  **B7** (the release instrument), **B6** (instrumentation of the product).
- **why it could not be bucketed:** ⛔ **answering it IS the ranking.** *"Implement them first"* is a
  COMMIT-beat act, and naming which bucket goes first is exactly the verb this seat does not have.
- **what would close it:** **Paul, at COMMIT.** ⭐ The board's job was to make the three findable and
  distinct so the question is one sentence rather than a re-read; that part is done.

*(Q7–Q13 are appended to `.plans/2026-09-07-product-steward-queue.md`, the charter's one queue file.)*

---

# 4 · THE INSTRUMENT, READ HONESTLY

**Round 3 of the R7 trial** — the round `--ledger` names as *"the first that could be read as a rate."*

| | measure | this round |
|---|---|---|
| **R7** | ≥80% of rows already written before the seat ran → redundant | 10 buckets, **9 built from rows and slates that already existed**; **1 minted** (B1) on a ruling that explicitly sited it here. ⭐ **That is the correct reading for a GROOM beat and it is NOT the R7 measure** — R7 counts *carried findings*, and this beat carries none by design |
| **§6.7** | questions-opened > writes → a bottleneck wearing a helper's name | **7 questions** against **0 BACKLOG writes**. ⛔ **IT FIRES ON ITS FACE — and the falsifier is mis-specified for this beat.** GROOM & BUCKET's own exit condition forbids writing to `BACKLOG.md` at all (*"it FLAGS; it never reorders"*), so the write count is **structurally zero** and the ratio is meaningless here |

⛔ **THAT SECOND CELL IS A FINDING ABOUT THE CHARTER, NOT A RESULT.** §6.7 was written for CARRY, whose
job is to write into rows. Applied to a beat whose job is to **propose and not touch the file**, it
reports a bottleneck every time, forever. ⚠️ **This is not a request to relax it** — it is a request
that it be **scoped to the beat it was written for**, or given a second form for GROOM. Recorded here
rather than in a commit message, which is this corpus's own most-repeated loss.

⛔ **AND THIS ROUND STILL CANNOT BE RECORDED.** `--record` remains dead (queue Q6, unrepaired at
`71119d6`). **`--ledger` will report two rounds when three have run**, and the trial's own stated
settlement condition — *three rounds, at least one clean* — is unreachable while its writer is broken.

⭐ **The confound, stated as the first two rounds' were:** this round read a board that **its own lap
had grown by 162 lines in one day**, and it proposes moving four sections **two of which that same lap
created**. A beat that grooms its own lap's output is not reading a steady state. **Round 3 is
therefore not the clean round either** — and three confounded rounds in a row is itself a reading about
the trial's design.

---

## Falsifier

**This round is falsified if any citation above does not resolve** —
`python3 tools/product-steward.py --cite .plans/2026-09-08-lap5-BOARD.md`.
⚠️ Its known limit, from the charter: `--cite` proves a citation **resolves**, never that it is **apt**.

**And it is falsified a second way, specific to this beat:** if applying §1.7 does not produce a
head-gap of **181** and a green `check-backlog-drift`. That is a *deterministic, pre-registered*
prediction, and it is checkable in one command the moment the moves land.

## QA

```
python3 tools/check-backlog-drift.py     # 672 today; 181 predicted after §1.2
python3 tools/product-steward.py --cite .plans/2026-09-08-lap5-BOARD.md
python3 tools/check-backlog-ready.py     # the 26 orphans of Q9
python3 tools/check-ux-sweep.py          # B1's supply: OWED, 8d / 120 commits vs 20
git status --porcelain BACKLOG.md        # ⛔ MUST be empty — this beat applied nothing
```
