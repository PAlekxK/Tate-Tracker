# LANE A — the business-analyst seat

Read `_PREAMBLE.md` first. It binds.

## OWNS (the only paths you may write)
- `~/.claude/agents/business-analyst.md`
- `~/.claude/agent-foundations/business-analyst.md` (+ its `~/.claude/skills/` symlink if the onboarding ritual creates one)
- `~/Developer/Tate-Tracker/.private/lane-a-notes.md` (scratch; gitignored)

## MUST NOT TOUCH
Any other file in `~/.claude/agents/` · `~/.claude/settings.json` · `MEMORY.md` or
any memory file · any tracked file in `~/Developer/Tate-Tracker`.

⚠️ **`~/.claude` currently has 7 uncommitted files from another session.** Explicit
paths only — rule 2 of the preamble is live here, not theoretical.

## The task
1. Run `/onboard-agent business-analyst` and let the ritual drive.
2. The seat's remit, from Paul's 2026-09-04 voice memos:
   - **Offering tiers** — access to the app · access to modules within it · access
     to updates/revisions driven by feedback. These are three different things and
     he wants them separated.
   - **The feedback SLA** — the open question: someone submits feedback; how long
     until they see a change? Today that requires Paul at a terminal running the
     mom cycle. Name the constraint; don't solve it.
   - **The hands-on setup interview** as a possible differentiator — what is done
     in-app vs. what Paul does with the person, then builds in the background.
   - **Target customer.** His framing: not "old" — *people who have stuff*, which
     correlates to wealth, which correlates to older. Build the profile as
     hypotheses, tagged, not as assertions.
   - ⭐ **The vision line, sourced to Bob's interview:** document everything you
     have and how to care for it, so it passes cleanly to his daughters. He cited a
     family friend whose father died unexpectedly and the family had to piece it
     back together. This is the most load-bearing sentence in the memos — it is a
     real person's stated motive, not a market inference. Treat it as such.
   - Paul also wants this seat to *maintain* the thinking continuously and run the
     latest on it — a standing seat, not a one-shot report.
3. Read `.private/voice-memos/2026-09-04-1220-*.txt` and `...-1342-*.txt` for the
   raw substance. Read `.private/` sibling research for Bob's actual interview
   before quoting him — **do not quote Bob from the transcript alone.**

## Monetization constraint — read before proposing anything
`[paul-decided 2026-08-13]` **Monetization is DEFERRED to ~end-2026.** Portfolio
now, revenue later. This lane builds the *model and the thinking*; it does not
propose launching a revenue play. If your output reads like a go-to-market plan
for this quarter, you have overshot.

## GATE (stop here and report)
The foundation file drafted, plus a one-page outline of what the seat would
produce first. **Paul stamps the foundation before the seat does real work.**
Do not have the new seat start producing analysis in this session.
