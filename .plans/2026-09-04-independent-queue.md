# Independent queue — 2026-09-04 (after the 11:50 AM ET rulings)
<!-- Paul: "set up a queue of everything that you can work on independently and run through it." Ordered; each item
     names the GATE it ends at. QA line only (staging). Mom's channels HELD. main untouched. -->

| # | item | class | ends at |
|---|------|-------|---------|
| 1 | **Vocabulary + nicknames plan** — concept registry (place · record · journal · safe · library · station…), per-estate display names with provenance, a person may later name a thing and the internal id never moves; seats: content-steward + engineering-partner | plan | **decision** — the scheme + the condo's record word |
| 2 | **Engine: the record's name is one key** — `journalTile/journalShort` → the registry's record keys; the Worker's `{journal}` → `{record}`; the 12 hard-coded "Almanac" strings route through the fill; a lint that greps engine text for concept words | engine → QA | green lint; Fernwood byte-identical |
| 3 | **C4 2b hygiene** — `momQueue.answered/snoozed/offered` estate-segmented before a second estate exists | engine → QA | check-storage-keys green; her keys migrate or are declared |
| 4 | **C7 engine rows** — R2 an OFF module looks intentional · R5 events gated by a module (part 1 shipped 6:20 AM: events declared absent at the condo; part 2 = fresh content up, a ux call) · R3 the manual-vs-automated population view — **measured 12:55 PM ET:** a code-derived writer scan is too noisy (a reader near a write call looks like a writer), so R3 needs a WRITERS roster + a guard that proves each row (the STORAGE_KEYS shape), then the recipe derives the table | engine → QA | condo walk clean; Fernwood byte-identical (R1 waits on a ux seat then Paul) |
| 5 | **C6 vault** — 5a the routes as a lookup · 5b the card declared per instance | engine → QA | qa-write-probe; 4a/4b/5c stay Paul's |
| 6 | **UX sweep, two-pass, on QA** — owed on 56 viewer commits | review | punch list, fixes gated |
| 7 | **Onboarding plan** — THE GOAL, and `[paul-stated 12:05 PM ET]` **the CONDO is the first run, and naming is a step the person answers** — a link → an account → first run at 414 × A+ → her data reaches the estate server-side; seats: user-researcher · ux-expert · engineering-partner · content-steward (the message copy) | plan | **proposal** — Paul stamps; the message itself is his |
| 7a | **The customer journey, laid out** `[paul-stated 2026-09-04 ~12:35 PM ET]` — *"a good customer journey laid out — what are all the interactions and requirements: naming yourself, setting your username, password, email, setting your color scheme, default text size — let's think through the things we've been building to be flexible and make them part of the onboarding process, with the focus on making everything feel super personalized."* → user-researcher journey map + personalization inventory (every instance/config value a person could set: names · theme color · text size · modules on/off · station · credential), each tagged assumption/inferred/validated; feeds the onboarding plan's steps | research | journey map → the onboarding plan |
| 7b | **END-TO-END JOURNEY TEST** `[paul-stated 2026-09-04 ~12:20 PM ET]` — *"full end-to-end testing … of UX review, actually walk through this login process according to an invite message and a link that we provide … test generative AI here being able to check both sides of the journey"* — a walk seat that drives the REAL flow on QA from the message text → the link → the account → first run, and reads it from BOTH sides (the person receiving; Paul minting and sending); the sibling of `/ux-sweep`'s fresh-eyes pass, sited in the onboarding plan | review | a repeatable walk with a punch list; fixes gated |
| 7c | **Process wiring review** `[paul-stated, same]` — the practice-steward reviews ALL process documentation and says where the latest thinking (the parallel-QA model + divergence ledger · onboarding by link · naming as a first-run step · E2E journey testing · the independent queue) slots in *"cleanly and in a way that gets wired in so it gets used"* | audit | findings + proposed wiring, Paul stamps |
| 8 | ✅ **Guru cost analysis** — DELIVERED 1:15 PM ET: `.engineering/2026-09-04-guru-cost-analysis.md` (cold turn 19¢ → 3¢ on the core path; her bill is cold turns; keep the 5-min TTL) | analysis | numbers to Paul |
| 9 | **Docs** — README title (stale by two renames) · handoff clearing-state | docs → staging | — |

Report-only (not mine to fix): the two orphan plans flagged by check-backlog-ready belong to the grooming session; the
18:00Z weather recorder run is the first since the KV cap lifted — read it, don't re-run it.
