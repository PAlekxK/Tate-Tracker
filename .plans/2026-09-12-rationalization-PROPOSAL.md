# The FOURTH backlog rationalization — PROPOSED, not applied

- row: `BACKLOG.md` § ▶️ NEXT (process; the rationalization is applied as a diff Paul approves, never as a row of its own)
- objective: O3 · class: engine · declared
- stage: `concept`
- ready: ⛔ **agent-proposed — Paul rules.** No `ready:` stamp is written by an agent.
- seats: practice-steward waived (this is reading order, not process design) · every other seat waived (no surface, no model, no person studied)
- Author: the standing backlog window, 2026-09-12, at `3a206ba1`

---

## ⛔ THE TRIGGER IS PAUL, NOT THE DETECTOR — and that changes what this may propose

`check-backlog-drift.py` reads **RESTED** (exit 0): *"Last 2026-09-11 (1d) · 25 sections above the
tracks · ranked list 260 lines below its head."* Its threshold is **400** lines and **30** days.
⭐ **So nothing is owed, and this proposal must not pretend otherwise.** `[paul-asked 2026-09-12: "and
re-rationalize our backlog"]` is the whole warrant.

⚠️ **Which means SIZE IS NOT THE THESIS.** The file is 5,196 lines and 93 sections and the detector is
right that this is healthy growth. **The defect is that the priority ORDERING changed today and the
document still reads in its old order.**

## The thesis, in one sentence

`[paul-ruled 2026-09-12: "let's adopt this chain as our priority"]` made **reachability** the first
question — and a reader hits **⏭ THE NEXT TWO LAPS (readiness, L95)** *before* **⛓ THE DEPENDENCY CHAIN
(reachability, L135)**. ⛔ **That is exactly backwards under the ruling he just made**, and a lane
reading top-to-bottom will pick a ready item that cannot ship.

## The moves — reading order ONLY, every move a MOVE

⭐ **Same discipline as the 2026-09-02 run: no row deleted, no row's status changed, nothing restated.**

| # | move | why |
|---|---|---|
| **1** | **§ ⛓ THE DEPENDENCY CHAIN moves ABOVE § ⏭ THE NEXT TWO LAPS** — whole, unedited | the adopted ordering is read before the readiness queue it now governs |
| **2** | § ⏭ THE NEXT TWO LAPS gains a **two-line pointer head** | so a rung there is read as *readiness*, and the reader is sent to the chain for *reachability*. ⛔ A POINTER, never a restatement |
| **3** | § ▶️ NEXT's pointer head gains one line naming the chain | that block declares "read this for what now"; it does not currently name the ordering |

## ⛔ What this deliberately does NOT propose, with the reason for each

- ⛔ **NOT moving the 25 struck rows out of TIER 1.** They are struck, which is this file's convention —
  *a correction is recorded, never silently swept.* Moving them is a **judgment edit**, and the 09-02
  run explicitly declined the same shape: *"splitting is a judgment edit, not a move."*
- ⛔ **NOT splitting TIER 1** despite 127 numbered rows. Size is not the defect and the detector says so
  on its own face. A split on volume would fire on healthy growth — the exact failure the detector was
  built to avoid.
- ⛔ **NOT re-ranking any row.** The chain orders *layers*; Paul ranks *rows* at beat 6. This proposal
  touches neither.
- ⛔ **NOT resolving the 8·3 / 9·2 conflict** (the weather card is ruled first; the chain places it above
  an unbuilt root). ⭐ **That is a live question for Paul and a rationalization may not settle a ruling
  by reordering the document around it.**

## Falsifier

If a lane reads the reordered head and still picks a layer-2+ item while layer 1 is unbuilt, the move
did not do its job and the fix is a **check**, not more reordering.

## What it costs to reverse

One `git revert`. The moves are contiguous-block relocations; the old order is preserved in git, as with
all three prior runs.
