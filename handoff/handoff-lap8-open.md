# Handoff: fernwood — OPEN LAP 8 ON ROW T (the testing architecture), its own fresh window

<!-- generated 2026-09-11 10:22 EDT · source: Tate-Tracker@59e6ccdb on LOCAL main · written by the lap-8 coordination window
     (tate-tracker-42) at hot context, with a QUOTED heredoc. RECEIVER: verify the sha against HEAD and that this file is
     not zero bytes before trusting it. Cite symbols; stamp every wall-clock from `date`, never authored — lap 7's authored
     stamps rendered an 8 h hold as ten minutes. -->

## 1. Mission

Paul, 2026-09-11: *"Close all the windows out when they're done with their work, and this window as well, and give me a
handoff to launch the next lap, focusing on testing, in a fresh window to manage context — do that as work actually
concludes, don't force it."*

**You open lap 8 and run it as the coordination window.** Lap 8's committed scope is **ROW T — the testing architecture —
ALONE**; the door rows join the beat-6 table only after the re-audit (§4). **Coordination ROUTES and never ABSORBS**
(memory `feedback_coordinator_routes_never_absorbs`): you own the chronicle (`cycle/release/CYCLE-LOG.md`), the freeze,
`release-state.py`, the gates to Paul (**question · recommendation · alternatives**), and the windows you open from briefs
via `python3 ~/.claude/tools/succeed.py --open <slug>`. You never write `BACKLOG.md` — the backlog window is the ONE DOOR.

⭐ **Every window from the last lap is CLOSED.** There is no live peer to check in with. You are alone on this tree unless
you open someone; check `ListAgents` before your first commit anyway.

## 2. ⛔ READ FIRST — the five things that are NOT in the plan

1. **`cycle/release/CYCLE-LOG.md`, from `### ✅ CLOSED — 2026-09-11 09:51 EDT` to the end.** Everything below is a pointer
   into it; nothing here restates it. In particular: Paul's fifteen rulings · the two watching rulings · the parked mom
   feedback · the pickup sweep **and its correction** · both windows' closes.
2. **`.plans/2026-09-11-testing-revamp-PLAN.md`** (`stage: ready`, all fifteen §13 questions **RULED**) and its **STATE AT
   CLOSE** stage-note — the eight things you would otherwise rediscover. **`.engineering/2026-09-11-testing-revamp-SIZING.md`
   §A is the BUILD AUTHORITY** for the 21 steps; the plan's §3 table is the order, not the spec.
3. **`.practice/2026-09-11-lap7-testing-cycle-AUDIT.md`** §3–§5 — why row T exists, with the numbers.
4. **`.plans/2026-09-11-lap8-build-PLAN.md`** — the DOOR rows, `stage: draft`, **written against `06c2a16` and NOT re-audited**
   since row T became first, since the account-first ruling, or since the apex. Treat every measured line in it as stale.
5. `VOCABULARY.md` §3i (three environments; households are ROWS) · CLAUDE.md's § THE ENVIRONMENT MODEL and its session-start
   block · memories `feedback_coordinator_routes_never_absorbs` · `feedback_commit_only_when_the_index_is_shared` ·
   `feedback_stamp_the_record_from_the_machine_clock`.

## 3. What ROW T is, in one paragraph

The gate's unit changes from a **folder name** to the **`(journey, lens)` cell**; every journey **declares** its routes,
pages, whether it should emit app events, and the **arrival state** it is entered in; a re-sha **re-runs only what the change
can reach** and prints the carried-forward pass with its byte proof; cells nobody walked **print UNWALKED** instead of being
absent; and the **tier each reading runs at is declared** rather than inherited. **21 steps, ≈28 h, and NOT ONE OF THEM MOVES
THE CANDIDATE** — so the first battery row T meets is the door's, in a later lap. Why it is first: the gate it replaces
certified `87c7aae` green **while twelve walks at that sha had failed an action**, because it kept one run per seat and broke
ties to the earliest. The door must not be certified by that gate.

## 4. THE ORDER PAUL RULED

**T lands WHOLE and FIRST.** No split without a **STRUCTURAL** reason a seat names (a ruling not given · a dependency on a
lap-8 row · a falsifier that cannot run before the door exists) — **never hours**; SIZING §D already applied that test to
seven candidates and found none. Then: **ux-expert closure** of the lap-8 plan's §10 four surfaces (the sign-in page · the
shelf after sign-in · the email editor + re-auth · the ribbon's empty state) under the **account-first** ruling →
**engineering-partner RE-AUDIT** of `.plans/2026-09-11-lap8-build-PLAN.md` (row T sized by symbol; the door rows
re-sequenced behind it; the seams with row H) → the build window's brief points at the **re-audited** plan.

**Row H FOLLOWS T and rebases on it** (plan §9): H1's second browser context is built by **T14's context factory**, never a
parallel `newContext` · H2's J9 carries **T10's four `JOURNEYS` keys** and sits in the declared cell list or the gate prints
it UNWALKED · the region-change stop for `href="#"` controls lands in H against **A11's** page, not the screen it replaces.
**And T owes A a duty:** every journey's routes/pages declaration is re-derived in row A's own commits as A11/A13/A7/A9 move
the routes — T10's subset selftest is what makes a stale declaration fail loudly.

⛔ **T6 is PAUL'S and carries TWO edits he makes at the open:** beat 8's exit condition (plan §5, quoted) **and** the
pilot-walk beat (S8). `check-release-docs.py` goes **RED between T5 and T6 — that is the checker working.** Do not quiet it.

## 5. ⛔ FOUR THINGS THAT MUST BE HANDLED AT OR BEFORE THE OPEN

1. **The plan fails the repo's own readiness check, and its CONTENT is fine.** `check-backlog-ready` exits 1 on the
   `.plans/` headers of **both** `2026-09-11-testing-revamp-PLAN.md` and `2026-09-11-lap8-build-PLAN.md`: missing
   `row:` / `objective:` / `class:` / `stage:` / `seats:` and the four required sections (`## Files touched` · `## Sequence`
   · `## Falsifier` · `## QA`); the revamp plan is an **orphan** (no `BACKLOG.md` row, objective `O5` absent from
   `OBJECTIVES.md`), and its `depends-on:` is an **annotated path** the checker reads as nonexistent. **Fix the filing before
   the build window reads it.** The ruling and the thinking are sound; only the trail is missing.
2. ⛔ **J3 is REFUSED for all five seats** (`walk-fixtures`), and **J3 sits in lap 8's proposed cell list three times.**
   J5 has no seat that can sign back in; J6 has no procedure; J8 needs its own credential. **The cell list cannot be walked
   until this is repaired** — size it as part of T, or declare the cells UNWALKED with the blocker named. Do not write a cell
   list whose cells cannot run.
3. ⛔⛔ **A HARD PRECONDITION ON LAP 8 · B (Mom's migration), which is NOT in this lap but must not be lost.** Two independent
   instruments read the same lockout shape at both her surfaces: `watch-accounts` (a credential live in the register and
   **absent from the store** at `home` for 22 h, and four qa grants that **vanished mid-run**) and `watch-door` (**`home` 42
   and `legacy` 227 `door_failed`, every one `unknown-or-other-estate`**, each showing **1 reached, 0 through**). ⛔ **Neither
   names a person, by construction**, so this is **NOT yet a claim that anyone is locked out** and much is plausibly
   synthetic traffic. **Before her row ever migrates, establish deterministically that her account can get through the
   door** — migrating a row whose credential the store may not hold moves a lockout, not a household.
4. 🔴 **The weather recorder is the 2026-08-08 shape again** — newest `weather-history.json` entry **2026-09-06**, five days
   old, recorder every 6 h. **Her weather card is the one card she demonstrably opens.** Two honest resolutions, both clear
   the flag: re-record (idempotent), or **acknowledge a genuine outage with the evidence that re-recording recovered
   nothing.** ⛔ Never add a date to the ack file to quiet a flag you have not understood. **Outside lap 8's scope; it is
   about the integrity of the record she reads, so it does not wait for a lap.**

## 6. State at this brief (measured, not relayed)

- **Production serves `87c7aae`** at `paul` and `home`; qa serves `87c7aae` and is **~68 commits behind HEAD**.
  `release-state.py`: *ARMED · beat 11/12 · owner paul · lap 7 closed · cleared_sha `87c7aae`* — the beat is a derived quirk
  (TIER 1 · 47), not lap 8's state. **`origin/staging` is level with local main** (pushed at Paul's word today).
- ⚠️ **RELAYED, NOT VERIFIED: a Pages/Worker split at `home`** — `post-deploy --env home` reports the Worker's `build_sha` as
  `05bfb6d7` against Pages serving `87c7aae`. **Its `expected 09661e3` is an ARTEFACT** of HEAD moving under the run, not a
  deploy anyone made. **Verify before acting.**
- **The pickup sweep ran all 50: 32 green · 17 red · 1 exit-3.** Read the chronicle entry **and its correction** — an earlier
  entry said nine were unmeasured and that is **false now**, appended rather than edited so the record stays auditable.
- **Other live reds not named above:** 730 undisposed feedback on our own envs (channel `geocode` at `home` holds 3 keys and
  **no tool reads it**) · 48 recovery requests, undisposed, names nobody · **18 ruling lines carried by nothing**
  (`product-steward`) · 92 SURFACE commits in no stage-note · **9 Fernwood needles in COMMENTS THAT SHIP** on five pages ·
  `seat-portfolio` `map-points` uncovered · `check-engine-manifest` P1 4 / P4 12 · `instance-recipe` stale ·
  `check-config-derivation` 12 typed values (**most look like selftest fixtures — classify, do not mass-fix**).
- **Mom: PARKED by ruling.** Two undispositioned arrivals (1 Guru, 1 cards; one dated 2026-09-06 tagged *Vehicles*), her last
  card answer **2026-08-20 (22 d)**, the ribbon **9 d stale / NOT SHIPPED / UNREAD**, and **nothing has ever read channel
  `guru`**. ⛔ **Parking is a decision about WHEN and is NOT a disposition** — a disposition is per (channel, record id) and
  only opening the record supplies one. **The checker will keep flagging both arrivals; that is it working.** The mom cycle
  does **not** fire. The cost is recorded as chosen: she gets no return leg while the stability work runs.

## 7. Paul's rulings today, so you never re-ask

All fifteen of the revamp plan's §13 **RULED as recommended** (see the chronicle for the consequence list): the lens roster is
`mom` · `wide-eyed` · `conformance` · `successor`, `owner` retired **as a lens** (it names a people-shape, not a posture) ·
the `other` free-text cell is IN · cadence = one pilot read per non-final candidate, **lens named by Paul at beat 6** · READ
tier **declared Opus 5** · shadow read + frozen corpus **unconditional** · two new agent files may be minted in his **global**
stack · **DRIVE · CAPTURE · GATE are NONE by doctrine, not revisitable on cost** · the pilot walk and the ranked lab
household are IN · §5's wording **as quoted** · per-run invites gain a TTL · `--teardown` **run-scoped, refusal wording
kept** · publication intended-and-constrained · seeded desktop profile confirmed · WebKit installed · T11 **full and
fail-closed**. Plus: **the stop rule** (two classes + a one-hour wait) · **J2 re-scoped** *"returning, founded nothing"* ·
**`watched` means VISIBLE, not attended** (an unattended battery is a legitimate pass; the print says *visible*) · **the human
cell H1 is walked through Claude in Chrome with the session observing** — a person walking with an observer, **never the
harness driving his profile**; everything recorded goes to `.private/`. ⛔ **The beat-9 gate kit (TIER 1 · 25) must be edited
to say so — that edit is YOURS and was deliberately not made.**

**Still his, unasked-for-now:** the lap-8 plan's Q0–Q8 · the ask-design plan's §13 fifteen rows · lap 10's Q-10·2 / Q-10·5 ·
the product-name revisit · 20 `.plans` proposals still `row: proposed`.

## 8. Guardrails

Never push `origin/main`. Never deploy `legacy`. Never mint an invite outside qa/lab. Never touch `home`'s or `paul`'s KV
from a lane. **`git commit --only <paths>` always** — the index is shared. **Every stamp from `date` or `git log --format=%ci`.**
⭐ **Re-read a file you cite immediately before the commit, not only before the edit** — the backlog window shipped a row
describing a defect fixed in the gap between its read and its write; **a `file:line` half-life measured under ten minutes**
with three windows on one tree. The AI boundary: no seat reads Mom's words beyond what is routed to the project; nothing
model-authored reaches a person unconfirmed; **a seat's trail in this PUBLIC repo names ids, counts, selectors, stop names
and engine copy — never an address, coordinates, email, phone or a real username.** Coordination routes, never absorbs; every
gate to Paul is question · recommendation · alternatives; **a relayed claim is a hypothesis — label it or measure it.**

## 9. What NOT to trust in this brief

Any `file:line`. The derived beat in `release-state.py`. The Pages/Worker split at `home` (relayed). Lap 7's authored
"~H:MM" stamps anywhere in the chronicle — cite `git log --format=%ci`. `PW3`–`PW6`'s label ORDER (the backlog window
inferred it and says so; the content is transcribed exactly). Whether the lap-8 build plan's measured lines still hold.
`build-viewer.py --check` green means **reproducible bytes, never a running page**. `check-estate-neutral` green covers
**five static pages and says nothing about `viewer.html` or the model's prompt**.

## 10. FIRST TASKS

0. Verify the stamp and that this file is not empty. Write `handoff/handoff-lap8-open.readback.md` — what the thread is, its
   state, the open decision, what is NOT verified, and what you would do next, **naming plainly anything thin**. Tell Paul;
   wait. (The writing window is closed, so **Paul grades it.**)
1. On his clear: run CLAUDE.md's session-start block yourself — it is **50 checks** and the last sweep is already stale.
2. **Fix the two plans' filing** (§5·1) — it is the cheapest thing on the board and it gates the build window's read.
3. **Open lap 8** — beat 1 sweeps with output recorded (UNREADABLE is never zero by assumption) · the dated heading with
   `<!-- outcome:open -->` on the NEXT line, plain form · the beat-6 table **in Paul's words**, row T alone · Paul makes T6's
   two CYCLE-MAP edits · then the build window from its own brief.
4. Put §5·3 (the door/lockout precondition) and §5·4 (the weather recorder) to Paul as **question · recommendation ·
   alternatives** — neither is lap 8's build, both are about the record a real person reads.
