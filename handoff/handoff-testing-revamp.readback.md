# Readback — TESTING REVAMP window · written by the incoming session

<!-- written 2026-09-11 between 08:58:55 and 09:02:00 by git (bebfca9c → 6cabc136), amended through 09:16:16 (bc41b919) and after · brief stamp a95413d1 · brief last committed at 6cabc136 · ⚠️ my first draft carried AUTHORED '~H:MM' stamps that ran ~35 min ahead of the machine clock (`date` read 09:30:42 while the text said ~10:05) — every stamp below is now git-bounded; see the audit's §8h rider
     Nothing below is started. This is what I understand, what I measured, and what looks thin. Grade it. -->

## 0. The stamp, and what moved since it

- Brief stamped `a95413d1`. HEAD is **`6cabc136`**, five commits later, all coordination's: `2962e9af` (the brief itself) · `7baaea3f` (the CYCLE-LOG entry opening this window) · `fbf3ee93` (§1b, Paul's caveat on the audit's framing) · `bebfca9c` (§1c, the model policy) · `6cabc136` (**row T moves to LAP 9**; this window gains a third duty). The brief file is **11,210 bytes, not empty**, and the copy on disk matches `6cabc136`. Coordination messaged me three times to re-read from disk; I did each time. **The brief moved three times in the hour I was reading it** — I will re-read it once more on clear before the first seat is convened.
- ⭐ **PLACEMENT CHANGED — `6cabc136`, 09:02:00 by git** (the brief's own *~9:10* is an authored stamp) `[paul-ruled: "keep lap 8 as it was planned in terms of the commitment, and move the testing lap implementation to lap nine so it can fully close out its audit of lap seven and also monitor lap eight"]`. Row T builds in **lap 9, beside the weather card**, not lap 8. The eight rulings are unchanged; the strict/instrumented finding and the route-level classifier stand; the lap-8 plan's row-T stage-note is superseded. **This window stays open across lap 8.**
- **The working tree is not mine.** `cycle/release/cycle-state.json` is modified (lap 7's `release-state.py --write-state` at 08:46, the build window's) and `handoff/handoff-lap7-build.readback.md` is untracked (the build window's). Three windows share this tree. Everything I commit goes through `git commit --only <my paths>`.
- **The two plans the brief points at carry an 08:46 mtime**, later than the brief, but git reports them clean at HEAD — a checkout touched them, not an edit. Nothing to act on; recorded so nobody reads the mtime as a pending change.

## 1. What I understand the thread to be

Paul opened this window because *"the testing is just going on and on and on"* — three full batteries for one lap-7 candidate, ~40 walk runs, page journeys re-driven on Worker-only shas whose page bytes never moved. The testing-architecture row (BACKLOG § 🧪 SPLIT THE JOURNEY FROM THE READER, `paul-proposed 2026-09-08`) was captured-not-ranked until ~7:00 AM today, when he walked its eight questions in the coordination window and **committed it to lap 8 as row T, one piece, gate change first.**

I own three duties and hand **lap 9's** build window one file:

| duty | what | where | status |
|---|---|---|---|
| **1 · the AUDIT of lap 7's testing cycle** | where the time went, what each stop bought, thorough vs mis-shaped | `.practice/2026-09-11-lap7-testing-cycle-AUDIT.md` — practice-steward, **running from coordination, not mine to re-run** | ✅ **LANDED** `b31a021e` (414 lines) · read whole · **EXTENDED as §8** at 09:15 `[paul: "review that audit and make sure you fully flush it out"]` — 8a the content read landed and revises §3e (the lens axis found the third product defect, invisible to every transcript) · 8b read wall-time bounded (median 27 min) · 8d the non-blocking channel: 63 bullets, 6 relayed by hand, 0 readers → **T-i** · 8e contradiction 2 resolved by symbol (`release-state.py:119` hardcodes the UX clause) · 8f four spot-checks, one 1-minute correction · 8g what stays unmeasurable |
| **2 · MONITOR lap 8's battery** (new — `6cabc136`, 09:02:00 by git) | the door's battery runs on the OLD unit; I read it the way the audit reads lap 7 — where the time went, what each stop bought — so row T is sized against **two laps**, not one | accrues as lap 8's battery runs; its home is unnamed in the brief (I will file it beside the audit unless told where) | not started; lap 8 has not opened |
| **3 · the SCOPE — row T sized by symbol, for LAP 9** | `.plans/2026-09-11-testing-revamp-PLAN.md`, `stage: ready`, `ready: agent-proposed`, in the lap-7 build plan's shape (steps by `file:symbol` · per-step CHECK · what moves the candidate · seams with lap 9's weather card · falsifiers · OUT with rulings · what the build brief must say · what Paul still rules) | supersedes the **Sequence** of `.plans/2026-09-10-testing-architecture-PLAN.md` (P0–P6) under the eight rulings | ✅ **WRITTEN and committed `44413e87`** (10:06:52 by `date`) — one piece for **lap 8** (holds for it; row T first), 21 steps ≈ 28 h, four seat trails cited; 15 rulings open in its §13 |

**The core idea I'm carrying:** a seat today welds a FIXTURE (the PO box, "the condo") to a LENS (skeptic, non-technical) to a JOURNEY (`handover` is a path wearing a lens's name). The 09-10 plan's one paragraph says the weld is a directory name — `journey-walk.py` writes runs to `.private/synthetic-walks/<role>/` and `release-gate.py` derives its unit from that tree. Two additive transcript keys (`journey`, `lens`) dissolve the gate ① best-run defect without moving a run folder. The expensive axis is PROPERTIES, capped at 3, not built.

**§1b is binding on both deliverables and it changes the audit's posture:** every cost normalised against build size (lap 7 = 59 steps across five rows, four pages, the Worker and the template, carrying a two-pass UX sweep's findings and an 11-ruling design bundle; lap 6 = one row, same basis) · THOROUGH (time that found F1, B) separated from MIS-SHAPED (unchanged paths re-driven, F2's harness self-test, A's battery-testing-its-own-cadence) · **no ceiling on testing volume** — the plan's steps are judged by cost-per-finding and re-work avoided, never by fewer walks.

**§1c is a third required section of the plan: a MODEL POLICY per act** — DRIVE · CAPTURE · READ (each lens) · CONTENT READ · SYNTHESIS · GATE, each assigned a tier or **none**, with a falsifier per downgrade (the same artifact read by both tiers; do the findings differ?). ai-advisor (consult) joins the seats and owns the tiering recommendation; engineering-partner wires where the tier is set; Paul rules. A tier change is never ruled on cost alone.

**First measurement, taken this morning so the section starts from the record, not from assumption:**

| act | who does it | model call found? |
|---|---|---|
| DRIVE | `journey-walk.py` from an action list | **none** — grep of the six harness tools (`journey-walk` · `walk-brief` · `walk-capture` · `release-gate` · `walk-integrity` · `journey-view`) for any model client, model id or agent spawn returns zero |
| CAPTURE | `transcript.json`, shots, `walk-capture.py` (capture-side events) | **none** |
| READ | one reading seat per lens, handed `walk-brief.py`'s output, writes REPORT.md | **a model, and its tier lives in no file** — the seat is spawned by the build window's session as a subagent, so the tier is whatever that session's default is. This is the spend §1c points at and the one place the policy has nowhere to attach today |
| CONTENT READ | `.content/walks/<sha>-walk-read.md` | content-steward, `model: opus` |
| SYNTHESIS | the walk read, the beat-10 consolidation, the chronicle entry | the coordinating session itself — no file sets it |
| GATE | `release-gate.py` | **none** |
| the named seats | `~/.claude/agents/*.md` | **every seat that sets a tier sets `opus`** (ai-advisor · business-analyst · career-coach · content-steward · engineering-partner · examiner-panel · practice-steward · security-steward · user-researcher · ux-expert); `backlog` and `_finding-authority` set none |

⭐ **Evidence for the reading tier, to be carried into the MODEL POLICY section verbatim `[coordination, between 09:16 and 09:24 by git]`:** the one finding tonight that only a reading could make — the receipt rendering `garden · motor-pool · equipment` as the person's words, found by content-steward on `owner/2026-09-11T083409/R01-arrive.fold.png` (J3), transcript clean because the ids are correct. A downgrade of the READ tier is falsified if the cheaper tier does not find this on the same frame. Paul ruled the stop itself *record it; proceed at 87c7aae*; it sits on the table at his clear.

So the honest first reading is: the drive and the gate already cost nothing, the reading seats are the whole spend, and **the reading seats' tier is set nowhere a policy could be enforced.** The plan's wiring step is therefore *where a lens's tier is declared* before it is *which tier*. **The "frame read" is not a harness symbol** (coordination, ≤ 09:02 by git): it is the DRIVING SESSION, a model, reading the screenshots the walk captured — the lane's own shake-out phrase, in CYCLE-LOG "THE CANDIDATE IS FROZEN — d7d6c9f" — and the reading seats do the same over their runs. So every model call in the cycle is **a session reading artifacts** (the build lane's shake-out reads · the five reading seats · the content read · the synthesis), and its tier is whatever spawned that session. The plan cites the chronicle line, not a symbol, and the falsifier per downgrade is *the same shots and transcript read by two tiers*.

**Paul, on reading this readback `[paul-stated 2026-09-11, in this window, between 08:58:55 and 09:02:00 by git]` — ROUNDS WITHIN A LAP ARE NOT THE SAME SIZE:** *"if we have multiple rounds of testing it's worth checking whether each round of testing needs to be the same size or we can get more specific or just have one run through… I don't wanna sacrifice coverage for speed, but we don't necessarily need to do every single walk every single time."* How I hold it: the impact-scoped re-run ruling already covers the *sha-to-sha* case; this widens it to the **round** — the first battery at a candidate is full coverage (the declared cell list), and each later round at a re-sha is sized to what moved, down to a single run-through when the change is narrow, with the untouched cells' evidence carried forward and the byte/route proof named. Coverage is the invariant; **the battery size is not.** The plan gets a section on round sizing with its own falsifier: a later round that is smaller than the first must be able to show which cells it carried forward and why, or it is a cut, not a scope.

**Paul, between 09:02 and 09:11 by git — THE SMALL THINGS ARE VALUE TOO, NOT ONLY THE FAIL CRITERIA:** *"as all these different perspectives are reading all the screenshots are they also coming up with smaller suggestions… that are not necessarily fail criteria, but could be helpful to load into the backlog because as we accumulate these small things and then bundle them together we come up with cohesive strategies… without overcomplicating things."* Measured from the record: the seats DO write them — REPORT.md carries a "What I noticed as a person" / "Small things" section under varying headings, **35 bullets across today's 36 reports**; CARRY (beat 4) is designed to consolidate a round into `.private/synthetic-walks/CONSOLIDATION-<sha>.md` with *already carried · carried this round · questions opened*, and at least one opened question from `bfa3f23` (the off-palette focus ring) did reach a BACKLOG row. **The gap is coverage, not design: consolidation files exist for two candidates (`c821051`, `bfa3f23`) and none for lap 5–7's.** The plan gets a section on the small-things channel: a fixed heading the consolidator can find, CARRY run per candidate not per memorable round, and a falsifier (a non-blocking bullet written by a seat at candidate N can be found in a row or an opened question at lap close, or the channel is decorative). **Build window's live practice (tate-tracker-94, before 09:11:35 by git):** the seat brief *requires* the small things — a "what you noticed as a person" section, the rule *report a sentence you could not understand as a finding*, and Paul's three last-screen questions. At `87c7aae` no consolidation file was written; beat 4 ran `product-steward.py --record` (a ledger of counts: carried 14 · already 0 · questions 3), and the stop-level findings plus **six cross-seat non-blocking findings reached the register only because the build lane relayed them by message to coordination**, who queued rows. The remaining bullets across the 15 counted reports live in REPORT.md only. Candidates 1 and 2 have **no reports at all** — seats were held until the final sha, so their run folders still carry WALK-REPORT-UNWRITTEN. **So: the ask exists, the capture exists, and the carry is a human relay with no reader behind it** — the same shape as *an event with no reader is not instrumentation*, one rung up.

**Paul, 09:28–09:30 by git (`4892227d` → `180e6f97`), from his own gate walk at `87c7aae` — WE ARE NOT TESTING THE RIGHT THINGS, AND THE BROWSER IS TOO STERILE:** *"the testing that I'm doing right now is a good indicator that we're not testing the right things… I'm finding pretty base-level issues… it's important that we try to simulate the user journey as well — do we open it in a Chrome that's signed into a profile, rather than just these sterile Chromes… I'm not sure whether it's better to use both… let's make sure we're testing the actual user journeys that we're building out and also trying to simulate what the user's actual experience is going to be rather than making it too sterile so it's not realistic."*

Two claims, and the second is measured now (`tools/journey-view.py:54-73`, engines under `~/Library/Caches/ms-playwright`):

| what a real person has | what every walk has had |
|---|---|
| Mom: **Safari on an iPhone**, A+ text, a profile that remembers her | **Playwright Chromium**, fresh context per run, emulating 414×848 @3x mobile; **no WebKit is installed**, so Safari has never been walked once |
| Paul: signed-in desktop Chrome with saved passwords, autofill, extensions, history | no persistent profile; no saved credentials; no autofill; one cookie injected when the journey needs it |
| a stored text-size preference (`lg` on her device, 8 of 8 reports) | nothing sets it — every walk renders the default size |
| a device that has seen the app before | a browser that has never seen anything |

**How I hold it for the plan:** *not too sterile* is a **PROPERTY of the arrival**, the same axis as the credential (Q1) — a journey declares the browser state it is entered in (profile: clean · returning-device · signed-in-desktop; engine: chromium · webkit; text size: default · A+), and the declared cell list at beat 6 picks cells across it. That is not a fourth property fixture (Q5's cap is about households) and it is not a second harness (the audit's do-not-build). ⚠️ The first claim — *not testing the right things* — is **his gate walk's evidence, not mine**: the base-level issues he is finding go to the audit's §2 column *could a cheaper instrument have found it*, and to §8d's channel if they are the small kind. I have asked coordination to route them to me by id.

**⭐⭐ PLACEMENT RULED AGAIN, in this window, 09:46:39 -0400 by `date` — LAP 8 HOLDS; ROW T LANDS WHOLE, FIRST** `[paul-ruled 2026-09-11]`: *"I'm good in investing now in getting a really good testing procedure down. So if we have to hold lap eight until all this is determined and we have a clear plan, that's fine. And I'd rather not split it up unless there's a really good reason to do it — that's not just time and effort."* Supersedes the 09:02 lap-9 placement (which predated his walk) and this window's own recommendation of a core/remainder split. How I hold it: row T is ONE piece, built and proven before lap 8's battery runs; lap 8's build waits on the plan being clear, not on the harness being finished; the plan carries no split unless a seat names a reason that is structural (a ruling not yet given, a dependency on lap 8's own rows, a falsifier that cannot run before the door exists) — never hours. The four seats were briefed before this ruling; the sizing seat is being told now.

## 2. The eight rulings as I hold them (`CYCLE-LOG.md:3243`, cite never re-open)

| q | ruled | what it does to the sizing |
|---|---|---|
| Q2 | gate ① unit `seat` → **(journey, lens)** — a release-condition change; falsifier ③ gates the change itself | `release-gate.py` `seats()` → units; backfill rule; the CYCLE-MAP beat-8 exit restated — **I quote that edit, never make it** |
| Q3 | a lens is a **reading posture only**, no inputs | `synthetic-identity.py` `ROLES` splits: posture stays, address/credential leaves with the journey |
| Q4 | first cut **J0 · J3 · J8 built; J1 · J5 · J7 named-unbuilt**, printed unwalked every lap; J2 re-scoped or retired (Paul's) | see the discrepancy in §4 — the code at HEAD does not read this way |
| Q5 | properties **cap 3**; a fourth only for a named code branch | P5 is OUT; no `property-fixture.py` |
| Q7 | the ratio becomes **a declared cell list per lap at beat 6**; the gate prints longest-unwalked; **no scheduler, no test-selection engine** | a print, not machinery |
| Q1 | **unbundle the credential axis, ship the per-run unspent invite first** — a property of the arrival, never a role | `grant-mint.py --fixture-out` per run on the J1 path; P0 ships first |
| Q6 | roster **five owners including Paul** (`.decisions/fernwood-20`, exists) | J7 second-member is on the critical path — and nothing in the harness has it (`journey-walk.py:324, :970` mention it only as *"spoken for"*) |
| Q8 | add the `→ PLAN ·` pointer | BACKLOG is closed to me; the register carries the row when the backlog window reopens |
| re-runs | **impact-scoped**: a re-sha re-runs the journeys that touch what changed, carries the untouched journeys' evidence forward with the byte proof named; a change classifier in `release-gate` | the least-specified ruling — §4 |
| placement | ~~lap 8, one piece, gate change first, before the door's battery~~ → **LAP 9, beside the weather card** `[paul-ruled — `6cabc136`, 09:02:00 by git]`; lap 8 stays as committed and runs its battery on the OLD unit | "one piece beside the door" dissolves; I recommend one piece vs two on lap 9's own terms; Paul rules |

## 3. Current state, measured this morning (counts and ids only)

- **Lap 7 is live at candidate 3 `87c7aae`** in the build window. The 08:46 `cycle-state.json` snapshot shows all five seats with a run at-sha, watched, no failed actions, not rate-limited — and **`countable: false` on all five** (REPORT.md present but unread at that moment) and **`instrumented: false` on `strict`**. Runs continued after the snapshot (latest 08:53). Today's runs: mom 7 · strict 7 · wide-eyed 8 · owner 7 · handover 7, every one with a REPORT.md. That is the build window's beat 8 in progress; **I do not touch `journey-walk.py`, `release-gate.py`, `walk-integrity.py` or anything it uses until lap 7 closes.**
- **Across 09-10/11 the record holds ~63 runs across five seats** (handover 14 · mom 10 · owner 15 · strict 11 · wide-eyed 13). That is the audit's raw material and the basis for §1b's normalisation.
- **The audit has not landed.** I cannot see the practice-steward agent from this window; only its output file will tell me it ran.
- **`seat-portfolio.py` is not run yet** — the brief's first task on clear.
- **Lap 8's build plan** (`.plans/2026-09-11-lap8-build-PLAN.md`) has rows P · A (the door, 16 steps) · C · E · F · G · riders · H (harness, 3 steps) · B (migration, its own gate). Row T's stage-note is at `:103` and says the re-audit at open sizes it and says whether one piece fits. **"The door" = row A.** Row H is the seam I most need to read — the harness already has three lap-8 steps and T lands in the same files.

## 4. What the brief left me unsure of, or that looks thin

1. **⛔ The Q4 ruling does not match the code at HEAD.** The ruling says *J1 · J5 · J7 named-unbuilt*. `journey-walk.py` `JOURNEYS` at HEAD holds **J0 · J1 · J2 · J3 · J4 · J5 · J8**, each with an arrival and an action list (`journey_bare_door`, `journey_founding`, `journey_resuming`, `journey_lifecycle`); `NAMED_UNBUILT` holds only **J6**. So J1 and J5 are *built* in the code's own vocabulary. Two readings: (a) "named-unbuilt" means *not in lap 8's declared cell list, printed unwalked* — a coverage statement, cheap; (b) the ruling was made against the 09-10 plan's Q4 text, which predates `06be8bd`'s J5 and J0 work, and is stale about the code. **These size differently.** I will read it as (a) unless told otherwise, and say so in the plan. CLAUDE.md's `walk-fixtures.py` line also still says J5 and J0 are the two standing coverage holes; that line is behind the code.
2. **J2 is built and unwalkable, not absent.** `journey_resuming` (U01–U09) exists, provisioned per run because the walk consumes its own entry state; CYCLE-LOG says *J2 unwalkable by model* this lap. "Re-scope ('returning, founded nothing') or retire" is Paul's; I need the actual failure reason from lap 7's beat-10 entry before I can recommend either.
3. **The impact-scoped re-run rule has no mapping yet, and the obvious one is wrong.** "The journeys that touch what changed" needs a classifier from *changed files* to *journeys*. Candidates 2 and 3 tonight were **Worker-only** shas — and every journey calls the Worker (session, grant, recover). A file-level classifier says "everything moved" on exactly the shas the ruling was written to skip. The classifier has to be by **route** (which Worker routes a journey's stops hit, against which routes the diff touched), with page bytes as the second proof. That is design work for engineering-partner, and it is the one piece of row T I cannot size from the brief alone.
4. ~~"One piece beside the door"~~ **dissolved by the lap-9 placement.** What survives: row H (lap 8 harness, 3 steps) still lands in `journey-walk.py` and `release-gate.py` **before** row T does, so T's symbols must be re-cited against lap 8's close, not today's HEAD — and the one-piece-vs-two question is now asked beside the weather card, the small lap, where it is far more likely to fit. ~~New thin point: where lap 8's monitoring trail is filed~~ **answered by coordination between 09:02 and 09:11 by git:** beside the audit under `.practice/` (shape `.practice/2026-09-1x-lap8-testing-cycle-MONITOR.md`); read-only on the harness tools runs to **lap 8's close** — lap 8's own row H is the build window's, I specify and never edit until lap 9's build window reads my plan. Paul's round-sizing note is now brief §1d and in the chronicle, verbatim, and coordination reads it the way I do.
5. **The 09-10 plan's line citations are stale.** It was written at `d661815` in a worktree and cites `journey-walk.py:573`, `:583-590`, `:554`, `:695`, `ROLES :43-56`, `release-gate.py:78-83`, `:233-240`. `JOURNEYS` alone now sits at `:876`. Every symbol in my plan gets re-cited at HEAD; nothing is carried by line number.
6. **§1b's "59 steps" is brief-asserted, not measured by me.** I will count it from the lap-7 build plan before it becomes a denominator.
7. **TIER 1 · 22's three instrument findings partly overlap row T** — `urlBefore` per stop and a `/homes/` shot · the `personId` vs `signedInAs` label · the J3 fixture (landed 09-08 as `--complete-setup`). The plan has to say which are already done, which fold into T, and which stay TIER 1 · 22. I have not verified which landed.
8. **Seat trail directories:** `.engineering`, `.user-research`, `.practice`, `.content` exist; **there is no `.security/`**. security-steward's trail needs a home before it writes; I will not mint a directory without saying so.
9. **`strict` reads `instrumented: false` at 87c7aae** in the 08:46 snapshot. That is lap 7's business, but if it holds it is exactly the class the audit should name — a seat whose walk lands zero app events is either an uninstrumented path or a fixture that never reaches the instrumented screens.
10. **If the audit never lands** the brief gives me no fallback other than "do not re-run it." I will wait one reasonable interval on clear and then ask coordination, not re-commission.
11. **The model policy's falsifier needs a corpus that does not exist yet.** "The same artifact read by both tiers" means one walk read twice, by two tiers, findings diffed. No run today has been read by more than one tier, so every downgrade the plan proposes ships **unfalsified** until lap 8 reads at least one walk both ways. The plan will say that on its face rather than present a tier table as settled.
12. **Where a lens's tier is declared is unruled and precedes the tiering.** Today it can only be the spawning session's default. The candidates are the seat brief (`walk-brief.py` output), a per-lens file, or the cycle map; engineering-partner's wiring step and ai-advisor's recommendation both hang on it, and the brief does not say who owns that choice.

## 5. What I would do next (on clear, nothing before)

**Pace, now that the window spans lap 8:** the lap-7 audit lands first and I extend it · lap-8 monitoring accrues as that battery runs (the content read and `release-gate` are the acts to watch again; lap 7's battery at `87c7aae` is CLEAN per coordination — J0/J3/J8 5 of 5, zero pageerrors, ~63 runs across three candidates) · the seats are convened once both laps' evidence exists · the plan finalises before lap 9 opens. Read-only on the tools until lap 8 closes, not lap 7.

1. Re-check for the audit; run `seat-portfolio.py` and `walk-fixtures.py --check` (read-only), and count lap 7's steps for the §1b denominator.
2. Read lap 8's row H and row A steps, the lap-7 beat-10 entries (F1, F2, A, B), and `release-gate.py` / `journey-walk.py` at HEAD by symbol.
3. Convene the seats in parallel: **engineering-partner** leads sizing (gate unit · lens/journey split · the route-level change classifier · the cell-list print · the per-run invite · where a lens's tier is declared), **user-researcher** names the lenses with falsifiers (never a persona as fact; `persona-mom.md` already exists and is cited, not re-invented), **security-steward** rules on fixtures and credentials per env and what a transcript may record (trail to `.engineering/<date>-<topic>-SECURITY.md`, the existing convention), **ai-advisor** (consult) owns the model-policy tiering with a falsifier per downgrade. Each writes its own trail.
4. Synthesise into `.plans/2026-09-11-testing-revamp-PLAN.md` in the lap-7 build plan's shape, with the CYCLE-MAP beat-8 edit **quoted**, the "do not build" list held (no scheduler, no selection engine, no sampling budget), and a plain one-piece-or-two recommendation.
5. Commit with `--only` my paths; forward any backlog row to coordination by message; tell Paul what he still rules (fit · J2 · the lens names · release-condition wording).

## 6. Coordination's grade, folded in `[tate-tracker-ea, before 09:02:00 by git — "CLEAN, and three of your ten are findings I did not have"]`

Recorded here so the file carries the answers, not only my questions. Paul still clears.

| my point | coordination's answer | what it does to the plan |
|---|---|---|
| 1 · Q4 vs the code | **coordination's compression, not Paul's ruling.** The code is right: J1 and J5 are built action lists, J7 is not. Operative ruling is reading (a): lap 8's **declared cell list walks J0 · J3 · J8**; J1 · J5 (built) and J7 (unbuilt) print UNWALKED every lap | write it that way; cite CYCLE-LOG "LAP 8 GAINS A ROW"; mark "named-unbuilt" as a corrected compression |
| 2 · J2 | failure reason is in CYCLE-LOG ~5:30 AM: `handleAccountCreate` writes only a route row, no grant; `mint_unfinished()` provisions an account with no estate, which `journey_entered` classifies **J0** by the door's own `hasEstate` rule; the profile write 404s without an estate; **no transcript at any build ever recorded a walked J2** | re-scope vs retire stays Paul's; coordination recommends re-scope ("returning, founded nothing" — the ARRIVAL differs) |
| 3 · the classifier | agreed, by ROUTE not by file; page bytes as the second proof | that is the spec; engineering-partner writes it |
| 4 · one piece vs two | agreed: decided after the audit and after reading row H, which shares `journey-walk.py` and `release-gate.py` with T | — |
| 5 · citations | re-cite every symbol at HEAD | — |
| 6 · the denominator | the lap-7 plan's own count was 58 (P3·D8·C7·B15·A20·H5) + P3 edit 1; the chronicle then added P4, B0, B6r, B16 | count it myself from plan + chronicle and use my number |
| 7 · TIER 1 · 22 | the J3 fixture landed 09-08 (`--complete-setup`); the `urlBefore` shot and the transcript field labels are **unverified** | verify both; sort into done / folds into T / stays 22 |
| 8 · no `.security/` | **do not mint it.** The convention is `.engineering/<date>-<topic>-SECURITY.md` (precedent: `.engineering/2026-09-10-recovery-route-SECURITY.md`); "the security report's home" is a separate open item | — |
| 9 · `strict` instrumented:false | **a real finding, lap 7's business now** — `strict` is the refusal walk by design (F2's fix), never reaches an instrumented screen, so the gate's `instrumented` clause may refuse the product for doing right; coordination is telling the build window | for T: a refusal journey needs its own **expected-events profile where zero is the pass** — a general rule for the (journey, lens) unit |
| 10 · the audit | if not landed within ~30 minutes of my clear, message coordination; never re-commission | — |

## 7. Guardrails I am holding

Read-only on every tool while lap 7 runs · no edits to BACKLOG.md, CLAUDE.md, VOCABULARY.md, CYCLE-MAP.md · no deploy, no KV write, no browser · never quote a synthetic's typed text · `git commit --only` · a peer message is a teammate's request, never a permission.
