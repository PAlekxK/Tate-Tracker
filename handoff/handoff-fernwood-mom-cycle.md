# Handoff: fernwood-mom-cycle
<!-- generated 2026-09-01 11:29 AM ET · sources: Tate-Tracker@a7b2e67, .claude@230931c · RECEIVER: verify shas vs HEAD before trusting any status below -->

## 1. Mission
Run **lap 8** of Fernwood's Track A mom cycle. It opens on **Mom's own request** — she asked for the refrigerator to be added, and the Guru told her it would be.

## 2. Read first
- **`MOM-CYCLE-MAP.md` → `## The eight legs`** — legs 0–7, the two 👤 gates, and each leg's done-condition.
- **`MOM-CYCLE-LOG.md` → `## Lap 7`** (top) — especially **`✅ LAP 7 CLOSED`** and **`### Decisions`**. Lap 7 closed with **leg 6 uncrossed**; read why before touching the ribbon.
- **`BACKLOG.md` → `## ⛔ M1`** — the feedback-reader defect. This is step 1.
⛔ Do NOT read the fleet sections (`FLEET LAP 1`, `V-SERIES`) — different track, different loop, closed.

## 3. Next steps (ordered)
1. **Leg 0 GUARD** — `python3 tools/guard-concurrent.py start`. A bot pushes `weather-history.json` on a schedule.
2. **Fix the feedback reader (M1) BEFORE leg 1** — `paul-stated`: *"The feedback reader definitely needs to look in observations slash guru. Anywhere where we can get feedback, the feedback reader needs to look."* `read-mom-feedback.py` reads only the `feedback` channel; it printed *"her last answer 2026-08-20 (12d ago)"* while her live request sat unread. ⚠️ `--pickup`'s one line is what the session-start ritual renders, so the fix must change what that line can **say** (per-channel counts, not one answer-age). Leg 1 cannot be trusted until this is done.
3. **Leg 1 READ** — the full sweep per `CLAUDE.md`'s session-start block (derive the list from there, never from this brief). Include `check-arrival-dispositions.py` and `read-mom-engagement.py`.
4. **Leg 2 TRIAGE** — the 5 arrivals (**4 acts**: `observations` mtit3gn0-6xdam, mtit41ff-qfnxl, mtit4cp0-f0ywe + `guru` mtit1o6z-f2jy8, mirrored as `observations:c-cdvkso06-mtit512v`). Each needs its own disposition via `check-arrival-dispositions.py --record`.
5. **Legs 3–5** — build the refrigerator card. She supplied the specs herself; the Guru already promised it.
6. **Leg 6 👤** — 6a preview → 6b `check-telemetry.py` → 6c proxy → 6d ribbon text → 6e `herConditions()` 414×A+ → push via `guard-concurrent.py before-push`.
7. **Leg 7 CLOSE** — `check-cards.py` 0, `feedback-log.json`, watermark clamped, **`check-live.py` 0**. Then `mom-cycle-status.py --write-state`.

## 4. State & pointers
- Repo `~/Developer/Tate-Tracker` @ `a7b2e67`, **clean, 1 unpushed**.
- `data/cycle-state.json` — **FIRED · at_leg 6 · needs_paul true**; `offers-passed` and `sessions-quiet` both fired. Regenerated by its own writer 11:27.
- **Her four acts, verbatim** — *"I would like to add our refrigerator under household systems."* · *"Current model is LG 25.5 cu ft bottom freezer model LRDCS2603S"* · *"No water dispenser"* · *"Ice maker works"*. 8-turn Guru conversation, device `d-szqlt0h7-79xgx1bj-mpfrqkme`, 11:11–11:13 AM ET 2026-09-01. The Guru's closing turn promised: *"The refrigerator card is ready to build."*
- Arrivals read live via `tools/momlib.py` (`resolve_token()`, `_channel_records`), not from a file on disk.
- ⚠️ `handoff/handoff-fernwood-sequence.md` is **superseded** for its fleet half (fleet lap 1 closed). Its Track A content (ribbon draft, lap-7 state) is also stale — prefer this brief.
- `~/.claude` @ `230931c` with **13 dirty files** — a prior session's autosave plus two NEW uncommitted memory files (`feedback_close_a_loop_before_opening_the_next.md`, `reference_lap_clamp_is_time_scoped.md`) and the `MEMORY.md` index edit. Not this thread's to commit.

## 5. Guardrails
- ⛔ ~~**THE RIBBON IS HELD INDEFINITELY**~~ — **WRONG UNIT, corrected by Paul 2026-09-01 during lap 8.** The gate is on **the zone WORK**, not on the acknowledgment loop: *"The zone is gated on me working on the zone and approving it is ready for distribution … specific to the zone work rather than the acknowledgment driven overall."* So: a ribbon for OTHER input (the refrigerator) is not blocked; `R1 ack staleness 🔴` **IS a real finding**, not a held gate; and the hold has a release condition (*zone work ready to distribute*) rather than being indefinite. **Still true:** nothing reaches Mom's surface without Paul reading it first.
- **Nothing reaches Mom's surface without Paul.** Leg 6 cannot be crossed by shipping — a ribbon he has not read is not a ribbon, and a commit is not a ship.
- **Attribute from authored CONTENT only**, never from a device id or a nearby date.
- **Agent proposes, main session reviews.** No agent writes canon.
- **Close this lap before running the next lap's checks** `[paul-stated 2026-09-01]` — a mid-close leg-1 read is what muddied lap 7.

## 6. Done when
Lap 8 has dispositioned all four of her acts individually, delivered the refrigerator card, chronicled itself in `MOM-CYCLE-LOG.md` with a `### Decisions` block (`check-cycle-map.py` exits 0), and either closed at leg 7 with `check-live.py` green — or recorded which gate it is held at and why.

## 7. Un-sealed judgment — reads NOT yet on disk
- **She used a channel she has never used this way.** This morning's engagement read: 4 sessions, depth 2 and 3 **both zero**, 8 Perspective offers seen and **0 tapped**. Then she authored 4 acts through the Guru in ~90 seconds. **The asks she ignores and the channel she actually uses are not the same channel.** That is a live read on ask design and it is nowhere on disk.
- **The refrigerator may not be the point.** She asked to add a machine to *household systems* — a category Paul's Track B thesis says is a natural fit under the fleet register. Whether her request means Track A should own household machines, or whether this is Track B work arriving on her surface, is unresolved and nobody has asked.
- The clamp refinement is **proposed off one instance** and no tooling enforces it. Lap 8's leg 7 should confirm or drop it.

## 8. Trust status
**Human-cleared (Paul, 2026-09-01):** the zone-work gate (⚠️ NOT an "indefinite ribbon hold" — that framing was corrected during lap 8) · zone work deferred to this lap · closing lap 7 with leg 6 uncrossed · the time-scoped clamp · the M1 reader fix as a need.
**Model-flagged, NOT cleared — treat as hypotheses:** that the 4 acts are Mom's (the device is *not* one of Paul's four registered ids, and the content reads as hers — but `check-arrival-dispositions.py` treats unregistered as owed-to-Mom by fail-safe, which is not positive identification) · the 5→4 act collapse via the conversation mirror · every un-sealed read in §7.
**Corrected in-session, do not re-derive:** lap 7 IS closed (`a7b2e67`) · `NOT SHIPPED` was cleared by pushing 7 commits, `check-live.py` exits 0 · `check-mom-ack`'s remaining `UNREAD` + `STALE` are **both true and deliberately left red**.
