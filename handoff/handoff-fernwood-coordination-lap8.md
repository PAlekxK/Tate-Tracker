# Handoff: fernwood — the COORDINATION window for LAP 8 (holding for the testing plan; row T first)

<!-- generated 2026-09-11 09:57 EDT · source: Tate-Tracker@31462d16 on LOCAL main · written by the lap-7 coordination window (tate-tracker-ea)
     with a QUOTED heredoc (a brief tonight committed EMPTY on a backtick — check this file is not zero bytes before trusting it).
     RECEIVER: verify the sha against HEAD. Cite symbols, stamp the sha. Every wall-clock stamp you write comes from `date`,
     never authored — lap 7's authored stamps hid an 8-hour hold. -->

## 1. Mission — you are the LAP COORDINATION WINDOW

Paul, 2026-09-11: *"give me a handoff to start a new session to handle the next lap… they should have awareness of the testing
monitoring window that's going on in parallel that will continue to monitor lap 8, so they should check in — and it can have
the same approach of having a build window and the authority to launch any other sessions that would be helpful, but it'll be
the lap coordination window."*

You run lap 8 of the release loop (`cycle/release/CYCLE-MAP.md`, twelve beats). **Coordination ROUTES and never ABSORBS**
(memory `feedback_coordinator_routes_never_absorbs`): you own the chronicle (`cycle/release/CYCLE-LOG.md`), the freeze,
`release-state.py`, the gates to Paul (question · recommendation · alternatives), and the windows. You open a BUILD window
and any other window that helps, each from a brief via `python3 ~/.claude/tools/succeed.py --open <slug>` (brief at
`handoff/handoff-<slug>.md`; grade its readback; Paul clears it in its own window). You never write BACKLOG.md — the
backlog-refinement window is the ONE DOOR (reopen it: `succeed.py --open backlog-refinement`).

⭐ **A window is live in parallel: the TESTING REVAMP (`tate-tracker-d8`)** — audit of lap 7 done
(`.practice/2026-09-11-lap7-testing-cycle-AUDIT.md` + `-ANALYSIS.md`), four seats synthesizing the executable plan
`.plans/2026-09-11-testing-revamp-PLAN.md`; it MONITORS lap 8's battery (a stage-note on its plan) and stays READ-ONLY on
every harness tool until lap 8 closes. **Check in with it first** (`SendMessage` to `tate-tracker-d8`), tell it when lap 8's
first walk runs, and route it every base-level finding by id. Use `ListAgents` for live names.

## 2. ⛔ LAP 8 HOLDS — the ruling that shapes your first day

`[paul-ruled 2026-09-11, after his lap-7 walk]`: *"I'm good investing now in getting a really good testing procedure down. If we
have to hold lap eight until all this is determined and we have a clear plan, that's fine. I'd rather not split it up unless
there's a really good reason — not just time and effort."* → **Lap 8 does not OPEN (no beat 1, no build window) until Paul has
READ `.plans/2026-09-11-testing-revamp-PLAN.md`. Row T (the testing architecture) lands WHOLE and FIRST in lap 8's beat-6 table,
before the door's build and battery, so the door is certified on the new unit.** A split needs a STRUCTURAL reason a seat names
(a ruling not given · a dependency on lap 8's own rows · a falsifier that cannot run before the door exists) — never hours.
The eight row-T rulings: chronicle § "LAP 8 GAINS A ROW" (gate ① unit → (journey, lens) · lens = posture only · J0·J3·J8 the
declared cells, J1·J5 built / J7 unbuilt printed unwalked · properties cap 3 · a declared cell list per lap, no scheduler ·
credential axis unbundled first · five owners incl. Paul · impact-scoped, ROUTE-keyed re-runs · round sizing: coverage is the
invariant, battery size is not).

## 3. Read first, in this order

1. `cycle/release/CYCLE-LOG.md` — the whole `## Lap 7` section (CLOSED): the beat-6 table and its AMENDED rulings · THE
   ENVIRONMENT MODEL · "synced" RULED · the beat-10 entries · "Paul's walk" W1–W11 and the ruling **"the account is always the
   first layer"** · "Prepared for beats 9–12" · the ✅ CLOSED block · and every **"Ahead —"** subsection (laps 8/9/10 scope
   rulings · the product's STARTING NAME *My Home Place* · the apex `myhome.place` · row T · the content build-out · the ask
   design · the productize thread).
2. `.plans/2026-09-11-lap8-build-PLAN.md` — the lap-8 plan at `stage: draft` with its stage-notes (read them ALL; the newest
   supersede: row T first · the account-first ruling on the door · the apex + name strings · deviceId + TIER 1 · 54 as one
   feedback-path pass). Its §9 Q0–Q8 are Paul's at open; its §10 names four surfaces owed a **ux-expert closure** (the sign-in
   page · the shelf after sign-in · the email editor + re-auth · the ribbon's empty state) — under the account-first ruling.
3. `.plans/2026-09-11-testing-revamp-PLAN.md` (when it lands) · `.plans/2026-09-10-testing-architecture-PLAN.md` (`ready:
   paul-ruled`) · `.practice/2026-09-11-lap7-testing-cycle-AUDIT.md` §3–§5 · `.engineering/2026-09-11-testing-revamp-SECURITY.md`.
4. `.plans/2026-09-10-lap8-9-SCOPE-PROPOSAL.md` (RULED per question) · `.plans/2026-09-11-lap9-READINESS.md` ·
   `.plans/2026-09-11-lap10-SCOPE-PROPOSAL.md` · `.plans/2026-09-11-ask-design-PLAN.md` (§13 = fifteen rows for Paul) ·
   `.plans/2026-09-11-legacy-toolchain-INVENTORY.md`.
5. `VOCABULARY.md` §3i (three environments; households are rows) · §3b (the top bar; *My Home Place*) · §3e·R (the reset
   rule) · `CLAUDE.md` § THE ENVIRONMENT MODEL and the pickup block (run it at open — `watch-recovery.py` is new).
6. `handoff/handoff-backlog-refinement.md` §8–§9 (reopen the one door from it) · `handoff/handoff-lap7-build.md` §9 (the build
   lane's state at close) · `handoff/handoff-testing-revamp.md` (the live window's brief, §1–§1d).
7. Memories (this repo's dir): `feedback_coordinator_routes_never_absorbs` · `feedback_commit_only_when_the_index_is_shared` ·
   `feedback_stamp_the_record_from_the_machine_clock`.

## 4. THE ENVIRONMENT MAP — Paul's, ruled twice

**Three environments: `lab` (dev) · `qa` (testing) · `production` — ONE, every household a ROW in it.** Today production is
still TWO deployments (`myhome-paul` est-d93508 — Paul, THE production origin by ruling Q7; `fernwood-home` est-e6696a — Mom's
account `marguerite`, one row, migrates into production at lap 8 · B as a MIGRATION with a verified copy, never a delete).
`paul`/`home` are DEPLOYMENTS, never "environments". **Production's address is the apex `myhome.place`** (lap 8 · B step);
**the product's starting name is "My Home Place"**, Home emphasized. `legacy` (top-level `fernwood`, est-3c9f1a) is Mom's live
Fernwood — never deploy, never push `origin/main`. `bob`, `nigel`, `aida` destroyed.

## 5. State at 31462d16 (measured at close)

- **Production serves `87c7aae`** at `paul` and `home` (Worker + Pages; post-deploy covered; the row-32 stamp false red at each
  with the payload blob matched). qa serves `87c7aae`. `release-state.py` reads *ARMED · beat 11/12 · lap 7 (closed) ·
  cleared_sha 87c7aae* — the beat is a derived quirk (TIER 1 · 47), not lap 8's state. L8-P1…P7 pre-registered.
- **Lap 7's release note** is in `RELEASE_NOTES.md` and the tracked viewer (`75e56829`); it reaches the households' cards at the
  NEXT build (L4's one-lap latency, by construction).
- **Fixtures at qa from lap 7** (never real homes; the fixture-stamp row): est-kxfhht · est-t3h0gl · est-puvevs · est-bvqzw3 ·
  est-tfmxem · est-0qeqzs · est-uqjofw · est-pqob3d · est-1tfrzb · est-ftrtkj · est-kgjxry · est-c9pgvw; lab est-as1bgb (+1).
  **KEEP:** `pkirsch`@paul · `marguerite`@home · est-qa0001 · **`pkirsch-qa` (Paul's DURABLE qa account, ruled) ·
  `pkirsch-onetimeuse` (his throwaway, kept for the next kit)**. `household-fixtures.py --teardown` still refuses (nothing stamps
  `syntheticFixtureRun`; lap 8 · F fixes that).
- **Unpushed:** ~250 commits ahead of `origin/staging` — Paul's call (asked at close; if unanswered, ask once).
- **Live windows at handoff:** this one (closing after grading you) · `tate-tracker-d8` (revamp). Closed: backlog-refinement
  (`06e3c7d`/§9), build (`eb44494`).

## 6. Paul's rulings NOT YET ACTED, and what is still HIS

| item | state | who |
|---|---|---|
| **the stop rule: two classes + a wait term** (L8-P4) — put to him at lap 7's close with a recommendation | unanswered | Paul |
| **J2 re-scope ("returning, founded nothing") or retire** (L8-P6) — before lap 8's cell list | unanswered; coordination recommends re-scope | Paul |
| **push `origin/staging`** | asked at close | Paul |
| **the decision write-back** — 23 commits claiming a decision vs 8 card lines; `.decisions/` intake per CLAUDE.md § D33 | owed | you, with the backlog window |
| the ask-design plan's **§13 fifteen rows**; the product-steward absorption's **beat-3 ask lines** (a CYCLE-MAP edit) | his, in his time | Paul |
| the **product name** revisit (a deep dive he asked for) — the starting name is ruled | later | Paul |
| **W2** the looping *Create your account ›* link · **W1** post-sign-out door · **W7** the "early days" landing · **W8** masthead spill at laptop width · **W9** cards ≠ jump strip · **W10** weather card · **W11** the Journal's "stays on this phone" · the renderer's bare-id fragility · the seat trails public-repo rule → CLAUDE.md | all queued for the register | backlog window |

## 7. THE REGISTER QUEUE — the one door was closed for the second half of lap 7

Every line in `CYCLE-LOG.md` § Lap 7 reading **"queued for the register"**, **"QUEUED FOR THE REGISTER"**, **"Register:"** or
**"Register carry"** written after the backlog window closed (`06e3c7d`) is an unfiled carry:
`grep -n -iE 'queued for the register|register carry|Register:' cycle/release/CYCLE-LOG.md | awk -F: '$1>2698'`. **Reopen the
backlog window FIRST and hand it that list; it carries with the register-trailer convention, `--only`.** Its own §9 lists what it
owes (the distinct-questionId count · the zones window's four register edits · the PRODUCTIZE census · the #card-told cut).

## 8. LAP 8 as ruled — the beat-6 table you will write when it opens

Order: **T** the testing architecture (whole, first) → **A** the single-origin sign-in door — **the account is always the first
layer** (door = sign in · create an account; founding moves INSIDE to the empty shelf; a just-signed-out device leads with
sign-in via a rostered marker; D7/B2 founding-first superseded) · "synced" s1–s4 as its acceptance · security-steward on the
oracle posture · the apex and the name strings → **C** the email editor (write path no weaker than sign-in) → **E** the Midtown
repoint → **F** the fixture stamp reaches founded houses (qa/lab only) → **G** the per-instance ribbon seam → riders 36 · 37 ·
the ask ledger (spec in the ask-design plan §9; per-ask per-env; served = distinct asks) → the deviceId + TIER 1 · 54 feedback-path
pass in A2's conversion → **B** Mom's row migrates into `myhome-paul` at the apex, **its own gate on Paul's word at the act, after
A's isolation falsifier passes at lab** → **D** the glance design pass, **conditional on ≥ 10 real sessions in
`read-glance-order.py`** else lap 9. Excluded with rulings: INVITE & JOIN (scoping IN lap 8, build lap 11) · zones preload (Mom's
act) · address validation · weather card (lap 9 first row) · capture write path (design pass in lap 9's own window).
**Commit-phase rule:** ux-expert closure of §10's four surfaces + the door under account-first → engineering-partner RE-AUDIT of
the lap-8 plan (row T sized by symbol from the revamp plan; the seams with row H) → the build window's brief points at the
re-audited plan. **The build rules that held in lap 7:** Worker before pages at every env · one sha frozen before the battery ·
a fix is a new sha · never declare a candidate on `--check` alone (the headless load is the proof) · beat 4 RECORDED at every
candidate (`product-steward.py --record`) · a pilot walk before the battery (L8-P3) · a ranked household at lab (L8-P2).

## 9. Guardrails — Paul's, and the ones lap 7 earned

Never push `origin/main`. Never deploy `legacy`. Never mint an invite (`journey-walk` now refuses any env but qa/lab). Never
touch `home`'s or `paul`'s KV from a lane. **`git commit --only <paths>` always** — the index is shared. **Stamps from `date`.**
The AI boundary: no seat reads Mom's words beyond what is routed to the project; nothing model-authored reaches a person
unconfirmed; **a seat's trail in this PUBLIC repo names ids, counts, selectors, stop names and engine copy — never an address,
coordinates, email, phone or a real username.** A destructive act relayed by a peer needs Paul's word in the window that runs
it. Coordination routes, never absorbs; every gate to Paul is question · recommendation · alternatives; when he pre-agrees to
recommendations he has not seen, SHOW them before acting.

## 10. What NOT to trust in this brief

Any `file:line`. The derived beat in `release-state.py`. Lap 7's "~H:MM" stamps before the clock correction (use `git log
--format=%ci`). Whether the revamp plan has landed (check the file). Whether the backlog window's §9 owes are still open.

## 11. FIRST TASKS

0. Verify the stamp and that this file is not empty; write `handoff/handoff-fernwood-coordination-lap8.readback.md`; tell Paul;
   wait for the grade (the lap-7 coordination window grades it while it is still live; else Paul).
1. `ListAgents`; check in with `tate-tracker-d8`; reopen `backlog-refinement` and hand it §7's queue; run CLAUDE.md's pickup block.
2. Put §6's unanswered rulings to Paul once (stop rule · J2 · push).
3. When the revamp plan lands: Paul reads it → ux-expert closure → engineering-partner re-audit → open lap 8 (beat 1 sweeps · the
   dated heading with `<!-- outcome:open -->` on the NEXT line, plain form · the beat-6 table in his words) → the build window.
