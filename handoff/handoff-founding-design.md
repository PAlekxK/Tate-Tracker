# HANDOFF — the FOUNDING-FLOW DESIGN window (Fernwood) · launched by Paul, 2026-09-10

- composed: 2026-09-10 ~8:10 PM ET · at HEAD `a96dfa1` (+ the register commit that carries this brief) · repo `/Users/paulkirschenbauer/Developer/Tate-Tracker`
- composed by: the **backlog-refinement** window · Paul: *"Go on the design lane/window."* `[paul-ruled 2026-09-10]`
- ⛔ **RECEIVER: verify the sha against HEAD, then write a readback to `handoff/handoff-founding-design.readback.md`** — what you understand the lane to be, its state, the rulings open, what is NOT verified, what you would do first. The coordination window grades it. **Do not start before it is graded.**

## 1 · The windows — and which you are

| window | owns | never |
|---|---|---|
| **coordination** (`paulkirschenbauer-96`) | routes, the candidate, the freeze, the map, lap 6's chronicle | writes a feature |
| **backlog-refinement** (opened you) | `BACKLOG.md`; places your rulings in Paul's words | commits code |
| **build** (`tate-tracker-c4`) | `worker/worker.js` · `tools/journey-walk.py` · `tools/walk-fixtures.py` — released after the battery | — |
| **zones** (`tate-tracker-97`) | the zones plan, cleanup, `LAND-SOURCES.md`, `tools/fetch-frame.py` | — |
| **YOU — founding-flow design** | `.plans/2026-09-10-founding-flow-design-PLAN.md` (create it; header per the readiness convention, `row: proposed — TIER 1 · 26` until refinement places the pointer) · `.ux-reviews/` trail · `/design-options` exhibits · **and, ONLY after the clear (§3), the diffs to `onboarding/index.html` and `homes/index.html`** | `BACKLOG.md` (message refinement) · `worker/worker.js` · any deploy · any origin |

⛔ Shared tree, HEAD moves under you: `git status <file>` before every write, `git add -- <path>` only, commit small. `cycle/release/cycle-state.json` and `worker/digest.json` are always dirty; never commit them.

## 2 · Mission

> ⭐ **SCOPE GROWN 2026-09-10 ~6:35 PM ET, after the readback was graded** `[paul-stated: "that could be even a separate session devoted to that UX review and really refining what changes we want to make, and we give it time to do that"]`. **This window IS that session.** Its mission is no longer the two surfaces alone but **the lap-7 UX bundle — `BACKLOG.md` TIER 1 · 27**, read there, never restated here: the two-pass sweep as spine (§3·2, unchanged), TIER 2 · 10 the glance consolidation as anchor (with its G6-first precondition), the founding flow (rows 26 · 28 · 29 · 30, (A)–(F) on row 19), the small copy/layout rows (TIER 2 · 21 · 25 · 19 · 20), the 08-31 leftovers, and decision cards `fernwood-11` and `fernwood-8`. Row 27 also lists three questions Paul has not answered (account lifecycle in or out · colour's four axes in or out · one candidate or slices) — put them to him from inside the window as the exhibits make them concrete. **Take the time**; the coordinator sequences the apply. Everything in §3 still binds.


Turn what five reading seats converged on at the founding flow into **one design decision set for Paul**, on the candidate `318416a` served at qa. Two surfaces: **the confirm card** (*"Does that look right?"*, which founds before it is answered) and **the empty shelf** (off a cold founder's path, two founding controls). Everything else the seats raised is a finding to carry, not a second mission.

## 3 · The two conditions (the coordinator's, as freeze holder — binding)

1. ⛔ **No edit to `onboarding/index.html` or `homes/index.html` until Paul's walk at `318416a` is recorded in the chronicle** (`cycle/release/CYCLE-LOG.md`, lap 6 · beat 10). A surface edit moves the candidate and voids the gate evidence five seats just produced. Design to a proposal — mocks, the compare exhibit, a diff for Paul — is free now. **The apply waits for his clear, or an explicit re-candidate from the coordinator.**
2. ⭐ **Your FIRST act is `/ux-sweep`, both passes, on the candidate at qa.** The holistic sweep is owed (L6-P3: 124 commits to `viewer.html` against the 20 threshold) and a single-seat review does not reset its clock. Its punch list becomes your findings list, in one vocabulary with the seeds below. The handover read has landed, so the battery's evidence is complete; start pass 1 as soon as your readback is graded.

## 4 · Read first

1. `BACKLOG.md` TIER 1 · 19 — the whole row: six builder findings, five seat reads verbatim, **(A)–(F) converged**, the gate line, and Paul's rulings already made (PO-box refusal said AT SUBMIT on the address step — a surface change for the NEXT candidate, and yours to design into the step). TIER 1 · 26 is your row.
2. The five seat reports at `318416a`: `owner/…180218` · `mom/…180634` · `wide-eyed/…180759` · `strict/…180920` · `handover/…181052` (`REPORT.md` each; the build lane knows the path — ask the coordinator if it is not obvious from `tools/walk-brief.py`).
3. `BACKLOG.md` § THE FIFTH LENS and § THE STANDING PRINCIPLE (both `[paul-stated 2026-09-10]`) — people found their own; capture what personalizes; *describe or infer* is open. And § 🤝 INVITE & JOIN — the second-person question is NOT yours to reopen; cross-reference it.
4. `~/.claude/design-principles/` — pass 2 of the sweep adjudicates against it automatically; the four standing rules for her surfaces (one affirmative grammar · everything is changeable · the ribbon is attribution · consistency over precision) are in `CLAUDE.md`.
5. `.plans/2026-09-10-interests-as-activities-PROPOSAL.md` and `.plans/2026-09-08-setup-journey-PLAN.md` — the neighbouring asks; do not redesign them, cite them.

## 5 · The findings seed — (A)–(F), five seats, verbatim on row 19

**(A)** the confirm founds before it is answered — all five; **gate or courtesy is Paul's ruling**, and your exhibit should show him both. **(B)** s3 never photographed — the build lane's harness fix, not yours. **(C)** the empty shelf off the cold path with two founding controls — four seats. **(D)** the Almanac composer as the primary control on an empty, unplaced, not-composed place, *"stays on this phone for now"* — strict and handover. **(E)** the profile 404 on the naming step, silent — strict's ruling (4), an engineering question you carry, not fix. **(F)** the 09-08 repeats: *"what grows there"* tense · the shelf drops the house number · email never shown back · coordinates on no screen.

Plus, from single seats and worth your exhibit: three same-styled filled commit buttons on one card (Yes that's it / Save these / Open Home) · the account-exists receipt nobody says (strict 3) · the F12 receipt's WHERE IT IS empty in the collapsed card (wide-eyed; Paul checks by hand on his walk) · *"Getting it ready to hand over"* ranked first and sitting tenth because badges do not reorder (handover).

## 6 · What you produce

1. The sweep's two passes and its punch list (the skill files its own trail under `.ux-reviews/`).
2. **One `/design-options` exhibit** for the confirm card (gate vs courtesy, the button grammar, the PO-box refusal at submit) and one for the shelf (on-path vs off-path; one founding control). Staged for Paul; he reacts; you iterate.
3. The plan file with the decision set: each option, the seats' reads, the falsifier, and **which of Paul's rulings it needs**. Copy that reaches a person is content-steward's before it ships.
4. Your rulings to refinement **in Paul's words**, as they land; refinement places them.

## 7 · What is NOT verified

Whether Paul's walk at `318416a` has been recorded yet (check the chronicle before any apply). Every `file:line` older than an hour. The seat reports' paths (named by run id above, not by path).

## 8 · Guardrails

⛔ Never `git push origin main` · never deploy · nothing outbound · no paid API · nothing reaches Mom or Bob · no canon write · no `.plans/*-PLAN.md` another lane owns. A ruling that is not in the register is not in force — send Paul's words to refinement. `[paul-stated]` means his words, never a paraphrase into a stronger claim.

## 9 · STATE AT PAUSE — 2026-09-10 ~10:55 PM ET · committed at `4fb7f64` (coordinator's urgent commit-now, usage 97%; Paul had said "pause")
- **Unfinished, by name:** pass 2 of the sweep (doctrine adjudication — agent in flight, trail placeholder stands) · the content-steward review, TIER 1 · 31 (agent in flight; target `.content/2026-09-10-founding-flow-copy-REVIEW.md`) · the two `/design-options` exhibits (confirm-as-GATE with the PO-box refusal; the shelf) and the account-lifecycle designs — NOT STARTED · `.plans/2026-09-10-founding-flow-design-PLAN.md` — NOT CREATED · messages owed: trail path + `est-gndlvf` to the coordinator, top findings to refinement. Distillations (seats · plans · bundle rows · rulings) live in this session's scratchpad only.

## 10 · STATE AT CLOSE — 2026-09-10 ~9:55 PM ET · committed at `4b3b06a` (the coordinator's close-out instruction, on Paul's word)

**Proposed (nothing applied; both surfaces untouched since `318416a`):**
- The two-pass sweep, complete: `.ux-reviews/2026-09-10-founding-flow.md` — pass 1 (14 findings, F1 a blocker), pass 2 (re-verification table, adjudication, 8 coherence findings, a 15-item gated punch list, 5 NEEDS-PAUL), the parent's own check of the checker. The sweep founded `est-gndlvf` at qa (on the coordinator's teardown list).
- The content read, TIER 1 · 31: `.content/2026-09-10-founding-flow-copy-REVIEW.md` — two CRITICAL findings ("Almanac" minted by arithmetic after the Journal ruling; the "stays on this phone" caption over a button that sends to a model), the gate card's copy drafted in both PO-box forms, the receipt line, the colour noun. All DRAFT.
- Six exhibits, staged at `~/Desktop/design-options/`, durable under `.private/ux-sweeps/2026-09-10-founding-flow/exhibits/`: the gate card + PO-box · the empty shelf · the receipt at naming · the account lifecycle · the door · the change links. Each carries one recommendation; the rulings each needs went to refinement in Paul's-word form.
- The plan: `.plans/2026-09-10-founding-flow-design-PLAN.md` — R1–R10 cited from the register, D1–D10, §2a the exhibits, §4 the ONE-candidate apply list, §5 pass 2's punch list and questions.

**Paul has ruled (on the register, verified):** the confirm is a GATE · the PO-box refusal is said at submit · account lifecycle IN · colour = the one-line noun fix only · ONE candidate for the bundle · the feedback bubble from account creation · unit number never forced · the front door for everyone (setup-journey D2) · G6 before any adaptive strip · content-steward reads every walk.

**Owed, by whom:** Paul — PO1 vs PO2; pass 2's Q1–Q5; his reactions to the six exhibits · engineering-partner — D1a (where the PO-box check runs) and D5 (the change links' write path) · security-steward — D4's username-recovery field · a build lane — G6 + card-face opens (co-requisite of the strip), C5 (email on whoami), the front door B2/B3 · the row-41 bug lane — the sign-in model · the coordinator — lifting the hold before any apply; the walk harness needs a sign-out/return stop for D4 · content-steward — the narrator introduction line and the Journal substitution (five sites).

**Not done, by name:** no apply · no deploy · no exhibit for D6 (waits on Q1) or the strip (waits on G6) · TIER 1 · 30/44 "formatting off" measured (trail C13) but not mocked · the sweep's own Refinement-log entry and the design-options run log are appended in `~/.claude/skills/` (not this repo).
- **Correction after close (~10:05 PM ET):** exhibit 3b's refusal sentence is SUPERSEDED by the bug lane's findings (`cross-device-signin-FINDINGS.md` §5/§6 P8; SECURITY L1(b): one constant string naming the door, byte-identical 404 kept; quarantine, never clear, on refusal). Marked in the plan §2a and D4·5; the house-in-the-lede half stands. Refinement told not to put 3b's refusal to Paul as drafted.
